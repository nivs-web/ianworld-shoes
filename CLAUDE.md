# CLAUDE.md — 오락실 이안월드 / 신발을 찾아서

이 파일은 Claude Code가 이 저장소에서 작업할 때 **매번 먼저 읽는 지침**입니다.
기획의 진실은 `docs/GAME_DESIGN.md` 에 있습니다. 충돌 시 기획서가 우선합니다.

---

## 1. 프로젝트 한 줄 요약

맨발의 주인공이 **신발 상자 계단**을 좌우로 오르며 신발을 갈아 신고, **130종 도감**과 **명예의 전당**을 채우는 세로형 **레트로 도트** 웹게임(PWA).
포털 이름은 `오락실 이안월드`, 첫 게임은 `신발을 찾아서`.

---

## 2. 기술 스택 (확정)

| 영역 | 선택 | 이유 |
|------|------|------|
| 빌드 | **Vite** | 빠른 HMR, 정적 배포, 설정 최소 |
| 언어 | **JavaScript (ESM)** + JSDoc 타입 | 빌드 마찰 최소화. TS 미도입 |
| 렌더링 | **Canvas 2D** (라이브러리 없음) | 180×320 저해상도 픽셀 렌더에 최적 |
| 사운드 | **Web Audio API 직접 합성** | 오디오 파일 0개. 8bit 칩튠을 코드로 생성 |
| 인증 | **Firebase Auth (Google)** | |
| DB | **Firestore** (계정/도감/랭킹) + **Realtime Database** (멀티 룸) | 랭킹은 쿼리, 멀티는 저지연 |
| 배포 | **Vercel** ← GitHub `nivs-web/ianworld-shoes` | main push = 자동 배포 |
| PWA | manifest + service worker (수동 작성) | |

> **금지**: React / Phaser / PixiJS / Tailwind / 애니메이션 라이브러리(GSAP 등).
> 이 게임은 순수 Canvas + 바닐라 JS로 만든다. 번들 크기와 픽셀 제어권이 우선이다.

---

## 3. 절대 규칙 (Non-Negotiable)

이 5가지는 **어떤 이유로도 어기지 않는다.** 위반 코드는 리뷰에서 되돌린다.

### 3-1. 픽셀 퍼펙트

```js
// 부트스트랩에서 단 한 번
const ctx = canvas.getContext('2d', { alpha: false });
ctx.imageSmoothingEnabled = false;      // 절대 금지: true
ctx.imageSmoothingQuality = 'low';
```
```css
canvas { image-rendering: pixelated; image-rendering: crisp-edges; }
```
- 논리 캔버스는 **정확히 180 × 320**. 절대 바꾸지 않는다.
- 확정 상수 (레퍼런스 「무한의 계단」 스크린샷 실측 기준, 2026-08-14):
  계단 블록 **32×13**(사용자 제작 돌블록 아트, 원본 160×65 비율 그대로), 수직 간격 **20**, 수평 간격 **30**, 발끝 y **212**,
  캐릭터 원본 35×50 → **1.5배**로 렌더(53×75), 조작 버튼 **48×48** @ y266.
  · 실측 환산: 원본 839×1489 이미지 = 논리 180×320 (1논리px = 4.65px)
  · 배율 1.5는 비정수지만 목적지 크기를 반올림 + 스무딩 off 라 nearest 확대가 된다.
    캔버스→화면 확대는 여전히 정수배이므로 §3-1 규칙은 지켜진다.
- 신발은 **마스터 50×30 체계** (2026-08-13 개정): 마스터 50×30(도감) → 계단용 25×15(정수 ½) → 착용용 15×9(근사).
  도감에서 신발을 탭하면 마스터를 3×~4×로 키운 상세 팝업이 뜬다. 기획서 §9-3 참조.
- **인게임 캔버스** 폰트는 **갈무리11 볼드**(11px) + **갈무리7**(7px) 두 벌이다(OFL-1.1).
  빌드 타임에 도트로 구운 `src/data/font.generated.json` · `font7.generated.json` 만 쓴다.
  `ctx.fillText` 는 어떤 경우에도 금지 — 안티앨리어싱을 끌 수 없다. 배율은 정수만.
  판돈·알림·등수처럼 **줄이 여럿 겹치는 정보는 7px**, 나머지는 11px.
- **로비 등 DOM 화면**은 **Neo둥근모** 웹폰트를 쓴다(`public/assets/fonts/neodgm.woff2`, 45KB).
  인게임까지 이 폰트로 통일해 봤다가 **도트 게임의 아기자기함이 사라져서 되돌렸다**(§9-0-29).
- **한글은 코드가 실제로 쓰는 글자만 굽는다.** `build-font.mjs` 가 `src/**` 의 **문자열 리터럴**에서
  한글을 긁어 모은다(주석 제외 — 이 저장소는 주석이 전부 한글이라 같이 긁으면 두 배가 된다).
  그래서 **캔버스에 그릴 한글은 반드시 `strings.ko.js` 등 문자열 리터럴로** 존재해야 한다.
  굽히지 않은 글자는 `?` 로 나온다 — 실제로 '맵바꾸기'가 '?바꾸기'로 나갈 뻔했다.
  `npm run build` 가 폰트를 먼저 굽게 묶어 뒀으니 문구만 고치고 잊어도 안전하다.
- 화면 표시는 **정수배 스케일만** (`scale = Math.floor(min(vw/180, vh/320))`, 최소 1).
- 오프스크린 버퍼가 있어도 **모든** 컨텍스트에 `imageSmoothingEnabled = false` 를 건다.

### 3-2. 좌표는 정수만

```js
// ✅ 이렇게
entity.x += STEP_X;                 // STEP_X는 정수 상수
ctx.drawImage(img, entity.x, entity.y);

// ❌ 절대 금지
entity.x += speed * dt;             // 소수 누적
ctx.drawImage(img, x + 0.5, y);     // 소수 좌표
```
- 소수가 필요한 계산은 **별도 accumulator**에 두고, 렌더에는 `| 0` 로 내림한 정수만 넘긴다.
- `lerp`, `easeOut`, `smoothstep` 류 **보간 함수 사용 금지**.

### 3-3. 스프라이트 시트 애니메이션 (보간 금지)

- 캐릭터는 정해진 컷 사이를 **뚝뚝 교체**한다. 중간 상태 생성 금지.
- 애니메이션 프레임 레이트: **8~12 FPS** (게임 루프 60FPS와 분리).
- 상승 시퀀스는 반드시:
  `방향컷 → [이펙트(번개) 컷 2~3프레임] → 새 계단의 방향컷`

### 3-4. 고정 타임스텝 루프

```js
const FIXED_DT = 1000 / 60;
let acc = 0;
function loop(now) {
  acc += Math.min(now - last, 100);   // 스파이크 클램프
  while (acc >= FIXED_DT) { update(FIXED_DT); acc -= FIXED_DT; }
  render();
  requestAnimationFrame(loop);
}
```
- `render()`에서 **보간(interpolation) 하지 않는다.** 마지막 상태를 그대로 그린다.

### 3-5. 밸런스 수치는 코드에 박지 않는다

모든 숫자(게이지 감소율, 회복량, 신발 확률, 계단 간격…)는 `src/config/balance.js` 한 파일에만 존재한다.
게임 로직 파일에 리터럴 숫자를 쓰면 안 된다.

---

## 4. 디렉터리 구조

```
ianworld-shoes/
├─ CLAUDE.md                     ← 이 파일
├─ index.html                    ← 단일 진입점 (SPA)
├─ vite.config.js
├─ package.json
├─ vercel.json
├─ .env.example                  ← Firebase 키 템플릿 (실제 .env는 커밋 금지)
│
├─ docs/
│  ├─ GAME_DESIGN.md             ← 기획 진실
│  ├─ PROJECT_INSTRUCTIONS.md    ← Claude 프로젝트 지침 붙여넣기용
│  ├─ ASSET_PIPELINE.md          ← 원본 → 게임 에셋 변환 절차
│  └─ DECISIONS.md               ← 결정 로그 (왜 그렇게 했는지)
│
├─ etc/                          ← 원본 참고 이미지 (게임이 직접 읽지 않음, 읽기 전용)
│
├─ public/
│  ├─ manifest.webmanifest
│  ├─ sw.js
│  ├─ icons/                     ← 192/512/maskable
│  └─ assets/                    ← 최종 게임 에셋 (빌드 산출물, 아래 §5)
│     ├─ characters/             ← ian_front.png, ian_right.png, ian_jump.png …
│     ├─ shoes/shoes_atlas.png   ← 160×140
│     ├─ bg/build_01.png … build_12.png, floor200.png … floor500.png
│     └─ ui/                     ← 버튼, 로고, 패널
│
├─ tools/                        ← Node 스크립트 (빌드 타임 전용)
│  ├─ slice-characters.mjs       ← 캐릭터 시트 → 개별 PNG 30장
│  ├─ build-shoe-atlas.mjs       ← 신발 시트 → 160×140 아틀라스 + shoes.json
│  ├─ downscale-bg.mjs           ← 배경 포스터 → 패널 크롭 → 180폭 축소 → 32색 양자화
│  ├─ bg-panels.json             ← 배경 패널 좌표 (수동 지정)
│  ├─ _bg-qa.mjs                 ← (진단) 포스터에 좌표 격자 얹기
│  ├─ _bg-preview.mjs            ← (진단) 변환 결과를 게임처럼 쌓아 미리보기
│  ├─ _shoes-preview.mjs         ← (진단) 신발 아틀라스를 체크무늬 위에 확대 — 투명 구멍 육안 확인
│  └─ _shoes-holes-qa.mjs        ← (진단) 130개 전수 내부 구멍 검사 (0이어야 통과)
│
└─ src/
   ├─ main.js                    ← 부트: 캔버스 셋업, 라우터 시작
   │
   ├─ core/                      ← 엔진 (게임 규칙 모름)
   │  ├─ canvas.js               ← 180×320 + 정수배 스케일 + 스무딩 off
   │  ├─ loop.js                 ← 고정 타임스텝
   │  ├─ input.js                ← 터치/키보드 → 추상 입력(LEFT/RIGHT), 버퍼 2
   │  ├─ sprite.js               ← 스프라이트/아틀라스 드로우 (정수 좌표 강제)
   │  ├─ anim.js                 ← 프레임 시퀀서 (8~12FPS)
   │  ├─ scene.js                ← 씬 스택 (push/pop/replace)
   │  ├─ pixelfont.js            ← 비트맵 폰트 렌더 (fillText 금지, font.generated.json 사용)
   │  ├─ assets.js               ← 로더 + 진행률
   │  └─ rng.js                  ← 시드 기반 난수 (멀티 동기화용)
   │
   ├─ audio/
   │  ├─ audio.js                ← AudioContext, 마스터 게인, unlock 처리
   │  ├─ synth.js                ← 파형 프리미티브 (square/pulse/tri/noise/sweep)
   │  ├─ sfx.js                  ← 효과음 정의 테이블
   │  └─ bgm.js                  ← 10트랙 시퀀서 (100층마다 교체)
   │
   ├─ game/                      ← 인게임 로직
   │  ├─ GameScene.js            ← 조립
   │  ├─ stairs.js               ← 계단 생성/방향/스크롤
   │  ├─ player.js               ← 상태머신 (IDLE/STEP/EFFECT/FALL/DEAD)
   │  ├─ shoes.js                ← 티어 롤, 배치, 착용, 팝 효과
   │  ├─ gauge.js                ← 시간 게이지
   │  ├─ background.js           ← 도로/1층/반복층, 구름, 층수별 교체
   │  ├─ hud.js                  ← 게이지/계단수/찾은신발/부활/일시정지
   │  └─ revive.js               ← 부활 카운트/사용
   │
   ├─ screens/                   ← 비-인게임 화면 (DOM 기반, S01~S18)
   │  ├─ SplashLogin.js  NicknameSetup.js  Portal.js  Lobby.js
   │  ├─ CharacterSelect.js  Collection.js  HallOfFame.js  Controls.js
   │  ├─ Pause.js  GameOver.js  Revive.js
   │  └─ multi/  MultiMenu.js  Room.js  CodeInput.js  Countdown.js  MultiResult.js
   │
   ├─ data/
   │  ├─ characters.js           ← 10종 메타 (id, 이름, 해금 비용, 노출 순서)
   │  ├─ shoes.json              ← 130종 (id, tier, atlas x/y/w/h)
   │  └─ backgrounds.js          ← 12종 + 층수별 교체 테이블
   │
   ├─ services/                  ← Firebase 래퍼 (화면은 이 계층만 호출)
   │  ├─ firebase.js  auth.js  profile.js  collection.js
   │  ├─ leaderboard.js  multiplayer.js  storageLocal.js
   │
   ├─ config/
   │  ├─ balance.js              ← ★ 모든 밸런스 숫자
   │  ├─ layout.js               ← ★ 모든 좌표/크기 상수
   │  └─ strings.ko.js           ← ★ 모든 한국어 문구
   │
   └─ styles/
      ├─ reset.css  pixel-ui.css  screens.css
```

---

## 5. 에셋 파이프라인 규칙

**원본(`etc/`)은 절대 수정하지 않는다.** 항상 `tools/` 스크립트로 `public/assets/` 를 생성한다.

| 원본 | 스크립트 | 산출물 |
|------|----------|--------|
| `etc/캐릭터/캐릭터에셋.png` | `slice-characters.mjs` | 캐릭터 10 × 3컷 = 30 PNG (35×50, 35×50, 50×50) |
| `etc/신발자료/신발130개.png` | `build-shoe-atlas.mjs` | `shoes_atlas.png` (160×140) + `src/data/shoes.json` |
| `etc/백그라운드건물/*.png` (16) | `downscale-bg.mjs` | `build_01~16_{road,floor1,tile}.png` (48장) |
| `etc/200층이상배경/*.png` (4) | `downscale-bg.mjs` | `floor200/300/400/500.png` (각 180×320) |
| `etc/게임배경시즌2/*.png` (30) | `build-season2-bg.mjs` + `bg-season2.json` | `build_17~46_{road,floor1,tile}.png` (90장) |
| `etc/신발자료/newdesign.png` | `slice-newdesign.mjs` | `shoes_master.png` / `shoes_game.png` / `shoes_worn.png` / `shoe_icon.png` |
| `etc/인터페이스 버튼.png` | `slice-buttons.mjs` | `ui/btn_{turn,left,up,right}.png` (48×48) |
| `etc/UI/계단.png` | `slice-stair.mjs` | `ui/stair.png` (32×13) |
| `etc/게이지바.png` · `etc/일시정지.png` | `build-hud-ui.mjs` | `ui/gauge_frame.png` (146×18) · `ui/btn_pause.png` (18×18) |
| `etc/신발을 찾아서 로고.png` · `etc/오락실 이안월드 로고.png` | `build-logos.mjs` | `ui/logo_game.png` (720×305) · `ui/logo_portal.png` (720×519) |
| `node_modules/galmuri/…/Galmuri11-Bold.ttf` | `build-font.mjs` | `src/data/font.generated.json` (11px, 아스키 67 + 한글 374자) |
| `node_modules/galmuri/…/Galmuri7.ttf` | `build-font-small.mjs` | `src/data/font7.generated.json` (7px, 상용 한글 2,350자) |
| `etc/1등이닷.png` + 폰트 | `build-bubble.mjs` | `ui/bubble_first.png` (68×34, 글씨까지 구움) |

**축소 규칙은 원본의 성격에 따라 다르다.**

| 원본이 | 방법 |
|--------|------|
| 진짜 픽셀아트 (7× 확대된 시트) | 7×7 블록 **중심 픽셀 그대로** 집기 = 무손실. 리샘플 금지 |
| 렌더 일러스트 (배경 포스터) | 패널 크롭 → **lanczos** 축소 → **32색 양자화** |

두 번째 경우 lanczos를 쓰는 건 예외가 아니다. 원본에 픽셀 격자가 없으므로 nearest는 노이즈만 남긴다.
**중요한 건 런타임이다** — 결과물은 180폭 고정이고 화면에서는 정수배로만 확대되므로 완전한 픽셀 퍼펙트다.

배경 패널 좌표는 `tools/bg-panels.json` 에 수동 지정되어 있다. 포스터를 다시 그리면
`node tools/_bg-qa.mjs <이름> <파일...>` 로 좌표 격자를 얹어 보고, `node tools/_bg-preview.mjs build 1 8` 로
타일이 매끄럽게 반복되는지 확인한 뒤 JSON을 고친다.
좌우 반전 컷(왼쪽 보기)은 파일로 만들지 않고 **런타임에 `ctx.scale(-1,1)`** 로 처리한다.

---

## 6. 코딩 규칙

### 6-1. 일반

- 파일당 300줄 이내. 넘으면 분리한다.
- 함수는 한 가지 일만. 이름은 동사로 시작 (`spawnShoe`, `drainGauge`).
- 전역 상태 금지. 상태는 씬이 소유하고 명시적으로 전달한다.
- `console.log` 는 커밋 전 제거. 디버그는 `DEBUG` 플래그 뒤에 둔다.
- 주석은 **왜**를 적는다. 무엇을 하는지는 코드가 말한다.

### 6-2. 문자열

- UI에 보이는 모든 한국어는 `src/config/strings.ko.js` 에만 존재한다. JSX/템플릿에 직접 쓰지 않는다.
- 기획서에 명시된 문구는 **토씨 하나 바꾸지 않는다.** 예:
  - `게임난이도(신발의 등장 빈도가 달라집니다)`
  - `티어가 높은 신발부터 사라집니다`
  - `내 소중한 신발 1켤레를 뺏겼습니다`
  - `신발이 1켤레 이하면 게임에 참가할 수 없습니다`

### 6-3. 인게임 vs 화면 UI

- **인게임(S09/S17)** 은 전부 Canvas에 그린다. DOM 오버레이 금지 (HUD 포함).
- **로비/메뉴(S01~S08, S10~S16, S18)** 는 DOM + CSS. 픽셀 폰트와 `image-rendering: pixelated` 를 CSS로 유지한다.
- 두 방식을 한 화면에서 섞지 않는다.

### 6-4. Firebase

- 화면 코드는 Firebase SDK를 **직접 import 하지 않는다.** 반드시 `src/services/*` 를 경유한다.
- 모든 쓰기는 실패를 가정한다. 실패 시 `sf_pendingScores` 에 큐잉하고 다음 접속에 재시도.
- 키는 `.env` (`VITE_FIREBASE_*`). `.env` 는 `.gitignore`. Vercel 환경변수에 동일하게 등록.
- 보안 규칙: 본인 `users/{uid}` 만 쓰기 가능. `scores` 는 create만 허용하고 update/delete 금지.

### 6-5. 커밋

```
feat(game): 계단 상승 시 번개 이펙트 컷 추가
fix(render): 캐릭터 y좌표 소수점 발생 수정
chore(assets): 신발 아틀라스 재생성
```
- 한 커밋 = 한 가지 변경. `main` 에 push하면 Vercel이 자동 배포되므로 **깨진 상태로 push 금지**.

---

## 7. 작업 시 반드시 확인할 것 (Claude용 체크리스트)

코드를 쓰기 전:

1. `docs/GAME_DESIGN.md` 의 해당 절을 읽었는가?
2. 필요한 숫자가 `src/config/balance.js` / `layout.js` 에 있는가? 없으면 **먼저 거기에 추가**한다.
3. 기획서에 `[빈칸]` 이 남아 있는 항목인가? → **추측하지 말고 사용자에게 묻는다.**

코드를 쓴 후:

4. 소수점 좌표가 생길 수 있는 경로가 있는가?
5. 새 캔버스/버퍼를 만들었다면 `imageSmoothingEnabled = false` 를 걸었는가?
6. 문자열을 하드코딩하지 않았는가?
7. 애니메이션에 보간이나 이징이 섞이지 않았는가?

---

## 8. 자주 하는 실수 (하지 말 것)

| ❌ 하지 마라 | ✅ 대신 |
|-------------|---------|
| `canvas.width = window.innerWidth` | 논리 180×320 고정, CSS로만 확대 |
| `devicePixelRatio` 로 캔버스 확대 | 정수배 스케일만. DPR은 CSS 크기 계산에만 |
| `y += velocity * deltaTime` | 정수 상수 이동 또는 accumulator + `|0` |
| `requestAnimationFrame` 안에서 물리 계산 | 고정 타임스텝 `update()` 안에서만 |
| CSS `transition` / `transform: scale(1.05)` 로 신발 팝 | 스프라이트 크기를 정수배(15→19px)로 2프레임 교체 |
| 배경을 별도 속도로 스크롤 | 계단 상승량과 **동일한 정수 픽셀**만큼 이동 |
| 새 화면마다 Firebase 재초기화 | `services/firebase.js` 싱글턴 |
| 오디오 파일(mp3/wav) 추가 | Web Audio 파형 합성 |
| 캐릭터 왼쪽 컷을 새 PNG로 생성 | 런타임 `ctx.scale(-1,1)` 반전 |
| 기획서 없이 숫자 정하기 | 기획서에 먼저 쓰고 승인받기 |

---

## 9. 현재 상태

- [x] 기획서 `docs/GAME_DESIGN.md` v0.2
- [x] `CLAUDE.md` v0.2
- [x] 확정: 캐릭터 10명 / 계단 46·30 / 멀티 패널티 랜덤 / 난이도별 3개 랭킹
- [x] 확정: 게이지 밸런스 (무한의 계단보다 살짝 빡세게 — 기획서 §5-3 표)
- [x] 확정: 신발 등장 완전 균등 / 함성 효과음 칩튠 합성
- [x] **기획 사양 전체 확정 → 기획서 v1.0**
- [x] **M0 프로젝트 세팅** — Vite 빌드 통과, 180×320 정수배 캔버스, 고정 타임스텝 루프,
      입력(선입력 버퍼 2), 스프라이트/애니 모듈, 비트맵 폰트, 씬 스택, 시드 RNG, PWA 셸
- [x] **M1 에셋 파이프라인 완료**
  - [x] 캐릭터 10명 × 3컷 = 30장 — 7×7 블록 중심 샘플링으로 무손실 복원, 배경 투명, 바닥 정렬
  - [x] **신발 130종 완료 (마스터 50×30 체계)** — `tools/generate-shoes.mjs` 가 사용자 제공 스니커즈
        스타일을 코드 템플릿(하이탑/로우컷)으로 재현, 130개 팔레트 전량 생성.
        아틀라스 3종: `shoes_master.png` 520×416 · `shoes_stair.png` 260×208(정수 ½) · `shoes_worn.png` 160×130.
        티어 5/10/15/40/60 검증 완료. 색 수정은 스크립트 팔레트만 고치고 재실행.
  - [x] 배경 건물 16종 × 3패널 = 48장 + 층수 배경 4장 — 좌표 수동 지정 후 타일 이음새 육안 검증 완료
- [x] **M2 코어 게임플레이 완료** — 계단 생성(시드 RNG)·상승·전환·추락·게이지·스코어 +
      신발 스폰/획득/착용/팝, 부활(20개당 1개, 3초 카운트다운), 일시정지/게임오버 오버레이,
      배경 3층 레이어 스크롤 + 100층 구름 + 200층↑ 풀스크린 교체, 조작 3모드, 로컬 최고기록.
      브라우저 봇 테스트 통과(30칸 등반·신발 5개 획득·사망 연출·재시작).
      main.js는 임시로 게임 직행 — M5에서 로그인→포털→로비 흐름으로 교체.
- [x] **2026-08-14 마감 수정 6건**
  - [x] 계단 블록을 사용자 개정 아트(160×65)에 맞춰 **32×13**으로 되돌림 (간격 30×20)
  - [x] 착용 신발 **0.7배(28×17)** + `characters.generated.json` 의 `cuts[*].foot.cx` 로 발 위치 정렬 → 맨발 노출 0
  - [x] 부활 카운트다운 **3초 → 10초** (기획서 §5-6)
  - [x] **엘리베이터(500층 스킵)** 기획 확정 — 기획서 §5-8-1 신설 (해금 500층 / 1회 신발 50켤레)
  - [x] 계단 가독성 — 1px 외곽선 + 드롭섀도로 배경색과 겹쳐도 또렷하게 (`PAL.stairOutline/stairShadow`)
  - [x] 신발 내부 투명 구멍 **130개 전수 제거** (`fillHoles()` — 원본 해상도 85,898px + 축소 후 재검사, QA 스크립트로 0 확인)
- [x] **2026-08-14 HUD 폰트 개편** — 5×7 자체 제작 폰트 → **갈무리11 볼드**(OFL-1.1) 비트맵 폰트.
      계단 수 ×4(20×28) → ×2(14×22, 레퍼런스와 동일), 신발·부활 수 ×2 → ×1.
      HUD를 **행 분리**(아이콘 19~34 / 계단 수 37~59)로 재설계해 자릿수가 늘어도 겹치지 않는다.
      일시정지·부활·게임오버 패널도 11px 폰트에 맞춰 y좌표 전면 재조정.
- [x] **2026-08-15 HUD 아트 교체** — 사용자 신규 아트(`etc/게이지바.png` · `etc/일시정지.png`)를
      `tools/build-hud-ui.mjs` 가 **팔레트만 뽑고 형태는 도트로 재작도**해 `gauge_frame.png`(146×18) ·
      `btn_pause.png`(18×18) 생성. 원본이 렌더 일러스트(고유색 6,186개)라 축소하면 1px 테두리가
      뭉개지므로 lanczos가 아니라 재작도를 택했다. 게이지 채움은 이미지에 굽지 않고 런타임 사각형.
      게이지 높이가 12→18로 커져 HUD 행을 3~21 / 23~38 / 41~63 으로 재배치.
- [x] **2026-08-15 계단 위 신발 위치** — 밑창이 돌블록을 2도트 파고들어 계단을 가렸다 →
      `SHOE.stairOffsetY: -1` 로 올려 계단 **위에 얹힌** 모양으로 수정.
- [x] **2026-08-15 착용 신발 개편** — 맨발 노출을 구조적으로 불가능하게 만들었다.
  - [x] 착용 전용 아틀라스 `shoes_worn.png` 신설 (31×19 = 알맹이 29×17 + **1도트 진회색 외곽선**,
        원본 크롭에서 직접 렌더). game 아틀라스를 0.7배로 줄여 그리던 방식은 가장자리가 들쭉날쭉했다.
  - [x] 크기 이전 대비 **1.1배** — 신발이 확실히 눈에 띄는 게 이 게임의 핵심
  - [x] **신발을 신으면 캐릭터 컷의 맨발 영역을 아예 그리지 않는다** (`Player.drawCut`).
        덮는 방식은 로우컷 신발에서 반드시 삐져나온다. 발을 안 그리면 어떤 신발이든 노출 0.
  - [x] 발 시작 행을 **살색 판정**으로 컷마다 측정 (`footAnchor()`, 맨다리 캐릭터는 5~9행 클램프)
  - [x] 캐릭터 4명 × 신발 3종 × 좌우 = 전수 육안 검증 (`tools/_foot-qa.mjs`)
- [x] **M4 사운드 완료 (2026-08-15)** — 오디오 파일 **0개**, 전부 Web Audio 합성.
  - [x] `audio.js` 컨텍스트 싱글턴 + 마스터/BGM/SFX 3버스 + 첫 제스처 unlock + 탭 전환 시 suspend
  - [x] `synth.js` 펄스파(듀티 12.5/25/50%) PeriodicWave, 결정적 노이즈 버퍼, 엔벨로프,
        지수 스윕, 비브라토 LFO, 밴드패스 포먼트 직렬
  - [x] `sfx.js` 효과음 **17종** (기획서 §9-7 표 전량). 상승음은 ±2반음 교차해 "착착착" 리듬
  - [x] `sfx_shout` 칩튠 함성 — 노이즈 어택 → 피치 벤드 → 포먼트 2단(F1 700 / F2 1200→1800)
        → 비브라토 6Hz. 캐릭터별 피치 ×0.85 / ×1.2 / ×1.35 실측 확인 (301 / 431 / 484Hz)
  - [x] `bgm.js` 10트랙 룩어헤드 시퀀서. BPM 120→200, 리드·베이스·아르페지오·드럼 4레이어.
        **100층마다 다음 마디에서** 교체 (곡 중간에 튀면 귀에 거슬린다)
  - [x] 검증: SFX 20종 + BGM 10트랙 전부 피크 측정 통과(무음 0 · 클리핑 0),
        층수 점프 0/100/250/500/900/1500 → 트랙 1/2/3/6/10/10 확인
- [x] **M5 화면·계정 1차 (2026-08-15)** — 로그인 → 포털 → 로비 → 인게임 → 로비 흐름 완성.
  - [x] `services/` 계층: `firebase.js`(설정 없으면 자동 비활성 + **동적 import** 라 미설정 시 번들 0KB),
        `auth.js`(구글 로그인 / 게스트), `profile.js`, `storageLocal.js`
  - [x] **원본은 항상 로컬**. Firebase는 뒤에서 밀어 올리고 끌어내리기만 한다 →
        키가 없어도, 네트워크가 끊겨도 게임이 그대로 돌아간다
  - [x] 화면 S01 로그인 · S02 닉네임(한글 2~4자 검증) · S03 포털 · S04 로비 ·
        S05 캐릭터 선택/구매 · S06 도감(티어탭+확대팝업) · S08 조작법
  - [x] **엘리베이터 구현** — 500층 해금, 누르는 즉시 신발 50켤레 차감, 500층에서 시작
  - [x] 게임오버 HOME → 로비 복귀 + 결과 반영 / RETRY → 결과 반영 후 새 판
        (다시하기를 눌렀다고 방금 주운 신발이 사라지면 안 된다)
  - [x] `docs/FIREBASE_RULES.md` — 콘솔에 붙여넣을 Firestore·RTDB 보안 규칙
  - [x] 브라우저 전 흐름 검증 (`tools/_screens-qa.mjs`) — 11화면 스크린샷, 콘솔 에러 0
  - 잡은 버그: `nav.refresh()` 가 화면 인스턴스를 다시 만들어 도감 티어탭·캐릭터 index가
    초기화되던 문제 → refresh는 render만 다시 돌리도록 분리
  - [x] `collection.js` 도감 동기화 — **합집합 병합**(최초 획득일은 이른 쪽, 누적 횟수는 큰 쪽).
        도감은 사라지면 안 되므로 어느 기기에서 먼저 찾았든 기록이 유지된다.
        게스트로 모은 도감도 로그인 시 자동으로 올라간다.
  - [x] `firebase.js` — 설정이 **절반만** 채워지면 콘솔 경고. "설정한 줄 알았는데 게스트로 돌던" 상황 방지
- [x] **2026-08-15 Firebase 실제 연결** — 웹 앱 설정 6개 값 전부 확보, `.env` 완성.
  - [x] SDK를 **쓰는 시점별로 3분할**. 한 덩어리면 로그인 한 번에 Firestore까지 딸려온다.
        `getFirebase()` app+auth(71KB gz, 부팅) / `getStore()` +firestore(93KB gz, 로그인 후) /
        `getRtdb()` +database(M7 방 입장). `vite.config.js` manualChunks도 같은 기준으로 쪼갬.
        초기 로딩 208KB gz → **98KB gz**.
  - [x] `initAuth()` **3초 타임아웃** — 네트워크가 느리면 로컬 상태로 먼저 화면을 띄우고,
        세션이 뒤늦게 확인되면 `onUserChanged` 로 갱신한다. 부팅이 SDK를 기다리다 멈추면 안 된다.
  - [x] S01에 **`로그인 없이 시작`** 추가 — Firebase가 켜지자 구글 로그인만 남아
        팝업 차단·오프라인·계정 없는 사람이 막히는 상태가 됐다. 로그인은 전제가 아니다.
  - [x] 닉네임 중복 확인은 **로그인 사용자에게만**. 게스트 닉네임은 서버에 없으니 겹쳐도 무해하고,
        막으면 이득 없이 Firestore 93KB만 받게 된다. 4초 타임아웃도 걸었다.
  - [x] 검증: 설정을 켠 채로 **네트워크가 없는 환경**에서 전 흐름 QA 통과 (`errors []`).
        Firestore 불통에도 도감 2/130 · 최고기록 12 · 난이도 저장이 전부 로컬에 남았다.
- [x] **M8 PWA 완료 (2026-08-15)** — 아이콘 3종 생성, 서비스 워커 **등록**(그동안 아무도 안 불렀다),
      캐시 3분리(shell/build/art), 오프라인 부팅 실증. 자세한 내용은 §9-0-12.
- [ ] M3 잔여(도감 영구 저장은 M5 Firebase와 함께) · M4~M8 (기획서 §11-1 참조)
- [ ] **엘리베이터 구현**은 로비(S04)가 생기는 M5에서 함께 (기획만 확정, 코드 없음)

### 에셋 원본 성격 (중요)

| 원본 | 성격 | 결론 |
|------|------|------|
| `캐릭터에셋.png` | 진짜 픽셀아트, 정확히 7× 확대 | 무손실 복원 가능 ✅ |
| `참고용사진/sneaker_strata_spec_sheet.png` | 진짜 픽셀아트, 정확히 7× 확대 | (참고용으로만 남김 — 사용 안 함) |
| `신발자료/신발130개.png` | 렌더 일러스트(런렝스=1, 중간톤 49.6%) | 사용 안 함 |
| **`신발자료/newdesign.png`** | 픽셀아트풍 렌더(중간톤 56%) — 원본 그대로는 도트 아님 | **✅ 최종 소스** — `tools/slice-newdesign.mjs` 가 축소+14색 양자화로 도트화. 티어는 화려함 점수 자동 랭킹 |
| `백그라운드건물/*.png` (16장) | 렌더 일러스트 + 설명 포스터 | 패널 크롭 → 축소 → 양자화 |
| `200층이상배경/*.png` | 렌더 일러스트 + 설명 포스터, 규격이 180×**320** | 패널 크롭 필요 |

**남은 미정값**
- 커스텀 도메인 사용 여부 → M8에 필요

**콘솔에서 켜야 하는 것** (코드는 준비 완료, 이게 안 되면 로그인만 실패한다)
- Authentication > Sign-in method > **Google 사용 설정**
- Authentication > Settings > 승인된 도메인에 **`ianworld-shoes.vercel.app`** 추가
- Firestore 데이터베이스 생성 (위치 **`asia-northeast3`**) + `docs/FIREBASE_RULES.md` 규칙 붙여넣기
- Vercel 환경변수에 `VITE_FIREBASE_*` 7개 등록 (없으면 배포본은 게스트 모드로 돈다)

**다음 작업**: `src/config/balance.js` · `layout.js` 초안 작성 → M0 (Vite + Vercel + 180×320 캔버스).

---

## 9-0. 2026-08-15 기획 수정 11건

- **일시정지에 `로비로나가기` 추가.** 로비로 빠져나갈 길이 아예 없었다. 버튼이 3개가 되면서
  입력(좌/우 둘뿐)이 모자라 **왼쪽=커서 이동 / 오른쪽=선택**으로 나눴다. 터치는 직접 누른다.
- **인게임 영문 전면 한글화.** SCORE→계단수, BEST→최고계단, SHOES→신발획득, HOME→로비로나가기,
  PAUSE→일시정지, GO!→재개, RETRY→맵바꾸기, GAME OVER→게임오버, SELECT→선택.
  게임오버 버튼은 가로 2개(48px)로는 '로비로나가기'(72px)가 안 들어가 **세로 2단(124px)** 으로 바꿨다.
- **`오락실 화면으로 돌아가기` 가 안 먹던 버그.** 닉네임이 있는 재방문자는 main.js가 로비로
  직행시켜 스택에 로비 하나뿐인데 `router.back()` 은 `stack.length <= 1` 이면 아무 일도 안 한다.
  → `nav.reset(Portal)` 로 바꿔 경로와 무관하게 포털로 간다.
- **로고 2종 투입** (`build-logos.mjs`). 10MB 원본 → 21KB/36KB.
- **어려움 난이도 2배.** 캡을 두 배로 올리면 13칸/초라 사람 손으로 불가능하다 →
  **벽에 부딪히는 층수**를 168 → 67층으로 당겨 같은 실력이면 점수가 반이 되게 했다.
- **로비 최고기록**: '칸 계단' → '계단', 숫자만 인게임과 같은 비트맵 폰트 ×3 + 외곽선
  (`screens/pixelText.js` — 비트맵 폰트를 작은 캔버스에 찍어 DOM에 끼운다).
- **도감 총계**가 `0/130` 이던 것 → **보유 켤레 총합**(profile.shoesOwned). 종류 수는 아랫줄로 분리.
  같은 신발을 2켤레 이상 들고 있으면 칸 우측 하단에 `N켤레 보유` (1켤레는 표기 없음).
- **멀티게임은 로그인 필수.** 방·랭킹 규칙이 전부 `auth != null` 이고, 게스트는 uid가 다 `'guest'`
  라 두 사람이 한 사람으로 취급된다. 눌러 보고 막히는 대신 미리 흐리게 + 안내로 바꿨다.

### 지갑을 신발별로 쪼갠 것 (같은 날, 후속)

`N켤레 보유` 를 붙이고 나니 그 숫자가 **도감 누적 획득 횟수**였다. 신발을 쓰면 줄어야 하는데
줄지 않으니 개별 합계가 총 보유량을 넘어선다. 그래서 지갑에 신발별 보유량을 추가했다.

| | 무엇 | 줄어드나 |
|---|---|---|
| `sf_collection[i].count` | 지금까지 **주운 횟수** | ❌ 절대 (기획서 §5-2) |
| `profile.shoesByIndex[i]` | 지금 **들고 있는 켤레** | ⭕ 캐릭터 구매·엘리베이터 |

- **`shoesByIndex` 가 유일한 진실**이고 `shoesOwned` · `shoesByTier` 는 거기서 파생된다.
  `loadProfile()` 이 **읽을 때마다** `reconcile()` 로 다시 만든다 — 쓰기 때만 맞추면
  원격에서 내려온 문서처럼 밖에서 들어온 값이 어긋난 채 화면에 뜬다. 130개 훑기라 공짜다.
- **소비 규칙**: 높은 티어부터(기획서 §5-8), 같은 티어 안에서는 **많이 가진 신발부터**.
  종류를 지키는 쪽이 도감 게임에 맞고, 동점은 index 작은 쪽으로 고정해 결과가 항상 같다.
- **예전 프로필 복원**: 도감 count 는 항상 보유 수 이상이므로, 도감에서 출발해
  `shoesByTier` 에 맞을 때까지 깎으면 원래 지갑이 나온다. `walletVersion` 으로 딱 한 번만 돈다
  (「비어 있으면 복원」으로 하면 신발을 다 쓴 사람에게 없던 신발이 되살아난다).
- **원격 병합**도 신발별 max 하나로 통일했다. 예전에는 `shoesOwned` 만 max 로 합치고
  `shoesByTier` 는 원격 값을 통째로 덮어써서 둘이 어긋날 수 있었다.
- 검증: `npm run qa:wallet` (9개 묶음). 브라우저 없이 도는 순수 계산 테스트다.

## 9-0-2. 2026-08-15 기획 수정 6건 (2차)

- **게스트 폐지.** 이 게임은 멀티플레이가 본체다. 로그인하지 않으면 어떤 화면에도
  못 들어간다. `initAuth` 의 로컬 폴백(닉네임만 있으면 게스트로 통과)도 없앴고,
  `main.js` 도 **계정이 있어야** 로비로 보낸다. Firebase 설정이 없으면 빈 화면 대신
  이유를 적어 준다.
- **터치 판정 = 화면 절반** (`layout.TOUCH`). 48×48 버튼만 받던 시절에는 엄지가 조금만
  빗나가도 입력이 씹혔다. 이제 버튼 그림은 **위치 안내**일 뿐이다.
    y  0~63   전부 일시정지 (계단 수 줄 포함, 그 위 전부)
    y 64~319  x<90 왼쪽 / x>=90 오른쪽
  오버레이는 `setTapHandler(fn, true)` 로 화면을 독점해, 빈 곳을 눌러도 좌우 입력으로
  새지 않는다. 키보드는 ←/A/J · →/D/K/Space · ESC/P/Enter(일시정지), 게임패드는
  스틱·십자키·✕/□·Start 를 받는다(`pollGamepads`, 루프에서 매 프레임 폴링).
- **인게임 전체화면** (`core/fullscreen.js`). 요청은 반드시 **클릭 핸들러 안**에서 해야
  브라우저가 받아 준다 — `await` 뒤로 미루면 제스처 컨텍스트를 벗어나 거절당한다.
  iOS 사파리는 API 자체가 없어 조용히 넘어간다(홈 화면 추가 = PWA로 같은 효과).
- **죽으면 로비로 못 나가던 버그.** 일시정지 '맵바꾸기'가 `onFinish` 없는 새 GameScene 을
  만들어서, 그 뒤로는 `leave()` 가 아무 일도 하지 않았다. 폴백 생성 경로를 지우고
  항상 `leave('retry')` 를 타게 했다. `leave()` 는 이제 onFinish 가 없으면 콘솔에 남긴다.
- **닉네임 2~5자** (기존 2~4자).
- **뱃지 진열대 2칸** (`data/badges.js`, `screens/pixelBadge.js`).
  1칸 도감완성 — **지금 들고 있는 종류**가 130종일 때만. 신발을 써서 한 종류라도
  0켤레가 되면 그 순간 빠진다(그래서 도감 기록이 아니라 지갑에서 센다).
  2칸 계단 — 500~5,000 열 단계 중 넘긴 최고 하나. 최고기록은 줄지 않아 안 뺏긴다.
  뱃지 한 변 **52px**는 계산값이다: 글자 두 줄이 중앙에 놓이면 바깥 줄 끝이 중심에서
  ~10.5도트 떨어지고, 그 높이의 안쪽 폭이 `2·√(r²−10.5²)` 다. 가장 긴 `1,000`(36도트)이
  들어가려면 r ≥ 22 → 테두리·엠보싱 4도트를 더해 52. 더 줄이면 숫자가 테두리를 뚫는다.
  진열대는 **세로 2단**이다 — 가로로 두면 좁은 화면에서 기록 줄이 '최고기 / 록' 으로 쪼개진다.
- **도감 표기 2줄**: `총 신발 갯수 000개` / `신발 종류 도감 0/130개 완성`.

## 9-0-3. 2026-08-15 기획 수정 8건 (3차)

- **로그아웃**을 포털(오락실 화면)에 추가. 인게임에 두면 판 도중에 눌리고 로비는 이미 버튼이 많다.
- **닉네임 입력칸**: 자간 6px → 0, `○○` 자리표시자 제거(빈 칸 + 커서만 깜박인다).
- **버튼 스킨을 게이지바 아트로 통일** (`screens.css`). 색·구조 모두 `build-hud-ui.mjs` 가
  원본에서 뽑은 것과 같다: 2도트 외곽선 → 크림 면 → 위 하이라이트 / 아래 그늘.
  모서리는 **십자 그림자 4장**으로 만든다 — `border-radius` 는 안티앨리어싱이 걸려 도트가 죽는다.
- **캐릭터 얼굴 투명 구멍 수리** (`slice-characters.mjs`의 `fillSpeckles`).
  7×7 블록 중심 한 점만 집는 방식이라, 머리카락 삐침처럼 얇은 선이 지나가는 칸은 중심이
  하필 배경색일 때가 있다. 그 칸만 투명해져 인게임에서 배경이 비쳤다(30장 합계 495점).
  이웃 색 **평균**으로 메운다 — 얼굴색 한 가지로 채우면 검은 머리 한가운데 살구색 점이 박힌다.
  판정은 1패스 `이웃 5개 이상`, 2·3패스 `6개 이상`. 계속 5로 반복하면 겨드랑이·다리 사이를
  타고 번져 실루엣이 부풀어 오른다(실측: 5로 8패스 = 1102점, 상당수가 몸 바깥).
- **로비 통계 3줄**: 최고기록(숫자만 위로 3px) / 나의 신발 보유량 / 나의 신발 도감.
- **닉네임 변경 유료화** (`screens/NicknameChange.js`, `NICKNAME.changeCost = 200`).
  최초 설정은 계정을 만들려면 반드시 거치는 단계라 공짜지만, 변경은 선택이다.
  값이 없으면 랭킹에서 이름만 바꿔 가며 도망칠 수 있다.
  신발 확인을 중복 검사보다 **먼저** 한다 — 몇 초 기다린 뒤 부족하다고 하면 짜증난다.
- **도감 후반 난이도** (`DEX_LATE_GAME`). 종류를 100종 모은 뒤부터 1·2티어 등장 확률 ×0.2.
  가중치는 `Stairs` 생성자로 **밖에서 넣는다** — 여기서 각자의 도감을 읽으면
  멀티플레이에서 두 사람이 다른 신발을 보게 된다.
- 문구: 맵바꾸기 → 게임재개.
  ※ 일시정지 메뉴가 `재개`(이어하기) / `게임재개`(새 판)로 비슷해졌다 — 확인 필요.

## 9-0-4. M6 명예의 전당 (2026-08-15)

- 화면 `screens/HallOfFame.js` — 1차 탭 신발왕/주간/월간/연간/역대,
  2차 탭 쉬움·보통·어려움(신발왕 제외, 기본값 = 마지막에 플레이한 난이도),
  상위 100명 + **하단에 내 순위 고정**.
- 서비스 `services/leaderboard.js` — **집계 서버 없이 클라이언트 쿼리만으로** 돈다.
  기획서는 Cloud Functions 가 `leaderboards/*` 를 굽는 그림이었지만 유료 요금제가 필요하다.
  - 신발왕·역대 → `users` 를 바로 정렬. 계정당 한 줄이라 중복이 없다.
  - 주간·월간·연간 → `scores` 원장. 제출할 때 기간 키(`wk`·`mo`·`yr`)를 **문자열로 박아 둔다**.
    `createdAt >= 시작` 같은 부등호를 쓰면 Firestore 가 그 필드로 먼저 정렬하라고 요구해
    "계단 수 상위"를 못 뽑는다. 등호 비교면 계단 수로 바로 정렬된다.
  - 한 사람이 목록을 도배하지 않게 300개를 받아 uid로 접고 상위 100만 남긴다.
- **오프라인은 빈 목록과 다르다.** 네트워크가 없으면 Firestore 는 예외를 던지지 않고
  로컬 캐시로 답한다. 그대로 그리면 연결이 끊긴 사람에게 "아직 기록이 없습니다"라고
  거짓말을 하게 된다 → `snap.metadata.fromCache && snap.empty` 를 연결 실패로 구분한다.
- 제출은 **큐에 먼저 넣고 올린다**. 올리고 나서 큐에 넣으면 그 사이에 앱이 닫혔을 때
  기록이 통째로 사라진다. 실패분은 큐에 남아 다음 로그인에 다시 올라간다.
- 복합 색인 6개가 필요하다 — `docs/FIREBASE_RULES.md` 하단 참고.
  탭을 한 번씩 눌러 보면 콘솔에 생성 링크가 찍힌다.
  ※ 아래 §9-0-5 로 원장을 바꾸면서 **3개로 줄었다.** 색인 6개는 이미 만들어져 있고
    남은 3개는 쓰이지 않을 뿐이라 지우지 않았다.

## 9-0-5. 원장을 "판마다 한 장"에서 "기간 최고기록 한 장"으로 (2026-08-15)

**계기는 질문 하나였다.** "닉네임을 바꾸면 이미 올라간 기록은 어떻게 되나?"
답이 나빴다 — 신발왕·역대 탭은 `users` 를 읽어 새 이름이 바로 뜨는데,
주간·월간·연간은 `scores` 문서에 박아 둔 이름을 그대로 보여 줘서 **옛 이름이 남았다.**
게다가 하단 '내 순위'는 로컬 프로필을 읽어 새 이름이라, 같은 화면에서 이름이 둘로 갈렸다.
200켤레를 내고 바꾼 이름이 순위표에 반영이 안 되면 산 게 아니다.

고치는 방법이 둘이었다. 이미 올라간 내 기록을 전부 찾아 이름만 갈아 끼우거나,
애초에 고칠 문서가 적어지게 원장을 바꾸거나. **후자를 골랐다** — 판마다 한 장씩 쌓으면
많이 한 사람은 수백 장이라 한 번의 개명이 무료 쓰기 한도를 통째로 먹는다.

`scores` 문서 하나 = **한 사람의 그 기간·그 난이도 최고기록**, ID는 `uid_난이도_기간키`.

| | 예전 (판마다 한 장) | 지금 (기간마다 한 장) |
|---|---|---|
| 개명 시 고칠 문서 | 수백 장 (사실상 불가) | **최대 9장** (난이도 3 × 기간 3) |
| 순위표 1회 조회 | 300건 읽고 uid로 접기 | **100건**, 접을 필요 없음 |
| 내 순위 (100위 밖) | 쿼리 + 색인 | **문서 하나 읽기**, 색인 불필요 |
| 필요한 복합 색인 | 6개 | **3개** |
| 판당 쓰기 | 1건 | 최대 3건, **최고 기록을 깼을 때만** |

- **ID를 못 박은 게 규칙의 핵심이다.** 자유롭게 두면 한 사람이 문서를 여러 장 만들어
  순위표를 도배할 수 있다. 규칙이 `uid + '_' + difficulty + '_' + key` 를 다시 계산해 대조한다.
- **덮어쓰기는 점수가 내려가지 않을 때만** (`stairs >= resource.data.stairs`).
  클라이언트를 조작해도 자기 기록을 낮출 수 없다. 같은 값을 통과시키는 건
  이름·캐릭터만 고치는 갱신(`leaderboard.syncIdentity`)이 지나가야 하기 때문이다.
- **기간 필드는 문서마다 하나만** (`wk` 또는 `mo` 또는 `yr`). 셋을 다 넣으면 주간 색인에
  연간·월간 문서까지 실려서 읽을 일 없는 문서를 훑는다. 없는 필드는 색인에 안 실린다.
- `sf_periodBest` 는 **헛쓰기를 막는 로컬 힌트**다. 이게 없으면 판이 끝날 때마다 세 장을
  무조건 다시 써서 대부분 규칙에 막히며 쓰기 한도만 먹는다. 다른 기기가 더 높은 기록을
  올렸다면 값이 낮게 남지만, 그때는 규칙이 막아 주므로 결과가 틀어지지는 않는다.
  규칙에 막히면 **서버 값이 내 점수보다 확실히 크므로**(규칙은 `>=` 만 통과) 하한을
  그만큼 올려 두고 재시도하지 않는다.
- 캐시 솎기는 `id.endsWith('_' + 키)` 로 판정한다. 단순 포함 검사면 `'2026'` 이
  `'2026-W33'` 의 접두사라 서로를 살려 버린다.
- **검증**: `npm run qa:rank` (10개 묶음). 브라우저 없이 도는 순수 계산 테스트다.
  기간 키 계산과 문서 ID 조합을 `src/services/periodKeys.js` 로 떼어 낸 이유가 이것이다 —
  leaderboard.js 는 firebase.js → `import.meta.env` 를 타서 노드에서 불러오는 순간 죽는다.
- 이 시점에 Firestore 는 완전히 비어 있어서(컬렉션 0개) 옮길 데이터가 없었다.
  이미 쌓인 데이터가 있었다면 옛 문서가 새 문서와 함께 조회돼 같은 사람이 두 번 떴을 것이다.

### 폰트 수집기가 주석까지 긁고 있었다 (같이 고침)

`build-font.mjs` 는 문자열 리터럴만 긁는다고 적어 뒀지만, 정규식이 문자열만 찾다 보니
**주석 안에 따옴표로 인용한 말**("이 값은 힌트다" 같은)이 문자열로 잡혔다.
문자열과 주석을 **하나의 교대(alternation)로 함께** 훑고 문자열만 남기게 바꿨다.
주석을 먼저 지우는 방식은 안 된다 — `'https://...'` 의 `//` 부터 줄 끝이 날아간다.
결과: 한글 303자 → **251자**, 번들 −1.3KB. `strings.ko.js` 전수 대조로 누락 0 확인.

## 9-0-6. 부활 50개 · 뱃지 영구화 · 명예의 전당 진단 (2026-08-15)

- **부활은 신발 50개당 1개** (기존 20개). 20개는 조금만 오르면 붙어서 판이 잘 안 끝났다.
  부활은 잘 달린 판에 주는 보상이지 기본 수명이 아니다. `REVIVE.shoesPerRevive` 하나만 고치면 된다.

- **도감완성 뱃지는 한 번 따면 영원하다.** 예전 판정은 "지금 들고 있는 종류가 130종"이라
  캐릭터를 사거나 엘리베이터를 타는 순간 훈장이 사라졌다. 훈장을 지키려고 신발을 못 쓰게
  되니 게임이 거꾸로 돈다. 이제 `profile.dexBadgeAt`(딴 시각) **도장 하나만** 본다.
  - 도장은 도감이 130종에 닿을 때 `commitRun` 에서 찍힌다. 판정 기준을 도감으로 옮긴 것도
    같은 이유다 — 도감은 "주운 적 있는가"라 절대 줄지 않는다(기획서 §5-2).
  - `badgeSlots` 는 도감을 **다시 세지 않는다.** 세는 순간 "지금 상태"에 의존하게 되고,
    데이터가 잠깐 비면 뱃지가 깜빡인다.
  - `ensureDexBadge()` 가 로그인 직후(도감 병합 **뒤에**) 한 번 돈다. 개정 전에 이미
    130종을 채운 사람이 판을 더 돌아야 훈장을 되찾는 건 말이 안 된다.
  - 원격 병합은 **0이 절대 이기지 못한다**(`earliestStamp`). 아직 못 딴 기기에 로그인했다고
    훈장이 사라지면 안 된다. 둘 다 있으면 이른 쪽 — 처음 해낸 시각이 진실이다.

### 명예의 전당이 "작동 안 한다"의 정체

**코드도 색인도 규칙도 정상이었다. `scores` 컬렉션이 그냥 비어 있었다.** 실측:

- 배포본은 새 원장 코드가 맞다 (번들에 `permission-denied`·`updatedAt` 있고
  옛 `createdAt`·`FETCH_MULTIPLIER` 없음).
- 주간 쿼리를 REST로 직접 쳐 봤다 → **200 OK, 결과 0건.** 색인 오류가 아니다.
- `users` 는 비로그인 조회 시 403 → 규칙이 제대로 걸려 있다는 뜻.
- 브라우저 로컬에 `sf_pendingScores` 3건이 **큐에 남아** 있었고 프로필 `uid` 가 `'guest'` 였다.
  게스트 폐지 이전 잔재다. 로그인 상태가 아니면 제출은 전부 큐로 가고 순위표는 비어 있다.

즉 **한 판을 로그인 상태로 끝내면 그때 첫 줄이 생긴다.** 큐에 남은 기록도 로그인 직후
`flushQueued` 가 같이 올린다.

규칙은 콘솔 **규칙 플레이그라운드**로 양쪽을 실측했다:
- 정상 제출(`scores/testuid_hard_2026-W33`, uid=testuid) → **허용**
- 문서 ID 위조(`..._2026-W33-DUP`, 한 사람이 여러 줄 만들기) → **거부**

## 9-0-7. 명예의 전당이 계속 비어 있던 진짜 이유 (2026-08-15)

앞 절에서 "데이터가 없을 뿐"이라고 결론지었는데 **틀렸다.** 로그인한 채로 여러 판을
돌려도 여전히 0건이었다. 다시 파 보니 증상이 훨씬 나빴다:

**Firestore 에 문서가 단 한 개도 없다.** 순위표(`scores`)뿐 아니라 **`users` 계정 문서도**
없다. 그런데 Authentication 에는 계정 3개가 멀쩡히 있고 최근 로그인 기록도 있다.
`users` 는 난이도를 바꿀 때마다, 판이 끝날 때마다 쓰는 문서다. 그게 없다는 건
**클라이언트에서 나가는 모든 쓰기가 서버에 닿은 적이 없다**는 뜻이다.

서버 쪽은 전부 정상이었다(실측): 규칙 플레이그라운드에서 정상 제출 **허용** ·
문서 ID 위조 **거부**, 색인 6개 사용 설정됨, REST 로 읽기 200, 비인증 쓰기는
`PERMISSION_DENIED`(= 규칙이 살아 있다), 번들에 설정값 6개 정상.

### 왜 아무도 몰랐나 — 쓰기 실패가 **예외가 아니다**

Firestore 웹 SDK 의 쓰기 프라미스는 **서버에 닿아야 resolve** 한다.
못 닿으면 reject 가 아니라 **영원히 pending** 이다. 이 저장소의 모든 원격 쓰기는
`pushRemote(...).catch(() => {})` 처럼 실패를 삼키게 되어 있는데(§6-4),
애초에 거절이 안 오니 `catch` 가 걸릴 일도 없다. 그래서:

- 화면은 완벽히 정상으로 보인다 (로컬이 원본이므로)
- 큐에 넣고 재시도하는 장치가 **통째로 무력해진다** — `flushQueued` 가 매달린 채 끝나지 않는다
- 콘솔에 아무것도 안 찍힌다
- **로그인(Auth)은 평범한 HTTPS라 멀쩡히 된다** — 이게 제일 헷갈리는 지점이다

### 고친 것 세 가지

1. **`initializeFirestore(app, { experimentalAutoDetectLongPolling: true })`**
   (`getFirestore()` 대신). Firestore 는 REST 가 아니라 **WebChannel(스트리밍)** 으로 붙는데,
   이 경로는 광고 차단 확장·회사망·일부 통신망 중간장비에서 조용히 막히는 일이 잦다.
   막히면 위에 적은 "영원히 pending"이 그대로 재현된다. 자동 감지를 켜면 롱폴링으로 갈아탄다.
   정상 환경에서는 동작이 같으므로 켜 두는 쪽이 손해가 없다.

2. **모든 원격 쓰기에 시한** (`withTimeout`, 12초). 시한이 지나면 거절해서
   "실패했다"는 사실이 비로소 존재하게 된다 — 그래야 큐·재시도·로그가 작동한다.
   SDK 는 그 뒤에도 자기 대기열에 들고 있다가 연결이 돌아오면 마저 보낸다.
   문서 ID가 고정이라 두 번 써도 결과가 같아서 안전하다.

3. **세션 복원 시에도 서버와 맞춘다** (`main.js`). `pullAll()` 은 SplashLogin 의
   구글 버튼을 **직접 누른 경우에만** 돌았다. 두 번째 방문부터는 세션이 남아 로그인 화면을
   건너뛰고 로비로 직행하므로 **영영 안 돌았다.** 큐에 쌓인 기록이 다시 올라갈 기회를
   잃는 경로가 이것이다.

### `await __dbg.selftest()`

`services/diagnose.js`. 브라우저 콘솔에서 부르면 설정값 → 로그인 → Firestore 연결 →
계정 문서 쓰기 → 밀린 기록 제출 → 순위표 조회를 순서대로 걸어 보고 ✅/❌ 로 찍는다.
쓰레기 데이터를 남기지 않는다(점수를 지어내지 않고, 큐에 있던 것만 올린다).
**모든 쓰기가 실패를 삼키는 설계에서는 이런 통로가 없으면 원인을 알 방법이 없다.**

## 9-0-8. 범인은 `getRedirectResult` 였다 (2026-08-15)

§9-0-7 에서 "전송이 막혔을 것"이라고 짐작했는데 그것도 **틀렸다.** 끝까지 파서 잡았다.

### 어떻게 좁혔나 (추측 말고 계측)

| 확인한 것 | 결과 | 배제한 가설 |
|---|---|---|
| 규칙 플레이그라운드 (정상 제출 / ID 위조) | 허용 / 거부 | 규칙 |
| 복합 색인 6개 | 전부 사용 설정됨 | 색인 |
| REST 읽기 · 비인증 쓰기 | 200 / `PERMISSION_DENIED` | 서버·규칙 적용 |
| **CDN Firestore SDK 로 실제 쓰기 시도** | **225ms 만에 `permission-denied`** | WebChannel 전송·롱폴링 |
| 배포 번들의 설정값 | apiKey·projectId·appId 정상 | Vercel 환경변수 |
| 로그인 버튼 `disabled` | `false` (= `configured()` 참) | 설정 누락 |
| `import('/assets/firebase-store-*.js')` | 성공(119 exports) | 청크 로드·서비스워커 |
| **Firestore 사용량 (지난 24시간)** | **읽기 0 · 쓰기 0** | ← 여기서 결론이 났다 |

**클라이언트가 Firestore에 요청을 한 번도 보낸 적이 없다.** 서버는 멀쩡한데
아무도 두드리질 않았다. 그러면 남는 건 하나뿐이다 — **로그인이 완성된 적이 없다.**

### 진짜 원인

`auth.js` 는 팝업이 막히면 `signInWithRedirect` 로 넘어간다. 그런데
**`getRedirectResult()` 호출이 저장소 어디에도 없었다.** (`grep` 으로 확인: 0건)

리다이렉트 로그인은 구글에서 돌아온 뒤 그 결과를 거둬야 완성된다. 안 거두면
화면은 넘어가는데 계정이 안 붙는다. 그리고 **모바일 브라우저는 팝업을 거의 항상
막으므로 이 경로가 기본**이다 — 즉 모바일 로그인이 통째로 미완성으로 끝나고 있었다.

방증도 딱 맞았다: 배포본 로컬 프로필의 `uid` 가 아직 `'guest'` 였다.
`patchProfile({uid})` 는 **팝업 경로에서만** 불렸기 때문이다.

### 고친 것

- **`initAuth` 가 `getRedirectResult()` 를 먼저 거둔다.** 이게 핵심 수정이다.
- **`adopt()` 하나로 통일** — 팝업·리다이렉트·세션 복원 세 경로가 전부 여기로 모이고,
  여기서만 `patchProfile` 을 부른다. 경로마다 따로 처리하다 한쪽을 빠뜨린 게 화근이었다.
- 팝업 실패 코드를 넓혔다(`cancelled-popup-request`·`web-storage-unsupported`).
  사용자가 창을 직접 닫은 `popup-closed-by-user` 는 실패가 아니므로 조용히 제자리.
- **`_lint-services.mjs` 에 짝 검사 추가** — `signInWithRedirect` 를 쓰면서
  `getRedirectResult` 를 안 부르면 빌드 전에 잡는다. (일부러 지워서 잡히는 것까지 확인했다)
- **순위표가 왜 비었는지 구분해서 말한다** (`fetchBoard` 가 `error: 'auth'|'offline'|'failed'`
  를 함께 돌려준다). 예전에는 셋 다 `null` 이라 화면이 전부 "아직 기록이 없습니다"로
  거짓말을 했다 — 이 한 줄 때문에 원인 추적이 며칠 늦어졌다.

### 교훈

**"조용히 실패"를 설계 원칙으로 삼으면(§6-4) 반드시 진단 통로를 같이 만들어야 한다.**
로컬 우선 설계 자체는 옳다. 하지만 실패가 전부 삼켜지면 "서버에 아무것도 없는데
화면은 멀쩡한" 상태가 몇 날이고 유지된다. 그래서 §9-0-7 의 `__dbg.selftest()` 와
쓰기 시한(`withTimeout`), 그리고 이 절의 짝 검사를 같이 남긴다.

## 9-0-9. 로그인이 죽은 진짜 자리 — 출처가 다르면 세션이 증발한다 (2026-08-15)

§9-0-8 에서 `getRedirectResult()` 누락을 고쳤는데도 여전히 안 됐다. **부를 게 없었다.**

### 통제된 실험

같은 코드·같은 계정·같은 브라우저로, **출처만 바꿔** 리다이렉트 로그인을 끝까지 돌렸다.

| 실행 출처 | `getRedirectResult()` | `onAuthStateChanged` |
|---|---|---|
| `find-shoes-f5c55.firebaseapp.com` (= authDomain) | **실제 계정 반환** | 계정 있음 |
| `ianworld-shoes.vercel.app` (배포 주소) | **null, 오류 없음** | null, IndexedDB 비어 있음 |

원인은 하나로 좁혀진다: **로그인 핸들러가 우리 사이트와 다른 사이트다.**
요즘 브라우저는 사이트가 다르면 저장소를 칸막이로 나눈다(third-party storage
partitioning). 구글이 돌려준 자격증명이 `firebaseapp.com` 칸에 갇혀서, 우리 도메인에서는
꺼낼 수가 없다. **예외가 아니라 `null` 이라 아무도 알아채지 못한다.**

앞의 모든 관측이 이 하나로 설명된다 — Auth 사용자 3명(PC 팝업 로그인), Firestore 문서 0개,
읽기·쓰기 0건, 프로필 `uid` 가 `'guest'` 에 머묾. 팝업이 막히는 **모바일은 이 경로가
기본**이라 모바일 로그인은 통째로 죽어 있었다.

### 고친 것

- **헛걸음 금지** — 리다이렉트가 빈손으로 돌아올 환경이면 아예 태우지 않고
  `팝업을 허용해 달라`고 말한다. 구글까지 갔다가 아무 일 없이 돌아오는 것보다 낫다.
- **`vercel.json` 에 `/__/auth/*` 프록시** — 켜면 로그인 핸들러가 같은 출처가 되어
  칸막이가 사라진다. 기존 SPA 리라이트가 `/__/auth/` 까지 index.html 로 삼키고 있어서
  그 예외도 같이 넣었다.
- `VITE_FIREBASE_SELF_AUTH=1` **опt-in** — 켜기 전에 Google Cloud OAuth 클라이언트에
  `https://<호스트>/__/auth/handler` 를 등록해야 한다. 안 하면 로그인이 아예 막히므로 기본 꺼짐.

### 파이프라인 실측 (2026-08-15, 실제 계정으로)

로그인이 되기만 하면 나머지는 전부 정상이라는 걸 실계정 세션으로 확인했다.

| 검수 1 — 동작 | 결과 |
|---|---|
| 계정 문서 쓰기 / 기록 제출 | OK |
| 주간 탭 쿼리(색인 사용) | 1줄 반환 |
| 내 순위(문서 하나 읽기) | OK |
| 신발왕 · 역대 탭 | OK |

| 검수 2 — 방어 | 결과 |
|---|---|
| 점수 상승 덮어쓰기 | 허용 |
| **점수 하락 시도** | 거부 |
| **문서 ID 위조(도배)** | 거부 |
| **남의 uid 로 제출** | 거부 |
| **남의 계정 문서 수정** | 거부 |
| 이름만 갱신(syncIdentity) | 허용 |
| **기록 삭제 시도** | 거부 |

## 9-0-10. 로그인 완전 복구 — 실전 검증 완료 (2026-08-15)

§9-0-9 의 처방을 끝까지 적용하고 **배포본에서 직접 재현해 확인**했다.

### 콘솔 작업 (완료)

- **Google Cloud → OAuth 클라이언트** `9232620026-grk44t...`
  - 승인된 리디렉션 URI 에 `https://ianworld-shoes.vercel.app/__/auth/handler` **추가**
  - 승인된 JavaScript 원본에 `https://ianworld-shoes.vercel.app` **추가**
  - ⚠ 기존 `find-shoes-f5c55.firebaseapp.com` 항목은 **지우지 않고 남겼다.**
    지우면 예전 경로가 즉시 끊긴다. 이 화면은 접근성 트리에 값이 안 보이고
    자리표시자만 보이므로, **반드시 스크린샷으로 기존 값을 확인한 뒤 `URI 추가`** 로 늘려야 한다.
    빈 칸인 줄 알고 덮어쓰면 전체 로그인이 죽는다.
- **RTDB 규칙 게시** (M7 rooms/userRooms)
- 프로젝트 소유자는 `info@nivs.com` **한 명**뿐이다. 다른 계정으로 Cloud Console 에
  들어가면 프로젝트가 아예 안 보인다.

### 실전 검증 — 같은 출처, 같은 코드, authDomain 만 다름

| authDomain | `getRedirectResult()` (ianworld-shoes.vercel.app 에서 실행) |
|---|---|
| `find-shoes-f5c55.firebaseapp.com` | **null** (오류도 없음) |
| `ianworld-shoes.vercel.app` (+프록시+OAuth URI) | **실제 계정 반환** |

이걸로 원인·처방이 둘 다 증명됐다. 그래서 `resolveAuthDomain()` 의 **기본값을 켜짐으로**
바꿨다 — Vercel 환경변수를 따로 넣을 필요가 없다.

### 새 도메인을 붙일 때 반드시 할 것

커스텀 도메인을 연결하면 authDomain 도 그 도메인이 된다. **먼저** ① Firebase 승인된 도메인
② OAuth 리디렉션 URI `https://<새도메인>/__/auth/handler` ③ `vercel.json` 프록시 —
셋을 해 두지 않으면 그 도메인에서 로그인이 통째로 막힌다. 급하면 `VITE_FIREBASE_SELF_AUTH=0`.

## 9-0-11. 순위표 이름이 `???`, 그리고 10초 지연 (2026-08-15)

### 이름이 빈 채로 박제되던 경로

`pullRemote()` 의 병합이 `{ ...local, ...remote }` 라 **원격이 무조건 이긴다.**
계정 문서는 **첫 로그인 시점**에 만들어지는데 그때는 아직 닉네임을 정하기 전이라
`nickname: ''` 로 박힌다. 그 뒤 로그인할 때마다 그 빈 값이 로컬 닉네임까지 지웠고,
`submitScore` 는 그 빈 이름을 그대로 기록에 실었다 → 순위표 전체가 `???`.

고친 것 셋:
- `pickName()` — **빈 값은 절대 이기지 못한다.** 원격이 비면 로컬을 쓴다.
- `finishRun` 이 **매 판마다 이름·캐릭터를 같이 올린다.** 예전에는 `saveNickname`
  한 곳만 이 필드를 썼는데, 그 한 번이 실패하면(로그인이 깨져 있던 동안 실제로 그랬다)
  영영 빈 채로 남았다. 이제 판이 끝날 때마다 저절로 복구된다.
- `pullAll` 이 로컬에 이름이 있으면 원격으로 밀어 올린다 — 이미 박제된 계정 구제용.

### 10초 지연 — 원인 셋

1. **`experimentalAutoDetectLongPolling` 을 뺐다.** §9-0-7 에서 "WebChannel 이 막혔다"는
   가설로 켰는데 **그 가설은 틀렸고**(진짜 원인은 로그인, §9-0-9), 자동 감지는 첫 연결에서
   스트리밍 실패를 **기다려 보는** 단계를 넣어 멀쩡한 회선에서도 첫 조회를 늦춘다.
   없어진 문제를 위한 대비를 남기고 매번 값을 치를 이유가 없다.
2. **`fetchBoard` 가 '내 순위' 조회를 기다렸다가 목록을 돌려줬다.** 목록은 이미 손에
   있는데 조회 한 번을 더 기다린 것이다 → `mePromise` 로 분리해 목록을 먼저 그린다.
3. **탭 캐시.** 탭을 옮길 때마다 다시 조회했다. 화면이 살아 있는 동안은 재사용한다.

### 곁다리 — 접속할 때 RTDB(192KB)를 모두가 받고 있었다

M7 의 `sweepUnsettled()` 를 부팅에 걸면서 싱글만 하는 사람도 RTDB SDK 를 받게 됐다.
`everPlayedMulti()` 로 **멀티에 들어간 적이 있을 때만** 붙는다.

## 9-0-12. M8 PWA — 껍데기만 있고 아무도 안 켰다 (2026-08-15)

"PWA 셸"은 M0에 체크돼 있었지만 **실제로 동작한 적이 한 번도 없었다.** 셋 다 조용히 죽어 있었다.

### (1) 아이콘 폴더가 통째로 없었다

`manifest.webmanifest` 도 `index.html` 도 `/icons/icon-192.png` 를 가리키는데 그 파일이 없었다.
크롬은 **쓸 수 있는 아이콘이 하나도 없으면 설치 조건 자체를 불충족으로 본다** →
`beforeinstallprompt` 가 영영 안 온다 → 스플래시의 "바로가기 아이콘 만들기" 버튼은 항상
iOS 안내 토스트로 빠진다. 안드로이드에서 설치가 안 되던 이유가 이것이다.

`tools/build-icons.mjs` 로 게임 에셋에서 굽는다 (돌계단 + MAXIMAL 3번 신발).
**논리 64×64 → 192(×3) · 512(×8)** 정수배라 nearest 확대에서 도트가 안 뭉갠다.
64의 배수가 아닌 크기를 넣으면 스크립트가 거부한다.
maskable 은 따로 그린다 — 안드로이드가 원·물방울 모양으로 **잘라내기** 때문에
안전 반지름(논리 25.6) 밖은 없는 셈 치고 신발만 가운데 놓는다.
`npm run build` 에 넣었으므로 png 가 커밋에서 빠져도 배포본에는 반드시 존재한다.

### (2) `serviceWorker.register` 가 코드 어디에도 없었다

`public/sw.js` 는 처음부터 있었는데 **부르는 사람이 없었다.** 캐시도 오프라인도 전부 없던 셈.
`services/pwa.js` 신설 — 등록 + 설치 프롬프트 + 갱신 확인을 한곳에서 맡는다.
개발 서버에서는 등록하지 않고 오히려 지운다(캐시가 HMR을 먹는다).

`beforeinstallprompt` 를 **`SplashLogin.js` 모듈 안에서** 듣고 있던 것도 고쳤다.
이 이벤트는 부팅 직후 한 번 오고 다시 오지 않는데, 화면이 아니라 앱이 들어야 한다.
그리고 이벤트는 화면을 그린 뒤에 도착하므로(보통 1초쯤) `onInstallChange` 로 구독해
도착하면 버튼을 다시 그린다. 설치가 불가능한 브라우저에서는 **버튼을 아예 감춘다** —
눌러도 아무 일 없는 버튼은 고장으로 보인다.

### (3) `VERSION = 'v1'` 이 코드에 박혀 있었다

배포를 몇 번을 해도 캐시 이름이 `shell-v1` 이라 낡은 셸이 지워질 일이 없었다.
지금은 `sw.js?v=<빌드ID>` 로 등록하고 워커가 자기 URL 에서 그 값을 읽는다.
빌드 ID 는 `vite.config.js` 의 define — 버셀에서는 커밋 해시, 로컬은 시각.

### 캐시를 셋으로 나눈 이유 (이번 설계의 핵심)

| 캐시 | 대상 | 전략 | 이유 |
|------|------|------|------|
| `shell-<빌드ID>` | HTML, manifest | 네트워크 우선(캐시 있으면 3.5초 제한) | 낡으면 안 된다 |
| `build` (버전 없음) | `assets/*.js`·`css` (해시 이름) | 캐시 우선, **배포를 넘겨 보존** | 배포 직후 열려 있던 옛 화면이 그제서야 `firebase-rtdb-….js` 를 부른다. 지우면 그 자리에서 멈춘다 |
| `art` (버전 없음) | `assets/{characters,shoes,bg,ui}/`, `icons/` | stale-while-revalidate | 파일명이 고정이라 **도트를 덮어써도 URL이 그대로**다. 캐시 우선이면 새 그림이 영영 안 뜬다 |

`vercel.json` 의 Cache-Control 경계와 **정확히 같다.** 한쪽만 고치면 어긋난다.

### 첫 방문에는 캐시가 비어 있다 (놓칠 뻔한 것)

워커가 통제를 잡기 전에 브라우저가 JS·CSS 를 이미 다 받아 버려서, 설치는 됐는데
`build` 캐시가 **비어 있었다.** 그런데도 오프라인 검사는 통과했다 — 브라우저 HTTP 캐시가
대신 답했기 때문이다. 검사에서 HTTP 캐시를 끄고 나서야 드러났다.

- 설치 시 `index.html` 을 읽어 거기 적힌 번들을 직접 담는다(`precacheBuild`)
- 앱이 `performance.getEntriesByType('resource')` 목록을 워커에 보내 빠진 것만 채운다
  (`postMessage({type:'warm'})`) — 파이어베이스 청크처럼 index.html 에 안 적힌 것들이 이 통로로 들어온다

### 검수 — `npm run qa:pwa` (반드시 `npm run build` 뒤에)

`tools/_pwa-qa.mjs` 가 dist 를 실제 서버로 띄우고 크로미움으로 검사한다. 27항목.
아이콘 HTTP 200 · 워커 통제 · 캐시 3분리 · **HTTP 캐시를 끈 채 오프라인 부팅** ·
새 배포 시 옛 셸 삭제 + **옛 번들 보존**까지 확인한다.

---

## 9-0-13. 진짜 범인은 **줄바꿈 한 글자**였다 (2026-08-15)

M8 배포본을 크롬에서 검수하다가 찾았다. 로그인은 되는데(`info@nivs.com`, uid 확인)
명예의 전당은 여전히 "네트워크 연결을 확인해주세요". 콘솔에는 우리 타임아웃만 찍혔다:

```
[로그인] 실패 deadline-exceeded  계정 읽기 응답 없음 (12000ms)
```

### 계측

네트워크 탭에서 파이어스토어 요청 URL 을 그대로 읽었다.

```
앱  : .../Listen/channel?...&database=projects%2Ffind-shoes-f5c55%0A%2Fdatabases%2F(default)
                                                              ^^^ 여기
검증: .../Listen/channel?...&database=projects%2Ffind-shoes-f5c55%2Fdatabases%2F(default)
```

`%0A` = **줄바꿈**. 프로젝트 ID 가 `"find-shoes-f5c55\n"` 이었다.
핸드셰이크 POST 는 200 으로 통과하고, 그 다음 스트리밍 GET(`TYPE=xmlhttp`)이
**503** 으로 끊긴다 → SDK 는 오류를 던지지 않고 그냥 응답을 안 준다 → 12초 뒤 우리 타임아웃.

번들을 직접 뒤져 확인 사살:

```
find-shoes-f5c55\xa`,sto      ← 템플릿 리터럴 끝에 \n
```

같은 페이지에서 CDN SDK 로 projectId 만 손으로 적어 같은 질의를 던지니 **160ms 에 성공**하고
문서도 실제로 들어 있었다(`..._easy_2026-W33`). 전송 방식 문제도, 규칙 문제도, 색인 문제도 아니었다.

### 왜 아무도 못 찾았나

- 콘솔·환경변수 화면·`selftest()` 어디에도 `find-shoes-f5c55` 로 **멀쩡하게** 찍힌다.
  줄바꿈은 눈에 보이지 않는다.
- 인증은 멀쩡했다. 로그인은 `apiKey`·`authDomain` 만 쓰고 **projectId 를 안 쓴다.**
  그래서 "로그인은 되는데 저장만 안 되는" 그림이 나왔고, 계속 인증 쪽을 의심하게 만들었다.
- 파이어스토어 쓰기는 실패해도 예외를 안 던진다(§9-0-7). 죽은 걸 알 방법이 없었다.
- 앞서 "Firestore 사용량 읽기 0 / 쓰기 0" 이었던 것도 이걸로 전부 설명된다.
  요청이 잘못된 데이터베이스 경로로 나갔으니 사용량에 잡힐 리가 없다.

출처는 버셀 대시보드에 값을 붙여 넣을 때 딸려 들어간 줄바꿈으로 보인다.

### 고친 것 (세 겹)

1. **`vite.config.js`** — 굽기 전에 `VITE_*` 환경변수 앞뒤 공백을 잘라 낸다. 잘라내면 빌드 로그에 경고.
2. **`services/firebase.js`** — 런타임에도 모든 설정값을 `trim()` 한다(.env 파일 경로 대비).
   다듬어야 했으면 콘솔 경고, `projectId` 가 `^[a-z0-9-]+$` 를 벗어나면 `console.error`.
3. **`selftest()`** — `projectId 모양` 단계 추가. **따옴표째** 출력하므로 공백이 눈에 보인다.

검증: `VITE_FIREBASE_PROJECT_ID=$'find-shoes-f5c55\n'` 로 일부러 빌드 →
경고가 뜨고 번들에는 깨끗한 값이 박힌다(줄바꿈 0건).

### 교훈

**환경변수는 문자열이 아니라 "사람이 붙여 넣은 것"이다.** 값이 맞는지만 보지 말고 모양도 봐야 한다.
그리고 SDK 가 조용히 멈추면 콘솔이 아니라 **네트워크 탭의 URL 을 통째로** 읽는다 — 이번 건은
로그를 아무리 봐도 안 나오고 URL 한 줄에 답이 있었다.

### 곁다리 — API 키에도 줄바꿈이 있었다 (한 번은 다시 로그인해야 한다)

`VITE_FIREBASE_API_KEY` 도 같은 상태였다. 파이어베이스는 로그인 세션을
`firebase:authUser:<apiKey>:[DEFAULT]` 라는 키로 저장하는데, 예전 배포는 **키에 줄바꿈이 붙은
이름**으로 저장해 뒀다. 이번 배포는 깨끗한 이름으로 찾으므로 그 세션을 못 읽는다.
즉 **기존 사용자는 배포 직후 한 번 로그아웃된 것처럼 보인다.** 다시 로그인하면 끝이고,
그 뒤로는 정상적으로 유지된다(실측 확인). 로컬 기록은 그대로 남아 있고 서버 기록도 다시 당겨진다.

### 실전 최종 검수 (2026-08-15, 배포본 · 실제 계정)

| 확인 | 결과 |
|------|------|
| `selftest()` 7단계 | 전부 ✅ (설정값 · projectId 모양 · 로그인 · Firestore 연결 · 계정 문서 쓰기 · 밀린 기록 제출 · 순위표 조회) |
| 명예의 전당 첫 표시 | **1.0초** (고치기 전: 12초 뒤 "네트워크 연결을 확인해주세요") |
| 이름 표기 | **이안** — `???` 아님 |
| 실제 플레이 1판 | 25칸 등반, 신발 1켤레 획득, 사망 → 로비 복귀 |
| 서버 반영 | REST 로 직접 확인 — `stairs:25 · nickname:"이안" · shoesFound:1 · easy · 2026-W33` |
| 주간 순위표 | `1 · 이안 · 25칸` + 내 순위 고정 행 |
| 새로고침 후 | 세션·기록·신발 그대로 유지, 로비 직행 |
| 멀티 진입 차단 | 신발 1켤레에서 "신발이 1켤레 이하면 게임에 참가할 수 없습니다" 정상 |
| PWA | 아이콘 3종 200 · 워커 통제(`v=<커밋해시>`) · 캐시 3분리 · 설치 버튼 노출(= 크롬이 설치 가능 판정) |

**남은 것은 멀티 2인 실기기 대전 1건뿐이다** (PC + 폰, 계정 2개 필요 — 혼자서는 못 한다).

### 검수 요령 — 탭이 가려지면 게임 루프가 멈춘다

브라우저 자동화로 게임을 돌릴 때, 다른 창이 앞에 있으면 `bindVisibility` 가 루프를 **정지**시켜
입력을 아무리 넣어도 계단이 안 올라간다(멈춘 게 아니라 설계대로다). 그럴 때는
`__dbg.Scene.updateCurrent(1000/60)` 을 직접 돌리면 렌더 없이 게임 로직만 진행시킬 수 있다.
키 입력은 `new KeyboardEvent('keydown',{code:'ArrowRight'})` 로 창에 던지면 그대로 먹는다.

---

## 9-0-14. 전면 감사 후 수리 — 10곳 + 곁가지 (2026-08-16)

`docs/AUDIT_2026-08-16.md` 의 10건을 순서대로 고쳤다. **전부 재현 경로를 확인한 것만** 손댔다.

### 1차 — 서버 규칙과 조용한 정지

| 무엇 | 어디 | 왜 위험했나 |
|------|------|------------|
| RTDB 규칙 전면 교체 | `docs/FIREBASE_RULES.md` | **상위 `.write` 가 하위 규칙을 통째로 덮는다.** 방에 들어온 사람이 방 전체를 쓸 수 있어 가짜 참가자 + `given` 수백 켤레로 **지갑·도감을 통째로 채울 수 있었다.** `.write` 대신 잎마다 `내 것이거나 값이 그대로` 검증을 걸어 막았다 — 클라이언트는 안 바꿔도 된다 |
| `hostUid` 검증식 | 같은 파일 | `!data.exists() \|\| data.val() == auth.uid` 는 **기존 값이 나여야** 통과다. 입장·이탈·결과확정은 방 노드 전체를 다시 쓰는 트랜잭션이라, **방장이 아니면 입장 자체가 거부**됐다. `newData.val() == data.val()` 항을 추가 |
| 계정 전환 오염 | `services/auth.js` | 로그인 시 저장된 uid 와 다르면 로컬을 비운다(`dropForeignData`). 예전에는 A의 신발·도감·닉네임이 B로 이식되고, B가 신규면 **서버 문서까지 A 데이터로 만들어졌다** |
| `withTimeout` 누락 | `services/collection.js` 외 4곳 | 파이어스토어는 막히면 **거절하지 않고 영원히 기다린다**(§9-0-7). 감싸지 않은 호출이 있으면 `finally` 도 안 돌아 **로그인 버튼이 죽는다.** 린트(`_lint-services.mjs`)에 규칙을 넣었더니 **순위표 조회까지 무방비**였던 게 드러났다 |

### 2차 — 멀티 무결성

- **정산 도장을 서버로** (`multiSettle.js` · `multiplayer.js`) — 도장이 localStorage 에만 있었는데
  재정산의 입력(`userRooms`·`result.given`)은 RTDB 에 영구히 남는다. 기기를 바꾸거나 저장소를
  지우면 승자가 같은 신발을 **다시 걷고**(복제), 패자는 또 내서(증발) 총량이 깨졌다.
  도장이 200건에서 잘려 나가는 **150판 즈음부터는 가만히 있어도** 벌어졌다.
  지금은 패자 = `given[내uid]` 존재 여부, 승자 = `result/settled/{uid}` **비트마스크**로 서버가 판정한다.
  비트마스크라 패자가 아무 순서로 늦게 내도 정확히 한 번씩만 걷는다.
- **중도 이탈 = 사망** (`GameScene.leave`) — 일시정지 → 나가기가 `reportDeath` 없이 순위를 먼저
  못 박았다. 신발 하나 줍고 일시정지(내 게이지만 멈춘다) → 나가기 = **이기고 도망**.
  게다가 내 `alive` 가 true 로 남아 **남은 사람은 자기가 죽을 때까지 방이 안 끝났다.**
- **멀티 결과를 계정에 반영** (`startMultiGame` → `MultiResult`) — `finishRun()` 호출부가 싱글
  한 곳뿐이라 멀티 한 판의 신발·계단이 통째로 버려졌다. 기획서 §5-9 대로 **승자만** 반영한다.
- **유령 방 자동 정리** (`multiplayer.armDisconnect`) — `onDisconnect` 가 저장소 전체에 0건이었다.
  탭을 닫으면 `open:true` 1인 방이 영구히 남고, **12개만 쌓여도 전 사용자의 자동 매칭이 고사**한다.
  게임이 시작되면 `holdRoomSeat()` 로 취소한다 — 판 중에 잠깐 끊겼다고 빠지면 안 된다.
- **카운트다운을 벽시계 기준으로** (`GameScene.enterMulti`) — 프레임 수로 깎고 있어서 에셋
  로딩 중에는 아예 멈췄다. 캐시가 빈 기기가 **5초 늦게 출발**해 같은 시드가 무의미했다.
- **구독 해제 경쟁** (`subscribeRoom`) — 붙기 전에 끊으면 그 뒤에 리스너가 붙어 영영 안 떨어졌다.

### 3차 — 체감·발열·갇힘

- **판이 끝나면 씬 스택을 비운다** (`core/scene.js` `clear()`) — 이게 없어서 로비·도감을 보는
  내내 죽은 GameScene 을 60fps 로 그렸고(프레임당 700회 넘는 `fillRect`), 멀티 결과 화면에서는
  게이지가 마저 닳아 **사망 효과음이 울렸다.**
- **HUD 글자 캐시** (`core/pixelfont.js` `textCached()`) — `outline` 은 글리프를 **9번** 찍는다.
  HUD 세 텍스트가 프레임당 **666 fillRect**였다. 글자 단위로 오프스크린에 구워 두고 붙인다
  ("999" = fillRect 333회 → drawImage 3회). 문자열이 아니라 **글자** 단위인 이유는 계단수가 매 층 바뀌기 때문.
- **갇히는 경로 3종** — 멀티 결과 → 다시하기 후 뒤로가 死(`nav.reset(MultiMenu)` → 깊이 1),
  대기방 하드웨어 뒤로 시 유령 참가자(`onLeave` 가 방을 안 나갔다), 로딩 화면에 버튼 0개.
- **팝업이 뒤로가기 스택 밖** (`ui.presentOverlay` + `router`) — 도감 팝업을 열고 뒤로가기를 누르면
  **팝업이 로비 위에 남았고**, 구매 다이얼로그는 떠난 화면의 구매를 끝까지 실행했다(취소한 줄 알았는데 결제).
- **`HallOfFame.onLeave`** — 늦게 온 순위 응답이 남의 화면을 다시 그려 **입력 중인 닉네임을 날렸다.**
- **닉네임 입력 2종** — 캐릭터 화면에서 화살표 한 번에 입력이 증발(`draftNick` 으로 보존),
  그리고 **한글 조합 중 Enter**(`isComposing`) 미처리 + iOS 키보드에 확인 버튼이 가리던 문제.
- **큐 유실** (`flushQueued`) — 큐를 먼저 비우고 올렸다. 한 건에 최대 12초라 여러 건이면
  1분이 넘는데 **그 사이 앱을 닫으면 대기 기록이 통째로 사라졌다.** 이제 성공한 것만 하나씩 지운다.
- **SW 백그라운드 갱신** (`sw.js`) — `event.waitUntil` 이 없어 워커가 먼저 죽으면 갱신이 유실됐다.

### 검수

`qa:multi` 에 **15) 서버 도장** 그룹을 새로 넣었다 — 기기를 바꿔도, 순서가 뒤죽박죽이어도,
지갑이 안 내려온 상태여도 총량이 안 깨지는지 전수로 본다.
`_lint-services.mjs` 에는 **원격 호출은 반드시 `withTimeout`** 규칙을 넣었다.
전체 통과: build · lint · qa:wallet · qa:rank · qa:multi(15군) · qa:pwa(27항목) · 화면 11개 흐름(에러 0).

**남은 것: 멀티 2인 실기기 대전.** 규칙 게시(콘솔)와 배포가 선행돼야 한다.

---

## 9-0-15. 멀티가 첫날부터 죽어 있던 이유 — 빈 환경변수 (2026-08-16)

규칙을 고쳐 게시하고 배포까지 끝난 뒤에도 **비밀방 만들기를 눌러도 아무 일이 안 났다.**
오류도, 콘솔 로그도, 네트워크 요청도 없었다. 화면만 그대로였다.

### 계측

`performance.getEntriesByType('resource')` 로 보니 **`firebase-rtdb-*.js` 청크가 아예 안 받아졌다.**
`getRtdb()` 를 다시 읽어 보니 답이 있었다:

```js
return () => (configured() && CFG.databaseURL ? load() : Promise.resolve(null));
```

`databaseURL` 이 비면 **청크를 부르지도 않고 곧장 null** 이다. 배포된 번들을 까 보니:

```
databaseURL:""
```

캐시에 남아 있던 **이전 배포 두 개까지 전부 확인했다 — 셋 다 빈 문자열.**
즉 멀티는 만들어진 이래 **한 번도 동작한 적이 없었다.** Vercel 환경변수
`VITE_FIREBASE_DATABASE_URL` 은 **등록은 되어 있는데 값이 비어 있었다**(Sensitive 라
대시보드에서도 안 보인다).

### 왜 아무도 몰랐나

- `getRtdb()` 가 **조용히** null 을 돌려준다. `once()` 의 오류 로그도 안 탄다 (실패가 아니니까).
- 그 위의 모든 멀티 함수가 `if (!fb) return null` 로 시작한다 → 전부 무해한 no-op 이 된다.
- 화면에는 `guard()` 가 띄우는 **1.6초짜리 "네트워크 연결을 확인해주세요"** 토스트뿐이다.
  6초 뒤에 확인하면 이미 사라지고 없다. 실제로 그것 때문에 한참을 헤맸다.

### 고친 것

1. **Vercel 환경변수에 실제 주소를 채웠다** (Production and Preview).
2. **`firebase.js` 에 기본값을 뒀다** — `DEFAULT_DB_URL`. 이 값은 비밀이 아니고 어차피
   번들에 박힌다. 환경변수는 이제 "다른 프로젝트를 붙일 때 덮어쓰는 용도"다.
   환경변수가 비어 있으면 **콘솔에 경고**를 찍는다.
3. **`selftest()` 에 `멀티(RTDB) 설정` 단계 추가** — 이 값이 비면 즉시 보인다.

### 실전 검증 (실제 계정 2개, 실제 데이터베이스)

`jachung18@gmail.com`(A) 과 `info@nivs.com`(B) 의 ID 토큰으로 REST 를 직접 때려
**방 생명주기 전체와 정산을 서버에서 검증했다.**

| 검사 | 결과 |
|------|------|
| 방 생성 → **B 입장(방장 아님)** → 레디 → 카운트다운 → 진행도 → 사망 → 종료 → 순위확정 | 10단계 전부 200 |
| 남의 계단 수 조작 (방 전체 재작성 / 잎 직접 쓰기) | **둘 다 401** |
| 가짜 참가자 삽입 · 남의 given 대리작성 · 미지의 필드 · 남의 정산 도장 · given 5개 초과 · 가짜 uid 를 1등으로 | **전부 401** |
| 방 전체 재작성하되 남의 값은 그대로 (= joinRoom·finalizeResult 가 하는 일) | 200 |
| 패자가 신발 내기 → 재정산 시 `given[내uid]` 존재로 **두 번 안 냄** | 확인 |
| 승자 수령 → 2·3차 수령 **0개** (서버 비트마스크 `settled` 가 막는다, 기기 변경 가정 포함) | 확인 |

이전 규칙이었다면 위의 401 들이 **전부 200** 이었다(상위 `.write` 가 하위를 덮으므로).
`hostUid` 검증식 때문에 **B 입장 자체가 거부**됐을 것이고.

검증에 쓴 방(`T001`·`M001`)과 `userRooms` 흔적은 전부 지웠다 — `rooms` 는 비어 있다.

### 교훈

**"설정값이 있다"와 "설정값이 채워져 있다"는 다르다.** 대시보드에 이름이 보인다고 값이 있는 게 아니다.
그리고 **없으면 조용히 꺼지는 기능**은 반드시 시작할 때 한 번 소리를 질러야 한다 —
`selftest()` 에 한 줄을 추가하는 비용이, 며칠을 헤매는 비용보다 훨씬 싸다.

---

## 9-0-16. 자동 매칭이 통째로 죽어 있던 이유 — 트랜잭션의 `null` 함정 (2026-08-16)

실기기 두 대로 '방 입장'을 눌렀더니 **둘 다 방만 만들고 서로 못 만났다.**

### 계측

두 계정의 ID 토큰으로 REST 를 직접 때려 확인했다.

| 확인 | 결과 |
|------|------|
| B 가 `orderBy="open"&equalTo=true` 스캔 | **정상** — A 의 방이 목록에 보인다 |
| B 가 A 의 방 3개에 입장 (방 전체 재작성) | **전부 200** — 규칙은 막지 않는다 |

즉 **서버는 아무 문제가 없었다.** 그러면 클라이언트다.

### 원인 — 트랜잭션은 **캐시 값으로 먼저 한 번 호출된다**

```js
runTransaction(roomRef, (cur) => {
  if (!cur) { verdict = 'notfound'; return; }   // ← 여기서 끝나 버린다
  ...
})
```

파이어베이스 문서에 그대로 적혀 있다 — *"핸들러는 여러 번 호출되며 `null` 을 처리할 수 있어야 한다.
원격에 데이터가 있어도 **로컬에 캐시되지 않았다면 첫 호출 값은 `null`** 이다."*
그리고 핸들러가 `undefined` 를 돌려주면 트랜잭션은 **서버 값을 기다리지 않고 그 자리에서 중단**된다.

`joinRoom` 은 **한 번도 읽은 적 없는 남의 방**에 트랜잭션을 건다. 그래서 첫 호출이 항상 `null`,
항상 `'notfound'`. 자동 매칭은 후보를 전부 그렇게 흘려보내고 마지막 줄
`return createRoom(...)` 으로 떨어진다 — **두 대가 각자 방만 만드는 그림**이 정확히 이것이다.
`코드로 입장`도 멀쩡한 코드에 "방을 찾을 수 없습니다"를 띄우고 있었다.

### 고친 것

1. **`joinRoom` 은 트랜잭션 전에 `get()` 으로 방을 한 번 읽는다.** 캐시가 채워져 첫 호출이
   진짜 값을 받는다. 덤으로 정원·상태 판정을 트랜잭션 밖에서 미리 해 헛트랜잭션이 준다.
2. **`finalizeResult` · `leaveRoom` 에도 같은 선(先)읽기를 넣었다.** 특히 `finalizeResult` 는
   `endMulti()` 가 **구독을 끊은 뒤**에 불리므로 캐시가 비워져 있을 수 있다 — 거기서 중단되면
   **순위가 영영 안 박혀 아무도 정산을 못 한다.** 증상이 안 보이는 자리라 더 위험했다.
3. **동시 입장 해소** — 둘이 같은 순간에 누르면 둘 다 빈 목록을 보고 각자 방을 만든다.
   그래서 방을 만든 뒤 `1.4초` 있다가 **한 번 더 훑고**, 나보다 먼저 만들어진 방이 있으면
   내 방을 접고 그쪽으로 옮긴다. 판정은 `matchRules.byPreference` 로 **결정적**이라
   (사람 있는 방 → 먼저 만든 방 → 코드 순) 양쪽이 같은 답을 내고 **정확히 한 명만** 움직인다.
4. 후보 필터에 `state === 'waiting'` 추가 — 시작한 방에 들어가려다 실패하고 그 실패가
   "방이 없다"로 흘러가 새 방을 파던 경로를 막는다. 참가자 0명인 유령 방은 **재사용**한다.

`joinable` · `byPreference` 는 순수 함수라 `matchRules.js` 로 옮기고 `qa:multi` 16번 그룹에서
**"양쪽이 같은 답을 내는지"** 를 전수로 검증한다.

### 교훈

**"규칙이 막는다"와 "클라이언트가 포기한다"는 증상이 똑같다.** 둘 다 조용히 실패한다.
그래서 이번엔 서버를 REST 로 먼저 때려 보고 **서버가 멀쩡하다는 것부터 확정**한 뒤 클라이언트를 봤다.
그 순서가 아니었으면 또 규칙만 고치고 있었을 것이다.

---

## 9-0-17. 세 대가 전부 방장이 된 이유 — 아직 안 붙은 연결 (2026-08-16)

폰 세 대로 `방 입장` 을 눌렀더니 **셋 다 방장**이 됐다. 앞서 고친 트랜잭션 문제(§9-0-16)를
배포한 뒤에도 그대로였다.

### 계측

브라우저에서 그대로 재현했다. 다른 계정으로 `8888` 방(참가자 1명·열림·대기중)을 세워 두고
`방 입장` 을 눌렀더니 **그 방을 두고 새 방(6954)을 만들었다.**
`코드로 입장` 으로 `8888` 을 직접 찍어 봤더니 — **"방을 찾을 수 없습니다".**
그 순간 REST 로 확인한 `8888` 은 멀쩡히 존재했다(참가자 1명, `waiting`, `open`).

즉 **서버에 있는 방을 클라이언트가 "없다"고 판정**하고 있었다.

### 원인 — `get()` 은 못 닿으면 오류 대신 **캐시**를 준다

`joinRoom` 은 트랜잭션 전에 `get(rooms/<code>)` 으로 방을 읽는다. 그런데 RTDB 의 `get()` 은
**서버에 닿지 못하면 예외를 던지지 않고 로컬 캐시 값을 돌려준다.** 캐시가 비어 있으면 `null` 이다.
→ `if (!seen) return 'notfound'` → "방이 없다".

그리고 **첫 멀티 동작 시점에는 연결이 아직 없다.** RTDB 는 그때 청크(192KB)를 받고
웹소켓을 새로 연다. `quickJoin` 은 그 직후 곧바로 스캔하므로 **빈 목록**을 보고,
마지막 줄의 `createRoom()` 으로 떨어진다. 방 만들기는 몇백 ms 뒤에 실행돼 그때는 연결이
붙어 있으니 **성공한다** — "만들기는 되는데 입장만 안 된다"가 정확히 이 그림이다.

세 대가 각자 처음 눌렀으니 셋 다 같은 창에 걸렸다.

### 고친 것

1. **`waitConnected()`** — `.info/connected` 가 `true` 가 될 때까지 기다린다(최대 6초).
   붙지도 않은 채 "방 없음"이라고 말하지 않는다.
2. **`readOnce()`** — `get()` 대신 `onValue(..., {onlyOnce:true})`. 이쪽은 못 읽으면
   콜백이 안 오므로 **"값이 없다"와 "못 읽었다"를 구별**할 수 있다. 못 읽었으면 `'error'` 다.
3. **`prewarm()`** — 멀티 메뉴를 여는 순간 미리 붙인다. 누를 때는 이미 준비돼 있다.
4. **빈 스캔은 한 번 더 본다** (0.9초 뒤). 막 붙은 직후의 빈 목록은 "없다"가 아니라
   "아직 못 받았다"일 수 있다.
5. **`__dbg.multi.diagnose(code)`** — 콘솔에서 연결 상태·방 목록·입장 결과를 한 번에 찍는다.
   멀티는 실패가 조용해서 이게 없으면 화면만 보고는 원인을 못 가른다.

### 곁가지 — 빈 방이 영원히 안 지워지고 있었다

`onDisconnect` 가 마지막 한 명을 빼고 나면 **참가자 0명인 방**이 `open:true` 로 남는다.
그런데 예전 규칙은 쓰기를 "방 멤버"에게만 열어 뒀으므로 **그 방은 누구도 못 지웠다.**
자동 매칭이 훑는 12칸을 갉아먹어 진짜 방이 안 보이게 만든다.

규칙에 한 항을 더했다 — `(!newData.exists() && !data.child('players').hasChildren())`.
**참가자가 0명인 방은 누구나 지울 수 있다.** 지우는 것 말고는 아무것도 못 하므로 안전하다.
실측으로 확인: 사람이 있는 방을 남이 지우려 하면 **401**, 빈 방은 **200**.
클라이언트도 스캔하는 김에 **3분 이상 묵은 빈 방**을 최대 5개까지 치운다.

그리고 **빈 방을 이어받으면 아무도 방장이 아니게 되는** 문제가 있었다. `hostUid` 는
남아 있는데 그 사람이 방에 없으니 `isHost` 가 전원 false → **시작 버튼이 영영 안 나온다.**
둘이 만나도 게임을 시작할 수가 없다. 이제 입장할 때 직전 방장이 방에 없으면 **내가 이어받는다**
(규칙도 그 경우만 `hostUid` 변경을 허용한다).

### 교훈

**"없다"와 "아직 모른다"를 구별하지 않는 API 는 반드시 사고를 낸다.**
`get()` 의 캐시 폴백은 오프라인 UX 를 위한 배려지만, 그 값을 **판단의 근거**로 쓰면
연결 지연이 곧바로 잘못된 결론이 된다. 읽기 실패는 실패로 다뤄야 한다.

---

## 9-0-18. 매칭을 통째로 다시 설계했다 — 방 목록 + 대기자 (2026-08-16)

### 진단 훅이 답을 줬다

`__dbg.multi.diagnose('4242')` 를 배포본에서 돌렸더니 한 줄에 답이 나왔다.

```
연결: true
방목록: [{ code:'4242', state:'waiting', 인원:1 }]     ← 스캔은 방을 찾는다
방읽기: { ...방 전체... }                              ← readRoom 도 잘 읽는다
입장결과: "notfound"                                   ← 그런데 입장만 못 한다
```

**같은 경로를 바로 앞 줄에서 읽었는데 트랜잭션만 `null` 을 본다.** `onlyOnce` 읽기는
리스너가 곧바로 떨어지고, **리스너가 없으면 RTDB 는 그 경로의 캐시를 버리기** 때문이다.
`get()` 으로 미리 읽어 캐시를 채우는 방법은 이래서 통하지 않았다.

### 트랜잭션을 버렸다

**입장은 결국 내 참가자 노드 하나를 쓰는 일이다.** 남의 값을 건드리지 않으므로
애초에 트랜잭션이 필요 없었다. 지금은 `set(rooms/<code>/players/<myUid>)` 한 방이고,
정원 초과만 쓰고 나서 확인해 **늦게 들어온 순으로 스스로 물러난다**(`joinedAt` 기준이라
모두가 같은 답을 낸다).

### 그리고 매칭 자체를 다시 설계했다

사용자 지적이 정확했다 — *"방 입장이 방을 만들기만 하면 게임을 할 수 없다"*.

| | 예전 | 지금 |
|---|---|---|
| 방 목록 | 없음 (자동 매칭만) | **`방 목록` 화면** — 눈으로 보고 손으로 고른다 |
| 게임 중인 방 | 후보에서 제외 | **자리 있으면 대기자로 입장**, 다음 판부터 함께 |
| 방 만들기 | 자동 매칭이 실패하면 바로 | **정말 아무 데도 못 들어갈 때만** |
| 4/4 방 | 구분 없음 | 목록에 `만원` 으로 표시, 입장 버튼 없음 |
| 판이 끝나면 | 방이 `finished` 로 굳고 해산 | **`방에 남기`** → 방이 `waiting` 으로 돌아가 다음 판 |

**대기자(`waiting: true`)는 이번 판의 순위·정산에서 통째로 빠진다.** 뛰지도 않은 판에서
꼴찌가 되어 신발을 뺏기면 그건 그냥 사고다. `matchRules.playersInRound` 가 그 경계다.

목록을 만든 이유는 기능 때문만이 아니다. **자동 매칭은 실패가 안 보인다** — 눌렀는데 방이
만들어지면 그게 "빈자리를 못 찾아서"인지 "정말 방이 없어서"인지 사용자는 알 수 없다.
목록이 있으면 눈으로 확인하고 손으로 고를 수 있어서, 매칭이 흔들려도 사용자가 막히지 않는다.

### 규칙도 같이 바꿔 게시했다 (실측 검증)

| 검사 | 결과 |
|------|------|
| 게임 중인 방에 대기자로 입장 | **200** (예전 규칙은 거부) |
| 대기자가 남의 진행도 조작 | **401** |
| 판이 도는 중 시드 변경 | **401** |
| 대기 상태로 되돌리며 시드 재추첨 | **200** |

입장 조건에서 `state == 'waiting'` 을 빼는 대신 **"내가 아직 없을 때만 나를 넣을 수 있다"**
로 좁혔고, `players/$uid/waiting` 과 대기 상태에서의 `seed` 재추첨을 열었다.

---

## 9-1. 배포에서 두 번 데인 것 (2026-08-15)

배포가 M4에서 멈춰 있었다. 원인이 **두 개**였고 둘 다 조용히 실패했다.

**(1) 레포가 두 개였다.**
Vercel은 `nivs-web/ianworld_shoes`(언더바)를 보고 있었는데
`push-to-github.bat` 은 `nivs-web/ianworld-shoes`(하이픈)로 밀고 있었다.
빌드가 실패한 게 아니라 **트리거 자체가 안 걸려서** 배포 목록에 흔적조차 없었다.
"푸시 성공 = 배포됨"이 아니다. 확인은 반드시 Vercel 배포 목록의 **커밋 해시**로 한다.

**(2) `vercel.json` 에 주석을 달았다.**
```
The `vercel.json` schema validation failed: `headers[0]` should NOT have additional property `//`
```
`headers[]` 항목은 `additionalProperties: false` 라 `source` · `headers` · `has` · `missing`
넷만 허용한다. JSON에 주석을 넣으려고 쓴 `"//"` 키가 스키마 위반이다.
**vercel.json 에는 어떤 형태의 주석도 넣지 않는다.** 설명은 여기 CLAUDE.md 에 적는다.

원래 `"//"` 에 적어 뒀던 캐시 규칙의 근거:
- `/assets/` 중 **Vite 번들** — 파일명에 콘텐츠 해시가 붙는다(`index-DE7J_FI8.js`).
  내용이 바뀌면 이름도 바뀌므로 1년 immutable 이 안전하다.
- `/assets/{characters,shoes,bg,ui}/` — **게임 스프라이트**. 파일명이 `ian_side.png` 처럼
  고정이라 내용이 바뀌어도 URL이 그대로다. 여기에 immutable 을 걸면 스프라이트를 고쳐도
  이미 방문한 브라우저는 1년 동안 옛 그림을 본다. 그래서 `max-age=0, must-revalidate` 로
  매번 재검증한다(변경 없으면 304, 본문 0바이트라 비용도 거의 없다).

**콘솔 쪽 설정은 전부 정상이었다** (실측 확인):
API 키 유효 · Google 로그인 ENABLED · 승인된 도메인에 `ianworld-shoes.vercel.app` 포함 ·
Firestore 생성됨(비로그인 읽기 → `PERMISSION_DENIED` = 규칙 적용됨) · RTDB 200 ·
Vercel 환경변수 8개 등록됨.

## 9-0-19. 두 번째 판을 영영 못 하던 이유 — 규칙이 '다음 판 준비'를 막고 있었다 (2026-08-18)

배포된 빌드로 **내가 직접** 한 판을 끝까지 돌리며 확인하다 찾았다.

`resetRoom`(= 결과 화면의 `방에 남기`)은 방 전체를 한 번에 되돌린다 — 모두의
`stairs · shoesFound · ready · alive` 를 초기값으로 적는다. 그런데 규칙의 필드 검증은
`$uid == auth.uid || 값이 그대로` 뿐이었다. 즉 **남의 칸은 값이 안 바뀔 때만** 통과한다.

- 상대가 한 계단도 안 올랐으면 → 값이 그대로 → 통과 (테스트에서 우연히 통과했다)
- 상대가 한 계단이라도 올랐으면 → `stairs: 2 → 0` → **거부**. 멀티패스 update 는
  하나만 걸려도 **통째로** 거부되므로 리셋 전체가 실패한다.

실제 대전은 **언제나 후자**다. 증상은 "'방에 남기'를 눌렀는데 네트워크 오류 토스트만
뜨고 그대로", 즉 **같은 방에서 두 번째 판을 시작할 방법이 없음.**

계측 (REST, 진짜 토큰 2개):

```
B 의 stairs 를 5 로 둔 뒤 A 가 리셋 모양으로 PATCH  →  401 Permission denied   (고치기 전)
같은 요청                                          →  200                     (고친 뒤)
```

고친 것 — 남의 칸이라도 **초기값으로 되돌리는 것만** 허용한다. 그것도 같은 쓰기에서
방이 `waiting` 으로 갈 때만:

```
"stairs": "... || (newData.val() == 0 && newData.parent().parent().parent().child('state').val() == 'waiting')"
```

점수를 **올려** 주는 조작은 여전히 불가능하고(401 확인), 게임 중에 남의 점수를 0으로
지우는 것도 막힌다(401 확인). 내 진행도 쓰기는 그대로 통과한다(200 확인).

### 곁가지 — `players/{남의uid}: true` 가 통과하고 있었다

`$uid` 아래 필드마다 검증이 있지만, **원시값**을 쓰면 자식이 없어 검증할 게 없다.
REST 로 넣어 보니 200 이 떨어졌고, 인원은 `Object.keys(players).length` 로 세므로
**방을 가짜로 만원**으로 만들어 남들의 매칭을 막을 수 있었다. `$uid` 노드 자체에
필수 자식 검사를 걸어 막았다(고친 뒤 401 확인).

## 9-0-20. 이번 검수에서 실측으로 확인한 것들 (2026-08-18)

배포본에 계정 2개(진짜 ID 토큰)로 붙어 **화면과 서버를 같이** 봤다.

| 확인 | 결과 |
|------|------|
| `뒤로` → 방에서 빠지는가 | ✅ 서버 `players` 에서 내 uid 삭제, 2명 → 1명, 방장 유지 |
| `방 입장` → 있는 방에 들어가는가 | ✅ 새 방을 안 만들고 대기 중인 방에 합류 (2/4) |
| 레디 → 시작 → 한 판 → 결과 | ✅ `ready` 반영, 카운트다운 동기 출발, 진행도 전송(25계단/신발3), 사망 시 `rankings` 확정, 결과 화면 순위·문구 정상 |
| 승자 기록이 계정에 남는가 | ✅ 보유량 983 → 987 (그 판에서 주운 4켤레) |
| 패자 신발이 승자에게 오는가 | ✅ `given` 올린 뒤 재접속 청산으로 987 → 988, 도감 수량도 +1 |
| `방에 남기` → 다음 판 준비 | ✅ (규칙 고친 뒤) 전원 0 으로 초기화, 새 시드, `waiting` |
| 방 목록 표시 | ✅ 대기중/게임중 구분, `n/max`, 만원 처리, 게임중 방은 뒤로 정렬 |
| 게임 중인 방에 대기자로 입장 | ✅ 서버에 `waiting: true`, 레디 버튼 없음, "다음 판을 기다리는 중" |

### 같이 고친 것 두 개

- **방 목록 줄이 화면 밖으로 터졌다.** `.pbtn` 은 로비용이라 `width: 100%` 인데
  `.room-row .pbtn` 이 폭을 안 되돌려서, 버튼이 행 전체를 먹고 `flex: 0 0 auto` 라
  줄지도 않았다 → 방 이름이 **23px** 로 눌려 한 글자씩 세로로 접히고 행은 1500px 를
  넘겼다(가로 스크롤바). `width: auto` + 이름에 `min-width: 0` 로 고쳤다.
  폰 폭(360px)에서 한 줄에 깔끔히 들어가는 것까지 확인했다.
- **`방에 남기` 가 아직 안 걷은 신발을 지웠다.** `resetRoom` 은 `result` 를 통째로
  지우는데 그 안에 패자가 내놓은 `given` 이 들어 있다. 패자가 결과 화면보다 늦게
  올리면(보통 몇 초) 그 신발은 **패자 지갑에서는 빠졌는데 아무도 못 받은 채 증발**한다.
  재현했다: `given=[9]` 직후 `방에 남기` → 승자 보유량 987 그대로, `result` 는 null.
  이제 지우기 직전에 `settleRoom` 을 한 번 더 돌린다. (리셋 뒤에 올라오는 신발은
  `rankings` 가 없어 패자도 안 내므로 손해가 없다)

## 9-0-21. 2차 정밀 검수 — 찾아서 고친 10건 (2026-08-18)

배포본을 계정 2개로 계측하고, 멀티 서비스·게임 루프·화면·PWA·저장소를 전수로 읽었다.
**증상이 안 보이는 것**들이 대부분이라 코드와 서버 응답을 같이 봐야 잡혔다.

### ① 방장이 나가면 그 방이 죽는다 (실측 401)

`leaveRoom` 이 ① 내 노드를 지우고 ② 그 다음에 방장 승계·`open` 갱신을 썼는데,
②는 **항상 거부**됐다 — 그 시점엔 내가 `players` 에 없어 방 규칙의 네 조건이 하나도
안 맞는다.

```
players/나 DELETE  → 200
rooms/코드 PATCH {hostUid}  → 401 Permission denied
```

떠난 사람이 계속 방장이면 남은 사람에게는 **시작 버튼이 영영 안 뜨고**(`WaitingRoom` 은
`hostUid == 내uid` 로만 판단한다), 4/4 라서 `open:false` 가 된 방은 한 명이 빠져도
`true` 로 못 돌아가 **목록에서 사라진다.**

고친 것: 삭제와 뒷정리를 **한 번의 멀티패스 update** 로 보낸다. `.write` 는 옛 데이터로
평가되어 내가 아직 참가자이므로 통과하고, `hostUid` 검증은 새 데이터를 보므로
"옛 방장이 이제 없다"가 성립한다. 실측 200.

### ② 판이 끝나도 아무도 방에서 안 나간다 → 유령 방이 매칭을 굶긴다

`leaveRoom` 호출부가 대기방 하나뿐이었다. 결과 화면의 `한 판 더`·`로비로` 는 방을
그대로 두고 나가고, `holdRoomSeat` 가 게임 시작 때 끈 `onDisconnect` 를 **다시 켜는
곳이 없어서** 탭을 닫아도 안 빠진다. 매칭은 `open == true` 인 방을 **앞 12개**만
훑으므로(`SCAN_LIMIT`), 끝난 방 12개면 전원이 새 방만 판다 — 폰 세 대가 전부 방장이
되던 그 증상이 이 경로로 **다시** 만들어지고 있었다.

고친 것: 결과 확정 때 `open:false`(매칭 창에서 즉시 하차) · 결과 화면을 떠날 때
`leaveRoom` · 판이 끝나면 `rearmRoomSeat` 로 자동 이탈 재무장 · 접속 청산이 볼일
끝난 방에서 빠져나오고 기록도 지운다. 단 **받을 신발이 남았으면 남는다** — 방을
나가면 규칙상 `settled` 도장을 못 찍어 영영 못 걷는다(3일이 지나면 포기한다).

### ③ 신발이 증발한다 (두 경로)

- 패자가 신발을 올린 직후 **패자 본인이** '방에 남기'를 누르면 `resetRoom` 이
  `result` 를 지운다. 정산은 승자만 걷어 가므로 그 신발은 아무에게도 안 간다.
- 리셋 뒤에 늦게 올라온 `given` 은 다음 판 `result` 에 얹혀서, 그 패자는 **다음 판
  패널티를 면제**받고(이미 낸 것으로 보인다) 그 신발은 엉뚱한 사람에게 가거나 사라진다.

고친 것: `resetRoom` 이 서버의 `result/settled` 비트마스크로 **안 걷힌 신발이 있으면
`'pending'` 을 돌려주고 리셋을 거부**한다. 결과 화면은 안내를 띄우고, 그 사이
2초 간격으로 최대 5번 다시 걷는다(패자는 보통 1~2초 뒤에 올린다).

### ④ 싱글 판 결과가 버튼을 눌러야만 저장됐다

`finishRun()` 은 `leave()` 안에서만 불렸고 `leave()` 는 게임오버 패널의 **버튼**에서만
불린다. 죽고 나서 전화를 받거나 앱을 닫으면 그 판의 계단·신발·도감이 **통째로** 사라졌다.
이제 `finish()` 가 패널을 띄우기 전에 반영하고(`onCommit`), `leave()` 는 이미 반영했으면
결과를 넘기지 않는다(이중 집계 방지).

### ⑤ 끝난 판이 계속 돌았다 + 종료 재진입 방지가 없었다

멀티 종료는 `finalizeResult`(RTDB 왕복 2회)를 기다린 뒤에야 화면이 바뀐다. 그동안
게이지가 마저 닳아 **사망음이 결과 화면 위로 겹치고**, 끝난 뒤의 계단이 서버에 덮어써졌다.
그 창에서 일시정지 → 나가기를 누르면 `onFinish` 가 두 번 불려 **결과 화면이 두 개** 뜨고
정산이 두 번 돌아 패자가 신발을 두 켤레 냈다(한 켤레는 증발). `over` · `leaving` 가드를 넣었다.

### ⑥ 멀티 일시정지에 '다시하기'가 있었다 = 실수 한 번에 몰수패

화면 위쪽 1/5 이 전부 일시정지 영역인데(`TOUCH.pauseBelowY`), 거기서 '다시하기'는
멀티에서 **사망 보고 → 몰수패**였다(멀티 `onFinish` 는 `action` 을 무시한다).
멀티에서는 항목을 [재개 · 기권하고 나가기]로 줄이고, 나가기는 **한 번 더 눌러야** 나간다.

### ⑦ 코드로 입장하면 유령이 됐다

`joinRoom` 은 게임 중인 방이면 `'waiting'`(대기자로 입장 완료)을 돌려주는데 `CodeInput`
만 그 값을 몰라 "네트워크 오류" 를 띄우고 코드를 지웠다. 사용자는 실패한 줄 알고
떠나지만 **자리는 계속 차지**했고, 다음 판에서 레디를 안 한 사람으로 남아 시작을 막았다.

### ⑧ 동점이면 먼저 나간 사람이 이겼다

`reachedAt` 은 `reportDeath` 에서만 찍히므로 **살아 있는 사람은 값이 없다**(`Infinity`).
카운트다운 직후 일시정지 → 나가기를 하면 전원 0계단·0신발인 그 순간에 나간 사람만
`reachedAt` 을 갖게 되어 **1등이 되고 남은 사람들의 신발을 걷어 갔다.** 이제 동점이면
**살아남은 쪽이 위**다. `reachedAt` 도 다른 시각들처럼 서버 보정을 쓴다(예전엔 이 값만
보정 없는 폰 시계라, 시계가 느린 폰이 동점을 항상 이겼다).

### ⑨ PWA 가 새 배포를 영영 못 받았다

등록 주소는 설치 당시의 `/sw.js?v=<그때 빌드>` 로 **고정**되고 `public/sw.js` 의 내용은
빌드마다 완전히 같은 바이트다. 그래서 30분마다 도는 `reg.update()` 는 언제나 "변경 없음"
이고 새 워커가 안 생긴다 — 홈 화면에 설치해 며칠 켜 두면 옛 번들에 갇힌다.
이제 서버의 `index.html` 이 가리키는 번들 파일 이름을 직접 비교해 새 배포를 알아채고,
**씬 스택이 비어 있을 때만**(게임 중이 아닐 때만) 한 번 새로고침한다.

### ⑩ 깨진 저장값을 조용히 초기화하고 곧바로 덮어썼다

`sf_profile` 파싱이 한 번 실패하면 기본값이 되고, `loadProfile` 이 그 기본값을 **저장까지**
해서(`migrateWallet` → `saveProfile`) 최고기록·도감·닉네임이 복구 불가로 사라졌다.
이제 원본을 `sf_profile.bak` 으로 남기고 경고를 찍는다.

### 검사로 못 박았다

`npm run qa:multi` 의 19번 묶음이 위 10건을 전부 되돌아가지 않는지 확인한다
(나가기가 한 번의 update 인지, 결과 확정이 `open:false` 인지, 동점 판정이 이탈자를
1등으로 올리지 않는지 등).

## 9-0-22. 3차 검수 — 고치면서 만든 구멍을 다시 잡았다 (2026-08-18)

§9-0-21 의 10건을 배포하고 **실측으로 확인**한 뒤, 그 변경분만 다시 적대적으로 읽었다.
고친 코드가 새 사고를 만든 게 넷이나 나왔다.

### 실측으로 확인한 것 (배포본)

| 확인 | 결과 |
|------|------|
| 방장이 `뒤로` → 승계 | ✅ 방장이 남은 사람에게 넘어감 (예전엔 401 로 실패) |
| 판 종료 시 `open:false` | ✅ 매칭 창에서 즉시 하차 |
| 멀티 일시정지 메뉴 | ✅ [재개 · 기권하고 나가기], 기권은 2단계 확인 |
| 사망 즉시 판 정지 | ✅ `over` 플래그, 400프레임을 더 돌려도 게이지·계단 그대로 |
| `방에 남기` 정산 | ✅ 미수령 신발 먼저 걷고(994→995) 리셋 |
| 승자 판 기록 반영 | ✅ 989 → 994 (그 판에서 주운 5켤레) |

### ⑪ 자동 이탈 재무장이 **미수령 신발을 죽였다** (내가 만든 버그)

§9-0-21 ②에서 "판이 끝나면 `onDisconnect` 를 다시 켠다"를 넣었는데, ②의 다른 규칙
("받을 신발이 남았으면 방에 남는다")과 정면으로 충돌했다. 결과 화면에서 새로고침 한 번에
**서버가 나를 방에서 빼 버리고**, 방 밖이라 `settled` 도장을 못 찍어 패자가 낸 신발을
영영 못 걷는다. 실측으로 그 상태를 그대로 재현했다(`given=[77]` 이 남은 채 방에서 제거됨).

방향을 뒤집어 해결했다 — **방 밖에서도 정산을 끝낼 수 있게** 규칙을 열고
(`result/settled/$uid`, `result/given/$uid` — 본인 것만, 순위 있는 방만),
**신발이 걸린 방은 아무도 못 지우게** 했다(`mustKeepRoom`). 이제 나가는 게 항상 안전하다.
실측: 방 밖 도장 200 → 재접속 청산이 995 → **996** 으로 실제 수령.

### ⑫ 대기자의 되돌리기가 **남의 정산을 지울 뻔했다** (내가 만든 버그)

§9-0-21 에서 "대기자가 갇히지 않게 `finished` 를 보면 `resetRoom`" 을 넣었는데,
그 순간 다른 사람들은 아직 결과 화면에서 정산 중이다. `resetRoom` 은 `result` 를 통째로
지우므로 **순위가 사라져 승자는 못 받고, 이미 지갑에서 신발을 뺀 패자는 그걸 잃는다.**

이제 **순위표에 있는 사람이 방에 한 명도 안 남았을 때만** 되돌린다 — 그들이 방에서
빠졌다는 건 결과 화면을 떠났다는 뜻이다. 실패하면(규칙 거부·미수령) 2.5초 뒤 재시도한다.

### ⑬ 종료를 기다리는 동안 일시정지 → 기권 = **두 번 끝내기** (남아 있던 구멍)

`endMulti()` 는 `over` 만 세우고 `leave()` 는 `leaving` 만 봤다. 그래서 결과 확정을
기다리는 몇 초 사이에 상단을 눌러 기권하면 `onFinish` 가 **두 번** 불려 결과 화면이
두 개 뜨고, 승자는 `finishRun` 이 두 번 돌아 **그 판 신발이 이중 지급**, 패자는
신발을 두 켤레 내고 한 켤레가 증발했다. 이제 끝난 판에서는 일시정지 자체가 안 열린다.

### ⑭ 패자가 앱을 꺼 버리면 **패널티가 영구 면제**됐다 (내가 만든 버그)

"나갈 때 방에서 빠진다"를 넣자, 패자가 안 낸 채 앱을 끈 방을 **승자가 나가며 지워
버렸다.** 방이 없으면 접속 청산이 훑을 대상도 사라져 그 신발은 영영 안 온다.
`mustKeepRoom` 이 **아직 안 낸 패자**(`hasUnpaid`)도 함께 본다. 일주일이 지나면 포기한다.

### 그 밖에 같이 고친 것

- **사망 보고가 순위 확정에 밀렸다** — `reportDeath` 에 서버 시각 왕복(`serverOffset`)이
  끼면서, 그 사이 `finalizeResult` 가 방을 읽으면 **죽은 사람이 살아 있는 것으로** 순위가
  매겨진다(동점 판정이 뒤집힌다). 오프셋을 세션당 한 번만 재고 기억한다.
- **접속 청산이 죽은 기록을 못 지웠다** — 방이 사라졌거나 내가 대기자였던 항목은 영원히
  남아 매 접속마다 왕복을 늘리고, 20칸이 차면 진짜 미정산 방을 밀어낸다. 이제 "더 볼 일이
  없는 방"도 지운다.
- **자동 새로고침이 대기방·결과 화면을 덮쳤다** — 새 배포 감지(§9-0-21 ⑨)의 금지 조건이
  캔버스(`Scene.depth()`)뿐이라, DOM 화면인 대기방·결과 화면은 그대로 새로고침됐다
  (자리 상실·정산 중단). `core/hold.js` 로 두 화면이 새로고침을 막는다.
- **결과 화면이 오지 않을 신발을 기다린다고 썼다** — 방이 지워지면 `settleRoom` 이 `null` 을
  주는데 그걸 "아직 안 왔다"로 취급했다. 순위가 살아 있을 때만 기다린다.
- **규칙 두 곳을 더 좁혔다** — ⓐ 초기화 허용을 **끝난 방이 되돌아갈 때만**으로(예전 조건은
  판 도중 `{state:'waiting', 남의 stairs:0}` 한 방을 허용했다 — 실측 200 → 401),
  ⓑ `$code/.write` 의 `!data.exists()` 항을 제거해 **유령 방**을 막았다(실측 200 → 401).

### 실측 검증 (규칙, 진짜 토큰 2개)

```
방 만들기 200 · 입장 200 · 정상 리셋 200 · 방 밖 납부 200 · 방 밖 도장 200
판 도중 남의 점수 지우기 401 · 남의 납부 401 · 유령 방 도장/상태 401
```

## 9-0-23. 멀티 역전 배틀 — 죽어도 끝나지 않는다 (2026-08-18)

기획 확정: **순위는 계단 높이만** / 부활 **1인당 10회** / **시간 제한 없음** /
부활하면 **무조건 1위보다 20칸 앞** / 2·3등도 기본 1켤레 손실.

### 대전제를 갈아 끼웠다

| | 예전 | 지금 |
|---|---|---|
| 한 명이 죽으면 | **전원 즉시 종료** | 그 사람만 **20초 부활 창**, 나머지는 계속 오른다 |
| 판 종료 | 첫 사망 | **전원이 죽고 아무도 부활 안 할 때** (`matchRules.roundOver`) |
| 순위 | 신발 → 계단 | **계단만** (부활하면 20칸을 얻으니 신발 기준은 의미가 없다) |
| 판돈 | 패자 1켤레씩 | **기본 1 + 부활마다 20** — 1등이 항아리를 통째로 |

### 판돈은 **항아리에 실물이 있는 것만** 센다

`revives` 숫자로 계산하면 조작된 클라이언트가 화면의 판돈만 부풀릴 수 있다.
`result/given` 은 그 사람이 **자기 지갑에서 실제로 뺀** 목록이므로 그걸 센다
(`matchRules.potShoes`). 표시와 실물이 항상 일치한다.

부활 순서도 그래서 정해져 있다 — **① 지갑에서 뺀다 ② 항아리에 올린다 ③ 되살아난다.**
②가 실패하면 ①을 되돌린다. 반대로 하면 "살아났는데 안 낸" 상태가 만들어진다.

정산은 뺄셈으로 바뀌었다: **내야 할 양(1 + 20×부활) − 이미 낸 양**. 여러 번 불려도
두 번 내지 않는다. 승자는 비트마스크를 **순위 전체**(자기 자신 포함)에 매겨 항아리를 걷는다 —
내가 건 부활 비용도 이기면 되돌아온다.

### 상대가 보인다

멀티는 **전원이 같은 시드**로 계단을 만든다. 그래서 상대의 층수 하나만 있으면
**그 사람이 선 계단 좌표를 내 화면에서 그대로 계산**할 수 있다 — 서버에서 좌표를 받을
필요가 없다. 세 겹으로 보여 준다: 화면 안이면 **반투명 고스트 + 이름표**, 가로로 벗어나면
**가장자리 화살표**, 그마저 벗어나면 오른쪽 **레이스 바**(최저~최고 구간을 늘려 그려
격차가 3칸이든 300칸이든 같은 크기로 읽힌다). 낙사·부활은 **알림 한 줄**로 뜬다.

### 글자 — 7px 폰트를 새로 구웠다 (진짜 버그 하나 동봉)

인게임 폰트가 갈무리11 하나뿐이라 `scale: 1` 이 이미 최소였다. 상대 줄 셋이면 계단이 안 보인다.
갈무리7 을 두 번째 폰트로 굽고(`tools/build-font-small.mjs`), 그 김에 **KS X 1001 상용 한글
2,350자**를 통째로 담았다. 이유가 있다 —

**인게임 상대 이름이 `?` 로 나오고 있었다.** 11px 폰트는 코드 문자열에 등장하는 한글만 굽는데
(§3-1) 닉네임은 사용자가 지은 글자다. 실측: `토`·`닙` 이 폰트에 없어서 `토토` → **`??`**.
로비·대기방은 DOM+CSS라 멀쩡했고 **캔버스만** 그랬다.

용량은 107KB(gzip **18.9KB**)이고 **멀티에 들어갈 때만 동적으로** 받는다. 싱글만 하는 사람은
받지 않는다. 캐릭터 얼굴 아이콘 16×16 도 함께 구웠다(`tools/build-faces.mjs`) — 머리 꼭대기에서
3도트 내려 1:1로 자른다(축소하면 도트가 뭉갠다).

### 규칙 (게시 완료 · 실측)

`players/$uid` 에 `revives`(0~10) · `deadAt` · `out` 추가, `result/given/$uid` 상한 4 → **220**
(20×10 + 기본 1 = 201이 한 사람 최대 납부액).

```
사망 보고 200 · 판돈 20켤레 200 · 부활 200 · 포기 200
남의 부활 횟수 조작 401 · 부활 상한(11) 401 · 판돈 221켤레 401
```

### 순위 확정은 `roundOver` 를 통과해야 한다

`finalizeResult` 가 예전처럼 "부르면 즉시 확정"이면, **먼저 포기하고 나간 사람이 남의 판을
끝내 버린다.** 이제 판정을 통과할 때만 쓴다. 결과 화면(`MultiResult`)은 방을 **구독**해
순위가 없으면 "다른 사람들이 아직 오르고 있습니다"를 보여 주고, 조건이 차면 그 자리에서
확정한다 — 마지막에 나간 사람이 순위를 못 박을 사람이 없어 판이 안 끝나는 걸 막는다.

## 9-0-24. 역전 배틀 실측 검수 — 되고 있던 것과 고친 것 (2026-08-18)

배포본에 계정 2개로 붙어 한 판을 끝까지 돌렸다.

### 실측으로 확인한 것

| 확인 | 결과 |
|------|------|
| 죽으면 20초 부활 창 | ✅ 오버레이 표시, 남은 초 카운트, 시간 다 되면 자동 포기 |
| 부활 비용 | ✅ 지갑 996 → **976** (정확히 20켤레) |
| 부활 위치 | ✅ 상대 12층 → 내가 **32층**(1위 + 20) 에서 재개 |
| 항아리 | ✅ `result/given` 에 20켤레 실물 등록, `revives: 1` |
| 종료 판정 | ✅ 상대가 포기하자 순위 확정 (`state: finished`) |
| 순위 | ✅ **계단 기준** — 32계단 A > 12계단 B |
| 정산 | ✅ 승자가 항아리 전부 수거, 지갑 976 → **996** (자기가 건 20 회수) |
| 결과 화면 | ✅ "1 아빠게임왕 신발0 · 32계단 **+20**", "걸린 신발 20켤레를 모두 가져왔습니다" |
| 상대 표시 | ✅ 얼굴 아이콘 + 7px 이름 + 고스트 캐릭터가 계단 위에 |

### 화면에서 바로 보인 것 (고침)

- **판돈 줄이 조작 버튼과 겹치고 아래가 잘렸다** (y 310 ↔ 버튼 266~314) → **y 252** 로 올렸다.
- **부활 버튼 문구가 패널을 넘쳤다** — "신발 20개 써서 1위보다 20칸 앞에서 부활하기" 는
  7px 로도 **170px** 인데 패널은 148px 다(폰트 데이터로 실측) → **두 줄**로 나눴다.
- **알림이 화면 폭(180)을 넘겼다** — 부활 알림은 **206px** → 글자 폭을 재서 **접는다**.
  문구를 줄이지 않은 건 "무슨 일이 일어났는지"가 흐려지기 때문이다.
- 결과 화면의 `+20` 이 방장 태그와 같은 스타일이라 헷갈렸다 → `tag-bet` 전용 색.
- 먼저 나온 사람의 대기 화면이 "기다리세요" 한 줄뿐이었다 → **지금 순위·판돈을 실시간으로** 보여 준다.

### 적대적 재검토에서 나온 진짜 구멍 (전부 고침)

1. **판이 영영 안 끝나는 경우가 있었다 (치명)** — 마지막까지 **살아남아** 판을 끝낸 사람은
   `alive: true` 로 남는데 종료 판정은 "전원 죽었나"만 봤다. 그 한 명 때문에 모두가
   "다른 사람들이 아직 오르고 있습니다" 화면에서 굳는다. → `out` 의 뜻을 **"이 판에서 손을
   뗐다"**로 넓히고, 살아서 끝낸 사람도 나갈 때 도장을 찍는다(`markOut`). 순위 계산은
   여전히 `alive` 를 보므로 "동점이면 살아남은 쪽이 위"는 그대로다.
2. **부활 신발이 복제될 수 있었다 (치명)** — ① 판돈 올리기 ② 되살아나기 두 단계였는데,
   ②가 실패하면 지갑을 되돌리면서 **판돈은 항아리에 남았다.** `withTimeout` 은 거절만 할 뿐
   RTDB 쓰기를 취소하지 못하므로 ①의 실패도 같은 함정이었다. → **한 번의 멀티패스
   업데이트**로 묶었다(전부 되거나 전부 안 된다). 실패해도 서버를 다시 읽어 "정말 안
   들어갔는지" 확인한 뒤에만 환불한다.
3. **도장 비트가 한 칸 어긋나 있었다 (심각)** — 승자는 **자기 몫까지** 걷으므로 `settleRoom` 은
   비트를 순위 **전체**에 매기는데 `hasUnclaimed` 만 `slice(1)` 로 셌다. 그래서 다 걷고도
   '방에 남기'가 영원히 `pending` 이 되고(대기자가 갇힌다), 반대 경우에는 아직 안 걷힌
   신발을 걷힌 것으로 보고 `result` 를 지워 **증발**시켰다.
4. **끝난 판에 신발을 걸 수 있었다** — 시계가 어긋나면 이미 순위가 박힌 뒤에 20켤레가
   올라가고, 그 신발은 아무도 안 걷는다. → `rankings` 가 있으면 부활 자체를 거부한다.
5. **부활 창을 폰 시계로 쟀다** — 남들은 내 `deadAt`(서버 보정)+20초로 판정하는데 내 화면만
   로컬 시계였다. 시계가 어긋난 만큼 창이 길거나 짧았다 → `deadAt` 기준으로 계산한다.
6. **건 신발이 다음 접속에 되돌아왔다** — 지갑 병합은 신발별 **max** 라, 부활 직후 서버로
   안 밀어 올리면 옛 값이 이긴다(항아리에는 그대로 있으니 복제). → 걸자마자 `syncWallet()`.
   같은 이유로 부팅 시 `pullAll()` **뒤에** `sweepUnsettled()` 를 돌리게 순서를 못 박았다.
7. **일시정지 기권이 남들을 20초 붙잡았다** — 기권은 부활할 생각이 없다는 뜻인데 도장을
   안 찍어 창이 열린 채로 보였다.
8. **안 낸 패자의 빚이 '방에 남기' 한 번에 면제됐다** — `resetRoom` 이 미수령만 봤다.
   이제 미납부도 3분간 기다린다(그 뒤에는 안 온 것으로 본다).
9. **판돈이 실제 수령액보다 1 많게 표시됐다** — 1등은 기본 1켤레를 안 내는데 전원 몫을
   더했다. 기본 몫은 **인원 − 1** 이 정확하다.


## 9-0-25. 랙과 "신발 100켤레 증발" — 둘 다 판이 안 끝나서 생긴 일이다 (2026-08-19)

사용자 신고 두 건으로 시작했다. **"랙이 있어 튕긴다"**, 그리고
**"신발 100개를 20개씩 다섯 번 걸었는데 이긴 사람이 못 가져가고 사라졌다."**
파 보니 뿌리가 하나였다 — **판이 끝나지 않으면 항아리가 주인을 잃는다.**

### 증발 경로 (정확히 재현되는 순서)

1. 신발 100켤레를 가진 사람이 20켤레씩 **다섯 번 부활**한다 → 지갑 0, 항아리 100.
2. 상대가 렉으로 튕긴다 → 아무도 `finalizeResult` 를 못 부른다 → **`rankings` 가 안 생긴다.**
3. `mustKeepRoom(room)` 이 `result.rankings` 가 없으면 곧장 `false` 를 돌려줬다
   → "볼일 없는 방"으로 분류 → 마지막 사람이 나갈 때 `leaveRoom` 이 **방을 지운다.**
4. 방이 사라지면 `given` 도 함께 사라진다. **100켤레가 증발한다.**

승자가 못 가져간 게 아니라 **가져갈 방이 없어진** 것이다.

### 고친 것 — 항아리는 순위보다 오래 산다

| 고침 | 무엇을 막나 |
|------|------------|
| `mustKeepRoom` 이 **순위가 없어도 `given` 이 비지 않았으면 방을 지킨다** | 위 3번. 순위가 없다는 건 "아직 아무도 못 받았다"는 뜻이므로 **오히려 더 지켜야 한다** |
| `reclaimStake(code)` — 순위가 끝내 안 박히면 **주인이 되찾는다** | 위 2번. **서버에서 먼저 지우고** 지갑에 넣는다. 반대 순서면 지우기 실패 시 다음 접속에 또 받아 복제된다 |
| `settleRoom` 의 환불 분기 — 순위가 없고 **살아 있는 참가자가 하나도 없을 때만** 회수 | 아직 뛰는 판의 판돈을 도로 빼가는 것 |
| 승자가 **순위에 없는 사람의 판돈까지** 걷는다 (`ORPHAN_BIT = 1 << 15`) | 판 도중 방에서 빠진 사람의 판돈. 규칙이 참가자만 `rankings` 에 넣으므로 그 사람은 순위에 못 들어간다 |
| `finalizeResult` 가 **혼자 남아도 순위를 박는다** (예전엔 `minPlayers` 미만이면 포기) | "다른 사람들이 아직 오르고 있습니다" 화면에서 영원히 갇히는 것 |
| RTDB 규칙: `given/$uid/.write` 에 `\|\| !newData.exists()` | 방을 나간 뒤에는 자기 판돈도 못 지웠다. **지우기만** 열었다 — 순위 없이 새로 거는 건 여전히 막힌다 |

### "튕김"의 정체 — 폰 시계로 남의 부활 창을 쟀다

`deadAt` 을 `Date.now() + offset` 으로 썼다. 그 보정값은 실패하면 **조용히 0**이 된다(§9-0-22).
그러면:

- 시계가 1분 **빠른** 폰이 죽으면 `deadAt` 이 미래 → 남들의 종료 판정이 **영원히 안 선다**
- 시계가 **느린** 폰이면 남들 눈에는 이미 20초가 지난 것 → **부활을 고르는 중에 판이 끝난다**

두 번째가 사용자가 말한 "튕김"이다. 이제 `reportDeath` · `reviveMe` 가
`serverTimestamp()` 를 쓴다 — **서버가 자기 시계로 채우므로 쓰는 쪽 시계가 무의미해진다.**
읽는 쪽은 각자 `.info/serverTimeOffset` 으로 보정하니 모두가 같은 답을 낸다.

### 진짜 렉 — 멀티 HUD 가 프레임마다 글자를 다시 구웠다

상대 표시가 붙으면서 매 프레임 문자열이 늘었다. `text()` 는 글자를 **그때그때 fillRect 로** 찍고,
외곽선이 붙으면 글리프 하나가 **9번**이다(§9-0-14). 상대 3명이면 이름·기록·판돈·알림까지
프레임당 수천 번이 된다. 전부 `textCached()` 로 바꾸고, 그 위에 세 겹을 더 얹었다:

- **판돈 줄**은 방 스냅샷이 바뀔 때만 다시 만든다 (`scene.potRoom` 비교) — `potShoes()` 는
  참가자 전원의 `given` 을 훑는 계산이라 프레임마다 돌릴 값이 아니다
- **알림 줄바꿈**은 메시지마다 한 번만 잰다 (`t.lines` 캐시) — 폭 측정은 글자 수만큼 돈다
- **얼굴 아이콘 로딩 확인**은 상대 목록 자체가 바뀔 때만 (`scene.assetsFor`)
- 글리프 캐시 상한 96 → **320** (한글 이름 + 7px/11px 두 벌이 96을 금방 넘겨 **캐시가 오히려
  매 프레임 버려지고 있었다** — 캐시를 넣고도 안 빨라지던 이유)

카운트다운 숫자도 캐시로 옮겼다. 배율 6 + 외곽선이면 글자 하나가 프레임당 수백 fillRect 다.

### 검사

`qa:multi` **23번 묶음**이 위를 전부 못 박는다 — 순위 없는 방의 판돈 보존, 회수 순서
(서버 먼저 → 지갑 나중), 고아 판돈 수거, 1인 확정, 서버 시각 사용, **HUD 에 캐시 없는
`text()` 호출이 하나도 없는지**. `qa:rules` 8번은 규칙에 회수 조항이 있는지 본다.


## 9-0-26. "둘 다 나가기를 눌렀는데 신발이 안 넘어가고 튕긴다" (2026-08-19)

사용자 신고 그대로다. 이번에는 **추측을 그만두고 서버를 흉내 냈다.**

### 먼저 도구부터 만들었다 — `npm run sim:multi`

실기기 두 대와 계정 두 개가 없으면 "둘이 동시에 나가기" 같은 **순서 버그**는 재현이 안 된다.
그래서 `tools/_sim/rtdb.mjs` 에 메모리 RTDB(경로·멀티패스 update·onValue·서버 시각·권한)를
만들고, 로더(`tools/_sim-loader.mjs`)로 `firebase.js`·`auth.js`·`profile.js` 만 대역으로 바꿔
**진짜 `multiplayer.js`·`multiSettle.js` 를 그대로** 돌린다. 사람마다 `localStorage` 를 따로 준다.

검사 기준은 하나다 — **신발 총량은 절대 변하지 않는다**(지갑 합계 + 아직 안 걷힌 항아리).
`Date.now()` 를 갈아 끼워 "2분 뒤"를 즉시 만들 수 있으므로 시간 규칙도 검사할 수 있다.

**만들자마자 신고된 버그가 그대로 재현됐다**(S1·S2 실패). 여기부터는 계측이다.

### 재현된 원인 넷

| # | 무엇이 | 왜 신발이 안 넘어가나 |
|---|--------|----------------------|
| ① | **판이 끝나기 전에 방을 나가면 순위에서 사라진다** | 규칙상 `rankings` 에는 **방에 남아 있는 사람만** 담긴다. 먼저 나간 사람은 낼 것도 받을 것도 없어지고, **둘 다 나가면 방까지 지워져 판이 통째로 증발**한다 |
| ② | **튕긴 사람은 `alive: true` 로 영원히 남는다** | 종료 판정이 영원히 거짓 → 순위가 안 박힘 → 아무도 정산 못 함. 항아리에 건 신발(최대 200켤레)이 통째로 묶인다 |
| ③ | **순위를 박는 일이 '화면'의 몫이었다** | 둘 다 앱이 죽으면 순위를 박을 사람이 없다. 다음 접속의 청산(`sweepUnsettled`)은 정산만 했지 **판을 끝내지는 않았다** |
| ④ | **도장이 "걷었다/안 걷었다"의 비트 하나였다** | 한 사람이 **두 번에 나눠 낸다**(부활 20켤레 먼저, 기본 1켤레 나중). 20켤레를 걷으며 비트를 찍으면 **뒤에 올라온 1켤레는 영영 안 걷힌다** |

### 고친 것

- **나가기 전에 판을 끝낸다** (`leaveRoom`). 끝낼 수 있으면 그 자리에서 순위를 박고 나가고,
  아직 뛰는 사람이 있으면 **자리를 지키고 물러난다**(`'kept'`). 순위에서 사라지는 경로가 없어진다.
- **생존 신호** (`seenAt`, 5초마다). 진행도만으로는 부족하다 — 일시정지 중이거나 죽어서
  부활을 고르는 20초 동안에는 계단이 안 바뀌어 아무 쓰기도 안 나간다. 그래서 **게임 화면·
  대기방·결과 화면이 각각 따로** 보낸다. 90초 넘게 끊기면 `matchRules.isStale` 이 판에서 뺀다
  (60초가 아니라 90초인 이유: 탭이 가려지면 브라우저가 타이머를 1분에 한 번으로 조인다).
  `seenAt` 이 아예 없으면 판정하지 않는다 — **모르는 것을 근거로 남을 빼면 안 된다.**
- **정산이 판을 끝낸다** (`settleRoom` → `roundOverNow`). 이제 **접속만 해도** 밀린 판이 풀린다.
  단 **대기 중인 방은 절대 끝난 판이 아니다** — 대기방에서는 아무도 진행도를 안 보내므로
  그 상태를 종료로 읽으면 로비에 앉아 있다가 순위가 박히고 신발을 잃는다.
- **도장을 켤레 수로** (`result/claims/{승자}/{낸사람}` = 걷은 켤레 수). "목록 길이 − 걷은 수"
  만큼만 더 걷으므로 몇 번에 나눠 내든 정확히 한 번씩 간다.
  **옛 비트마스크(`result/settled`)도 함께 쓴다** — 배포 직후에는 옛 클라이언트가 아직
  돌아다니고(PWA 캐시), 옛 코드가 맵을 `Number()` 로 읽으면 `NaN` 이 되어 **이미 걷은 신발을
  처음부터 다시 걷는다(복제).** 잃는 것보다 불어나는 게 훨씬 나쁘다.
- **다음 판에는 자리에 있는 사람만 데려간다** (`resetRoom`). ①번 고침 때문에 먼저 나간 사람이
  방에 남는데, 그대로 다음 판에 태우면 **자리에 없는 사람이 자동으로 꼴찌가 되어 신발을 잃는다.**
  되돌릴 때 `seenAt` 도 새로 찍는다 — 안 그러면 지난 판의 낡은 값 때문에 **다음 판이 시작하자마자
  전원이 '튕긴 사람'으로 판정된다.**
- **순위가 박혔으면 게임 화면도 즉시 끝낸다.** 내가 잠깐 튕긴 사이 남들이 판을 끝냈으면,
  나 혼자 계속 오르는 건 아무 의미가 없다.

### 규칙 (게시 완료 · 콘솔에서 되읽어 해시까지 확인)

`players/$uid/seenAt`(숫자, 본인 또는 다음 판 준비), `result/claims/$uid/$from`(0~220).
`result/settled` 는 **숫자 그대로 남겼다**(옛 클라이언트가 읽는다).

### 검사

`npm run sim:multi` 가 18개 시나리오를 돈다 — 둘 다 나가기, 결과 화면을 스쳐 지나가기,
튕김, 100켤레 부활 뒤 상대 튕김, 3인, 재정산 반복, 다음 판, 옛 도장 호환, 나눠 내기,
그리고 **무작위 60판**(부활·튕김·이탈 순서를 섞어서). 전부에서 총량 보존과 "안 끝난 판 0" 을 본다.
`qa:multi` 24번 묶음이 각 고침의 코드 경로를 못 박는다.


## 9-0-27. 레이스 게이지 개편 · 부활 6회 · 승자 방장 (2026-08-19)

기획 변경 6건. 핵심은 **인게임에서 글자를 없애고 오른쪽 게이지 하나로 모은 것**이다.

### 바뀐 수치

| | 전 | 후 | 왜 |
|---|---|---|---|
| 부활 상한 | 10회 | **6회** | 게이지 테두리를 6칸으로 나눠 "남은 목숨"을 그림으로 보여 준다 |
| 부활 창 | 20초 | **10초** | 20초는 남은 사람들이 멀뚱히 기다리기에 너무 길다 |
| 걸 신발이 없을 때 | 20초 | **5초** | 고를 수 있는 게 '나가기' 하나뿐인데 셀 이유가 없다 |

**창을 짧게 하는 건 내 화면에서만** 한다. 남들의 판정 기준(`reviveExpired`)은 여전히 10초다 —
남은 사람은 내 지갑을 모르기 때문이다. 내 쪽이 먼저 닫히면서 `out` 도장을 찍으므로
남들도 그 즉시 기다림을 멈춘다. 규칙을 두 개로 갈라 놓으면 반드시 어긋난다.

### 자리 색 — 로비와 게임을 잇는 유일한 끈

들어온 순서대로 **1번 빨강 · 2번 노랑 · 3번 파랑 · 4번 초록**(`matchRules.slotIndex`,
`palette.SLOT_COLORS`). 대기방에는 번호 상자로, 인게임에는 얼굴 테두리 색으로 나온다.
**인게임에서 아이디를 통째로 지웠기 때문에** 색이 곧 신원이다.
판정은 `joinedAt`(같으면 uid)이라 누가 계산해도 같은 답이 나온다 — 사람마다 색이 다르면
"노랑이 앞섰다"는 말 자체가 성립하지 않는다.

### 오른쪽 레이스 게이지 (`multiHud.js` 전면 개편)

- **나는 항상 정중앙(y=150)에서 절대 움직이지 않는다.** 보여 줄 것은 절대 층수가 아니라
  **나와의 차이**다. 눈금은 위로 10칸·아래로 10칸, **한 칸 = 계단 10칸**.
  30칸 앞이면 3칸 위, 100칸 넘게 앞서면 맨 위에 붙는다.
- 각자 **얼굴 아이콘 16×16**, 그 둘레에 **3도트 테두리**가 자리 색으로 둘러진다.
  테두리는 **6칸**(위 2 · 오른쪽 1 · 아래 2 · 왼쪽 1)이고 **부활 한 번에 한 칸씩 꺼진다.**
  마지막 칸이 마지막 목숨이라 **아꼈다가 늦게 쓰는 쪽이 유리하다** — 승부처가 게이지에 다 있다.
- 지운 것: 상대 이름·층수 줄, 고스트 이름표. 대신 고스트 발밑에 **자리 색 점**을 찍어
  계단 위의 그 사람과 게이지의 그 얼굴을 잇는다.

**미리보기 도구를 만들어 눈으로 고쳤다** (`npm run preview:hud` → `tools/_out/hud.png`).
실기기 두 대로만 볼 수 있는 화면이라 안 만들면 확인할 방법이 없다. 실제로 두 가지를 잡았다 —
① 테두리에 구분선이 없어 **몇 칸 남았는지 셀 수가 없었다**, ② 알림 글자가 게이지 위로 올라탔다
(알림 폭을 게이지 왼쪽으로 제한했다).

### 이긴 사람이 방장이 된다

`finalizeResult` 가 순위를 박으면서 `hostUid` 를 1등으로 넘긴다. 값이 **순위 1등**이라
누가 계산해도 같으므로, 규칙도 딱 그 값만 허용한다
(`newData.val() == newData.parent().child('result').child('rankings').child('0').val()`).
"한 판 더"의 주도권이 승자에게 가고, 방장이 먼저 나간 방도 자동으로 주인을 찾는다.

### 남아 있던 "튕김" 두 가지

- **판이 끝난 뒤 화면이 20초 멈추는 문제.** `onFinish` 가 `await finalizeResult()` 를 먼저
  했는데, 그 안의 서버 읽기가 최대 8초·쓰기가 12초다. 이제 **화면부터 넘기고** 확정은
  뒤에서 돌린다 — 어차피 결과 화면·정산·다음 접속 청산이 각각 한 번씩 더 시도한다.
- **결과 화면이 "로딩"에서 멈추는 문제.** 방을 못 읽거나 이미 지워졌으면 구독 콜백이
  아예 안 온다. 8초 안전망을 걸어 **어떤 경우에도 화면이 뜨고 로비로 나갈 수 있게** 했다.

한 명이 튕겼을 때의 정산은 §9-0-26 의 생존 신호가 이미 처리한다 —
신호가 끊긴 사람은 판에서 빠지고, **계단이 높은 쪽이 항아리를 통째로 가져간다**
(`sim:multi` S20 이 이 값을 그대로 검사한다).


## 9-0-28. 마지막까지 버틴 사람이 이긴다 — 규칙·화면 7건 (2026-08-19)

### 판을 끝내는 방식이 바뀌었다 (이번 회차의 핵심)

| | 전 | 후 |
|---|---|---|
| 1:1 종료 | 둘 다 빠져야 끝 | **한 명이 빠지면 그 자리에서 끝** |
| 순위 1순위 | 계단 수 | **판에 남아 있는가** (기권·튕김은 아래로) → 그다음 계단 수 |

죽고 10초 안에 부활하지 않으면 **계단이 아무리 높아도 진다.** 이게 없으면
"앞설 때 나가 버리면 이긴다"가 필승법이 된다 — 상대는 계속 오를 수 있는데 판이 굳기 때문이다.
그래서 기권도 튕김도 결과가 같다: **남아서 계속 오른 사람이 가져간다.**
셋 이상은 한 명이 빠져도 판이 계속되고, 전원이 빠진 뒤에는 계단 수가 승부를 가른다.

`rankPlayers` 는 이제 `now` 를 받는다 — 신호가 끊긴 사람(§9-0-26)을 "남아 있다"로 세면
튕긴 사람이 1등이 되기 때문이다. 종료 판정과 **같은 시각**을 본다.

### 결과 화면이 유일한 출구다

- 판이 끝나면 **무조건** 멀티 결과 화면으로 간다. 인게임 '기권하고 나가기'도 마찬가지다.
  1:1 이면 곧바로 결과가, 3~4인이면 **현재 상태창**(누가 몇 계단인지 실시간)이 뜬다.
- 로비로 나가는 길은 결과 화면의 **`대기하지 않고 나가기`** 하나뿐이다. '한 판 더' 버튼은 없앴다 —
  로비를 거치지 않고 새 방을 잡으면 정산이 끝나기 전에 화면을 떠나는 경로가 생긴다.
- **1등이면 "걸린 신발 N켤레를 모두 가져왔습니다"가 반드시 뜬다.** 예전에는 정산 결과
  객체가 있을 때만 문구를 띄웠는데, 정산은 늦거나 `null` 이 되는 경우가 많아
  **이긴 사람이 아무 말도 못 듣는** 일이 있었다(사용자 신고). 순위표만 있으면 승패는
  확정이므로 이제 **순위로 판단**하고, 숫자는 항아리 전체를 쓴다.

### 오른쪽 게이지 — 등수 · 왕관 · 죽음 · 겹침

- 얼굴 왼쪽에 **등수**. 1등은 11px 볼드 **흰색 + 검은 외곽선 + 머리 위 왕관**,
  2~4등은 7px **노랑**. 크기·색이 다르면 숫자를 읽기 전에 "쟤가 1등"이 먼저 보인다.
- 상대 상자에는 자리 색 테두리 **바깥으로 1도트 검은 테두리**를 더 두른다 —
  밝은 배경에서 노랑·초록이 묻힌다.
- **죽으면 얼굴이 회색이 되고 그 위에 남은 초가 카운트다운된다.** 상대가 부활하면
  1위보다 20칸 앞에서 살아나므로, 그 숫자가 곧 "따라잡을 시간"이다.
- 20칸 안쪽으로 붙으면 상자(22도트)가 눈금 간격(9도트)보다 커서 서로를 가린다 →
  **겹치는 사람은 왼쪽으로 비켜 세우고**, 등수는 얼굴을 다 그린 뒤에 찍는다
  (비켜선 사람은 상자 위에). 둘 다 미리보기로 확인하고 고쳤다.

### 1등 말풍선 — 계단 위 1등 머리 위

사용자가 준 `etc/1등이닷.png` 를 빌드 타임에 도트로 굽고 **글씨까지 함께 구웠다**
(`tools/build-bubble.mjs` → `ui/bubble_first.png` 38×24). 매 프레임 글자를 찍지 않으려는
것도 있지만, 위치가 매번 어긋나지 않게 하려는 이유가 더 크다.
글자 진행 폭은 런타임(`pixelfont.blit`)과 **글자 그대로 같은 식**을 쓴다 — 다르면 삐뚤어진다.
1등을 빼앗기는 순간 말풍선이 머리에서 떨어져 나가므로, 그것만 봐도 역전당한 걸 안다.

### 그 밖

- **부활 상한 6회 · 창 10초**(걸 신발이 없으면 5초)는 §9-0-27 그대로.
- **자리 번호 상자**(1빨강·2노랑·3파랑·4초록)가 대기방에 뜬다 — 인게임에는 아이디가 없으므로
  로비에서 외운 색이 곧 신원이다.
- 검사: `qa:multi` 26번 묶음(결과 화면 강제·1:1 즉시 종료·순위 우선순위·1등 표시·말풍선),
  `sim:multi` S21(1:1 부활 포기 = 즉시 패배)와 무작위 60판.


## 9-0-29. 폰트 — DOM 은 Neo둥근모로, 인게임은 갈무리 그대로 (2026-08-19)

**"게임 화면 글씨가 안 읽힌다"** 는 지적에 인게임 폰트를 Neo둥근모 16px 한 벌로 갈아 봤다.
또렷하긴 한데 **도트 게임의 아기자기한 맛이 사라졌다** — 글자가 화면에서 차지하는 비중이
커지면서 계단·캐릭터보다 UI가 먼저 눈에 들어왔다. 그래서 **인게임은 되돌리고
로비 등 DOM 화면만 Neo둥근모를 남겼다.**

| 어디 | 폰트 | 왜 |
|---|---|---|
| **인게임 캔버스** | 갈무리11 볼드(11px) + 갈무리7(7px) | 도트 게임 특유의 작고 아기자기한 맛. 판돈·알림·등수는 7px |
| **로비 등 DOM 화면** | **Neo둥근모** 웹폰트(45KB) | 예전엔 `Courier New` 라 한글이 시스템 고딕으로 떨어져 캔버스와 따로 놀았다 |

### 남긴 것 (되돌리지 않은 것)

문구 개편은 폰트와 무관하게 좋아서 그대로 뒀다.

| 전 | 후 |
|---|---|
| `이 게임에 걸린 신발 64켤레` | **`1등하면 신발 64켤레!`** |
| `○○님이 신발 20개를 걸고 20칸 앞으로 부활했습니다` | **`노란색이 1등 부활!`** |
| `○○님이 계단에서 떨어졌습니다` | **`빨강색이 떨어졌다!`** |

인게임에 아이디를 안 쓰기 때문에(§9-0-27) **자리 색이 곧 신원**이다 —
게이지에서 그 색 테두리를 찾으면 그 사람이다. 짧아서 7px 한 줄에 넉넉히 들어간다(69px).

### 이 왕복에서 남은 도구

- `npm run preview:screens` — 인게임 캔버스 화면 7종(HUD·카운트다운·일시정지 싱글/멀티·
  게임오버·부활·멀티 사망)을 한 장에 모아 찍는다(`tools/_out/screens.png`).
  **폰트나 문구를 건드리면 모든 줄의 자리가 한꺼번에 틀어진다** — 이걸로 눈으로 확인한다.
- `qa:multi` 27번 묶음이 "인게임은 갈무리 두 벌 · DOM 은 Neo둥근모"를 못 박고,
  알림·판돈 문구가 7px 한 줄(180)에 들어가는지 폭으로 잰다.

### 교훈

**"안 읽힌다"는 크기 문제가 아닐 수 있다.** 실제 원인은 7px 한 벌이 아니라 *어떤 줄을*
7px 로 그렸느냐였고, 크기를 통째로 키우자 이번엔 게임의 분위기가 깨졌다.
바꾸기 전에 **화면 한 장을 찍어 두고** 바꾼 뒤와 나란히 보는 게 가장 빠른 판단 방법이다.


## 9-0-30. 결과 화면 개편 · 승부 규칙 재확정 · 게이지 손질 (2026-08-19)

### ★ 승부 규칙을 되돌렸다 — **계단이 높은 사람이 이긴다**

직전 회차에 "1:1 은 먼저 포기한 쪽이 진다"(판에 남아 있는 사람이 1순위)로 바꿨는데,
사용자가 두 번에 걸쳐 다시 못 박았다 — *"방이 사라지던 튕기던 무조건, 걸린 신발은
게임이 종료된 시점에서 **더 높은 계단에 있던 사람**이 가져간다"*, *"계단 최상위에
위치한 사람이 승리"*. 그래서 `rankPlayers` 의 1순위는 **계단 수**로 되돌리고,
생존(`판에 남아 있는가`)은 **동점일 때만** 본다.

1:1 이 한 명 빠지는 순간 끝나는 것(§9-0-27)은 그대로다. 그래서 **끝난 순간의 진행도**가
그대로 순위가 된다 — 진행도는 300ms마다 올라가므로 실제 대전에서는 거의 최신값이다.

### 결과 화면 (`MultiResult`)

- **승패를 먼저, 크게.** 사람이 제일 먼저 알고 싶은 건 "내가 이겼나"다.
  - 승리: `etc/승리깃발.png` 를 168×252 로 구워(`ui/victory_flag.png`) 위에 놓고,
    **승리하셨습니다 ! / 1등을 축하합니다** 를 26·18px 로, 가져온 신발 수만 **초록 20px**로.
    글자에는 도트답게 **흐림 없는 각진 그림자**(text-shadow 2~3px)를 넣었다.
  - 패배: **게임에서 패배하였습니다** + 기획서 문구 그대로 `내 소중한 신발 N켤레를
    뺏겼습니다`, 그 아래 **팁 상자**(부활을 아껴쓰세요 / 상대가 다 쓰고 나면 역전하세요).
    `이번 게임에서 신발 N켤레를 잃었습니다` 줄은 삭제했다.
- 순위 줄의 왼쪽 숫자는 **의미 없는 목록 번호**였다 → **1등·2등·3등·4등**.
- 얼굴을 **자리 색 3~4도트 테두리**로 감싸고 그 테두리를 **6칸**으로 나눠
  부활을 몇 번 썼는지 보여 준다(안 쓰면 꽉 참, 6번 다 쓰면 전부 꺼짐).
  인게임 레이스 게이지와 **같은 그림**이라 "몇 개 써서 이겼는지"가 결과에서도 읽힌다.
- 버튼: `한 판 더` 삭제, `방에 남기` → **`계속하기`**.

**`el()` 의 진짜 버그 하나** — `style` 을 `Object.assign` 으로 넣고 있어서 **CSS 변수
(`--slot`)가 조용히 무시됐다.** 그래서 위 테두리가 통째로 안 보였다(미리보기로 확인).
이제 `--` 로 시작하면 `setProperty` 로 넣는다.

### 로비

- `플레이어 이름 : ○○` 줄 삭제 → 그 자리에 **`나의 멀티게임 승률 N승 / M게임`**.
- 캐릭터 이름은 **그림 바로 아래 노란 글씨**로, 캐릭터 칸 폭은 56 → **73px**(+30%).

### 인게임

- **내 얼굴이 레이스 게이지에 안 나오던 버그.** `GameScene` 은 내 캐릭터의 front·side·jump
  만 받고 `_face` 는 **상대용으로만** 받고 있었다 — 그래서 내 칸만 비어 있었다.
- **눈금 한 칸 = 5계단**(전 10계단), 위아래 10칸 = ±50계단. 접전이 가운데 한 칸에 뭉쳐
  차이가 안 보이던 문제. 게이지 세로도 26~240 으로 늘렸다(멀티는 우상단 부활 하트가 없다).
- 계단 숫자 바로 아래 **`(1등과의 거리 N계단)`**, 내가 1등이면 **`(1등 유지중)`**.
- **1등 말풍선**을 사용자가 준 새 그림(`etc/bubble_first_new_font.png` 68×34)으로 교체.
  머리 **정중앙 위 3도트**에 띄우고, 왼쪽을 보고 있으면 **좌우로 뒤집어** 꼬리가 항상
  정수리를 가리킨다 — 얼굴·몸을 절대 가리지 않는다.
- 문구: 부활 버튼 `신발 20개 써서 / 1위로 부활`, 사망 화면 `승리시 신발 N켤레 !` +
  한 단계 작은 `나의 남은 신발 N켤레`, 패널 여백을 위아래로 벌렸다.
- 게임오버 `다시하기` 아래에 8도트 여백.

### 검사

`qa:multi` 28번 묶음(결과 화면 · 메뉴 순서 · 로비 · 게이지 · 말풍선 · 문구),
`sim:multi` 는 계단 우선 규칙에 맞춰 21개 시나리오를 전부 다시 맞췄다.
화면은 `npm run preview:screens`(인게임 9종) · `npm run preview:result`(승/패)로 눈으로 봤다.

## 9-0-31. 14+ 항목 일괄 처리 — 부활 30개 · 130종 별자리 이름 · 계단 사라짐 버그 ·
게임패드 리매핑 · 설정 화면 · 대기방 참가자 카드 (2026-08-19)

사용자가 큰 배치로 올린 요청을 순서대로 처리했다. 절반은 문구·수치 조정, 절반은
버그 수정과 새 기능이라 항목별로 남긴다.

### "커밋이 안 됨 / 재배포 에러" — 둘 다 정상 동작이었다

git reflog(`.git/logs/HEAD`)를 직접 읽어 확인했다. 지난 파일 전달 이후 커밋
(`d5f56aec...`)이 실제로 만들어지고 푸시까지 됐고, 그 뒤 `push-to-github.bat` 을
다시 돌려서 뜬 `Nothing to push - already up to date.` 는 **성공 메시지**를 실패로
오인한 것이었다. 깨끗한 폴더에서 `npm ci && npm run build` 로도 재확인해 문제 없음을
확인했다. 이 왕복에서 배운 것: 이전 턴에 **새 `commit-msg.txt` 를 전달하지 않아서**
기기에는 8/15 날짜의 옛 메시지가 남아 있었다 — 배치 작업 마지막에는 항상 그 판의
`commit-msg.txt` 를 새로 구워 같이 보낸다.

### 문구·수치 조정 (기획 그대로 반영)

| 항목 | 전 | 후 |
|---|---|---|
| 인게임 1등 표시 | `(1등 유지중)` | `현재 1등 유지중!` |
| 인게임 격차 표시 | `(1등과의 거리 N계단)` | `1등까지 N계단 남음` |
| 1등 말풍선 | 계단 위에 표시 | **삭제** — 자리 색 테두리만으로 충분하다는 판단 |
| 부활 알림 | `노란색이 1등 부활!` | `노랑, 1등 부활` (4색 전부) |
| 부활 원가(싱글) | 50켤레 | **30켤레** |
| 멀티 결과 화면 제목 | `멀티 결과` | `게임 결과` |
| 난이도 설명 | `게임난이도(신발의 등장 빈도가 달라집니다)` | `게임난이도를 높이면, 신발도 많이 등장합니다` |
| 도감 메뉴명 | `나의 신발 컬렉션` | `신발 도감` |
| 멀티 로비 제목 | `멀티게임` | `멀티게임 로비` |
| 대기방 제목 | `방 코드` | `멀티게임 N번 방` (실제 방 번호) |

죽은 참가자의 등수 라벨(1등~4등)은 회색 + 7px 로 줄어든다(`multiHud.js drawRankTag`,
`dead` 플래그). 130종 신발 이름은 "스페셜 01" 류 자동 생성 이름을 사용자가 준 별자리
이름 목록으로 전량 교체했다(`tools/slice-newdesign.mjs` 의 `STAR_NAMES` 도 함께 고쳐
재생성해도 이름이 안 돌아가게 했다). 로비·메뉴 화면 타이틀(`title()` 헬퍼 하나를
전 화면이 공유한다)은 20px → **24px**(2단계, 2px/단계)로 키웠다.

### 판 도중 주운 신발이 승자에게 간다

멀티 정산은 그동안 **판돈(부활 비용)만** 걷었다 — 판 중에 주운 신발(`shoesFound`)은
계정에 반영은 됐지만 승자에게 넘어가지 않고 그냥 사라졌다. `matchRules.foundShoesTotal`
(패자들의 습득 총량 계산) + `rollFoundShoe`(그 개수만큼 새로 굴려 승자 지갑에 추가)를
`multiSettle.settleRoom` 의 기존 판돈 정산 도장(`payTag`) 안에 얹었다 — **새로 생기는
신발**이라 "총량 보존" 검사로는 못 잡고, `sim:multi` 에 총량이 **정확히 N만큼 늘어나는지**
보는 새 시나리오(S22·S23)를 추가했다.

### '계속하기' 다음 판 — 기존 참가자는 레디가 자동으로 켜진다

`multiplayer.js resetRoom()` 이 되돌리는 참가자 레코드의 `ready` 를 `false` → `true` 로
바꿨다. 결과 화면에서 `계속하기` 를 눌렀다는 것 자체가 다음 판 의사 표시이므로 레디를
다시 누르게 할 이유가 없다 — **새로 들어오는 사람만** `joinRoom`(→`meRecord`)을 타므로
그쪽은 여전히 `false` 로 시작한다. RTDB 규칙의 `ready` 필드가 `newData.val() == false`
로 리셋값을 못박아 둬서 이 변경이 그대로 막혔던 것도 같이 고쳐 게시했다
(`docs/FIREBASE_RULES.md`, 값 제약 없이 "waiting 전환 때만" 허용).

### 계단이 조작 버튼 틈에서 사라지던 버그

`STAIR.drawBelow: 2` 였을 때 마지막으로 그리는 계단의 바닥(sy=265)이 조작 버튼
윗변(y266)과 정확히 맞아떨어져서, **두 버튼 사이 틈(x54~126)이 항상 텅 비어 보였다.**
버튼이 계단보다 나중에 그려져 옆은 가려 주므로, `drawBelow` 를 8로 늘려 버튼 뒤까지
넉넉히 그려도 화면 밖은 기존 `sy > 340` 컷오프가 알아서 걸러 준다. 계산으로
버튼 영역(y266~314)이 실제로 덮이는지 확인하고 `qa:multi` 에 못박았다.

### ESC / 게임패드 리매핑 + "메뉴 버튼은 어디서나 뒤로가기"

- ESC 키는 원래도 일시정지였다(변경 없음). 게임패드는 표준 매핑 기준
  Y(3)·B(1) 를 X(2)·A(0) 와 완전히 같은 좌/우 버튼으로 묶고, RB/R1(5)·RT/R2(7) 는
  전부 "left", LB/L1(4)·LT/L2(6) 는 전부 "right" 로 묶었다(`core/input.js pollGamepads`).
- **DOM 화면(로비·도감)에서는 ESC/게임패드 메뉴 버튼이 안 먹고 있었다** — 게임 입력이
  꺼져 있을 때(`enabled=false`)는 `pollGamepads()` 전체가 조기 반환했기 때문이다.
  메뉴/뷰 버튼(8·9) 판정만 `enabled` 와 무관하게 항상 돌게 분리하고, 인게임 중이면
  기존 `pauseHandler` 가, DOM 화면이면 새로 만든 `domBackHandler` (router.js 가
  "팝업부터 닫고, 없으면 뒤로가기"로 등록)가 대신 받는다. ESC 키도 같은 이유로
  `router.js` 에 별도 `bindEscBack()` 를 달아 `body.ui-mode` 일 때만 반응하게 했다
  (인게임과 이중 처리되지 않도록 서로 배타적이다). 도감 팝업도 이 경로 하나로 닫힌다 —
  `presentOverlay`/`closeTopOverlay` 인프라를 그대로 재사용했다.

### [조작법 변경] → [설정]

로비 메뉴가 [설정] 으로 바뀌고 그 안에 기존 [조작법 변경] + 새 [음향 설정]
(BGM·SFX 개별 on/off)을 담는다(`screens/Settings.js` 신설). 소리 on/off 자체는
`audio/audio.js` 에 `getSettings/setEnabled` 로 이미 있었는데 **꺼내 쓰는 화면이
없었다** — 저장·게인 반영은 진작 되는데 버튼만 없던 상태였다.

### PWA "앱 바로가기 만들기" — 포털 화면, 로그아웃 위

`services/pwa.js` 의 기존 설치 프롬프트 인프라(`canInstall/promptInstall`, 안드로이드·
데스크톱 크롬은 진짜 설치 대화상자, iOS 는 안내 문구)를 포털 화면에도 노출했다.
그 둘 다 안 되는 PC 브라우저(파이어폭스 등)를 위해 `promptInstall()` 이 돌려주는
`'unavailable'` 을 받아 **Ctrl+D 북마크 안내**로 대신한다 — 설치가 안 되는 브라우저라도
같은 목적(바로가기)은 이룰 수 있게.

### 대기방 참가자 카드 — 보유신발 배지 + 클릭하면 승률 팝업

`players/$uid` 에 `shoesOwned`·`multiWins`·`multiLosses` 스냅샷을 추가했다
(`multiplayer.js meRecord`). **입장 시점 값**이다 — `nickname`·`characterId` 와 같은
패턴으로, 판 중에 신발을 더 모아도 방 안의 숫자는 안 바뀐다. `resetRoom` 이 다음 판을
위해 참가자를 다시 쓸 때도 이 세 값은 그대로 옮겨 적는다. 대기방 이름 옆에 신발
아이콘 + "보유신발 N켤레" 배지(이름이 길어도 `flex`+`ellipsis` 로 자리가 안 밀린다),
이름을 누르면 "승률 N승 / M게임 / 보유신발 N켤레" 팝업이 뜬다(`Collection.js` 의
`presentOverlay` 팝업 패턴 재사용). 새 필드는 RTDB 규칙에도 선언하고
(안 하면 $other 차단에 걸려 방 생성 자체가 거부된다) 콘솔에 실제로 게시했다 —
게시 후 새로고침해 서버 값과 타이핑한 값이 바이트 단위로 같은지 diff 로 확인했다.

### 200~1000층 배경 세분화 (2차에서 완료)

1차에서 "원본이 없다"고 보류했는데 **내가 잘못 봤다.** 아래 §9-0-32 참조.

### 검사

`qa:multi` 30번 묶음이 이번 배치 전체(계단 버그·PWA 버튼·게임패드 리매핑·ESC 바인딩·
설정 화면·타이틀 폰트·참가자 카드)를 못박는다. `qa:rules` 가 새 RTDB 필드 3개를
검증한다. `sim:multi` S22-pre(자동 레디)·S22·S23(판중 습득 신발)이 새로 추가됐고
기존 S1~S21과 함께 전부 통과한다.

## 9-0-32. **배포가 몇 판째 조용히 죽어 있었다** — `etc/` 를 읽는 빌드 단계 (2026-08-19)

§9-0-31 을 전달했더니 *"수정이 안된게 많던데"* 라는 신고가 왔다. 코드를 다시 훑으니
17개 항목이 전부 들어 있었고, 기기 파일도 최신이었고, `git reflog` 에는
`d5f56ae..ac4e995` 푸시 성공까지 찍혀 있었다. **그런데도 사이트는 옛날 그대로였다.**

### 계측

`etc/` 를 잠깐 치우고 빌드를 돌려 봤다 — 그게 곧 Vercel 의 환경이다.

```
$ mv etc etc_hidden && npm run build
Error: Input file is missing: etc/승리깃발.png
종료코드 1
```

`npm run build` = `assets:font && assets:bubble && assets:icons && vite build` 인데,
가운데 **`assets:bubble`(= `tools/build-bubble.mjs`)이 `etc/승리깃발.png` 를 읽는다.**
그리고 `etc/` 는 **`.gitignore` 대상이라 저장소에 없다**(수백 MB 원본, §5).
즉 이 스크립트가 빌드 체인에 들어간 순간부터 **Vercel 빌드는 100% 실패**했다.

증상이 지독한 이유: 커밋도 푸시도 정상이라 로컬에서는 아무 문제가 안 보이고,
사용자에게는 **"고쳤다는데 하나도 안 바뀐 화면"** 만 남는다. 1차 때 내가 이 신고를
"Vercel 대시보드 문제 같으니 스크린샷을 달라"고 넘긴 것도 오진이었다 —
**빌드를 그 환경 그대로 재현해 보는 것**이 확인의 유일한 방법이었다.

### 고친 것

원본이 없으면 **커밋된 산출물을 쓰고 넘어간다**(`public/assets/` 는 커밋한다, §5).
둘 다 없을 때만 실패한다 — 그건 진짜 고쳐야 하는 상태다.

```js
if (!existsSync(SRC)) {
  if (existsSync(OUT)) console.log('원본 없음 — 커밋된 산출물 사용');
  else process.exit(1);
} else { /* 굽는다 */ }
```

`qa:multi` 31번 묶음이 **`build` 체인의 스크립트를 package.json 에서 훑어, `etc/` 를
읽는 것이 있으면 `existsSync` 가드를 갖췄는지 자동으로 검사한다.** 앞으로 이 함정에
다시 빠지지 않게 하는 장치다 — 사람이 기억할 종류의 규칙이 아니다.

### 층 배경 200~1000 (§14, 1차 보류분 완료)

**"원본이 없다"던 1차 판단이 틀렸다.** 나는 내 샌드박스의 `etc/`(오래 전에 staged 된
복사본)와 업로드 폴더만 뒤졌고, **연결된 기기의 실제 폴더를 조회하지 않았다.**
`device_list_dir` 로 보니 `etc/floor200 … floor1000.png` **9장 전부** 있었다.
교훈은 단순하다 — **원본이 있느냐는 원본이 사는 곳에 가서 확인한다.**

9장 모두 이미 180×320 완성본이라 자를 것도 줄일 것도 없었다. 다만 성격이 갈렸다:

| | 고유색 | 처리 |
|---|---|---|
| floor200 · 700 · 800 · 1000 | 16색 | **그대로 복사** — 이미 도트다 |
| floor300 · 400 · 500 · 600 · 900 | 4,465 ~ 17,213색 | 32색 양자화 |

이미 16색인 그림을 양자화에 다시 태우면 **디더링이 없던 노이즈를 새로 만든다.**
그래서 색을 세어 보고 갈린다(`tools/build-floor-bg.mjs`). 이 스크립트는 만들 목록을
`FLOOR_EVENTS.bgSwap` 에서 읽으므로 표에 한 줄 더하면 그림도 같이 생긴다(§3-5).

`downscale-bg.mjs` 와 따로 둔 이유: 그쪽은 **설명 포스터**에서 패널을 찾아 잘라내는
파이프라인이라, 이미 규격이 맞는 완성본을 넣으면 멀쩡한 그림을 잘라먹는다.

> 곁가지: 사용자가 준 `floor300·400·500` 은 게임에 들어 있던 것과 **픽셀이 달랐다** —
> 즉 200~500 도 새 그림으로 교체된 것이었다. "600 이상만 새것"이라고 짐작했으면
> 이것도 놓쳤다. 넷 다 해시로 비교해서 알았다.

### 로비 버튼 폰트 — 1차에서 절반만 고쳤다

§9-0-31 §9 에서 `.screen-title` 만 20→24px 로 키웠는데, 사용자가 예시로 든 넷
("나의 신발 컬렉션 · 캐릭터 바꾸기 · 명예의 전당 · 멀티게임")은 **전부 로비의 버튼
라벨**이다. **로비에는 `.screen-title` 이 아예 없다** — 제목 자리가 로고 이미지다.
그래서 각 화면에 들어가야만 커진 게 보였고 정작 로비는 그대로였다. `.pbtn` 도
15→19px 로 키우고, 한 줄에 셋이 서는 난이도 선택(`.seg .pbtn`)만 15px 로 되돌렸다
(방 목록·티어 탭은 이미 자체 폰트 지정이 있어 영향 없음). 스크린샷으로 확인했다.

### 그 밖

- **방 목록에도 보유신발 배지** — §11 요청이 "대기방 / 방 목록"이었는데 1차에서
  대기방만 했다. `listRooms` 가 방장의 `shoesOwned` 를 함께 싣는다.
- **말풍선 문자열(`firstBubble`)까지 삭제** — 그리는 코드는 1차에서 지웠지만 문자열이
  남아 폰트에 헛글자('닷')를 굽고 있었다.

## 9-0-33. 13건 배치 — 모바일 설치·명예의 전당 sticky·입장 속도·대기방 표 (2026-08-19 3차)

### ★ 명예의 전당 11위가 공중에 뜨던 버그 — **CSS 중복 정의**

증상: 내가 11위면 그 줄만 스크롤 바닥에 달라붙어 9·10위 위에 겹쳐 떴다.

원인은 `screens.css` 에 `.rank-row` 정의가 **두 벌** 있었던 것이다. 뒤쪽(M6) 규칙이
`background` · `border-color` 는 덮었지만 **`position` 은 선언하지 않아서**, 앞쪽 옛
블록의 `.rank-row.me { position: sticky; bottom: 0 }` 가 그대로 살아남았다.
캐스케이드는 **속성 단위**로 덮는다 — 같은 선택자를 다시 쓴다고 앞 블록이 사라지지 않는다.

딸린 `.rank-row .no` · `.rank-row .name` 은 현재 마크업에 없는 옛 선택자였다
(지금은 `.rank-no` · `.rank-name`). 죽은 규칙이라 블록째 지웠다.
**같은 클래스를 쓰는 멀티 결과 화면도 같은 증상을 앓고 있었다** — 한 번에 같이 나았다.

> 검사에 함정이 하나 있었다. 위 내력을 주석으로 적어 두자 `qa:multi` 의
> "sticky 가 없는가" 검사가 **그 설명 주석에 걸려** 거짓 실패를 냈다.
> 그래서 검사는 주석을 걷어낸 뒤에 본다.

### 안드로이드에 `Ctrl+D` 안내가 나가던 문제

`promptInstall()` 이 "iOS 냐 아니냐" 둘로만 갈라서, 설치 프롬프트를 아직 못 받은
**안드로이드 크롬에 PC용 즐겨찾기 안내**가 나갔다(폰에는 Ctrl 키가 없다).
`isAndroid()` · `isMobile()` 을 두고 반환값을 셋으로 늘렸다 —
`ios` / `android`(⋮ 메뉴 → 홈 화면에 추가) / `unavailable`(PC 즐겨찾기).
스플래시 버튼도 `canInstall()` 로만 막던 것을 **모바일이면 항상 보이게** 바꿨다.
프롬프트가 없어도 "메뉴에서 추가하세요"를 알려 주므로 버튼이 제 역할을 한다.

### '방 입장'이 굼뜨던 이유 — 헛왕복과 그냥 세는 시간

세 군데였다.

| 무엇 | 고침 |
|---|---|
| 후보마다 `readOnce` 로 **같은 방을 한 번 더 왕복** | `joinRoom(code, known)` — 스캔해 둔 방 객체를 그대로 넘긴다. 정원 초과는 **쓰고 나서 확인해 스스로 물러나므로** 낡은 값으로 판단해도 안전하다(원래의 안전장치) |
| 빈 목록이면 **무조건 0.9초** 기다렸다 재스캔 | 연결이 붙은 지 1.5초가 지났으면 빈 목록은 진짜 빈 것이다 → 건너뛴다 (`connectedAt`) |
| 방을 만든 뒤 동시생성 확인에 **1.4초** | 700ms. RTDB 전파는 보통 100~200ms 다. 놓쳐도 손해는 "잠깐 방이 둘"뿐이고 목록에서 합쳐진다 |

여기에 **로비에서 미리 붙인다**(`prewarmMultiIfReturning`). 단 `everPlayedMulti()` 인
사람만 — 싱글만 하는 사람이 RTDB 192KB를 받게 되는 건 §9-0-11 에서 고친 회귀다.

### 대기방 참가자 목록을 **표(grid)** 로

flex 였을 때는 레디 태그가 `...` ↔ `레디` 로 바뀔 때마다 그 폭 차이가 앞쪽을 전부 밀어서
**보유신발 배지가 좌우로 움직였다.** 이제 앞의 세 칸은 고정폭이고 남는 폭은 이름 뒤
빈 칸(`.player-gap`)이 먹는다 — 태그가 어떻게 변하든 배지는 같은 자리다.

눈으로만 보면 놓치므로 **측정으로 못 박았다**: `npm run preview:room` 이 360px 폰 폭에서
레디 섞임/전원 레디 두 경우를 렌더하고, `tools/_fitcheck.mjs` 가 각 행의
`scrollWidth - clientWidth`(넘침)와 배지의 x 좌표를 찍는다. 처음엔 방장 행이 2px
넘쳤고 그 값을 보고 칸 폭을 조정했다.

이름을 누르면 뜨는 카드에 **캐릭터 그림**을 넣었다(자리 색 테두리). 인게임에는 아이디가
없고 자리 색·생김새가 신원의 전부라, 대기방에서 얼굴을 붙여 둬야 판 중에 상대를 찾는다.

### 인게임 두 가지 — 겹칠 때의 우선순위

- **상대 고스트를 내 캐릭터보다 먼저 그린다.** `multiHud()` 안에 있던 고스트를
  `multiGhosts()` 로 떼어 `player.render()` **앞**에서 부른다. 캔버스는 나중에 그린 것이
  위에 얹히므로 순서가 곧 우선순위다. 게이지·문구는 그대로 맨 위에 남는다.
- **레이스 게이지는 최대 2개까지만 겹친다.** 셋이 겹치면 세 번째가 안쪽으로 44도트
  튀어나와 계단을 가렸다. 비켜서기를 한 칸까지만 허용하고 그 이상은 **아예 안 그린다**
  (앞사람 뒤에 가려진 것으로 본다). 미리보기에 3명 겹침 패널을 새로 넣어 확인했다.

### 그 밖

- **패배 깃발**(`etc/패배깃발.png` → `defeat_flag.png`). 승리 깃발과 **같은 규격·같은
  여백**으로 굽고, 패배창을 승리창과 같은 뼈대(깃발 → 큰 문구 → 작은 문구 → 숫자 줄)로
  맞췄다. 색만 반대다.
  ※ 이때 `tools/_result-preview.html` 이 **MultiResult.js 를 손으로 베낀 복제본**이라
    깃발을 넣었는데도 미리보기만 옛 모양이었다. 미리보기가 거짓말을 하면 안 되므로
    같이 고치고 "구조를 맞춰 둘 것"을 주석으로 남겼다.
- **설정 개편** — [설정] 은 메뉴 목록만 두고, [사운드 설정] · [싱글게임 배경설정] 을
  하위 화면으로 뺐다. 배경은 16종 + 랜덤 중 고르며 **싱글에만** 적용한다(멀티는 방 시드가
  정한다 — 네 사람이 다른 건물을 보면 같은 판이 아니다).
- 숫자패드의 `↵` · `←` 를 28px 로 키웠다. 기호는 같은 font-size 라도 획이 차지하는 폭이
  숫자의 절반쯤이라 나란히 두면 작아 보인다("엔터가 엔터처럼 안 보인다"는 신고의 원인).
- 문구: `멀티게임 N승 / M게임` · `캐릭터 변경` · `비밀방 생성` ·
  `멀티 게임을 위해서는, 신발 1켤레가 필요합니다`.

### 검사

`qa:multi` 32번 묶음이 13건 전부를 못 박는다. 이번에 **30·31번 묶음 3건이 깨졌는데**,
전부 이번 리팩터로 코드가 옮겨 간 것을 검사가 못 따라온 경우였다(음향 토글이
`SoundSettings.js` 로 이동 등). 검사가 낡은 자리를 가리키고 있었다는 뜻이라 같이 갱신했다.

## 9-0-34. ★ "판 중에 주운 신발이 사라진다" — 세 번 고치고도 안 됐던 진짜 이유 (2026-08-19 4차)

사용자가 **같은 요청을 네 번** 했다. 코드에는 기능이 있었고 시뮬레이터도 통과했는데
실제 대전에서는 안 됐다. 이번엔 추측을 멈추고 **실제 사용자 행동 그대로** 재현했다.

### 재현

```
S22   패자가 방에 **남아 있는** 상태로 검사 → 통과 (그래서 그동안 몰랐다)
S22-b 패자가 결과 화면에서 **먼저 나간 뒤** 승자가 정산 → 승자 지갑 21 (기대 26) ❌
```

### 원인 — `shoesFound` 가 사는 곳이 사라진다

주운 개수는 `players/<uid>/shoesFound` **한 곳에만** 있었다. 그런데 `leaveRoom` 은
`players/<uid>` 를 **통째로 지운다**(시체 방이 매칭 창을 막지 않게 하려고 그렇게 만들었다).

패자는 결과 화면에서 곧장 나간다 — 그게 정상 동작이다. 그 순간 승자가 셀 근거가
증발하고, `foundShoesTotal` 은 조용히 **0** 을 센다. 판돈(`given`)은 `result` 아래
있어서 멀쩡히 걷혔기 때문에 **"판돈은 오는데 주운 신발만 안 온다"** 는 정확히 그 증상이 됐다.

내가 세 번 놓친 이유도 여기 있다. 매번 정산 **계산식**만 들여다봤고, 계산에 쓰는
**데이터가 그 시점에 살아 있는지**를 확인하지 않았다. 시뮬레이터도 내가 짠 대로
"패자가 남아 있는" 경우만 검사했다 — **테스트가 실제 행동을 흉내 내지 않으면
통과는 아무 의미가 없다.**

### 고침 — 결과는 **자립해야 한다**

`finalizeResult` 가 순위를 박을 때 `result/found/<uid>` 도 함께 쓴다. `rankings` ·
`given` 과 같은 이유다: **사람이 방을 떠나도 정산에 필요한 값은 `result` 안에 남아야 한다.**
정산은 `result.found` 를 먼저 보고, 없으면(이 기능 이전에 끝난 방) `players` 로 폴백한다.

> RTDB 규칙에 `result/found` 를 **반드시** 같이 넣어야 한다. `$other: false` 라서
> 규칙에 없으면 **결과 확정 update 가 통째로 거부되고 판이 영영 안 끝난다.**
> 게시 후 콘솔에서 되읽어 바이트 단위로 대조했다.

`내 소중한 신발 N켤레를 뺏겼습니다` 의 N 에도 **내가 주운 몫을 더한다** — 진 사람의
습득분은 지갑에 들어오지 않고 그대로 승자에게 가므로, 사용자 입장에선 그것도 뺏긴 것이다.

### 인게임 가독성 — 7px 를 버렸다

`1등하면 신발 N켤레!` · `노란색 1등 부활!` · `1등까지 N계단 남음` · `현재 1등 유지중!`
전부 **7px+그림자 → 11px+외곽선**. 외곽선은 글리프를 8방향으로 한 번 더 찍으므로
어떤 배경 위에서도 글자가 뜬다. 폭은 **미리 쟀다**(`tools/_measure.mjs`) —
가장 긴 `1등하면 신발 146켤레!` 가 125px 로 화면(180)에 여유 있게 들어간다.

함정 하나: 알림 줄바꿈 계산(`wrap`)이 여전히 7px 로 재고 있었다. 그대로 두면 안 접혀서
오른쪽 레이스 게이지 위로 올라탄다 — 글자 크기를 바꿀 때는 **재는 쪽도 같이** 바꾼다.

사망 화면은 `신발 20개 써서 / 1위로 부활`(두 줄 7px) → **`신발 20개로 부활`(한 줄 11px)**,
그 아래 있던 `나의 남은 신발 N켤레` 줄은 삭제했다(어두운 색이라 배경에 묻혀 "검은 숫자만
떠 있는" 것처럼 보였고, 부활 가능 여부는 버튼 상태가 이미 말해 준다).
못 누르는 상태의 글자색도 `textShadow`(어두움) → `deadGray`(흐린 회색)로 바꿨다 —
꺼져 있어도 읽히기는 해야 한다.

### 로비

- **그림 아래 이름은 캐릭터명이 아니라 내 닉네임**이다. 로그인하고 정한 2~5자가
  "나"를 가리키는 이름이고, 캐릭터 이름은 고를 때만 필요하다.
- **좁은 폰에서 뱃지가 아래로 밀리던 문제** — `flex-wrap: wrap` 이라 폭이 조금만 좁아도
  뱃지 두 칸이 통째로 다음 줄로 떨어졌다. 줄바꿈을 막고(`nowrap`) 가운데 통계 칸만
  줄어들게 했다. 캐릭터 칸은 73 → **66px**(10% 축소). `tools/_lobbyfit.mjs` 로
  **320·360·390·412px** 네 폭에서 뱃지가 같은 줄에 남고 넘침이 0인지 측정한다.
- `캐릭터 닉네임 변경하기` → `닉네임 변경하기`.

### 검사

`qa:multi` 33번 묶음. 이번에도 **28~30번 묶음의 낡은 검사 6건이 깨졌는데**, 전부
이번 변경으로 코드가 옮겨 간 것을 검사가 못 따라온 경우였다(부활 문구가 두 줄→한 줄,
로비 이름이 캐릭터명→닉네임 등). 깨진 검사는 그대로 두면 다음 사람이 "원래 저런가 보다"
하고 넘기게 되므로 같이 갱신했다.

## 9-0-35. 부활창 통일 · 순위표 윈도잉 · 「고대박물관」 간판 다시 새기기 (2026-08-19 5차)

### 부활 창은 지갑과 무관하게 **항상 10초**

한동안 "걸 신발이 없으면 5초"로 줄였다(§9-0-27). 그런데 그러면 **사람마다 죽은 뒤
화면이 머무는 시간이 달라져** 예측이 안 되고, 남들의 종료 판정은 어차피 10초 고정이라
규칙이 둘로 갈렸다. 하나로 통일하는 쪽이 게임을 읽기 쉽게 만든다.

패배 화면의 `부활을 아껴쓰세요…` 3줄 팁도 뺐다 — 깃발·큰 문구·잃은 수까지 읽고 나면
더 읽을 것이 남아 있지 않은 게 낫다.

### 순위표는 **내가 가운데** 오도록 자른다

*"내가 11등이면 굳이 1등이 누군지 볼 필요가 있을까?"* 맞는 지적이다. 순위표에서
사람이 실제로 궁금해하는 건 **바로 위·아래에 누가 있는지**다. `rankWindow()` 가
내 앞뒤 5줄씩(총 11줄)을 잘라 준다.

**길이를 항상 일정하게 유지한다** — 내가 1등이라 위로 못 뻗으면 그만큼 아래로 더 준다.
안 그러면 상위권일수록 목록이 짧아져 화면이 들쭉날쭉해 보인다. 줄마다 등수 숫자가
찍히므로 목록이 6등부터 시작해도 헷갈리지 않는다.

`leaderboard.js` 에 넣지 않고 `services/rankWindow.js` 로 뗀 이유는 `periodKeys.js` 와
같다 — 그쪽은 `firebase.js` → `import.meta.env` 를 타서 **노드에서 부르는 순간 죽는다.**
순수 함수라 브라우저 없이 검사할 수 있어야 해서 따로 뒀다(`qa:rank` 11번 묶음).

### 「조선총독부」 → 「고대박물관」 — 간판을 실제로 다시 새겼다

이름만 바꾸면 **화면과 메뉴가 서로 다른 말을 한다.** 1층 입구 위 석판에 한자 두 줄
(`朝鮮總督府` / `- 慶城府廳 -`)이 새겨져 있어서 그림도 같이 고쳤다
(`tools/build-museum-sign.mjs`).

지우는 방법을 세 번 갈아엎었다. 그 과정이 이 그림의 성격을 말해 준다:

| 시도 | 결과 |
|---|---|
| 글자 픽셀을 **좌우** 이웃으로 메우기 | **가로 줄무늬** — 한자가 가로로 길어 어떤 행은 거의 전부가 글자다. 먼 픽셀 하나를 길게 늘여 칠하게 된다 |
| **세로** 이웃으로 메우기 | **세로 줄무늬** — 한자가 세로로도 두꺼워 같은 문제가 방향만 바꿔 생긴다 |
| 깨끗한 여백 띠(x47~61)를 **가로로 반복** | 좋아졌지만 띠의 잡티가 15칸마다 되풀이돼 **점선 무늬**가 보였다 |
| 줄 **중앙값으로 평평하게** ← 채택 | 깨끗하다 |

마지막이 정답인 이유가 있다. 이 그림은 **32색 양자화**를 거쳤고(§5), 석판 안쪽은
실측하면 줄마다 중앙값이 전부 132로 **사실상 한 가지 색**이다 — 나머지는 ±8·±11짜리
양자화 디더뿐이다. 그 디더를 복사해 붙이면 **원본에 없던 규칙**이 생긴다.
평평하게 까는 쪽이 오히려 원본에 가깝다.

글자는 **갈무리11 볼드**로 찍는다 — 게임이 인게임 글자를 굽는 바로 그 폰트라(§3-1)
화면 안에서 이질감이 없다. 안티앨리어싱은 알파 128 기준으로 잘라 각을 세운다.
잉크 색은 **지우기 전에 원래 한자 색을 재서** 그대로 쓴다(측정값 rgb(83,73,61)).

두 가지를 못 박아 뒀다:

- **멱등** — 지우는 범위가 좌표로 고정이라 여러 번 돌려도 결과가 같다(md5 로 확인).
- **`assets:bg` 가 이 스크립트를 이어서 부른다.** 원본(`etc/`)에는 한자가 그대로 있어서
  `downscale-bg.mjs` 를 다시 돌리면 되살아난다 — 파이프라인에 묶어 두지 않으면
  다음에 배경을 다시 구울 때 조용히 옛 간판으로 돌아간다.

### 로비 버튼 폰트 19 → 17px

15px 는 작았고 19px 는 컸다(§9-0-32 에서 한 번 키웠다). 로비 메뉴 여섯 줄이 한 화면에
편하게 들어오는 지점이 그 사이였다.

### 검사

`qa:multi` 34번 묶음 · `qa:rank` 11번 묶음. 이번에도 **낡은 검사 3건**이 깨졌다
(5초 창·패배 팁·버튼 19px) — 전부 이번에 **의도적으로 되돌리거나 지운 것**이라
검사도 같이 갱신했다. 되돌리는 변경일수록 옛 검사가 남아 있으면 다음 사람이 헷갈린다.

## 9-0-36. "계산이 안 맞는다"의 정체 — **표시가 정산을 못 따라가고 있었다** (2026-08-19 6차)

지난 회차에 정산 자체(`result.found`)는 고쳤다. 그런데도 사용자는 여전히
*"여러 명이서 계산해 보면 안 맞는다"* 고 했다. 이번엔 **4인 판을 그대로 흉내 내서**
숫자를 하나씩 맞춰 봤다.

### 정산은 맞았다 (S22-c 로 확인)

4인, 습득 4·2·3·5, 나가는 시점 제각각(한 명은 결과 화면도 안 보고 이탈):

```
D(1등) = 20 + 판돈 3 + 남들이 주운 9 = 32   ✅
A·B·C  = 각자 기본 1켤레만 잃음            ✅
총량   = 남들이 주운 만큼(9)만 늘어남       ✅
```

먼저 나간 사람의 기본 1켤레는 **잃은 게 아니라 미납**이고, 다음 접속의 청산이
받아 내 승자가 마저 걷는다(그 과정도 검사에 넣었다).

### 안 맞은 건 **화면에 뜬 숫자**였다

하단의 `1등하면 신발 N켤레!` 는 **판돈(참가비 + 부활비)만** 세고 있었다. 그런데 1등은
주운 신발까지 전부 가져간다 — 그러니 **받는 양이 늘 표시보다 컸다.** 사용자가 결과와
화면을 대조하면 당연히 안 맞는다.

이제 둘이 **같은 식**을 쓴다:

```
화면 = (남은 기본 판돈) + Σ부활비용 + Σ모두가 주운 것
수령 =  걷은 판돈              + 남이 주운 것 + 내가 주운 것(finishRun)
```

`S22-d` 가 이 등식을 실제 판으로 검산한다 — 부활 1회 낀 3인 판에서
**정산으로 받은 양 + 내가 주운 양 = 화면에 뜬 숫자**.

덤으로 요청대로 **누가 신발을 줍든 숫자가 곧바로 올라간다.** 내가 주운 것은 서버가
되돌려주기를 기다리지 않는다(진행도 전송이 300ms 간격이라 그만큼 굼떠 보였다) —
`potShoes(room, {uid, shoesFound})` 로 내 최신 값을 바로 넘기고, 하단 줄의 캐시 열쇠에도
내 습득 수를 넣었다(예전엔 방 스냅샷이 바뀔 때만 다시 그렸다).

### 그 과정에서 나온 **진짜 버그** — 기본 판돈 이중 계산

식을 검산하다 발견했다. 기본 1켤레는 **판이 끝난 뒤에** 내므로 판 도중 `given` 에는
부활비만 있다. 그런데 정산이 끝나면 거기 기본 1켤레가 더해진다 — 그 상태에서
`(인원−1)` 을 통째로 또 더하니 **결과 화면에서만 판돈이 부풀었다**(실측 37 vs 35).
이미 낸 사람 수를 세서 **남은 것만** 더하도록 고쳤다.

> 검사에서 두 번 헛다리를 짚었다. 처음엔 4인 판에서 `rankings` 가 안 박혀 실패했는데,
> 그건 버그가 아니라 **셋 이상이면 마지막 한 명은 계속 오른다**는 설계(§9-0-28)를
> 내 시나리오가 안 지킨 것이었다. 두 번째는 부활 시뮬에서 `L.removeShoesByIndex` 를
> 빠뜨려 지갑이 안 줄었다. **테스트가 실제 순서를 안 지키면 통과도 실패도 거짓말이다.**

### 사망 화면 — 카운트다운에 제 칸을 줬다

숫자가 제목·상금과 같은 바닥에 얹혀 있어 "남은 시간"의 경계가 없었다. 살짝 어두운
칸(80×42)을 깔고 위아래 여백을 준 뒤 숫자에 **외곽선**을 둘렀다(배율 3이라 외곽선이
3도트로 두껍게 나와 크림색 패널 위에서도 획이 선다). 배율 3 숫자는 높이가 33도트라
칸이 42여야 위아래 4~5도트가 남는다 — 처음엔 34로 잡아 숫자가 칸을 뚫었다(미리보기로 확인).

상금 줄 바로 아래에 **한 단계 작은 검은 글씨로 `나의 남은 신발 N켤레`** 를 되살렸다.
지난 회차에 "배경에 묻힌다"며 지웠는데, 자리를 패널 안으로 옮기니 오히려 또렷하다 —
걸지 말지 정하려면 "얼마 걸리나"와 "내가 얼마 가졌나"가 나란히 있어야 한다.

### 검사

`qa:multi` 35번 묶음(판돈 식·즉시 갱신·사망 화면 좌표) + `sim:multi` S22-c(4인)·S22-d(검산).
낡은 검사 3건은 이번에 의도적으로 되돌린 것들이라 같이 갱신했다.

---

## 9-0-37. 방 목록 가독성 · **죽으면 색이 빠진다** · 낙사/부활 효과음 (2026-08-19 7차)

### 방 목록 — 줄이 답답했고 배지가 안 보였다

`.room-row` 의 위아래 여백이 4px 뿐이라 세 줄이 붙어 한 덩어리로 읽혔다. 9px 로 늘리고
줄 간격도 4→6px. 그리고 방장 보유신발 배지가 **흰 글씨**였는데, 이 줄의 바탕은
크림색이다 — 흰 글씨를 크림색 위에 얹으면 사실상 안 보인다. 방번호와 같은 본문색으로
되돌렸다. 문구도 `보유신발 N켤레` → **`신발 N켤레`**: 이 줄에는 상태·방번호·닉네임·인원·
배지·버튼이 한 줄에 들어가므로 두 글자가 곧 여유다.

문구는 **방 목록에서만** 짧다(`S.roomShoes`). 대기방 참가자 카드는 한 줄에 한 명씩이라
자리가 넉넉하고, 거기선 `보유신발` 이라는 말이 있어야 무엇의 개수인지 헷갈리지 않는다.

### 죽으면 자리 색을 잃는다

*"떨어지고 죽은 느낌이 없다"* — 맞는 지적이었다. 예전에는 **얼굴만** 반투명 회색으로
덮이고 6칸 테두리는 여전히 쨍한 자리 색이었다. 이 게이지에서 색은 "살아서 뛰는 사람"의
표시인데, 죽은 사람이 제일 눈에 띄는 색을 그대로 들고 있으면 신호가 뒤집힌다.

카운트다운이 도는 동안에는 **테두리(`on`)도 빈 칸(`off`)도 회색**으로 내린다. 판정은
`countdown != null` 하나뿐이라 **부활하는 순간 제 색이 그대로 돌아온다** — 따로 되돌리는
코드가 없다. 등수 글자는 이미 지난 회차에 회색+작게 내려 뒀으니, 이제 죽은 사람은
얼굴·테두리·등수 셋 다 색이 빠진다.

미리보기(`preview:hud`)의 가짜 판을 손봐야 확인할 수 있었다 — 원래 데이터는 죽은 사람이
1등 뒤에 겹쳐 **아예 안 그려지고 있었다**(겹침 2명 제한, §9-0-33). 죽은 사람을 30계단
앞에 세워 놓으니 그제야 회색이 눈에 들어왔다.

### 알림 색 — 낙사는 빨강, 부활은 노랑

`ticker` 가 색을 **줄마다** 들고 다닌다. 한 화면에 `파란색이 떨어졌다!`(빨강)와
`노란색 1등 부활!`(노랑)이 동시에 뜰 수 있기 때문이다. 둘 다 외곽선을 두르므로
하늘·건물 어느 배경 위에서도 획이 산다.

### 효과음 두 개 — 그리고 **검사가 왜 이걸 놓쳤나**

`sfx_rival_fell` 은 내려가는 삼각파 슬라이드(A4→E3) + 낮은 쿵 + 저역 잡음 — "아쉽다"는
느낌은 **떨어지는 음정**이 만든다. `sfx_rival_revive` 는 반대로 C4→C6 로 훅 올라가는
비브라토 스윕 + 위에서 흔들리는 지속음 + 반짝이는 짧은 두 음 — "와우~" 의 화들짝.

중요한 건 소리 자체가 아니라 **검사가 이 둘을 몰랐다**는 것이다. `_audio-qa.mjs` 가
효과음 목록을 **손으로 적어** 들고 있었다. 새 소리를 만들 때마다 거기 옮겨 적기를
잊으므로, 새 소리는 구조적으로 늘 검사 밖에 있었다. 이제 `src/audio/sfx.js` 에서 키를
직접 긁는다 — **만들면 자동으로 검사 대상이 된다.** 녹음 도구도 같이 고쳤다.

그리고 검사가 32건 전부 "무음"이라고 했는데 **소리는 멀쩡했다.** 잠금을 풀려고 찍던
클릭 좌표(270,880)가 로비 개편으로 빈 곳이 되어 있었다. 좌표 하나에 검사 전체가
매달려 있었던 것이다 — 이제 안 풀렸으면 직접 열고, 그래도 안 되면 **거기서 멈춘다.**
조용히 "무음"이라고 보고하느니 검사가 죽는 편이 낫다.

> 오늘 두 번째로 만난 같은 모양이다(§9-0-32 의 `etc/`). **검사가 손으로 적은 목록이나
> 고정 좌표에 기대면, 검사는 통과와 실패 대신 침묵을 만든다.**

### 검사

`qa:multi` 36번 묶음(여백 px·배지 색·회색 전환·알림 색·효과음 등록과 호출·QA 자동화).
`_audio-qa` 로 19개 효과음 + 슬라이스 3 + BGM 10트랙 전량 피크 측정 — 새 소리는
0.15·0.17 로 기존 효과음과 같은 대역. 낡은 검사 2건(알림 인자 추가·배지 문구 축약)은
이번에 의도한 변경이라 갱신했다.

---

## 9-0-38. 배경 30종 추가 — **자동 검출이 셋 다 실패했다** (2026-08-19 8차)

시즌2 포스터 30장(`etc/게임배경시즌2/`)을 잘라 넣고, 겹치는 옛 배경 둘을 뺐다.
배경은 16종 → **44종**(시즌1 14 + 시즌2 30).

### id 를 정렬 순서에 맡기지 않는다

시즌1(`downscale-bg.mjs`)은 폴더를 정렬한 순서로 `build_01..16` 을 붙인다. 거기에 30장을
더 넣는 순간 **기존 배경의 번호가 통째로 밀린다** — 이미 구워 커밋한 그림, `build_10`
간판 스크립트(§9-0-35), 그리고 **사용자가 설정에 저장해 둔 배경**이 전부 엉뚱한 곳을
가리킨다. 게다가 원본 파일명이 Gemini 난수라 정렬 순서에 아무 의미도 없다.

그래서 시즌2는 `tools/bg-season2.json` 이 **파일명 → id · 이름**을 손으로 못 박고,
전용 스크립트가 시즌1 파일은 **건드리지 않는다.** 언제 다시 돌려도 안전하다.

같은 이유로 겹치는 둘(`build_06` 동물의 집 · `build_15` 숲속의 별장 II)은 목록에서만
빼고 **뒤를 당기지 않았다.** 그림 파일도 안 지웠다 — 원본을 치우면 `build_07` 이후가
밀리므로, 79KB 를 남기는 편이 훨씬 싸다. 없는 id 가 설정에 남아도 안전하다
(`BUILDINGS.find` 가 undefined → 랜덤으로 흘러간다).

### 자동 검출을 버리고 **판형을 쟀다**

시즌1의 국소 표준편차 검출기를 그대로 돌렸더니 30장 중 18장이 "패널 2개"로 실패했다.
그런데 진짜 문제는 **나머지 12장이었다** — 3개를 찾아서 통과처럼 보였지만 좌표가
`[16,224,2736,1232]`, 즉 **포스터 전체**였다. 통과처럼 보이는 실패가 제일 위험하다.

세 가지를 더 시도했고 셋 다 실패했다:

| 방법 | 왜 실패했나 |
|---|---|
| 국소 표준편차(시즌1 방식) | 시즌2 배경은 모눈 무늬가 진해 판 전체가 한 덩어리로 붙는다 |
| 배경색과의 차이로 마스킹 | 모눈 배경에 좌우·상하 그라데이션이 있다 — 여백은 133, 한가운데는 226. 여백 색을 기준으로 잡으면 **화면 전체가 "배경이 아님"** 이 된다 |
| 열 투영 + 조각 잇기 | 하늘·단색 벽에서 끊겨 패널이 2~5조각으로 갈라진다 |

네 번째로 **판형을 의심**했다. 30장을 재 보니 세 패널의 자리가 2752×1536 기준으로
**전부 같았다**(절반 크기 원본 2장은 정확히 배율 0.5). 그래서 좌표를 못 박았다:

```
도로   88, 840, 832×480      1층  1000, 568, 752×752      2·3층  1880, 472, 744×848
```

30장 × 3패널을 전부 잘라 눈으로 확인했고, 판형을 벗어나는 **2장만** 명단에 좌표를 적었다:

- **오사카 성** — 오른쪽에 "완성된 탑" 장식 그림이 하나 더 있어서 2·3층 옆에 그게 딸려 온다
- **요르단 페트라** — 2·3층 그림이 판형보다 좁게 그려져 오른쪽 1/4 이 모눈 배경이다

> 자동으로 재는 것보다 **판형 하나를 눈으로 확인하고 못 박는 편**이 정확했고, 무엇보다
> 틀리면 눈에 바로 보인다. 30장을 재고 나서야 알았다 — **먼저 데이터를 봤어야 했다.**

검출기와 굽는 방식은 `tools/_bg-lib.mjs` 로 떼어 **시즌1과 공유한다.** 복사해 두면
한쪽만 고쳐서 두 시즌의 결과가 갈린다. 떼어 낸 뒤 `assets:bg` 를 다시 돌려
시즌1 그림 147장의 md5 가 **한 장도 안 바뀐 것**을 확인했다.

### 고른 배경을 한 번 더 누르면 크게 본다

44종이 되면서 **54px 짜리 썸네일로는 고를 수가 없어졌다.** 그렇다고 격자를 키우면 한
화면에 몇 개 안 들어온다. 그래서 고르는 것과 구경하는 것을 나눴다 — 첫 탭은 선택(빨간
테두리), 이미 고른 카드를 한 번 더 누르면 **도로 + 1층 + 2·3층을 이어 붙인 세로 한 컷**이
뜬다. 그림 세 장을 세로로 쌓는 것뿐이라 **새로 구울 파일이 없다.**

**확대는 정수배로만** 한다(§3-1). 여기서 진짜 버그가 하나 나왔다 — 저장소 기본이
`border-box` 라 `width: 360px` 짜리 상자에서 테두리 4px 를 빼고 **356px** 이 남았고,
배율이 356/180 = **1.978** 이 되어 도트가 뭉갰다. 눈으로는 2배와 구분이 안 된다.
`box-sizing: content-box` 로 고치고 **자로 재는 검사**를 만들었다(`npm run qa:bgfit`,
320·390·412·1200px 네 폭에서 세 장의 배율이 전부 정수인지).

배율 계산(`cutScale`)은 화면 밖으로 뺐다 — **미리보기와 검사가 같은 식을 쓰게** 하려는
것이다. 미리보기가 배율을 따로 계산하면 그 미리보기는 언젠가 거짓말을 한다(§9-0-33).

### 안내 문구를 두 줄로

`싱글게임에서 사용할 배경을 선택하세요` / `멀티게임은 배경이 랜덤입니다`.
한 줄로 붙이면 좁은 폰에서 세 줄로 접혀 **어디가 끊기는지 사용자가 정할 수 없다.**
`.hint` 에 `white-space: pre-line` 을 줘서 문구 안의 줄바꿈이 그대로 살아난다.

> 곁가지: `el()` 이 `style` 을 객체로만 받아서, 문자열을 주면 `Object.assign` 이
> **글자 하나씩** 넣으려다 "Indexed property setter is not supported" 로 터졌다.
> 무엇이 잘못됐는지 알 수 없는 메시지라 문자열도 받게 했다(`cssText`).

### 손으로 적은 개수를 지웠다

`layout.BG.variantCount: 16` 은 **읽는 곳이 하나도 없는데** 손으로 적혀 있었다.
44종이 된 뒤에도 16 그대로였을 것이다. **읽는 곳이 없는 숫자는 언젠가 거짓말이 된다** —
지웠다. 개수가 필요하면 `BUILDINGS.length` 를 쓴다.

### 검사

`npm run qa:bg`(목록 44종 · 빠진 자리를 안 메웠는지 · 명단과 이름 일치 · 그림 132장 존재와
규격 · 설정 화면 · 파이프라인 가드) · `npm run qa:bgfit`(정수배 실측) ·
`npm run preview:bg`(목록과 큰 그림) · `node tools/_bg-preview.mjs build 17 22`
(2·3층 타일이 매끄럽게 반복되는지 — 30종 전수 확인).

---

## 9-0-39. **"잠깐 나갔다 오면 튕긴다"의 정체** · 프레임 비용 · 접속 속도 (2026-08-19 9차)

사용자 신고 세 건 — *"멀티게임 작동시 버벅임"*, *"소스를 전부 재분석해서 속도 최적화"*,
*"전화를 받거나 창 밖으로 잠깐 빠져나갔다가 돌아오면 거의 튕긴다"*.

### ★ 튕김의 진짜 원인 — **"조용하다"와 "끊겼다"를 구별하지 않았다**

살아 있는지를 **클라이언트가 5초마다 찍는 `seenAt`** 하나로만 판단했다. 그런데:

- 브라우저는 탭이 뒤로 가면 `setInterval` 을 **1분에 한 번**으로 조인다
- 폰에서 전화를 받으면 페이지를 **통째로 얼린다**(freeze) — 신호가 아예 안 나간다

즉 **멀쩡히 붙어 있는 사람이 신호가 끊긴 것처럼 보였다.** 90초는 2분짜리 통화 한 번을
못 버틴다. 게다가 시뮬레이터에는 "잠깐 자리를 비웠다 돌아온다"는 시나리오가 **아예
없었다** — 튕겨서 영영 안 돌아오는 경우만 검사하고 있었으니, 신고된 상황은 구조적으로
검사 밖이었다(§9-0-34 와 같은 실수를 또 했다).

**서버에게 직접 물어보는 쪽으로 바꿨다.** `onDisconnect` 는 RTDB 서버가 소켓이 끊긴 걸
감지했을 때 대신 실행하는 예약이다. 여기에 **끊긴 시각(`offAt`)** 을 찍게 해 두면:

| 상황 | 예전 (`seenAt` 만) | 지금 (`offAt` 우선) |
|---|---|---|
| 탭만 숨고 소켓은 살아 있다 | 90초 뒤 판에서 빠짐 | **영원히 자리를 지킨다** — `offAt` 이 안 찍힌다 |
| 정말 끊겼다 | 마지막 신호로부터 90초 | 서버가 찍은 시각부터 **45초** (더 빠르고 정확) |
| 돌아왔다 | 다음 타이머(최대 5초)를 기다림 | 붙는 순간 지워지고, 화면 복귀 즉시 신호 |

타이머로 어림하는 것보다 정확하고, 무엇보다 **브라우저가 얼려도 서버는 안 얼린다.**
`seenAt` 임계는 이제 **자리 지킴이 없는 옛 클라이언트용 안전망**이라 90 → **150초**로
넉넉하게 잡았다(진짜 끊김은 45초에 걸리므로 손해가 없다).

### 시뮬레이터가 잡아 준 **진짜 버그** — 끊기면 방에서 통째로 사라진다

"잠깐 끊겼다 돌아온다"를 시뮬에 넣으려고 가짜 RTDB 에 `onDisconnect` **발동**
(`dropConnection`)을 구현했더니, 첫 실행에서 참가자가 통째로 사라졌다.

방에 들어올 때 `armDisconnect` 가 **"끊기면 나를 `players` 에서 지워라"** 를 예약한다
(유령 방을 막는 장치, §9-0-14). 게임이 시작되면 `holdRoomSeat` 가 취소하는데 —

- 그 취소가 **끊기기 전에 서버에 닿지 못하면** 예약이 그대로 살아 있고,
- SDK 는 **재접속 때 예약을 다시 보낸다.**

그러면 잠깐의 끊김 한 번으로 방에서 사라진다. **돌아와도 방에 내가 없다** — 순위에도
못 들고 판돈도 못 받는다. "거의 튕긴다"는 표현이 이걸로 설명된다.

세 겹으로 막았다:
1. `holdRoomSeat` 이 지도(Map)에 의존하지 않고 **경로를 직접 만들어** 취소한다
2. **붙을 때마다** 그 취소를 다시 건다(`armPresence` 안에서)
3. 그래도 사라졌으면 생존 신호가 알아채고 **지금 진행도 그대로 자리를 되찾는다**
   (`rejoinIfDropped` — 단 순위가 이미 박힌 판에는 안 들어간다)

> 예약 열쇠도 `code` → `uid|code` 로 바꿨다. 브라우저에서는 한 사람뿐이라 값이 같지만,
> 한 프로세스에 여러 사람이 있는 시뮬에서는 뒤에 들어온 사람이 앞사람 예약을 덮어써
> **취소가 엉뚱한 사람에게 걸렸다.** 검사가 실제와 같은 상태에서 돌아야 한다.

### 버벅임 — 재는 것부터 만들었다 (`npm run perf:frame`)

"버벅인다"는 숫자 없이는 못 고친다. 실제 렌더 함수를 가짜 상태로 300번 돌려
**프레임당 ms 와 `fillRect` 호출 수**를 센다(캔버스 API 를 감싸서 실측).

첫 측정에서 답이 바로 나왔다:

| 화면 | fillRect/프레임 | → | 지금 |
|---|---|---|---|
| **멀티 사망 패널** | **3,647** (초당 21만) | → | **26** |
| 게임오버 패널 | 1,478 | → | 16 |
| 일시정지 패널 | 299 | → | 21 |
| 멀티 HUD + 사망 패널 | 2.02ms | → | **0.44ms** |

원인은 하나였다 — **오버레이가 캐시 없는 `text()` 를 쓰고 있었다.** 오버레이는 씬 스택
위에 얹히므로 아래 게임 화면과 **함께 매 프레임 다시 그려지는데**, 외곽선이 붙으면
글리프 하나가 `fillRect` 9벌이다(§9-0-14 에서 HUD 는 고쳤는데 오버레이는 예외로 남아
있었다). 하필 이 화면은 **멀티에서 죽은 직후** — RTDB 왕복이 가장 바쁜 순간 — 에 뜬다.

같이 잡은 것:
- **`getProfile()` 을 매 프레임 호출**하고 있었다. 그건 `localStorage` 를 읽어 JSON 을
  파싱하고 신발 130칸을 순회한다(`reconcile`). **초당 60번**이다. 값은 부활을 누를 때만 바뀐다.
- `potShoes()`(참가자 전원의 항아리를 훑는 정산 계산), `rankPlayers()`(정렬 + 이탈 판정),
  1등 계단 수 — 전부 렌더 안에서 매 프레임 돌았다. 방 스냅샷은 **초당 최대 13회**만
  오므로(진행도 300ms 스로틀) 나머지 47번은 같은 답을 다시 구한 것이다.
- **덮이는 하늘을 매 프레임 칠했다** — 타일·1층·도로가 화면을 빈틈없이 덮는데
  그 밑에 180×320 전체(57,600px)를 채우고 있었다. 로딩 중에만 칠한다.
- **1픽셀도 안 보이는 계단 3개**를 매 프레임 그렸다 — 컷오프에 40도트 여유가 있었다.
- 프레임마다 새로 만들던 배열들(구름 좌표·6칸 테두리 좌표·일시정지 항목)을 상수로 올렸다.

### 접속 속도

| 무엇 | 전 | 후 |
|---|---|---|
| 부팅 직후 **빈 화면** | 최대 **3초** (`hideBoot()` 가 `await initAuth()` 앞에 있었다) | **0** — 첫 화면을 띄운 뒤에 끈다 |
| 판 시작 전 받는 그림 | **262,698 B** | **145,977 B** (−44%) |
| 프로필·도감 당기기 | 순차 (각 12초 시한) | **동시** — 느린 쪽만큼만 |

그림이 절반으로 준 건 **층수 배경 9장(115KB)** 때문이다. 200층↑ 교체 배경인데 200층에
닿기 전에는 한 장도 안 그린다 — 그걸 기다리느라 **모든 판의 시작이 늦어지고 있었다.**
이제 뒤에서 받는다(도착 전이라도 게임은 어두운 색으로 폴백한다). 조작 버튼도 4장 중
**그 모드가 쓰는 2장만** 받는다.

### 검사 — 1,200판을 돌렸다

`sim:multi` 에 **S24~S28** 을 넣었다: 잠깐 자리 비움(소켓 생존) · 진짜 끊김(서버가 찍은
시각부터 유예) · 재접속 시 표시 삭제 · 명단에서 사라졌을 때 자리 되찾기 · 끝난 판에는
안 들어가기. 그리고 무작위 소크 테스트에 **끊김·복귀 사건을 섞고** 판 수·시드를 밖에서
정할 수 있게 했다(`SIM_ROUNDS=300 SIM_SEED=600`) — 고정 60판만 돌면 매번 똑같은 60판이라
반복 실행에 의미가 없다. **시드를 옮겨 가며 1,200판**을 돌려 총량 보존·안 끝난 판 0 을 확인했다.

`qa:multi` 37번 묶음이 자리 지킴·임계값·복귀 신호·프레임 비용·접속 속도를 전부 못 박고,
`qa:rules` 는 **잎 하나만 따로 쓰는 경로**(`players/<uid>/offAt` 처럼)도 규칙에 있는지
새로 본다 — 그런 쓰기는 `onDisconnect` 로 걸리는 경우가 많아 **실패해도 로그조차 안 남는다.**

RTDB 규칙에 `offAt` 을 추가해 **게시하고 되읽어 확인**했다. 규칙이 없더라도 그 쓰기만
조용히 거부되고 예전처럼 `seenAt` 으로 동작한다(§9-0-34 의 교훈 — 새 필드는 항상 이렇게 넣는다).

> **교훈이 또 같다.** §9-0-34 는 "테스트가 실제 행동을 흉내 내지 않으면 통과는 아무
> 의미가 없다"였다. 이번엔 **시뮬에 없는 사건**(잠깐 끊김)이 정확히 사용자가 겪는
> 사건이었다. 신고를 받으면 먼저 물을 것은 "코드가 맞나"가 아니라
> **"내 검사가 그 상황을 만들어 보기는 하나"** 다.

---

## 9-0-40. **30초 규칙 하나로** · 유령 방 청소 · 보유 신발 갱신 (2026-08-19 10차)

사용자 신고 세 건. 앞 회차(§9-0-39)에서 내가 고른 값이 틀렸던 것 하나가 섞여 있다.

### ★ 150/45초를 **30초 하나로** 되돌렸다

직전 회차에 임계를 둘로 갈랐다 — 신호 150초, 끊김 45초. 끊긴 사람을 오래 기다려 주려는
의도였는데, 사용자가 바로 **"렉이 있는 것 같다"** 고 했다. 맞는 지적이다:
**안 돌아오는 사람을 기다리는 시간은 그대로 노는 사람이 멍하니 서 있는 시간**이다.
게임을 기다리는 쪽에 맞춰야지 떠난 쪽에 맞추면 안 된다.

사용자가 준 규칙 그대로 통일했다 — *"30초 안에 돌아오면 계속 게임 가능하게 하고,
30초동안 접속 안하면 그 유저는 게임 로비로 강제로 내보내게"*.

**같은 30초를 양쪽이 본다**는 게 핵심이다:

| 어디 | 무엇을 하나 |
|---|---|
| 남들의 판정 (`matchRules.isStale`) | 30초 넘게 신호가 없으면 **판에서 뺀다** |
| 내 화면 (`GameScene.kickOut`) | 30초 넘게 자리를 비웠으면 **스스로 로비로 나간다** |

숫자가 갈리면 "나는 아직 게임 중이라고 생각하는데 남들은 나를 뺐다"는 구간이 생긴다.
그 구간이 바로 사용자가 "튕긴다"고 부르던 것이다.

자리를 비운 시간은 **타이머로 셀 수 없다** — 브라우저가 백그라운드 타이머를 얼리기
때문이다. 그래서 `visibilitychange` 의 **내려간 시각과 돌아온 시각의 차이**로 잰다.
나갈 때는 조용히 사라지지 않는다: ① 판에서 손을 뗐다는 도장(남들이 안 기다리게)
② 자리 비우기(유령이 안 되게) ③ **왜 나왔는지 말해 주기** — 같은 현상이라도
설명이 있으면 규칙이고, 없으면 버그로 보인다.

### 유령 방 — `onDisconnect` 는 **소켓이 죽어야** 터진다

*"게임을 안하는 유저가 방 만들고 창을 닫거나 내려두면 그 유저가 사라져야 하는데
유령처럼 남아있는 버그가 존재함"*

창을 **닫으면** 소켓이 죽어 예약이 발동한다. 그런데 **내려두기만 하면 소켓은 살아
있다** — 서버가 보기에 그 사람은 멀쩡히 접속 중이고, 영원히 앉아서 레디를 안 누른다.
방은 목록에 뜨고, 들어간 사람은 시작을 못 한다. 자동 매칭은 `open == true` 인 앞
12개만 훑으므로 이런 방 12개면 **전원이 새 방만 파게 된다**(§9-0-17 의 재발).

서버가 못 치우니 **보고 있는 사람이 치운다**(`purgeAbsent`). 규칙상 방 참가자는 방
노드를 쓸 수 있어 남의 칸을 **지울** 수 있다(값을 바꾸는 건 여전히 막혀 있다 —
잎마다 "내 것이거나 값이 그대로" 검증이 걸려 있다). 방장이 유령이면 승계까지
**한 번의 쓰기로** 보낸다(나눠 보내면 그 사이 "방장이 방에 없는" 상태가 생겨
규칙이 승계를 거부한다, §9-0-21 ①).

**대기 중인 방에서만** 한다. 판이 도는 중에 자리를 지우면 순위·정산의 근거가 사라진다 —
그쪽은 `isStale` 이 "판에서만 뺀다"로 처리하고 자리는 남긴다.

아무도 안 들어온 1인 유령 방은 치울 사람이 없으므로 **방째로 지운다**
(`sweepEmptyRooms` 를 "참가자 0명" → "전원이 자리를 비운 대기방"까지 넓혔다).
목록에서도 감춘다 — 감추는 것과 지우는 것을 나눠 둬야 **지우기가 실패해도 안 보인다.**

### 보유 신발 수가 옛날 숫자였다

*"신발 200개를 갖고 있는 사람이 150개를 잃으면 (…) 이전 신발 갯수가 그대로 노출됨"*

`shoesOwned` 는 **입장 시점 스냅샷**이다(`meRecord`). 닉네임·캐릭터와 같은 취급이었는데,
그 둘과 달리 **신발은 판마다 변한다** — 그게 이 게임의 판돈이다.

남의 칸은 남만 쓸 수 있으므로(규칙), **각자 자기 것을 갱신하면 방 전체가 맞아떨어진다**
(`refreshMyCard`). 지갑이 실제로 바뀌는 자리는 정산 직후이므로 결과 화면에서 한 번,
대기방에서 스냅샷마다 한 번 확인한다. 값이 같으면 쓰지 않는다 — 안 그러면 헛쓰기가 쌓인다.

### 렉의 한 갈래는 내가 만든 것이었다

직전 회차에 넣은 자리 되찾기(`rejoinIfDropped`)가 **첫 방 스냅샷이 오기 전에도** 돌고
있었다. `!this.room?.players?.[uid]` 는 방을 아직 못 받았을 때도 참이라, 5초마다
**방 전체를 다시 읽었다.** 모르는 것을 근거로 움직인 것이다 — 방을 받은 뒤에만 따진다.
대기방의 카드 갱신도 이미 들고 있는 스냅샷을 넘기게 고쳐 왕복을 하나 더 없앴다.

### 검사

`sim:multi` **S29~S31** 신설 — 창을 내려둔 방장이 치워지고 방장이 승계되는지,
판이 도는 중에는 자리를 **안** 지우는지(대신 판정으로만 빠지는지), 판이 끝나 신발이
오간 뒤 카드 숫자가 따라오는지(그리고 값이 같으면 안 쓰는지).
`qa:multi` 38번 묶음이 30초 통일·유령 청소·카드 갱신을 못 박는다.
무작위 소크 **1,200판**(시드 0/400/800 × 400판) 전수 통과.

> 이번 회차의 교훈은 **값 선택도 설계**라는 것이다. 150초는 "끊긴 사람을 배려"한 값인데,
> 그 배려의 비용을 **남아서 노는 사람**이 낸다. 누가 비용을 내는지 보지 않고 고른 값은
> 기능이 맞아도 체감이 틀린다.

---

## 9-0-41. 판돈 줄 · 5초 출발 규칙 · 유저상태창 · 현재접속자 · 쪽지/대결 (2026-08-19 11차)

사용자 요청 7건. 앞의 넷은 화면이고, 뒤의 셋은 **게임에 사람을 붙이는 기능**이라
RTDB 최상위 노드가 둘 늘었다(`presence` · `inbox`).

### ① 판돈 줄을 계단 숫자 **바로 위**로

*"'1등하면 신발 0켤레' 메세지가 화면 아래쪽에 있는데 이걸 위로 올리자 — 현재 계단
카운팅 숫자 큰 글씨로 나오는 부분 바로 위로 옮겨"*

예전 자리(y252)는 조작 버튼(266~)과 알림 줄 사이라 **눈이 가지 않는 자리**였다.
지금은 계단 숫자 바로 위(41~52)에 붙어 있어 "얼마가 걸렸나"와 "지금 몇 계단이냐"가
한 덩어리로 읽힌다. 숫자는 그만큼 내려갔다(`layout.HUD.scoreMulti`, 55~77).

**아이콘 행(23~38)에 끼워 넣을 수는 없었다.** 판돈 문구는 11px 로 최대 128px
(`1등하면 신발 523켤레!`, 폰트 데이터로 실측)이고, 왼쪽 신발 아이콘(4~25)과 개수
(29~56)가 이미 그 줄을 쓴다 — **물리적으로 겹친다.** 그래서 줄을 하나 새로 냈다.

가운데 x 도 화면 정중앙(90)이 아니라 **82** 다. 오른쪽 레이스 게이지가 x150~178 을
위아래로 통째로 쓰고 얼굴 상자가 겹치면 128까지 밀려 나오기 때문이다. 82 면 최대 폭
128 이 18~146 에 들어가 절대 침범하지 않는다(알림 줄이 왼쪽에 붙는 것과 같은 계산).

### ② ★ 5초 안에 출발하지 않으면 진다

*"멀티게임에서 늦게 출발하면 땡 하면 출발해야 하는데 기다렸다가 부활 써서 쉽게 1등
하려고 한다 (…) 상대방이 거의 부활 6개중에 3~4개 쓸때까지 기다렸다가 늦게 출발하면
늦게 출발한 사람이 너무 유리해"*

**근본 원인은 게이지가 첫 입력부터 줄기 시작한다는 규칙이다**(`GameScene.started`).
싱글에서는 "생각할 시간"이라 옳지만, 멀티에서는 **가만히 서 있는 것이 공짜**가 된다.
그동안 상대는 오르다 죽고 부활을 태우고, 부활 위치가 **1위보다 20칸 앞**이라
(`MULTI.reviveAhead`) 상대의 부활 6개가 바닥난 뒤 0계단에서 출발해도 이기는 판이 나온다.

- 판이 시작되면 곧바로 카운트다운을 보여 준다. **처음부터 보여 주는 이유**는 규칙을
  모르는 사람이 없어야 하기 때문이다 — 3초쯤 지나 뜨면 이미 늦었다고 느낀다.
- 시간이 다 되면 **기권과 똑같이** 처리한다(사망 보고 → `out` 도장 → 결과 화면).
  **부활 창을 주면 안 된다** — 그게 곧 위의 그 전략이다.
- 기준은 방의 출발 시각이 **아니라** 판이 실제로 돈 첫 프레임이다(`raceOpenedAt`).
  에셋이 아직 안 받아졌으면 `update()` 는 그 앞(`!this.ready`)에서 통째로 멈추므로,
  방 시각으로 재면 **로딩이 5초를 넘긴 기기는 뜨자마자 패배한다**(§9-0-14 와 같은 함정).
- 경고는 **레이스 게이지보다 나중에** 그린다. `출발하지 않으면 패배합니다` 가 11px 로
  154px 라 게이지를 피해 왼쪽에만 쓸 수 없다 — 5초짜리 경고가 게이지를 잠깐 가리는 쪽이
  경고 글자가 얼굴 위에서 뭉개지는 쪽보다 낫다.

### ③ 명예의 전당 — 내 위아래 **50명**씩, 스크롤

*"11위 12위 인데 1위가 안보이는것도 이상하다. 즉 내 순위 위 아래로 50명까지 보이게해"*

`RANK_WINDOW_RADIUS` 5 → **50**. 반경 5 는 잘라 낸 것이 아니라 **목록이 그것뿐인 것처럼**
보였다. 대신 목록이 100줄까지 나오므로 화면이 두 가지를 더 한다:

- `.rank-list` 에 `max-height: 48vh` — 안 묶으면 화면이 통째로 길어져 **하단 고정
  '내 순위' 줄과 뒤로 버튼이 스크롤 저 아래로 밀린다.**
- `scrollToMine()` 이 내 줄을 가운데로 내려 준다. **탭이 바뀔 때만** 한다 —
  `nav.refresh()` 는 내 줄이 늦게 도착할 때도 불리므로, 매번 내리면 사용자가 손으로
  올려 본 위치가 그때마다 튕겨 돌아온다.

### ④ 신발왕 탭에만 `멀티승률 N승/M게임`

*"신발 많은 사람이 몇 승 했나 보이도록"*. 값은 이미 계정 문서에 있으므로
(`multiSettle.pushWallet`) 조회가 늘지 않는다. **다른 탭에는 없다** — 그 줄들은
`scores` 에서 와서 승패를 모른다.

"줄 정렬"은 **고정폭 + 오른쪽 정렬**로만 성립한다. 자동 폭이면 승수 자릿수에 따라 칸이
늘었다 줄었다 하며 오른쪽 값까지 민다. `npm run qa:hoffit` 이 320·360·390·412px 에서
**넘침 0 · 승률 칸의 오른쪽 끝이 줄마다 같은지**를 잰다 — 눈으로는 안 보이는 종류의 어긋남이다.
그 자로 재면서 두 가지를 고쳤다: `.rank-name` 의 `min-width` 가 `auto` 라 좁은 폰에서
줄이 통째로 넘쳤고(0으로), 글자 10px 은 세 자리 승수에서 칸 밖으로 잘렸다(9px 로).

### ⑤ 유저상태창 — 화면 셋이 **같은 창**을 쓴다

*"명예의 전당에서 유저 아이디 누르면 무반응인데 (…) 멀티게임에서 아이디 누르면 팝업으로
뜨는 창 뜨게 해줘 (…) 이제부터 '유저상태창'"*

대기방에만 있던 팝업을 `screens/UserCard.js` 로 떼어 냈다. 세 화면이 각자 만들면 언젠가
셋이 서로 다른 말을 한다 — 실제로 대기방 카드만 캐릭터 그림이 있었다.

**남의 계정 값은 `getDoc` 이 아니라 쿼리로 받는다.** Firestore 규칙이 `users` 를
`get: isMe(uid)` / `list: signedIn()` 으로 갈라 뒀다 — 남의 문서를 직접 읽으면 거부되지만
쿼리(=list)는 열려 있다(닉네임 중복 확인 때문에 원래 그렇다). 그래서
`where(documentId(), '==', uid)` 한 번으로 받는다(`leaderboard.fetchUserCard`).

조회를 **기다렸다 띄우지 않는다** — "눌렀는데 아무 일도 안 난다"가 된다. 아는 값으로
먼저 띄우고 도착하면 그 줄만 갈아 끼운다(`opt.load`). 이걸 안 하면 주간·월간·연간 탭의
카드가 `승률 0승 / 0게임 · 보유신발 0켤레` 라고 **거짓말을 한다.**

### ⑥ 현재접속자

멀티 메뉴 맨 아래 `[현재접속자]`. 방과 무관한 목록이라 로비에서 도감을 보는 사람도,
싱글을 도는 사람도 나온다. **신발이 부족해도 들어간다** — 쪽지는 판돈과 상관이 없다.

정렬은 **대기중 먼저**다. 대결을 신청할 수 있는 사람이 위에 있어야 한다 — 게임 중인
사람은 눌러 봐야 "게임중 상태에선 …" 만 뜬다.

**못 붙었을 때는 "아무도 없다"라고 하지 않는다.** `subscribeOnline` 이 실패에 `null` 을
주고 화면은 그때 네트워크 안내를 띄운다. 빈 배열로 뭉뚱그리면 §9-0-6 에서 순위표가 했던
그 거짓말을 그대로 반복한다.

### ⑦ 쪽지 · 대결신청

- **쪽지는 누구에게나** 보낼 수 있다(게임 중이든 미접속이든). 받는 사람이 DOM 화면으로
  나오는 순간 팝업으로 뜬다 — `답장보내기` / `닫기`.
- **대결신청은 접속 중 + 대기중일 때만.** 게임 중인 사람에게는 신청을 띄울 화면이 없고
  (인게임은 캔버스다, §6-3), 미접속이면 아예 못 받는다. 사용자 문구 그대로
  `게임중 상태에선 메세지를 보낼 수 없습니다`.
- 받는 쪽은 `수락(10)` / `거절`, **10초가 지나면 자동 거절**이고 거절은 신청자에게 통보된다.
- 신청은 **방을 먼저 만들고** 그 코드를 실어 보낸다. "수락하면 그때 만든다"로 하면 수락한
  사람이 빈 화면에서 기다리고, 신청자가 그 사이에 앱을 닫으면 방이 영영 안 생긴다.

구현에서 조심한 것 넷:

1. **인게임에서는 절대 안 띄운다** (`body.ui-mode` 일 때만). 계단을 오르는 중에 DOM
   다이얼로그가 뜨면 입력이 통째로 막혀 그 판이 끝난다. 안 띄운 쪽지는 **서버에 그대로
   둔다** — 지우지 않으므로 나오는 순간 다시 보인다. 그게 곧 "게임 끝내고 나오면 팝업".
2. **같은 쪽지를 두 번 안 띄운다.** 구독은 목록 전체를 다시 준다 — 지우기가 서버에 닿기
   전에 스냅샷이 한 번 더 오면 방금 띄운 그 쪽지가 그대로 있다(`handled` Set).
3. **강제로 닫혀도 정리된다.** 화면이 바뀌면 `router.draw()` 가 팝업을 치우는데
   (`closeAllOverlays`), 뒷정리를 버튼 핸들러에만 두면 그때 `showing` 이 참으로 굳어
   **그 뒤로 어떤 쪽지도 안 뜬다.** 정리는 `presentOverlay` 의 `onClose` 한 곳에서만 한다.
4. **오래된 신청은 버린다**(90초). 앱을 껐다 몇 시간 뒤에 켰는데 "참여하시겠습니까"가 뜨면
   수락해도 상대는 그 방에 없다.

### '게임중'의 경계는 **두 곳뿐이다**

`router.toCanvas()` → `playing`, `router.mount()` → `lobby`. 화면 하나하나가 각자
알리게 두면 언젠가 한 곳을 빠뜨린다. 예외는 대기방 하나 — 거기서도 `playing` 으로 둔다.
이미 한 방에 앉아 있는 사람이 다른 방 초대를 수락하면 **앞 방에 유령으로 남기** 때문이다.

### 전원이 RTDB 에 붙는다 (§9-0-11 의 결정을 뒤집었다)

§9-0-11 에서 "싱글만 하는 사람이 RTDB 192KB 를 받는" 회귀를 고쳤다. 그런데 쪽지·대결신청은
**접속해 있는 모두**가 받아야 성립하는 기능이라, 이제는 전원이 붙어야 한다 — 안 붙으면
그 사람은 현재접속자 목록에 아예 없다.

대신 **부팅을 막지 않는다.** 첫 화면이 그려지고 1.5초 뒤에 조용히 붙고(`startLater`),
쪽지함 구독은 **붙은 뒤에** 건다 — 먼저 부르면 RTDB 청크를 앞당겨 받아 미루기가 통째로
무의미해진다(실제로 처음엔 그렇게 짰다).

### RTDB 규칙 (게시 완료 · 되읽어 **바이트 단위로 대조**)

최상위에 `presence` · `inbox` 를 더했다. 쓰기 규칙의 핵심은 `inbox/$uid/$id` 두 갈래다:

```
(!data.exists() && newData.child('from').val() == auth.uid)   ← 보내기: 새 쪽지만, 보낸이는 나
|| (!newData.exists() && auth.uid == $uid)                    ← 지우기: 받은 사람만
```

**덮어쓰기가 없다**(`!data.exists()`) — 있으면 아직 안 읽은 쪽지의 **대결 방 코드를
엉뚱한 방으로 갈아 끼울 수 있다.** `text` 100자 상한은 화면의 `maxlength` 와 같은 숫자다
(`qa:rules` 가 그 둘이 어긋나지 않는지 본다) — 다르면 규칙에서 거부되는데 화면에는
"보내기를 눌렀는데 아무 일도 안 났다"로만 보인다.

게시는 콘솔 편집기의 CodeMirror 인스턴스에 직접 넣었다(`.CodeMirror.setValue`).
7,391자를 키보드로 치면 CDP 가 자주 시한을 넘긴다. 게시 후 새로고침해 **djb2 해시와
길이가 로컬 파일과 같은지** 확인했다(1773708454 / 7391).

### 검사

`qa:multi` **39번 묶음** · `qa:rules` **9번 묶음**(접속 카드·쪽지 필드·쓰기 규칙·길이 상한) ·
`qa:rank` 11번 묶음(반경 50, "11등이면 1등이 보인다") · **`npm run qa:hoffit`**(새로 만듦) ·
`npm run preview:hof`(새로 만듦 — 명예의 전당·현재접속자·유저상태창 한 장).
`preview:screens` 에 **출발 경고 패널**을 넣어 게이지와 겹치는 모양을 눈으로 봤다.

그 김에 두 가지를 같이 고쳤다:
- `_screens-qa.mjs` 가 **§9-0-31 이후로 죽어 있었다** — 메뉴 이름이 `나의 신발 컬렉션` →
  `신발 도감`, `캐릭터 바꾸기` → `캐릭터 변경` 으로 바뀌었는데 검사만 옛 이름을 눌렀다.
  이름을 고치고 **명예의 전당 → 멀티 메뉴 → 현재접속자** 경로를 추가했다(콘솔 오류 0 확인).
- `preview:screens` 가 **항상 "오류 1건"** 이라고 했다. 정체는 이 저장소에 없는
  `favicon.ico` 의 404 였다 — 진짜 오류가 그 뒤에 묻힌다.

무작위 소크 **1,200판**(시드 0/400/800/1600 × 300판) 전수 통과.

> 이번 회차의 함정은 ⑤ 였다. 카드를 **띄우는 것**까지는 됐는데, 값이 늦게 오는 탭에서
> 카드가 조용히 `0승 / 0게임 / 0켤레` 를 보여 준다. 화면이 비는 것보다 **틀린 숫자를
> 자신 있게 보여 주는 쪽**이 훨씬 나쁘다 — 사용자는 그걸 사실로 읽는다.

---

## 9-0-42. 받은 메세지함 · 수신 거부 · 차단 · 메뉴 키보드 조작 · 마지막로그인 (2026-08-19 12차)

사용자 요청 7건. 지난 회차에 만든 쪽지 기능이 **한 번 쓰고 버리는 팝업**이었는데,
이번에 **이력·설정·차단**이 붙으면서 제대로 된 메세지 시스템이 됐다.

### ① 로비 문구에서 `나의` 를 뺐다

`나의 신발 보유량` → `신발 보유량`, `나의 신발 도감` → `신발 도감`.
내 화면이라는 건 이미 안다.

### ② 쪽지함을 **이력**으로 바꿨다

지난 회차에는 팝업을 닫으면 그 쪽지를 **서버에서 지웠다.** 사용자 요청
(*"다른 사람에게 메시지 주고, 받은거 전부다 이력 뜨게끔하자"*)에 맞춰 셋을 바꿨다.

- **지우지 않고 `read: true` 만 찍는다**(`markRead`). 팝업은 `!out && !read` 만 띄우므로
  "다시 안 뜬다"는 그대로 성립한다. **대결 신청만** 예외로 지운다 — 지나간 신청이
  목록에 남아 봐야 아무 쓸모가 없다.
- **보낸 것도 내 함에 사본을 남긴다**(`out: true`, `to`·`toName`). 대화방을 따로 만들면
  규칙이 한 벌 더 늘고 두 사람이 같은 노드를 쓴다. 각자 자기 함에만 쓰면
  **"내 쪽지함은 내가 쓴다"는 기존 권한 하나로 끝난다.**
- **200통까지만 남긴다**(`INBOX_KEEP`). 오래된 쪽부터 지운다 — 최근 것이 곧 대화다.

`받은 메세지함`(설정 안)에서 줄을 누르면 `답장하기 / 차단 / 닫기`.

### ③ 수신 거부와 차단은 **화면이 아니라 규칙이 막는다**

새 노드 `prefs/$uid` 둘뿐이다. 핵심은 **읽기 범위가 갈린다**는 것:

| | 누가 읽나 | 왜 |
|---|---|---|
| `accept` (켜짐/꺼짐) | **로그인한 누구나** | 보내는 사람이 이유를 알아야 `상대방에 메세지 수신 거부중` 이라고 말할 수 있다 |
| `blocked` (차단 목록) | **본인만** | 목록이 공개되면 그 자체가 사고다 |

그래서 못 보낸 이유를 **두 갈래로** 구분한다:

```
보내기 전  prefs/<상대>/accept 를 읽는다 → false 면 "상대방에 메세지 수신 거부중"
쓰기 시도  규칙이 거부하면            → "상대방이 차단 설정을 했습니다"
```

차단 목록을 못 읽으므로 **누가 나를 차단했는지 알아낼 방법이 없다** — 그게 맞다.
규칙에 `accept.val() != false` 로 쓴 것도 의도적이다. `== true` 로 쓰면 값이 **없는**
사람(설정을 한 번도 안 만진 대다수)이 전부 막힌다.

### ④⑤ 어디서 쪽지를 주고받을 수 있나

- **게임 결과 화면** — 순위 줄을 누르면 유저상태창. 방금 같이 뛴 사람에게 말을 거는 자리다.
- **대기방** — 지난 회차에 `actions: false` 로 버튼을 통째로 껐는데, 사용자 요청대로
  **쪽지는 열고 대결신청만 막는다**(`challenge: false`). 대기방은 DOM 화면이라 팝업이
  그대로 뜨므로 방장이 시작을 누르기 전까지 **실시간으로 주고받는다.**
- **인게임(캔버스)에서는 여전히 안 뜬다.** 안 뜬 쪽지는 서버에 그대로 있다가 판이 끝나
  결과 화면(=DOM)이 뜨는 순간 팝업으로 올라온다.
- 팝업·목록 모두 **받은 시각**을 함께 찍는다(`screens/timeText.js`). 오늘 것은 `시:분`,
  지난 것은 `월.일 시:분` — 목록 대부분이 방금 온 것이라 날짜를 붙이면 같은 글자가
  줄마다 되풀이된다.

> `stamp()` 를 별도 파일로 뺀 이유는 **순환 참조**다. 쪽지함이 유저상태창의
> `openComposer` 를 부르고, 유저상태창이 쪽지함의 `stamp` 를 부르면 서로를 import 한다.
> 포맷 함수는 화면이 아니라 도구이므로 밖으로 뺐다.

### ⑥ 키보드·게임패드로 메뉴를 움직인다

*"터치 없이도 게임 가능하게끔"*. `screens/menuNav.js` 신설.

**브라우저 기본 포커스를 그대로 쓴다.** 직접 "선택된 항목" 상태를 들고 있으면 라우터가
`innerHTML` 을 갈아 끼울 때마다 그 상태가 실제 DOM 과 어긋난다. 대신 `element.focus()`
만 부르고 나머지는 브라우저에 맡긴다 — **포커스된 버튼은 Enter·Space 로 저절로 눌린다.**

- 방향키(상하좌우) = 커서 이동, Enter/Space = 선택, ESC = 뒤로(이미 있던 것).
- 게임패드는 이벤트가 없어 매 프레임 폴링이 필요한데 그 폴링은 엔진(`core/input.js`)에만
  있다. 그래서 `setDomNavHandler` 콜백으로 넘긴다 — **엔진이 DOM 을 직접 만지면
  §6-3 의 경계가 무너진다.** 십자키·왼쪽 스틱·A 버튼을 받고, **`enabled === false`
  (=DOM 화면)일 때만** 돈다. 인게임에서는 같은 십자키가 좌우 조작이다.
- **누른 순간에만 한 번** 보낸다(`padNav`). 누르고 있는 동안 매 프레임 보내면 목록이
  주르륵 흘러간다.
- 입력칸에 글자를 칠 때는 가로채지 않는다(`typing()`), 팝업이 떠 있으면 **그 안에서만**
  움직인다(뒤 화면 버튼으로 새면 안 된다).
- 화면이 바뀌면 포커스를 잃으므로 `MutationObserver` 로 첫 항목을 다시 잡는다. 단
  **키보드·패드를 한 번이라도 쓴 뒤에만** — 마우스로 쓰는 사람에게 테두리가 튀면 안 된다.

### ⑦ 마지막로그인

유저상태창 맨 아래 `마지막로그인: 2026.01.01 19:34`. **접속 중이면 `현재로그인중`** —
그 사람이 지금 여기 있다는 게 날짜보다 훨씬 쓸모 있는 정보다.
값은 접속 때 한 번만 쓴다(`profile.pullAll` → `lastLoginAt`). 판마다 쓸 이유가 없다.

### 규칙 (게시 완료 · 되읽어 해시 대조: `3413475137 / 8034`)

`prefs` 노드가 늘고, `inbox/$uid/$id` 의 쓰기 규칙이 두 군데 느슨해졌다 —
**둘 다 자기 쪽지함 안에서만**이다.

- `.write` 첫 항이 `auth.uid == $uid`. 내 함은 내가 고친다(읽음 표시·정리·보낸 사본).
  남의 함에는 여전히 **새 쪽지를 만드는 것만** 되고 덮어쓰기는 안 된다.
- `from` 검증에 `|| newData.val() == data.val()`. 안 그러면 **받은 쪽지에 읽음 표시를
  다는 순간** `from` 이 내 uid 가 아니라서 거부된다 — 이걸 안 풀면 팝업이 영원히 다시 뜬다.

새 필드 넷(`to`·`toName`·`out`·`read`)은 `$other: false` 때문에 **규칙에 적어야만** 쓰인다.

### 검사

`qa:multi` **40번 묶음** · `qa:rules` 9번 묶음 확장(수신 거부·차단·읽음 표시가 규칙에
막히지 않는지, 기본값이 허용인지, 차단 목록이 비공개인지) · `preview:hof` 에 쪽지함
패널 추가 · `_screens-qa` 에 **받은 메세지함 → 메세지 수신 설정 → 키보드 커서** 경로 추가
(콘솔 오류 0, 방향키 두 번에 두 번째 버튼이 잡히는 것까지 확인).
소크 900판(시드 0/700/1400 × 300판) 통과.

> 이번 회차에서 가장 조심한 곳은 **"거짓말하지 않기"** 다. 쪽지함도 현재접속자처럼
> 못 붙었을 때 `null` 을 주고 화면이 네트워크 안내를 띄운다 — 빈 배열로 뭉뚱그리면
> "주고받은 메세지가 없습니다"라고 **사실이 아닌 말**을 하게 된다(§9-0-6 의 그 실수).

---

## 9-0-43. 현재접속자 새로고침 · **접속 726ms → 362ms** (2026-08-19 13차)

*"이번에 업데이트 하고 좀 느려진거 같아, 전체적으로 속도를 개선할 수 있는 방향이 있는지
꼼꼼히 검수하고 추론해서 속도 개선 방안을 찾아서 적용해봐"*

### 먼저 잴 것을 만들었다 — `npm run perf:boot`

프레임 비용은 `perf:frame` 이 재고 있었지만 **접속 속도를 재는 도구가 없었다.**
그래서 "느려진 것 같다"에 답할 방법이 없었다. dist 를 실제 서버로 띄우고 4G 로 조여
**첫 화면까지의 시간 · 그때까지 받은 바이트 · 판 시작까지의 시간**을 잰다.

> 처음엔 `content-length` 로 바이트를 셌다가 **44KB 라는 거짓 숫자**를 봤다.
> 압축 전송에는 그 헤더가 없다. `request.sizes()` 로 바꾸니 185KB 였다.

### 첫 계측이 범인을 짚어 줬다

```
+313ms  index.js       64.9KB
+476ms  neodgm.woff2   43.6KB
+703ms  firebase.js    70.9KB   ← 이게 끝나야
 726ms  첫 화면                  ← 화면이 뜬다
```

**세션 확인(firebase 청크)이 첫 화면의 앞을 막고 있었다.** 지난 회차에 늘어난 코드가
아니라 원래 있던 구조였다 — 다만 번들이 커지면서 더 두드러졌다.

### 고친 것 (효과 순)

| 무엇 | 왜 | 첫 화면 |
|---|---|---|
| **계정이 있으면 세션 확인을 안 기다린다** | 이 게임은 **로컬이 원본**이다. 로컬에 `uid`+닉네임이 있다는 건 예전에 실제로 로그인했다는 뜻이고 그 화면은 어차피 로비다. 먼저 그리고, 세션이 정말 끊겼으면 그때 로그인 화면으로 되돌린다 | 726 → **453ms** |
| **인게임 코드를 따로 받는다** (`game/loadGame.js`) | 청크를 갈라 재 보니 `src/game/**` 이 **158KB(gzip 49KB)** — 부팅 자바스크립트의 3/4 가 아직 시작도 안 한 게임 코드였다. 한가할 때 미리 받아 두므로 누를 때는 이미 있다 | 453 → **~420ms** |
| **메뉴 화면을 누를 때 받는다** (`screens/lazyScreen.js`) | 도감의 신발 130종 표, 배경설정의 배경 44종 명단, 명예의 전당, 캐릭터 선택, 멀티 화면 일체 — 로비를 그리는 데 한 줄도 안 쓴다 | ~420 → **382ms** |
| **라우터가 쪽지 팝업을 안 물게** | `router` 가 `inboxPopups` 를 정적으로 import 해서 유저상태창까지 부팅 번들에 있었다. **화면이 라우터를 물지, 라우터가 화면을 물지 않는다** — 훅으로 뒤집었다 | |
| **`initAuth()` 를 제일 먼저 띄운다** | 캔버스·입력·오디오를 세운 **뒤에** 불러서 그만큼 요청이 늦게 나갔다. 프라미스만 먼저 띄우고 기다리기는 나중에 | |

**부팅 번들 66.4 → 48.1KB(gzip), 첫 화면 726 → 362ms (−50%).**

### 재 보고 **안** 넣은 것

- **글꼴 프리로드.** 넣으면 Neo둥근모가 먼저 오는 대신 `index.js` 가 밀린다.
  실측 453 vs 460ms — **차이가 없었다.** 차이가 없다면 느린 회선에서 JS 가 이기는 쪽이
  안전하므로 뺐다(글꼴은 `font-display: swap` 이라 어차피 화면을 안 막는다).
- **명예의 전당 가상 스크롤.** 101줄을 만들고 강제 레이아웃까지 **7.2ms** 였다 —
  문제가 아니다. 짐작으로 손댔으면 멀쩡한 코드를 복잡하게만 만들 뻔했다.

### 매 프레임 낭비 둘 (지난 회차에 내가 만든 것)

- **`navigator.getGamepads()` 를 초당 60번** 불렀다. 이 함수는 **부를 때마다 배열을
  새로 만든다.** 게임 루프는 로비에서도 60fps 로 돈다 — 아무도 패드를 안 꽂았는데
  초당 60개의 배열이 생겼다. `gamepadconnected` 로 **연결된 뒤에만** 훑고,
  이벤트를 놓치는 브라우저를 위해 2초에 한 번만 확인한다.
- 12차에 넣은 DOM 커서가 `Object.entries({...})` 를 매 프레임 돌렸다 → 조건문으로 폈다.
- `MutationObserver` 도 `document.body` **subtree** 였다 — 토스트 하나가 뜨고 지는 것까지
  콜백을 깨웠다. 필요한 신호는 둘뿐이라(`#ui` 자식 교체 · 팝업 열고 닫기) 거기만 본다.

### 접속 표시는 **한가할 때** 붙는다

12차에 "전원이 RTDB 에 붙는다"로 바꾸면서 1.5초 뒤에 청크(44KB gzip)를 받게 했는데,
1.5초는 **사용자가 로비를 보고 싱글게임을 누르는 바로 그 순간**이라 판 에셋(146KB)과
회선을 다퉜다. 2.5초 + `requestIdleCallback` 으로 옮기고, **탭이 숨어 있으면 아예
안 붙는다**(돌아오면 그때). 로비의 멀티 미리붙이기(§9-0-33)도 같은 이유로 한가할 때만.

접속 카드도 값이 그대로면 안 쓴다 — 로비를 드나들 때마다 부르므로 헛쓰기가 쌓였다.

### 현재접속자 새로고침 (사용자 요청)

목록은 원래 **구독**이라 저절로 따라온다. 그런데 소켓이 끊겼다 붙는 사이에는 스냅샷이
안 와서 사용자에게는 "멈춘 목록"으로 보인다 — 그때 누를 것이 있어야 한다.
버튼은 **구독을 끊고 다시 건다**(연결부터 새로 잡힌다) + 내 카드도 다시 올린다.

### 검사

`qa:multi` **41번 묶음** · `perf:boot`(신규) · 화면 흐름 전수(모든 지연 로딩 화면을
실제로 열어 보고 콘솔 오류 0) · `perf:frame` · 소크 600판.

> 검사에서 또 같은 함정을 밟았다 — "`subtree: true` 가 없는가" 검사가 **그 사실을 적어 둔
> 주석**에 걸렸다(§9-0-33 의 sticky 검사와 똑같다). 소스 검사는 주석을 걷어내고 본다.

---

## 9-0-44. **버튼이 안 눌린 게 아니라 읽기 권한이 없었다** · 알림 한 칸 아래 · 판돈 7px (2026-08-19 14차)

사용자 요청 7건. 앞의 셋은 인게임 자리 조정이고, 뒤의 넷 중 **둘(⑥⑦)이 같은 뿌리**였다 —
내가 12차에 만든 규칙에 구멍이 하나 있었다.

### ★ ⑥⑦ 근본 원인 — `prefs/$uid` 에 `.read` 가 없었다

*"메세지 꺼짐 누르면 활성화가 안되네 버튼 자체가 안누르네 이유가 뭐야? 꼭 답변해"*
*"지금 차단은 가능한데 차단 푸는 기능이 없는거 같아"*

**두 신고가 같은 한 줄에서 나왔다.** 12차에 `prefs` 노드를 만들면서 읽기 권한을
**잎에만** 걸었다 — `accept`(누구나) 와 `blocked`(본인만). 그런데
**RTDB 읽기 권한은 아래로만 흐른다.** 자식에 아무리 걸어도 **부모는 안 열린다.**

`subscribeMyPrefs()` 는 정확히 그 부모(`prefs/<uid>`)를 구독한다. 그래서 늘 권한 거부 →
오류 콜백 → `cb(null)`. 그 결과가 화면마다 다른 얼굴로 나타났다:

| 화면 | 사용자가 본 것 | 실제로 일어난 일 |
|---|---|---|
| 메세지 수신 설정 | 꺼짐을 눌러도 **아무 반응이 없다** | 쓰기는 **성공한다**. 상태가 안 내려와 버튼이 안 바뀔 뿐 |
| 받은 메세지함 | **차단 해제 버튼이 없다** | `blocked` 가 늘 `{}` 라 항상 '차단하기'로만 그려졌다 |

**쓰기는 되는데 읽기만 막히면 "권한 오류"로 안 보이고 "버튼이 고장났다"로 보인다.**
§9-0-15 와 같은 모양이다 — 없으면 조용히 꺼지는 것이 제일 비싸다.

고친 것 셋:

1. **규칙** — `prefs/$uid` 자리에 `".read": "auth != null && auth.uid == $uid"`.
   깊은 규칙은 권한을 **더하기만** 하므로 `accept` 의 공개 읽기는 그대로 살아 있다.
   게시하고 되읽어 대조했고(8077자 / djb2 1938703942), **규칙 플레이그라운드로 세 갈래를
   실측**했다 — 본인 읽기 **허용** · 남의 것 읽기 **거부** · 남의 `accept` 읽기 **허용**.
2. **화면이 서버 한 곳에만 매달리지 않게** 했다. `setAccept` 가 성공하면 구독을 기다리지
   않고 그 자리에서 뒤집고, `setBlocked` 도 마찬가지다(`markBlocked`). 구독이 또 죽어도
   **누른 대로 움직인다.**
3. 못 읽었을 때 **지금 값을 지우지 않는다.** 예전에는 `cb(null)` 이 기본값으로 덮어써서
   방금 차단해 둔 것이 화면에서 사라졌다.

> 콘솔에 저장되는 규칙은 **줄바꿈 없는 한 줄**이다. 그래서 문서의 들여쓴 원문(10,899자)과
> 길이가 다르다 — 대조는 반드시 `JSON.stringify` 한 값으로 한다. 이걸 모르고 보면
> "3천 자가 사라졌다"로 읽혀 멀쩡한 게시를 의심하게 된다.

### ①② 알림은 **위로 기어오르지 않는다**

*"게임에 방해되고 너무 복잡해서 줄 바꿈 해서 한칸 아래 떴으면 좋겠어 (…) 3명이
플레이하거나 4명이 플레이하면 떨어지고 부활하고 복잡한데 게임 화면을 가린다"*

예전에는 **마지막 줄이 232 에 고정**되고 새 알림이 생길 때마다 앞엣것을 **위로** 밀었다.
그래서 3~4인 판에서 낙사·부활이 겹치면 글자가 발끝(212)을 넘어 계단·캐릭터 위로 올라갔다.

지금은 반대다 — **윗줄이 238 에 고정되고 새 알림이 그 한 칸 아래(251)** 에 붙는다
(`TICKER_TOP` + `i * LINE_H`). 두 줄이면 238~262 로 **조작 버튼 윗변(266) 앞에서 멈추고**,
줄이 몇 개든 위로는 한 도트도 안 올라간다. 그리고 **두 줄까지만** 그린다(`TICKER_ROWS`) —
겹쳐 뜨는 걸 다 보여 주는 것보다 최근 둘만 보여 주는 쪽이 판을 읽는 데 낫다.

검사는 눈이 아니라 **자로 잰다** — `qa:multi` 42번이 `TICKER_TOP > 212`(발끝 아래에서 시작)
와 `TICKER_TOP + (rows-1)*LINE_H + 11 <= 266`(버튼 앞에서 멈춤)을 계산으로 확인한다.

### ③ 판돈 줄 — 7px 로 낮추니 **가운데 정렬이 저절로 풀렸다**

*"폰트 크기를 2단계만 줄여 (…) 그리고 '1등하면 신발 0켤레!' 멘트가 글 가운데 정렬이
아닌거 같아 (…) 큰 숫자 글씨 부분처럼 가운데 정렬 맞춰"*

두 요청이 **하나의 원인**을 가리키고 있었다. 이 저장소의 글자 단계는 2px 이고
인게임 폰트는 11px·7px 두 벌뿐이라(§3-1) **11 → 7 이 정확히 두 단계**다. 그런데
가운데가 어긋나 있던 이유가 바로 폰트 크기였다 —

| | 11px (전) | 7px (후) |
|---|---|---|
| 최대 폭 (`1등하면 신발 523켤레!`) | 128 | **83** |
| 중심 x | **82** (일부러 왼쪽으로 8도트) | **90 = 화면 정중앙** |
| 차지하는 구간 | 18~146 | **48~132** |

11px 로는 정중앙에 두면 오른쪽 레이스 게이지(150~178, 얼굴이 비켜서면 132까지)를
침범해서 **어쩔 수 없이 82 로 밀어 둔 것**이었다(§9-0-41). 글자를 낮추자 그 제약이
사라져 **계단 숫자와 같은 축**에 놓을 수 있게 됐다. 게다가 옛 구간(18~146)보다 오히려
게이지에서 멀다. 폭은 짐작하지 않고 **폰트 데이터로 쟀고**, 42번 묶음이 그 계산을 그대로 검사한다.

> §9-0-34 에서 "인게임 가독성 때문에 7px 을 버렸다"고 적었는데, **판돈 줄만 되돌린 것**이다.
> 거리 줄·알림은 11px 그대로다. 검사도 셋을 갈라 두었다 — 되돌린 것과 안 되돌린 것이
> 검사에서 구별되지 않으면 다음 사람이 "원래 저런가 보다" 하고 넘긴다.

### ④ 마지막로그인 — 만들어 놓고 **넘겨주는 화면이 하나뿐이었다**

*"이거 다시 요청하는건데 처리한거 맞아?"*

만들긴 했다(12차). `유저상태창` 맨 아래 줄도 있고 `현재로그인중` 분기도 있다.
문제는 **값을 채우는 길이 `opt.load` 하나뿐**이었고 **그걸 넘기는 화면은 명예의 전당뿐**이었다는
것이다. 그래서 대기방·게임 결과·현재접속자에서 접속이 끊긴 사람을 열면 언제나
`마지막로그인: 기록 없음`. 사용자가 "안 됐다"고 느낀 게 맞다.

이제 **카드가 스스로 받아 온다.** 다만 정말 필요할 때만 — 접속 중이면 어차피
`현재로그인중` 이라 읽을 이유가 없고, 값을 이미 들고 왔으면 그것으로 끝이다.
**접속이 끊긴 사람의 카드**에서만 계정 문서를 한 번 읽는다.
`leaderboard.js` 는 동적 import 다 — 이 창은 로비에서도 열리는데 Firestore 코드를
부팅 번들에 끌어들일 이유가 없다(§9-0-43).

### ⑤ 답장은 **받은 그 창에서** 바로 쓴다

*"[답장하기] 눌러서 (…) 그렇게 하지말고 메세지 내용이 온 순간 바로 그 아래 칸에
'보낼 메세지를 입력하세요'라는 text 입력 칸이 뜨는거야"*

팝업을 닫고 다른 팝업을 여는 두 단계였다. 그 사이에 화면이 바뀌면 답장을 놓쳤고,
무엇보다 한 번 더 눌러야 했다. 지금은 받은 본문 **바로 아래**에 입력칸이 있고
뜨자마자 커서가 간다.

입력칸은 `screens/replyInput.js` 로 **부품**으로 뺐다 — 같은 칸이 받은 쪽지 팝업과
쪽지함 팝업 두 곳에 붙기 때문이다. 각자 만들면 언젠가 둘이 다른 말을 한다(§9-0-41).

### ⑦ 쪽지함 팝업 — 긴 차단 버튼 + `[닫기][삭제][답장]`

사용자가 지정한 배치 그대로다. 차단은 **누를 때마다 뒤집히고**(이미 차단한 사람이면
`이 사용자 차단해제`), 차단할 때만 한 번 되묻는다 — 실수로 누르면 그 사람 쪽지가 통째로
안 오는데, 푸는 건 되돌리는 일이라 물을 이유가 없다. `삭제` 는 그 한 통을 서버에서 지운다.

### 미리보기가 **거짓말을 하지 않게**

*"캡쳐로 어떻게 만들었나 보여줘"* — 그래서 `npm run preview:msg` 를 새로 만들었다
(`tools/_out/msg.png`, 세 경우를 한 장에). 마크업을 손으로 베끼지 않으려고 팝업 알맹이를
`messageCard()` · `inboxCard()` 로 내보내 **미리보기가 진짜 화면 코드를 그대로 부르게** 했다.

그리고 여기서 **하마터면 오판할 뻔했다.** `preview:hud` 로 판돈 줄을 찍었더니 7px 로
안 줄어 보였다 — `multiHud` 는 7px 폰트를 **비동기로** 받고 아직 없으면 11px 로 그리는데
(그게 정상 폴백이다), 미리보기는 **한 번 그리고 끝**이라 늘 그 폴백만 찍혔다.
`await loadSmallFont()` 를 넣고 나서야 실제 화면과 같아졌다.

> §9-0-33·§9-0-37 과 같은 모양이 또 나왔다. **미리보기와 검사는 실제와 같은 상태에서
> 돌아야 한다.** 아니면 통과도 실패도 의미가 없다.

### 검사

`qa:multi` **42번 묶음**(알림 좌표를 자로 계산 · 판돈 폭을 폰트 데이터로 계산 ·
`prefs` 읽기 규칙 · 인라인 답장 부품 · 새 버튼 구성) · `qa:rules` · `qa:rank` · `qa:wallet` ·
`qa:bg` · `qa:bgfit` · `qa:hoffit` · `perf:frame`(전부 60fps 예산 안) ·
화면 흐름 전수(`_screens-qa`, 콘솔 오류 0) · `preview:msg`(신규) · `preview:hud` · `preview:screens` ·
소크 300판. 낡은 검사 4건은 **이번에 의도적으로 되돌리거나 바꾼 것**이라 같이 갱신했다.

---

## 9-0-45. **구독 한 번이 팝업을 죽이고 있었다** — 전면 점검 10건 (2026-08-19 15차)

*"메세지 보내는 것이 안되거나, 대결신청이 안되거나, 게임 로비에 있는 사용자에게 메시지
보내는 것이 안되거나 (…) 버그나 오류, 버퍼링, 작동 안하는 모든 부분들을 찾아내서
최적화해"* — 우선순위도 함께 받았다: **속도·안정성 → 기능 → 쪽지/대결 → 정산.**

신고된 세 증상은 **전부 같은 한 줄**에서 나왔다. 나머지 일곱은 그 김에 전 경로를
다시 읽으면서 잡은 것이다. 열 건 모두 **재현 경로를 확인한 것만** 손댔다.

### ★ ① 라우터가 `refresh()` 때마다 팝업을 걷어 갔다 (신고 3건의 공통 원인)

`nav.refresh()` → `draw()` 는 맨 앞에서 무조건 `closeAllOverlays()` 를 불렀다.
화면이 **바뀔 때**는 맞는 처리다(남으면 새 화면 위에 얹힌다). 그런데 `refresh()` 는
**같은 화면을 다시 그리는 것**이고, 그것을 부르는 쪽은 대부분 **구독 콜백**이다:

| 화면 | 얼마나 자주 |
|---|---|
| 대기방 | 생존 신호(`seenAt`)만으로 사람당 5초에 한 번 → 4인이면 **1.2초에 한 번** |
| 현재접속자 | 접속자 **아무나** 카드를 다시 쓸 때마다 |
| 받은 메세지함 | 쪽지가 하나 올 때마다, 설정이 바뀔 때마다 |

즉 **아무것도 안 해도 몇 초 안에 팝업이 저절로 닫혔다.** 증상이 셋으로 갈렸다:

- 쪽지를 쓰려고 입력칸을 띄우면 **타이핑 중에 창이 사라진다** → "메세지가 안 보내진다"
- 받은 쪽지 팝업은 뜨자마자 닫히면서 `읽음` 이 찍혀 **영영 다시 안 뜬다** → "메세지가 안 온다"
- 대결 신청 팝업은 닫히면서 **자동 거절**로 처리돼 신청자에게 거절 통보가 간다 → "대결신청이 안 된다"
- 쪽지함에서 쪽지를 열면 `markRead` → 구독 → refresh → **자기가 자기를 닫는다**

고친 것은 두 겹이다.

1. **`refresh()` 는 팝업을 건드리지 않는다** (`draw(keepScroll, keepOverlays)`).
   화면이 실제로 바뀌는 `mount()` 에서만 걷는다.
2. **닫힌 이유를 구별한다** — `presentOverlay(node, onClose)` 의 `onClose` 가
   `'user'`(직접 닫음) / `'screen'`(강제로 치워짐)을 받는다. 강제로 치워진 쪽지는
   읽음을 안 찍고 **기억에서 빼서 다음 화면에 다시 띄우고**, 대결 신청은 거절 통보를
   보내지 않는다. 닫히는 것 자체가 결정인 팝업이라 이 구별이 없으면 안 된다.

   > 돌려주는 `close` 는 **버튼 핸들러로 그대로 쓰인다**(`button(S.close, close)`).
   > 그때 첫 인자는 클릭 이벤트다 — 그래서 **문자열일 때만** 사유로 읽는다.

**브라우저에서 직접 확인했다**(`_screens-qa`): 팝업을 연 뒤 `refresh()` → **남아 있고**,
화면을 옮기면 → **사라진다**. 소스 검사만으로는 이 종류를 못 잡는다.

### ② 대결신청 — 상태 문자열을 boolean 으로 보고 있었다

```js
const ok = await Presence.sendChallenge(p.uid, code);
if (!ok) toast(S.networkError);      // ← 'error' 도 'blocked' 도 전부 참이다
```

`sendChallenge` 는 `'ok' | 'off' | 'blocked' | 'error'` 를 준다. **빈 문자열이 아닌 모든
문자열은 참**이므로 실패가 통째로 성공으로 읽혔다. 그래서 신청이 아예 안 갔는데도
**"대결 신청을 보냈습니다"** 가 뜨고, 신청자는 아무도 오지 않는 방에서 기다렸다.

이제 `r !== 'ok'` 로 보고, 못 갔으면 **방을 남기지 않는다** — 빈 방은 자동 매칭이 훑는
12칸을 갉아먹는다(§9-0-17·§9-0-21 ②에서 두 번 데인 자리다). 이유도 그대로 알려 준다
(`상대방에 메세지 수신 거부중` / `상대방이 차단 설정을 했습니다`).

### ③ 보내기 전에 **연결을 기다린다**

RTDB 쓰기는 소켓이 없으면 거절하지 않고 **큐에 쌓인다.** 그래서 `withTimeout` 이
12초를 세고 나서야 실패가 되는데, 사용자에게는 그 12초가 **"눌렀는데 아무 일도 안 난다"** 다.
접속 표시는 2.5초 뒤 한가할 때 붙으므로(§9-0-43) **로비에 들어오자마자 보내면 정확히
그 창에 걸린다.**

방(`multiplayer.js`)은 이미 같은 이유로 `waitConnected` 를 쓴다(§9-0-17). 쪽지 쪽에는
없었다. 여기서 그 파일을 물면 **쪽지 하나 보내려고 멀티 모듈 전체를 받으므로** 10줄짜리를
따로 뒀다. 곁다리인 수신 설정 확인은 시한을 **3초**로 줄였다 — 못 읽어도 규칙이 어차피
막아 주는데, 12초를 기다리면 곁다리가 본 줄기를 막는다.

### ④ 구독 스냅샷마다 화면을 **통째로** 다시 세웠다 (버퍼링)

`nav.refresh()` 는 `innerHTML = ''` 뒤 전체 render 다. 대기방은 생존 신호만으로 1.2초에
한 번, 현재접속자는 남의 카드가 갱신될 때마다 — 참가자 줄의 `<img>` 까지 매번 새로
만들어져 **얼굴이 깜빡이고** 그 순간의 입력이 새 노드로 넘어가지 못한다.

그래서 **화면이 실제로 읽는 값만** 뽑아 비교하고, 같으면 안 그린다. 대기방 열쇠에는
`seenAt`·`offAt`·`stairs`·`alive`·`revives`·`result` 가 **없다** — 그 화면에 한 글자도
안 나오는 값들이다. 현재접속자 열쇠에는 `at` 이 없다. 검사가 그 부재를 못 박는다.

### ⑤ 정산 도장이 **방 코드만**이라 '계속하기' 2판째부터 샜다

결과 화면의 `계속하기` 는 **같은 방 코드로** 다음 판을 돈다(`resetRoom`). 그런데 도장이
`` `${code}:pay` `` 라 2판째에는 이미 찍혀 있었고, 그 안에 있던 것이 통째로 건너뛰어졌다 —
**남들이 판 도중 주운 신발**을 승자가 받는 것과, **승패 기록**(로비 승률이 첫 판에서 멈춘다).
한 방에서 계속 이어 하는 것이 가장 흔한 플레이인데 그 경로가 §9-0-31 이후 계속 새고 있었다.

두 갈래로 고쳤다:

- **주운 신발 보너스는 서버 도장으로** (`result/claims/<나>/found`). 로컬 도장은 저장소를
  지우거나 기기를 바꾸면 사라져 **없던 신발이 다시 굴려진다** — 판돈 쪽은 §9-0-14 에서
  같은 이유로 이미 서버로 옮겼는데 여기만 남아 있었다. `result` 는 판마다 지워지므로
  판 단위 구분이 저절로 된다.
- **승패 기록은 로컬 통계**라 서버 도장이 없다 → 도장 열쇠 자체를 판 단위로 바꿨다
  (`${code}:${seed}:${endedAt}:pay`). 종료 시각과 시드를 **겹쳐** 쓴다 — 하나만 쓰면 두
  판이 같은 밀리초에 끝나거나 시드가 겹칠 때 충돌한다.

> 15차 이전 클라이언트가 찍은 `claims` 에는 `found` 키가 없다. 그 경우는 **이미 굴린 것**
> 으로 본다 — 배포 직후 한 판이 두 번 굴려지는 걸 막는다.
> 그리고 `claims` 에 uid 가 아닌 키가 생겼으므로 `claimMask` 가 **판돈을 낸 사람만** 세게
> 좁혔다(안 그러면 `ORPHAN_BIT` 가 켜져 옛 클라이언트가 오해한다).

### ⑥ 진 사람의 미납이 **영구 면제**되고 있었다

`sweepUnsettled` 는 "볼일 없는 방"의 재시도 기록(`userRooms`)을 지운다. 그 판정이
`res && !res.pending` 인데 `pending` 을 **승자 쪽에서만** 세고 있었다 — 진 사람은 언제나 0
이라 **무조건 지워졌다.** 그래서 납부가 한 번이라도 실패하면(시한 초과·지갑이 아직 안
내려온 새 기기) 그 빚은 영영 안 갚아지고 승자는 못 받는다.

이제 납부 실패와 "지갑이 비어 보여 못 냄"을 둘 다 `pending` 에 넣는다.

### ⑦ 진 사람이 나가면 화면의 판돈이 **폭삭 줄었다**

`potShoes` 는 명단을 `players` 에서 만들었다. 그런데 `leaveRoom` 은 `players/<uid>` 를
통째로 지우고, 진 사람은 결과 화면에서 곧장 나가는 게 정상 동작이다. 실측: 1:1 에서
부활 1회 + 습득 6 인 판이 **27 → 2** 로 줄었다. 실제 수령액은 그대로인데 표시만 틀린 것이라
§9-0-36 에서 고친 "계산이 안 맞는다"가 같은 자리에서 재발한 셈이다.

정산은 이미 `result`(rankings·given·found)를 진실로 쓴다(§9-0-34) — **표시도 같은 곳을 본다**
(`potRoster`). 떠난 사람의 부활 몫은 낸 양에서 되짚는다(`floor(paid / 20) * 20`).

### ⑧ 읽기 한 번 실패에 **승리 화면이 무너졌다**

`readRoom` 은 못 읽으면 예외가 아니라 `null` 을 준다(시한 초과). `MultiResult` 가 그걸
그대로 담아서, 이긴 화면이 **"다른 사람들이 아직 오르고 있습니다"** 로 되돌아갔다.
새로 읽은 값이 있을 때만 갈아 끼운다.

### ⑨ `계속하기` 와 재정산 폴링이 **같은 판돈을 두 번** 걷을 수 있었다

미수령이 있으면 결과 화면은 2초마다 다시 걷는다(`poll`). 그 왕복 도중에 `계속하기` 를
누르면 그쪽도 `settleRoom` 을 부른다. 둘 다 같은 `claims` 를 읽고 같은 답을 계산하므로
**서버는 아무것도 못 막는다**(두 쓰기가 같은 값이다). 이제 진행 중인 정산이 있으면
**그 프라미스를 그대로 돌려준다** — 계산은 한 번만 돈다.

### ⑩ 판 시작 실패가 아무 데서도 안 잡혔다

`startMultiGame` 은 인게임 청크를 동적으로 받는다. PWA 캐시가 낡았거나(§9-0-21 ⑨)
그 순간 회선이 끊기면 **거부**되는데, 대기방은 `.catch` 없이 불렀다. 그러면
`nav.toCanvas()` 도 `Scene.reset()` 도 안 돌고 구독은 이미 끊긴 뒤라 **대기방이
얼어붙는다.** 게다가 `launched` 가 참이라 나갈 때 방에서 빠지지도 않아 남들의 판까지
30초 붙잡는다. 이제 잡아서 자리를 비우고 로비로 내보낸다.

### 검사 — 실제 사용자 행동을 흉내 내지 않으면 통과는 의미가 없다

§9-0-34·§9-0-39 의 교훈대로, **고치기 전에 실제로 실패하는지 먼저 확인**하고 넣었다.

| 시나리오 | 고치기 전 |
|---|---|
| `sim:multi` **S32** 계속하기로 두 판 연속 | 승수가 **1** (2여야 함) |
| `sim:multi` **S33** 납부 실패 뒤 다음 접속 | 재시도 기록이 **삭제됨** → 빚 영구 면제, 승자 20 (21이어야 함) |
| `sim:multi` **S34** 진 사람이 나간 뒤 판돈 표시 | **2** (27이어야 함) |
| `_screens-qa` 팝업 생존 | `refresh()` 한 번에 **0개** (남아야 함) |

`qa:multi` **43번 묶음**이 열 건의 코드 경로를 전부 못 박는다(주석을 걷어내고 본다 —
§9-0-33 의 그 함정). 그 밖: `qa:rules`·`qa:rank`·`qa:wallet`·`qa:bg`·`qa:bgfit`·
`qa:hoffit`·`_lint-services`·`perf:frame`(fillRect 수 변동 0) · 화면 흐름 전수(오류 0) ·
**소크 1,200판**(시드 0/700/1400/2100 × 300판).

> 이번 회차의 교훈: **"안 된다"는 신고 세 개가 한 줄에서 나올 수 있다.** 기능마다
> 따로 파고들었으면 셋 다 못 찾았을 것이다. 공통점을 먼저 찾은 것 —
> "세 경우 모두 **구독이 도는 화면**에서 일어난다" — 이 한 문장이 답이었다.

---

## 9-0-46. **`max-height` 한 줄이 순위표 아래를 검게 비웠다** · 로비 숫자 크기 (2026-08-19 16차)

*"신발왕 표기 부분에서 1위에서 7위까지 (…) 그 아래 부분은 검은색으로 표기되는데?
iframe 처럼 어떠한 영역이 있고, 그게 1위에서 7.5위까지 표기 되는데 그 밑으로는 표기가
안 되는 느낌이야, (…) 내 순위 부분 영역 설정이 잘못된 느낌이야"*

세 가지를 받았다. **셋 다 처리했고, 하나씩 아래에 적는다.**

### ★ ① 명예의 전당 — `iframe` 이 아니라 CSS 두 줄이었다

사용자가 "영역"이라고 부른 것은 실제로 있었다. 다만 프레임이 아니라 **`.rank-list` 의
`max-height`** 였다.

```css
.rank-list { max-height: 48vh; flex: 1 1 auto; overflow-y: auto; }
```

`48vh` 는 **화면 높이의 절반**이다. 390×780 폰에서 374px — 줄 높이가 44px 남짓이니
정확히 **8줄 반**에서 잘린다. 사용자가 본 "1위에서 7.5위" 가 이것이다.

문제는 그 아래였다. 같은 세로 flex 안에 `.spacer { flex: 1 }` 가 있었다:

```
title / tabs / body(.rank-list, flex:1 이지만 48vh 에서 멈춤) / .spacer(flex:1) / 내 순위 / 뒤로
```

목록이 자기 몫을 **48vh 에서 포기하면 남는 공간은 전부 여백이 가져간다.** 그래서
목록 아래가 통째로 검게 비었다. **실측한 값이 신고 그대로였다:**

| 화면 | 목록 높이 | 보이는 줄 | 목록 아래 검은 공백 |
|---|---|---|---|
| 390×780 | 374px | 8줄 | **173px** |
| 412×915 | 439px | 9줄 | **243px** |

고친 것은 두 줄이다.

1. `.rank-list` 에서 **`max-height` 를 뺐다.** 스크롤은 `flex: 1 1 auto` 로 남는 높이를
   다 먹고 `overflow-y: auto` 로 걸린다 — 높이 상한은 애초에 필요가 없었다.
   (11차에 "목록이 100줄이라 화면이 길어진다"고 넣은 것인데, 그때 이미 `.rank-list` 는
   스크롤 컨테이너였다. 상한은 문제를 안 풀고 새 문제를 만들었다.)
2. **목록이 있을 때는 `.spacer` 를 넣지 않는다.** 둘 다 `flex:1` 이면 남는 공간을 나눠
   갖는다 — 늘어나야 할 자리에 여백이 끼어 있으면 안 된다. 반대로 로딩·오류·빈 목록일
   때는 여백이 있어야 '내 순위' 와 '뒤로' 가 바닥에 붙으므로 `listed` 로 갈랐다.

같은 짝이 **현재접속자(`.online-list` 56vh)** 와 **받은 메세지함(`.inbox-list` 60vh)**
에도 그대로 있었다 — 신고는 안 됐지만 같은 증상이라 함께 고쳤다.

**고친 뒤 실측:**

| 화면 | 목록 높이 | 보이는 줄 | 검은 공백 |
|---|---|---|---|
| 360×640 | 393px | 8줄 | 14px |
| 390×780 | **533px** | **11줄** | **14px** |
| 412×915 | **668px** | **14줄** | **14px** |

'내 순위' 와 '뒤로' 의 바닥 좌표는 그대로고 페이지 스크롤은 0이다 — 사용자가 요청한
*"아래쪽 내 순위 부분면 길이 고정"* 은 원래도 고정이었고, 지금도 고정이다.

### ② 로비 최고기록 숫자 — 3단계 작게

이 저장소의 "단계" 는 **2px** 이고 `pixelText` 의 `scale` 은 정수만 된다(§3-1).
7px 폰트에 `scale: 5` → **`scale: 2`** 가 정확히 3단계(35px → 14px)다.

### ③ `신발 보유량 00켤레` → `보유신발 00켤레`, 숫자는 최고기록과 같은 크기

*"보유 신발 수량이 생각보다 중요한데 너무 작게 표기되는 거 같아서"*

문구를 `myShoesOwned(n)` 한 덩어리에서 `myShoesLabel`/`myShoesUnit` 둘로 쪼갰다 —
가운데 숫자는 이제 **그림(bitmap)** 이라 문자열 안에 들어갈 수 없다. 두 줄이 같은
`STAT_SCALE` 상수를 쓰므로 **앞으로도 따로 놀 수 없다.**

> 크기를 키우면 자리를 더 먹는다. 네 자리(1460켤레)를 넣고 320~412px 에서 다시 쟀다 —
> 패널 높이 133px 그대로, 넘침 0, 뱃지도 같은 줄에 남는다.

### ④ (이어서) `신발 도감 0/130켤레` → `신발도감 0 / 130켤레`

*"메인 로비에 문구 '신발 도감 0/0켤레' 변경하자 '신발도감 0 / 0켤레' 이걸로 변경해"*

이름은 붙이고 빗금 앞뒤는 띄운다. 바로 윗줄이 `보유신발` 로 붙어 있으므로 라벨 모양이
맞는다. 이 줄은 숫자가 작은 일반 글자라 문장형 그대로 둔다(`myDexProgress`).

> ※ **빗금 띄어쓰기는 바로 다음 회차(§9-0-47)에 되돌렸다** — 숫자만 크게 찍게 되면서
>   빈칸이 있으면 큰 숫자와 작은 글자 사이가 더 벌어져 보였기 때문이다.

> `_lobbyfit` 이 **도감을 비워 두고** 재고 있었다 — 화면엔 늘 `0 / 130` 이라 자릿수가
> 늘었을 때 줄이 터지는지 볼 수 없었다(지갑에서 이미 한 번 데인 것과 같은 함정).
> 87켤레를 채워 넣고 문구 정규식까지 검사에 박았다: 320~412px 전부 한 줄, 넘침 0.

### 검사 harness 가 거짓말을 하고 있었다 (두 건)

- **`_lobbyfit.mjs` 의 프로필에 `walletVersion: 2`** 가 박혀 있었다. 실제 값은 1이라
  앱이 켜질 때마다 `migrateWallet()` 이 돌았고, 도감이 비어 있으니 지갑이 0으로
  복원돼 **화면에는 늘 `보유신발 0 켤레`** 가 찍혔다. 1460켤레를 넣어 두고 정작 재는
  건 한 자리였다 — 숫자를 키우는 이번 변경에서는 이 상태로 재면 아무 의미가 없다.
  버전을 맞추고, **자릿수와 두 숫자의 높이가 같은지**까지 검사에 넣었다.
- **`npx vite` 를 `kill()` 해도 서버가 안 죽는다.** 껍데기(npx)만 죽고 vite 는 남아
  다음 실행에서 포트를 빼앗는다. 이번에 `qa:hoffit` 이 "줄이 0개" 로 **코드가 멀쩡한데
  실패했다.** 서버를 띄우는 도구 9개 전부 `detached: true` + `process.kill(-pid)` 로
  **그룹째** 죽이게 고쳤다.

### 검사

| 검사 | 결과 |
|---|---|
| `qa:hofvert` (신규) | 360×640·390×780·412×915·360×800 전부 검은 공백 14px |
| ↳ `max-height` 를 되돌려 보면 | **100/173/243/183px 로 4건 실패** — 신고 그대로 재현된다 |
| `qa:lobbyfit` (승격) | 320~412px 한 줄 유지 · 넘침 0 · 두 숫자 높이 26px 동일 · 네 자리 |
| `qa:multi` **44번 묶음**(신규) | `max-height` 없음 · 조건 없는 `.spacer` 0개 · `STAT_SCALE` 공유 · 새 문구 |

그 밖: `build` · `qa:multi`(44묶음 포함) · `qa:rules` · `qa:rank` · `qa:wallet` ·
`qa:bg` · `qa:bgfit` · `qa:hoffit` · `qa:pwa` · `_lint-services` · `_screens-qa`(오류 0) ·
`sim:multi` · `perf:frame`(전부 60fps 예산 안) · `perf:boot`(첫 화면 393ms).

> 이번 회차의 교훈: **사용자가 "iframe 같은 영역"이라고 말할 때, 진짜로 영역이 있다.**
> 다만 그 영역은 프레임이 아니라 CSS 값이었다. 그리고 **검사 도구가 거짓 통과를
> 하고 있으면 고친 것도 안 고친 것도 알 수 없다** — 이번에 두 건을 찾았고, 새 검사는
> **고치기 전 상태에서 실패하는 것을 먼저 확인한 뒤**에 넣었다.

---

## 9-0-47. 숫자 크기 사다리에는 **칸이 정해져 있다** — 로비 통계 3건 (2026-08-19 17차)

*"최고기록, 보유신발, 갯수 나오는 부분 숫자의 크기를 딱 1단계만 크기를 줄여줘,
그리고 신발도감 87/130켤레, 멀티게임 17승/50게임 이렇게 띄어쓰기 없이 붙여,
그리고 87, 130, 17, 50 이렇게 4곳 숫자부분만 글씨 크기를 2단계 크게"*

### ★ ① "1단계"를 그냥 정할 수가 없었다 — 고를 수 있는 크기가 여섯 개뿐이다

로비의 큰 숫자는 CSS 글자가 아니라 **비트맵**(`pixelText`)이고, 배율은 **정수만** 된다(§3-1).
글꼴은 갈무리11(11px)과 갈무리7(7px) 두 벌뿐이다. 그래서 실제로 존재하는 크기는

```
11 · 14 · 21 · 22 · 28 · 33px
```

**여섯 칸이 전부**다. 16차에 정한 22px(11px 글꼴 ×2)에서 "한 칸 아래"는 21px 인데,
그건 **1px 차이**다. 그 아래는 14px 로 뚝 떨어진다. 어느 쪽도 "딱 1단계"가 아니다.

그래서 **짐작하지 않고 네 후보를 실제로 렌더해 한 장에 담아 보여 줬다**
(22 / 21 / 14 / 11px, 도감·멀티 숫자를 이미 키운 상태로). 눈으로 보면 바로 갈렸다:

| | 크기 | 실제로 어떻게 보이나 |
|---|---|---|
| A | 22px (11px 글꼴 ×2) | 지금 크기 |
| **B** | **21px (7px 글꼴 ×3)** ← 채택 | 1px 작지만 **획이 3도트라 또렷하다** |
| C | 14px (7px 글꼴 ×2) | 확실히 작아지는 대신 도감·멀티 숫자(17px)보다 작아져 **위아래가 뒤집힌다** |
| D | 11px (11px 글꼴 ×1) | 라벨 글자와 같은 크기 — '최고기록'이 강조되지 않는다 |

> **크기를 재는 것과 크기로 보이는 것은 다르다.** B 는 A 보다 1px 작은데 획이 두꺼워
> 오히려 더 굵어 보인다. 표로만 골랐으면 이걸 몰랐다.

**함정 하나:** 7px 글꼴은 **비동기로 받는다**(`loadSmallFont`, 19KB gzip). 아직 안 왔는데
`small: true` 로 그리면 `pixelText` 가 11px 글꼴로 떨어져 **×3 = 33px, 즉 더 커진다.**
그래서 도착 전에는 배율 2(=22px)로 그리고 도착하면 다시 그린다 — 22 → 21 은 1px 이라
바뀌는 순간이 눈에 안 띈다. 33 → 21 이면 화면이 출렁였을 것이다.

글꼴은 **한가할 때** 받는다(`requestIdleCallback`). 로비를 그린 직후는 사용자가 곧바로
`싱글게임` 을 누르는 순간이라 판 에셋(146KB)과 회선을 다툰다(§9-0-43 의 그 자리).
첫 화면 시간은 393ms 그대로고, 총 전송량만 4초까지 216 → 235KB 로 늘었다
(예전엔 멀티에 들어가야 받던 것을 이제 전원이 받는다 — 대신 멀티 첫 판이 그만큼 빠르다).

### ② 빗금 앞뒤를 붙였다

`신발도감 87 / 130켤레` → **`신발도감 87/130켤레`**, `멀티게임 17승 / 50게임` →
**`멀티게임 17승/50게임`**. 바로 앞 회차(§9-0-46 ④)에 띄웠던 것을 되돌린 것이다 —
숫자만 크게 찍게 되면서 빈칸까지 있으면 큰 숫자와 작은 글자 사이가 두 번 벌어진다.

### ③ 문구 속 숫자 네 곳만 두 단계 크게 (13px → 17px)

`.stats` 본문이 13px 이고 DOM 글자의 한 단계는 2px 다(§9-0-31 에서 타이틀을 20→24px 로
키운 것과 같은 기준).

문구를 `신발도감`/`87`/`/`/`130`/`켤레` 로 쪼개 `strings.ko.js` 에 두는 방법도 있었지만,
그러면 **문구 하나가 코드 다섯 줄이 되고** 말을 바꿀 때 두 곳을 고쳐야 한다. 대신
`statLine()` 이 **숫자 덩어리만 찾아 감싼다**(`/(\d+)/`) — 문구는 문장 그대로 남아
`qa:multi` 의 문구 검사도 한 줄로 유지된다.

`line-height: 1` 을 안 걸면 큰 글자가 줄 높이를 밀어 **패널이 통째로 커지고** 캐릭터·뱃지
칸이 따라 내려간다. `qa:lobbyfit` 이 패널 높이 133px 를 재고 있어서 바로 잡혔다.

### 검사

| 검사 | 무엇을 보나 |
|---|---|
| `qa:lobbyfit` | 320~412px 한 줄 유지 · 넘침 0 · **두 숫자 높이가 같은지**(27px) · 네 자리 렌더 · 문구 정규식 · 숫자 칸 4개가 17px 인지(본문 13px) |
| `qa:multi` 44번 | `STAT_SCALE === 3` + **폴백 배율이 있는지**(없으면 33px 사고) · 두 숫자가 **같은 객체**를 쓰는지 · 한가할 때 받는지 · 새 문구 |

그 밖: `build` · `qa:multi` · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` · `qa:bgfit` ·
`qa:hoffit` · `qa:hofvert` · `qa:pwa` · `_lint-services` · `_screens-qa`(오류 0) ·
`sim:multi` · 소크 900판 · `perf:frame`(전부 60fps 안) · `perf:boot`(첫 화면 393ms).

낡은 검사 2건(도감·승률 문구의 빗금 띄어쓰기)은 **이번에 의도적으로 되돌린 것**이라
같이 갱신하고 그 사실을 주석에 남겼다 — 안 그러면 다음 사람이 "원래 저런가 보다" 한다.

> 이번 회차의 교훈: **"한 단계 작게"는 도트 화면에서 자명한 요청이 아니다.** 배율이
> 정수뿐이라 사다리에 칸이 여섯 개밖에 없고, 그중 둘은 1px 차이거나 위아래가 뒤집힌다.
> 이럴 때는 표로 설명하는 것보다 **네 가지를 실제로 그려서 나란히 보여 주는 쪽**이 빠르다.

---

## 9-0-48. **외곽선 여백이 곧 가짜 띄어쓰기였다** · 통계 네 줄 통일 · 캐릭터 칸 (2026-08-19 18차)

*"계단과 켤레라는 글씨가 숫자 옆에 바로 붙어 있어야 하는데 한칸 띄어쓰기 한거 같다 (…)
멀티게임이 위로, 신발도감이 아래로 (…) 00승 00게임 숫자도 21px (…) 모든 줄 간격이
똑같게 (…) 캐릭터 칸 자체를 10% 넓혀 (…) 작은 스마트폰에서 깨지지 않도록"*

### ★ ① "붙여 달라"는 요청의 원인이 **두 겹**이었다

**첫 겹 — `gap`.** 줄이 `display: flex; gap: 4px` 였다. 라벨·숫자·단위가 전부 flex
아이템이라 **숫자와 단위 사이에도 똑같이 4px** 이 들어간다. 게다가 flex 는 아이템
경계의 공백을 지우므로 문구에 적힌 띄어쓰기는 **오히려 사라진다** — 붙일 곳은 벌어지고
벌릴 곳은 붙는, 정확히 거꾸로 된 상태였다.

그래서 줄을 **인라인**으로 바꾸고 문구를 **문장 하나**로 되돌렸다.

```js
myBestRecord: (n) => `최고기록 ${n}계단`     // 라벨 뒤 한 칸, 단위 앞 0칸 — 문구 그대로
```

16·17차에 `myShoesLabel`/`myShoesUnit` 으로 쪼갰던 것을 되돌린 것이다. 쪼개 두면 화면이
조각을 **직접 붙여야 하고**, 그 순간 간격 규칙이 CSS 로 새어 나간다. 문장으로 두면
띄어쓰기가 곧 문구다. 가운데 숫자는 `statLine()` 이 `/(\d+)/` 로 찾아 비트맵으로 갈아 끼운다.

**둘째 겹 — 외곽선 여백.** 그런데도 화면에는 여전히 `5432 계단` 처럼 벌어져 보였다.
`pixelText` 는 외곽선이 있으면 **캔버스 사방에 배율만큼(3px) 여백**을 둔다(잘리지 않게).
그 여백이 문장 안에서 **투명한 한 칸**이 된다 — 눈에는 "띄어쓰기를 한 것 같다" 로만 보이고
어디서 온 건지 알 수가 없다.

음수 여백으로 상쇄해 봤더니 이번엔 `17승/50` 의 **`승` 과 `/` 가 짓눌렸다** — 여백이
비어 있는 게 아니라 **외곽선 잉크가 거기까지 그려지기** 때문이다(offset ±3).
그래서 방향을 바꿨다: **로비 통계 숫자는 외곽선을 안 쓴다.**

- 이 패널은 배경이 어둡고 글자가 크림색이라 **외곽선이 보이지도 않았다** — 자리만 먹었다
- 여백이 0이 되니 문장의 띄어쓰기가 **그대로** 나온다(실측: 붙어야 할 여섯 자리 전부 0px)
- 덤으로 캔버스 높이가 27 → 21 이 되어 **패널이 오히려 129px 로 줄었다**(전 133)

> 상쇄 코드(`marginRight: -pad`)는 **남겨 뒀다.** 언젠가 외곽선을 다시 켜면 같은 함정에
> 바로 다시 빠지기 때문이다. `pixelText` 가 `dataset.pad` 로 값을 알려 준다.

### ② 네 줄을 **같은 모양**으로 통일 · 순서 교체

```
최고기록 5432계단      ← 전부 같은 함수(statLine)·같은 옵션(statNum)
보유신발 1460켤레         → 크기가 갈라질 수 없다
멀티게임 17승/50게임    ← 위로 (사용자 지정)
신발도감 87/130켤레     ← 아래로
```

`17`·`50`·`87`·`130` 도 **21px 비트맵**이다 — 17차의 CSS 17px 글자에서 바뀌었다.
네 줄이 같은 높이의 캔버스를 품으므로 **줄 간격이 저절로 같아진다**(실측 28·28·28px).
17차에 있던 `.stat-shoes { margin-top: -2px }` 같은 줄별 보정은 없앴다.

### ③ 캐릭터 칸 — 10% 넓히고, 그림도 10% 키우고, 이름과 띄웠다

칸 66 → **73px**(4차에 줄인 것을 되돌림), 그림 50×76 → **55×84**, 그림과 닉네임 사이
2 → **5px**.

### ★ 좁은 폰 — **잘림은 넘침으로 안 잡힌다**

숫자가 커지면서 통계 줄이 **179px** 를 요구하게 됐다. 한 줄에 다 넣으려면

```
통계 179 + 캐릭터 73 + 뱃지 52 + 간격 12 + 패널여백 16 + 화면여백 24 = 356px
```

가 필요하다. 그 아래(320px 폰)에서는 무엇을 조여도 안 들어간다. 그런데 **억지로 밀어
넣어도 검사는 통과한다** — `.stats` 는 `flex: 1 1 auto` 라 패널을 넘치는 대신 **줄어들고**,
글자는 `text-overflow: ellipsis` 로 **말없이 잘린다.** 넘침은 끝까지 0이다.

그래서 두 가지를 했다:

1. `qa:lobbyfit` 에 **줄마다 `scrollWidth - clientWidth`** 를 재는 검사를 넣었다.
   실제로 이 검사가 360px 에서 5~7px 잘림을 잡아냈고, 패널 여백을 8→6·10→8 로 조여 풀었다.
2. **355px 이하에서는 뱃지를 아랫줄로 내린다**(가로 2칸). 통계를 내리는 방법도 있었지만
   그러면 윗줄의 캐릭터와 뱃지 사이가 통째로 비어 패널이 **267px** 까지 커진다 —
   뱃지를 내리면 **210px**(지금은 186px)에서 멈춘다. 둘 다 실측하고 골랐다.

   > 미디어쿼리를 `.badges` 로 썼더니 **안 먹었다.** `.badges` 기본 규칙이 파일에서
   > **뒤에** 있어 같은 특이도면 그쪽이 이긴다. `.panel .badges` 로 올려 해결했다.

### 검사

| 검사 | 무엇을 보나 |
|---|---|
| `qa:lobbyfit` | **단위가 실제로 붙었는지**(캔버스 오른끝 ~ 다음 글자 왼끝 = 0px) · 네 줄의 문구와 **순서** · 줄 간격이 전부 같은지 · **잘림 0** · 숫자 높이 21 · 캐릭터 칸/그림/이름 간격 · 320px 접힘 |
| `qa:multi` 44번 | 네 줄이 같은 함수·같은 옵션인지 · `statNum` 에 `outline` 이 **없는지** · 여백 상쇄 코드 · 통계 줄이 flex 가 아닌지 · 미디어쿼리 · 새 문구 |

그 밖: `build` · `qa:multi` · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` · `qa:bgfit` ·
`qa:hoffit` · `qa:hofvert` · `qa:pwa` · `_lint-services` · `_screens-qa`(오류 0) ·
`sim:multi` · 소크 900판 · `perf:frame` · `perf:boot`(첫 화면 388ms).

낡은 검사 4건(라벨/단위 분리 문구 · 캐릭터 칸 66px 두 곳)은 **이번에 의도적으로 되돌린
것**이라 같이 갱신하고 그 사실을 주석에 남겼다.

> 이번 회차의 교훈: **보이지 않는 것이 자리를 먹는다.** 사용자는 "띄어쓰기한 것 같다"
> 고 했고 실제로 띄어쓰기는 없었다 — 벌어진 것은 `gap` 4px 과 **투명한 외곽선 여백 3px**
> 이었다. 그리고 **잘림은 넘침으로 안 잡힌다**: `flex` 가 줄어들고 `ellipsis` 가 지우면
> 검사는 끝까지 0을 보고한다. 자로 잴 때는 **무엇이 잘리는지도** 같이 재야 한다.

---

## 9-0-49. **"접속 중"은 연결이 아니라 활동이다** · 오늘 순위 · 엘리베이터 잠금 (2026-08-19 19차)

사용자 요청 다섯 건. 그중 하나는 *"조사해서 나한테 이 부분은 이렇게 만들어야 합니다
라고 제안을 먼저해"* 였다 — 그래서 먼저 조사하고, 그 결론대로 고쳤다.

### ★ ① 현재접속자 — 아무도 없는데 5명이 접속 중이던 이유

*"게임을 켜둔채로 화면을 내리면, 30초 지나면 아웃되어야 하는거 아냐? (…) 핸드폰도
안하고 있는 사람들이 접속중이라고 나오고, 대결신청도 보내지는것은 문제가 심각한듯"*

**우리가 잰 것은 "소켓이 살아 있는가" 하나뿐이었다.** `presence/$uid` 는 붙을 때 한 번
쓰고, 끊기면 `onDisconnect` 가 지운다. 그런데 그 둘 사이에 **"사람이 있는가"는 어디에도
없다.** 탭을 열어 둔 채 폰을 주머니에 넣으면 소켓은 멀쩡하고 서버는 알 방법이 없다.

#### 다른 게임·서비스는 어떻게 하나 (조사 결과)

예외 없이 **신호를 둘로 나눈다.**

| | 무엇을 재나 | 어떻게 |
|---|---|---|
| 연결성(connectivity) | 소켓이 살아 있나 | `onDisconnect`, ping/pong |
| **활동성(activity)** | **사람이 실제로 거기 있나** | **주기적 `lastActive` + 만료(TTL)** |

- **Zulip**(오픈소스, 실서비스)은 아예 컬럼을 둘로 나눠 둔다 — `last_connected_time`(연결)과
  `last_active_time`(활동). 값은 ping 60초 / 오프라인 판정 200초이고, 그 비율의 근거를
  코드 주석에 적어 뒀다: *"왕복 2번 + 한 번 실패해도 되게"*.
- **Xbox Live** 는 `Away` 를 1급 상태로 둔다 — *"로그인은 되어 있지만 어떤 타이틀에서도
  활동 중이 아님"*. 연결과 활동을 개념부터 갈라 놓은 것이다.
- **Pusher** 는 ping 60초 / pong 15초라 최대 75초, **PubNub** 은 기본 320초에
  heartbeat 를 `timeout/2 - 1` 로 권한다, **Ably** 는 비정상 종료 감지에 최대 30초.
- **Firebase 는 서버가 죽은 소켓을 알아채는 데 걸리는 시간을 공개하지 않는다.**
  게다가 SDK 소스에 **`"Window isn't visible. Delaying reconnect."` → 재연결을 최대
  5분 미룬다**고 박혀 있다. 즉 `onDisconnect` 는 **빠른 길일 뿐 정확성의 근거가 아니다.**

#### 그래서 이렇게 고쳤다

```
lastActive  = 서버 타임스탬프          ← 폰 시계를 믿지 않는다
심장 박동   = 15초마다, **활동이 있을 때만**
접속 판정   = now - lastActive < 60초  ← 사용자 지정
다시 거르기 = 5초마다
```

| 값 | 왜 |
|---|---|
| 60초 | 사용자 지정 (*"60초 동안 (…) 아무런 행동도 하지 않으면, 그 사용자는 나간 사용자"*) |
| 15초 | 60 = 15×4. **연속 3번 놓쳐도** 안 잘린다. Zulip 3.33배, PubNub 2배와 같은 계열 |
| 5초 | **오래됨은 이벤트를 만들지 않는다** — 아무도 쓰지 않으면 RTDB 콜백이 안 오므로 목록이 그대로 멈춘다. 이 타이머가 없으면 `lastActive` 를 넣어도 유령이 그대로 남는다 |

네 가지를 같이 넣었다.

1. **활동이 없으면 아예 안 쓴다.** 자리를 비운 사람은 트래픽이 0이 되고, 60초 뒤
   저절로 목록에서 빠진다 — 지우러 갈 사람도, 서버 잡도 필요 없다.
2. **화면을 내리면 스스로 빠진다**(`visibilitychange` → `hidden`). `beforeunload`·`unload`
   는 **안 쓴다** — 크롬 문서가 "쓰지 말라"고 못 박았고, 모바일에서 앱을 전환한 뒤
   브라우저를 종료하면 셋 다 안 불린다(MDN). 믿을 수 있는 종료 신호는 `hidden` 하나뿐이다.
3. **거르는 일은 읽는 쪽이 한다.** 데이터는 이미 모두에게 내려와 있어 서버를 한 번 더
   거칠 이유가 없다(추가 쓰기 0). 60초 판정을 서버 잡으로 두면 그 주기만큼 지연이 더 붙는다.
4. **`readOne` 도 같은 잣대로 본다.** 목록만 거르면 *"목록에는 없는데 대결신청은
   보내지는"* 상태가 된다 — 사용자가 신고한 바로 그것이다.

**활동의 정의는 DOM 이 아니라 게임에 둔다**(`core/activity.js`). 게임패드는 DOM 이벤트를
아예 만들지 않고, 캔버스 안에서만 처리하는 입력도 있다. 그래서 `core/input.js` 의
`push()` — 터치·키보드·게임패드가 모두 모이는 그 한 곳 — 에서 찍는다.
`mousemove` 는 **일부러 뺐다**: 마우스가 살짝 흔들리는 것만으로 "사람이 있다"고 치면
자리를 비운 PC 사용자가 영원히 남는다.

> **예외 하나** — 판·대기방에 앉아 있으면 60초를 안 세고 계속 살아 있는 것으로 본다
> (*"게임로비에 있거나, 등등 게임중이면, 상대방이 현재 게임중입니다 라고 뜨게"*).
> 상대 차례를 보거나 시작을 기다리는 사람은 60초 동안 아무것도 안 누를 수 있다.
> 안전한 이유는 **화면이 보이는 동안만** 인정하기 때문이다 — 폰을 잠그면 그 즉시
> `hidden` 이 오고 우리는 스스로 빠진다.

규칙에 `lastActive` 를 더하고 **`newData.val() == now` 로 서버 시각만 통과**시켰다.
클라이언트가 미래 시각을 넣을 수 있으면 영원히 접속 중으로 남을 수 있다.
게시하고 되읽어 대조했다(8,127자 / djb2 2725051820, 게시 전 8,077 / 1938703942).

### ② 명예의 전당 — 연간을 빼고 **오늘**을 넣었다

*"오늘 순위가 있으면 더 재밌을 듯 오늘 순위를 올리기 위해서 매일 할 수 있을 듯"*

탭이 **신발왕 / 오늘 / 주간 / 월간 / 역대** 다섯이 됐다. 1년짜리 판은 사실상 역대와
같아서 볼 이유가 적었다. 이미 올라간 `yr` 문서는 **지우지 않았다** — 조회하는 화면이
없어졌을 뿐이고, 규칙상 `delete` 는 아예 막혀 있다.

### ★ ③ 주간 순위가 안 바뀌던 진짜 이유 — 기간 키가 **기기 시간대**였다

*"1주일이 지났는데 주간 순위가 반영이 안되고 있는 것 같아"*

`weekKey()` 는 `d.getFullYear()`·`getMonth()`·`getDate()` 로 **로컬 날짜**를 읽었다.
실측하면 이렇게 나온다:

```
2026-08-17T00:00:00+09:00  (월요일 0시 KST)  →  2026-W33   ← 아직 지난주다
```

UTC 기기에서는 그 순간이 일요일 15시이기 때문이다. 같은 순간에 올린 두 기록이
**서로 다른 순위표로 흩어진다.** 해외 접속이나 폰 시간대 변경도 같은 결과를 만든다.

사용자 지정대로 **전부 KST(UTC+9) 고정**으로 바꿨다 — 시각을 +9시간 옮긴 뒤 UTC 부품을
읽으면 어느 기기에서 계산해도 답이 같다.

| | 리셋 시점 | 키 |
|---|---|---|
| 오늘 | 매일 KST 자정 (= 23:59 까지 오늘) | `2026-08-20` |
| 주간 | **일요일 23:59** → 월요일 0시부터 새 주 (ISO 주는 월요일 시작이라 그대로 맞는다) | `2026-W34` |
| 월간 | 말일 23:59 | `2026-08` |

Firestore 규칙의 `periodOk()` 에 `dy` 를 더해 게시했고(2,102자 / djb2 2631878241),
**색인도 만들었다** — `scores`: `difficulty↑ dy↑ stairs↓` (`CICAgJjmiJEK`).
색인이 없으면 오늘 탭이 통째로 빈 채로 뜬다.

### ④ 엘리베이터 — 스위치 하나로 잠갔다

*"이 엘리베이터 기능 때문에 순위가 엉망이 되는거 같아서 (…) 나중에 (…) 다시
사용하자 라고 말하면 다시 엘리베이터 버튼을 보이게 해줘"*

`ELEVATOR.enabled = false` 한 줄이다. **기능은 통째로 남겼다** — 지우면 되살릴 때 다시
만들어야 하고, 그 사이 다른 코드가 바뀌어 되살리기가 더 어려워진다.
버튼만 숨기는 게 아니라 **`startGame` 의 시작 경로도 막았다**: 화면만 감추면 옛 링크나
자동화가 그대로 뚫는다.

### ⑤ 유저상태창 — 입력칸이 창 안으로

*"어차피 유저 아이디를 눌렀다는 것은, 메세지 보내려고 누른건데"* — 맞는 말이다.
예전에는 `메세지 보내기` → 창을 닫고 → 입력 팝업을 새로 여는 **두 단계**였다.

```
[ 보낼 메세지를 입력하세요        ]
[      메세지 보내기 (빨강, 한 줄)      ]
[ 1:1대결신청 ]  [ 닫기 ]
```

입력칸은 받은 쪽지 팝업과 **같은 부품**(`replyInput`)이다 — 엔터로도 보내지고, 한글
조합 중 엔터는 무시한다. 쓸 자리가 없어진 `openComposer` 는 **함수째 지웠다**:
안 쓰이는 채로 남겨 두면 다음 사람이 되살린다.

### ⑥ (이어서) 커서가 입력칸 안에 없었다 — **부품을 합치며 한 줄이 딸려오지 않았다**

*"왜 커서가 '보낼 메세지를 입력하세요' 라고 써 있는 text입력창 안에 없어?"*

⑤ 를 만들면서 옛 팝업(`openComposer`)의 **`setTimeout(() => input.focus(), 30)` 한 줄이
같이 넘어오지 않았다.** 창은 멀쩡히 뜨고 글자도 잘 보내지므로 **소스만 봐서는 아무
문제가 없어 보인다** — 창을 열고 `document.activeElement` 를 봐야만 드러난다.

고치면서 같은 일이 다시 일어나지 않게 두 가지를 했다.

1. **커서를 넣는 방법을 부품 한 곳에만 둔다**(`replyInput().focus()`). 받은 쪽지 팝업도
   14차부터 같은 일을 자기만의 `setTimeout` 으로 하고 있었다 — 두 곳이 각자 방법을
   가지면 언젠가 한쪽만 고쳐진다. 실제로 그랬다. 그쪽도 이 부품을 쓰게 바꿨다.
2. **자동으로 걸지 않고 부르는 쪽이 부른다.** 이 부품은 자기가 언제 화면에 붙는지
   모르고, **아직 안 붙은 노드에 `focus()` 를 걸면 조용히 아무 일도 안 일어난다.**
   그래서 `presentOverlay` 직후에 부른다.

**동기 호출 + 다음 프레임 재시도** 두 번을 건다. 첫 번째가 동기여야 하는 이유는
아이폰이 **탭 처리와 같은 작업 안**에서 불러야 키보드를 올려 주기 때문이다
(전체화면 요청이 같은 이유로 클릭 핸들러 안에 있다 — §9-0-2). 두 번째는 그 사이
메뉴 커서(`menuNav`)가 첫 버튼을 잡아가는 경우를 위한 보험인데, 그쪽은 입력 중이면
(`typing()`) 물러나므로 **먼저 잡아 두면 뺏기지 않는다.**

검사는 **브라우저에게 묻는다** — `npm run qa:cardfocus` 신설. 창을 연 뒤
`document.activeElement` 가 그 칸인지, 그리고 **아무 키나 쳤을 때 그 글자가 정말 그 칸에
들어가는지**까지 본다. 넣기 전에 **고친 코드를 도로 빼서 실제로 두 줄이 실패하는 것을
확인**하고 넣었다.

### 검사

| 검사 | 무엇을 보나 |
|---|---|
| `qa:rank` | KST 경계 — 일요일 23:59 는 지난주 · 월요일 0시는 새 주 · 오늘 키가 자정에 바뀐다. **날짜를 `+09:00` 문자열로 못 박아** 검사가 돌리는 기계의 시간대에 안 흔들리게 했다 |
| `qa:rules` | `lastActive` 가 서버 시각만 받는지 · 클라이언트가 실제로 그 필드를 쓰는지 |
| `qa:multi` 45번 | 탭 다섯의 순서 · 기간 셋(`dy`·`wk`·`mo`) · 엘리베이터 두 겹 잠금 · 60/15/5초 · `beforeunload` 를 안 쓰는지 · `readOne` 도 거르는지 · 유저상태창 구성 · **커서 넣는 방법이 한 곳뿐인지** |
| **`qa:cardfocus`** (신규) | 창을 열면 커서가 입력칸에 있는지 · 그대로 친 글자가 그 칸에 들어가는지 · 순서(입력칸→빨간 보내기→버튼 줄) · 아래 줄이 `[1:1대결신청][닫기]` 이고 색이 없는지 |

그 밖: `build` · `qa:wallet` · `qa:bg` · `qa:bgfit` · `qa:hoffit` · `qa:hofvert` ·
`qa:lobbyfit` · `qa:pwa` · `_lint-services` · `_screens-qa`(오류 0) · `sim:multi` ·
소크 900판 · `perf:frame` · `perf:boot`(첫 화면 357ms) · `preview:hof`(유저상태창 눈으로 확인).

> 이번 회차의 교훈: **무엇을 재고 있는지 물어야 한다.** 접속자 목록은 처음부터
> "연결"을 재고 있었는데 사용자가 원한 건 "사람이 있는가"였다. 코드에 버그가 있던 게
> 아니라 **측정 대상이 틀렸다** — 그래서 아무리 `onDisconnect` 를 손봐도 안 고쳐졌을 것이다.

## 9-0-50. **차단은 눈에 보여야 한다** — 받은 메세지함 배지 · 수신 설정 두 버튼 (2026-08-19 21차)

사용자 요청 네 건. 전부 **"지금 내가 어떤 상태인지"를 화면이 먼저 말해 주는가**에 대한 것이다.
기능은 12·14차에 이미 다 있었다 — 차단도 되고 수신 거부도 됐다. 다만 **그 상태가 어디에도
안 보였다.** 그래서 사용자는 자기가 무엇을 눌러 뒀는지 알 방법이 없었다.

### ① 받은 메세지함 — 차단한 사람 줄에 빨간 [차단] 배지

*"내가 실수로 차단을 누른 사용자가 누구인지 한눈에 알 수 있도록"*

이름 오른쪽에 **빨간 바탕(#B23A2E) + 흰 글씨(#FFF4D6)** 로 `차단`. 판정은 이미 화면이 들고
있는 `prefs.blocked` 하나뿐이라 조회가 늘지 않는다.

**색까지 검사에 박았다.** §9-0-37 에서 방 목록의 보유신발 배지가 정확히 이 자리에서 한 번
데었다 — 배지는 떴는데 **흰 글씨를 크림색 바탕에 얹어** 사실상 안 보였다. 배지는 "떴다"가
아니라 "읽힌다"가 통과 조건이라, `qa:msg` 가 브라우저에서 **계산된 색**을 읽어 대조한다.

### ② 메세지 수신 설정 — 켜짐/꺼짐 → **[수신차단] [수신허용]**

토글 두 개는 **누르기 전에는 지금이 어느 쪽인지 알 수 없다.** '켜짐'이 지금 상태인지
누르면 켜진다는 뜻인지 화면만 봐서는 갈리지 않는다. 두 버튼 중 하나가 켜져 있는 모양이면
그 모호함이 사라진다. 순서는 사용자 지정 그대로 `[수신차단] [수신허용]`.

### ③ 버튼 위 `현재상태 : 수신허용` 한 줄

버튼 색만으로 상태를 말하면 색을 못 가리는 사람에게는 아무 정보도 없다. 글자로 한 번 더
말한다. 차단이면 이 줄도 빨간 글씨가 된다(`.msg-accept-now.off`).

**값을 아직 못 받았으면 단정하지 않는다** — `현재상태 : 수신허용` 이라고 써 놓고 잠시 뒤
뒤집히는 쪽이 제일 나쁘다(§9-0-6 의 그 거짓말). 그동안은 `불러오는 중…` 이다.

아래 안내는 **상태와 무관하게 늘 같은 문장**이다: `수신차단 버튼을 누르면, 메세지 받기,
대결신청 등의 기능이 모두 중지됩니다.` 누르기 **전에** 무엇이 멈추는지 알아야 하기 때문에
상태에 따라 문장을 바꾸면 안 된다.

### ④ 수신차단 상태면 받은 메세지함 맨 위에 빨간 경고

*"아, 내가 수신거부 눌러서 아무런 메세지도 받을 수 없구나 라는 것을 한 눈에 알 수 있도록"*

이 화면이 그 자리인 이유가 있다 — 차단해 둔 사람이 "왜 아무 쪽지도 안 오지?" 하고
들여다보는 곳이 **바로 여기**다. 그때 빈 목록만 보이면 그건 고장으로 읽힌다.

**허용 상태에서는 아무것도 안 띄운다**(`prefs.accept === false` 일 때만). 늘 뜨는 안내는
곧 안 읽히는 안내다. 그리고 값을 아직 못 받았을 때(`accept` 초기값 `true`)도 안 띄운다 —
없는 상태를 있다고 말하는 쪽이 훨씬 나쁘다.

### 검사 — **진짜 화면**을 띄워서 본다

`npm run qa:msg` 를 새로 만들었다. 마크업을 손으로 베낀 미리보기는 언젠가 거짓말을 한다
(§9-0-33 에서 결과 화면 미리보기가 실제로 그랬다). 그래서 import map 으로 **`presence` 하나만
대역**으로 갈아 끼우고(`tools/_presence-stub.js` — 접속·쪽지·차단 목록을 쿼리스트링으로 준다)
**진짜 `Inbox` · `MessageSettings` 를 그대로 마운트**한다.

확인하는 것: 배지가 **차단한 그 한 사람에게만** 붙는지 · 그 색이 실제로 빨강/흰색인지 ·
수신허용이면 경고가 **없는지** · 수신차단이면 경고가 **목록보다 위에** 뜨는지 ·
버튼 순서가 `["수신차단","수신허용"]` 인지 · 현재상태 줄과 안내 문구의 **순서**가
현재상태 → 버튼 → 안내인지 · 콘솔 오류 0.

`qa:multi` **46번 묶음**이 같은 것을 소스에서 못 박는다. 넣기 전에 **배지와 경고를 도로 빼서
두 검사가 실제로 실패하는 것을 먼저 확인**했다(§9-0-45 의 순서 그대로).

낡은 검사 하나를 같이 고쳤다 — `_screens-qa` 가 아직 `켜짐`/`꺼짐` 버튼을 세고 있었다.
문구가 바뀌었으므로 그 검사는 화면이 멀쩡해도 **영원히 0/0** 을 보고한다. 이제 새 이름으로
세고 현재상태 줄·안내 문구까지 함께 찍는다(1/1 확인).

### 검사 전량

`build` · `qa:multi`(46묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` · `qa:bgfit` ·
`qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · **`qa:msg`(신규)** · `qa:pwa` ·
`_lint-services` · `_screens-qa`(오류 0) · `sim:multi` + 소크 300판 ·
`perf:frame`(전부 60fps 예산 안) · `perf:boot`(첫 화면 **371ms**).

> 이번 회차의 교훈: **기능이 있는 것과 상태가 보이는 것은 다른 일이다.** 차단도 수신 거부도
> 진작 동작하고 있었는데, 사용자는 자기가 무엇을 눌러 뒀는지 알 수 없어서 "안 된다"고 느꼈다.
> 상태를 바꾸는 기능을 만들 때는 **그 상태를 되읽는 화면**을 같이 만들어야 한다.

---

## 9-0-51. 받은 메세지함 탭 셋 · **받음은 파랑 보냄은 빨강** · 도감완성 배지 (2026-08-19 22차)

사용자 요청 세 갈래. 화면 개편 둘과 로비 문구 하나다.

### ① 타이틀 아래 `현재상태 : 수신허용` — **수신 설정 화면과 같은 부품**

받은 메세지함에도 지금 상태를 띄운다. `.msg-accept-now` 와 `S.msgAcceptNow` 를 그대로
쓴다 — 두 화면이 같은 상태를 **각자 만든 문구**로 말하면 어느 쪽이 진짜인지 알 수 없다.
아직 못 읽었으면 단정하지 않고 `불러오는 중…` 이다(§9-0-6 의 그 거짓말).

수신차단이면 그 아래에 21차의 빨간 경고가 그대로 뜬다. 한 줄은 **상태**를, 상자는
**그래서 무슨 일이 일어나는지**를 말하므로 겹치지 않는다.

### ② 탭 셋 `[전체보기][받은 메세지][보낸 메세지]` — 기본은 전체보기

**서버를 다시 부르지 않는다.** 구독은 하나뿐이고 목록은 이미 손에 있다 — 탭마다 새로
받아 오면 같은 데이터를 세 번 받고, 탭을 옮길 때마다 화면이 비었다가 채워진다.

빈 목록 문구도 탭마다 다르다(`inboxEmptyIn`·`inboxEmptyOut`). '받은 메세지' 탭을 보고
있는데 화면이 `주고받은 메세지가 없습니다` 라고 하면 **보낸 것까지 없다는 말로 읽힌다.**

### ③ 받음은 파랑, 보냄은 빨강 — **테두리와 앞머리 글자에 같은 색**

색 네 개를 변수로 뒀다(`--c-msg-in`·`--c-msg-in-text`·`--c-msg-out`·`--c-msg-out-text`).
테두리용과 글자용을 **짝으로** 두는 이유: 검은 배경(#0d0a08) 위에서 테두리용 진한 색을
그대로 글자에 쓰면 안 읽히고, 반대로 밝은 글자색을 테두리에 쓰면 눈이 아프다.

두 가지를 같이 손봤다. 둘 다 **색이 방향을 뜻하게 되면서 자리를 비켜 준 것**이다:

- **안 읽음 표시가 테두리를 쓰고 있었다**(`border-color: 강조색`). 이제 테두리는 방향의
  것이므로 안 읽음은 **바탕을 옅게 깔고 앞머리에 밑줄**로 옮겼다. 보낸 쪽지에는 애초에
  `unread` 가 안 붙으니(내가 쓴 것이다) 파란 바탕으로 충분하다.
- **보낸 줄을 `opacity: 0.78` 로 흐리게** 하고 있었다 — 빨강이 죽는다. 뺐다.

앞머리 `받음`/`보냄` 은 10 → **11px 굵게**. 목록을 훑을 때 가장 먼저 읽히는 두 글자다.

### ★ ④ 로비 도감 — 분모를 빼고, 다 모으면 **배지**

*"'신발도감 0/130켤레' 를 '신발도감 0켤레' 로 (…) 총 130켤레라는 것은 굳이 안 써도 될
것 같아, 그리고 도감 완성하면 '신발도감 130켤레, 도감완성' 으로"*

분모를 빼면 **숫자만 봐서는 끝인지 알 수 없다** — 그래서 끝났다는 사실은 글자로
말해야 한다. 그런데 문장으로 이어 붙였더니 **360px 폰에서 16px 잘렸다.**

검사가 먼저 잡았다. `qa:lobbyfit` 에 **도감을 130종 채운 상태**를 한 번 더 재는 단계를
넣어 뒀는데(87종만 재면 완성 문구의 폭을 영영 못 본다), 거기서 바로 걸렸다:

```
360px  도감완성 → "신발도감 130켤레, 도감완성"  잘림=16px  ❌
```

**잘림은 넘침으로 안 잡힌다**(§9-0-48). `.stats` 는 `flex: 1 1 auto` 라 자리가 모자라면
패널을 넘치는 대신 줄어들고 `ellipsis` 가 말없이 지운다 — 패널 넘침은 끝까지 0이었다.

그래서 문장 대신 **작은 초록 배지**(`.stat-done`)로 뺐다. 자리를 덜 먹고, 무엇보다
*"한번에 알 수 있도록"* 이라는 요청에는 색이 글자보다 빨리 답한다. 여백·글자 크기도
눈이 아니라 자로 정했다 — 10px/여백 5·4 로는 아직 4px 이 잘려서 9px/여백 4·3 으로 내렸다.
초록은 이 화면에서 **여기에만** 쓴다. 다른 곳과 겹치지 않아야 "다 모았다"의 신호로 굳는다.

| 폭 | 87종 | 130종(완성) |
|---|---|---|
| 320 · 360 · 390 · 412 | 잘림 0 | **잘림 0** (고치기 전 360px 에서 16px) |

### 검사

`qa:msg` 에 **⑤묶음**을 더했다 — 현재상태 줄이 타이틀 **바로 다음**인지, 탭 셋의 문구와
기본값, 그리고 **탭을 실제로 눌러** 받은 것만/보낸 것만 남는지. 색은 클래스 이름이 아니라
**계산된 값**으로 본다(`rgb(143,192,255)` · `rgb(76,141,224)` · `rgb(255,154,138)` ·
`rgb(198,64,46)`) — CSS 를 옮기다 규칙이 죽어도 클래스는 그대로 붙어 있다(§9-0-33).

`qa:multi` **47번 묶음**이 같은 것을 소스에서 못 박고, 특히 **안 읽음이 테두리를 도로
가져가지 않는지** · **보냄이 다시 흐려지지 않는지** 를 본다. 넣기 전에 기본 탭과 파란
테두리를 도로 바꿔 두 검사가 실제로 실패하는 것을 확인했다.

낡은 검사 셋을 같이 갱신했다(통계 줄 옵션 세는 정규식 · `statLine` 서명 · 경고 위치) —
전부 이번에 코드가 옮겨 간 것을 검사가 못 따라온 경우다.

전량 통과: `build` · `qa:multi`(47묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` ·
`qa:bgfit` · `qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · `qa:msg` ·
`qa:pwa` · `_lint-services` · `_screens-qa`(오류 0) · `sim:multi` + 소크 300판 ·
`perf:frame`(전부 60fps 예산 안) · `perf:boot`(첫 화면 **361ms**).

> 이번 회차의 교훈: **문구를 바꾸면 폭도 바뀐다.** `, 도감완성` 여섯 글자가 360px 폰에서
> 줄을 잘라 먹었는데, 그 상태는 **도감을 다 모은 사람에게만** 나타나므로 평소 검사로는
> 영영 안 보인다. 조건에 따라 길어지는 문구는 **그 조건까지 만들어서** 재야 한다.

---

## 9-0-52. **왕관을 걸고 경쟁시킨다** — 멀티게임순위 신설 · 로비 딱지 · 9px 미니 글꼴 (2026-08-19 23차)

사용자 요청 한 묶음. *"딱지 붙이고 싶은 사람은 경쟁하게끔 만들어"* 가 이번 회차의 한 줄 요약이다.
화면 세 곳(명예의 전당·멀티 메뉴·로비)과 새 화면 하나, 그리고 **새 글꼴 한 벌**이 걸렸다.

### ① 신발왕 승패 줄 — 머리말을 떼고, 이름 옆으로

`멀티승률 0승/0게임` → **`0승/총0멀티게임`**, 9 → **11px**, 그리고 오른쪽 고정폭에서
**이름 바로 옆 왼쪽 정렬**로 옮겼다(사용자 지정: *"아이디가 어차피 5글자만 허용되니깐"*).
이름 칸을 5글자 고정폭으로 묶고 승패 칸이 남는 폭을 가져간다 — 예전에는 반대라
짧은 이름일수록 승률이 저 멀리 떨어져 보였다.

**360px 폰에서는 한 줄에 안 들어간다.** 최악의 문자열(`128승/총190멀티게임`) 기준으로
391px 이 필요한데, 글자를 도로 줄이는 건 방금 키우라고 한 것을 되돌리는 셈이고 잘라
버리면 승수가 안 보인다. 그래서 **380px 이하에서는 승패 줄만 아랫줄로 내린다**
(값 칸은 윗줄에 그대로 — `order` 로 이 칸만 맨 뒤로 보낸다).

### ★ ② 등수는 `N위`, 1·2·3위에는 금·은·동 왕관

다섯 탭 전부, 그리고 **하단 '내 순위' 줄에도** 붙는다(같은 `row()` 를 지나므로 저절로).

왕관은 `tools/build-crowns.mjs` 가 굽는다. 손으로 세 벌을 그리지 않은 이유: 셋은
**모양이 같고 색만 달라야** 하는데, 손으로 그리면 도트가 미묘하게 어긋나고 한 벌을
고치면 나머지를 잊는다. 실루엣 도면 한 장에 팔레트 셋을 곱하고, **명암도 규칙으로 칠한다**
(줄 맨 왼쪽=빛, 맨 오른쪽·맨 아래=그늘, 뿔 끝과 띠 가운데=보석). 그림자는 흐리게
번지는 대신 **실루엣을 한 도트 아래·오른쪽으로 옮겨** 어두운 색으로 깐다 — 도트에서
입체를 내는 정석이고 밝은 배경에서도 왕관이 떠 보인다.

**두 가지가 검사에서 잡혔다.**

| 증상 | 원인 |
|---|---|
| 왕관이 전부 **깨진 그림**으로 떴다 | `src` 가 상대 경로였다. 실제 앱은 뿌리에서 열려 괜찮지만 `/tools/…` 미리보기에서는 404 다. 그래서 `qa:mrank` 는 클래스가 아니라 **`naturalWidth`** 로 "그림이 실제로 떴는지"를 본다 |
| 1·2·3위만 **등수 숫자가 옆으로 밀렸다** | 왕관이 붙은 줄만 칸이 넓어졌다. 왕관이 없어도 **자리는 남긴다**(`.crown-slot`) — `qa:hoffit` 이 등수 숫자의 왼쪽 끝이 줄마다 같은지 잰다 |

### ③ 멀티게임순위 (신설) — 승리왕 / 승률왕 / 오늘 / 주간 / 월간

멀티 메뉴 넷 아래, 현재접속자 위에 **[멀티게임순위]** 버튼을 뒀다. 이 화면에서 혼자만
붉은 버튼에 양쪽 금관이다 — 나머지 넷은 "지금 한 판 하러 가는" 버튼이고 이건 "누가
잘하나 보러 가는" 버튼이라 하는 일이 다르다. 신발이 부족해도 막지 않는다(구경은
판돈과 무관하다).

**명예의 전당에 탭을 더하지 않은 이유**: 한 화면에 계단 순위와 승리 순위를 섞으면
같은 `역대` 탭이 무엇의 역대인지 흐려지고, 이 화면의 1·2·3위는 **로비 딱지**가 되므로
어디를 보고 딱지를 받았는지가 분명해야 한다.

**승률왕만 규칙이 하나 더 있다** — 10판 미만은 목록에 아예 안 나오고, 맨 위에
`승률왕은 최소 멀티게임을 10판 이상 유저만 측정합니다` 를 먼저 띄운다. 없으면
**1승 0패가 영원히 100% 로 1위**가 된다. 거르는 자리는 화면이 아니라 **계정 문서**다 —
10판을 넘겨야 `winRate` 필드가 생기고, 없는 필드는 색인에 안 실린다(§9-0-5 의 그 수법).

### ★ 복합 색인을 **하나도 안 만들었다**

오늘·주간·월간은 "그 기간의 승수 순"이다. 그대로 짜면 `where(기간키) + orderBy(승수)` 라
Firestore 복합 색인 3개가 필요하고, **하나라도 콘솔에서 빠지면 그 탭이 빈 채로 뜬다**
(원인이 화면에 안 보인다 — §9-0-6 에서 며칠을 잃은 그 모양이다).

그래서 키와 승수를 **한 필드에** 담았다:

```
mwDy = "2026-08-20#9995"    ← 9999 - 4승. 오름차순 정렬이 곧 '많이 이긴 순'
```

같은 필드로 범위 조회 + 정렬이므로 Firestore 가 **모든 필드에 자동으로 만들어 두는
단일 색인**만으로 돈다. 콘솔에서 할 일이 없다는 뜻이다. 자릿수를 네 자리로 채우는 게
중요하다 — 안 채우면 문자열 비교가 숫자 비교와 달라진다(`999` < `99`).

기간 키는 `currentKeyMap()` 이 **이름표를 붙여** 준다. 배열 순서로 짝을 맞추면
`PERIODS` 에 한 줄만 끼워 넣어도 엉뚱한 기간에 승수가 쌓이는데, **그 버그는 다음 주가
되어서야 드러난다.**

### ④ 로비 딱지 — 보유신발에 [신발왕], 멀티게임에 [승리왕]

2·3위면 `신발2위`·`승리3위`. **최고기록에는 딱지가 없고**, 오늘·주간·승률 같은 다른
탭도 딱지를 만들지 않는다(사용자 지정) — 딱지가 흔해지면 아무도 안 쳐다본다.

값은 상위 **3장만** 읽는다. 내 순위를 정확히 알 필요가 없다 — 4위부터는 딱지가 없으니
**거기 있느냐 없느냐**만 보면 된다(두 항목 합쳐 문서 6장). 10분간 캐시하고, 로비는
**기다리지 않는다**: 지난 값을 먼저 그리고 달라졌을 때만 다시 그린다. 캐시는
`storageLocal` 의 키 표에 넣어 **계정이 바뀌면 같이 지워진다** — 남의 왕관을 물려받으면 안 된다.

### ★ ⑤ "17~18px" 은 사다리에 없던 칸이었다 — 9px 글꼴을 한 벌 더 구웠다

딱지가 붙으면 자리가 좁아지므로 숫자를 줄여 달라는 요청이었다(21px → 17~18px).
그런데 배율은 정수뿐이고 글꼴이 11px·7px 두 벌이라 만들 수 있는 크기는

```
11 · 14 · 21 · 22 · 28 · 33px
```

여섯 칸이 전부다 — **17~18 이 아예 없다.** 21 바로 아래가 14 라 세 단계가 뚝 떨어진다.
그래서 **갈무리9 를 숫자만 구웠다**(`font9.generated.json`, 797바이트). 9 × 2 = **18px**,
요청한 크기가 정확히 나온다.

작은 게 오히려 이득이 됐다. 7px 글꼴은 비동기라 도착 전에는 11px 로 떨어지므로
"도착 전엔 배율 2, 도착하면 3" 같은 폴백이 필요했는데(로비가 22 → 21 로 한 번 출렁였다),
**이 글꼴은 1KB 미만이라 정적 import 로 들어간다** — 폴백도 미리받기도 통째로 사라졌다.
덤으로 로비가 7px 글꼴(19KB gzip)을 미리 받던 것도 없앴다: 싱글만 하는 사람은 이제
그걸 안 받는다(첫 4초 전송량 235 → **220KB**).

`core/pixelfont.js` 는 폰트 슬롯이 둘에서 셋이 됐다. 글리프 캐시 열쇠에 폰트 구분을
`'L'|'S'|'M'` 으로 넣는 게 중요하다 — 안 그러면 **9px 글리프가 11px 자리에 붙는다.**

### 자로 재서 정한 것들 (눈으로는 못 고른다)

| 무엇 | 실측 | 결정 |
|---|---|---|
| 로비 `멀티게임 17승/50게임 [승리2위]` | 360px 에서 **15px 초과** | 딱지 8px·여백 3/2, 좁은 폭에서 본문 13→12px, 패널 여백 8→6 |
| 명예의 전당 승패 줄 | 391px 필요 | 380px 이하에서 아랫줄로 |
| 승률왕 값 칸 | `총 168게임중 128승으로 76% 승률` | 10px·이름 쪽이 먼저 줄어들게 |

### 검사

- **`qa:mrank` 신설** — 탭 다섯·기본 탭·`N위`·왕관 세 개(그리고 **실제로 그림이 뜨는지**)·
  내 순위 줄의 왕관·승률 문장·규칙 안내 위치·320~412px 넘침/잘림.
- **`qa:hoffit` 을 진짜 화면으로 옮겼다.** 지금까지 손으로 베낀 미리보기를 재고 있었는데,
  왕관·등수 칸이 붙는 이번 변경에서 베낀 마크업은 그 칸을 **아예 모른다** — 그러면 검사는
  없는 화면을 재고 통과한다(§9-0-33 의 그 함정).
- `qa:lobbyfit` 에 **왕관 딱지가 붙은 상태**를 재는 단계를 더했다(22차에 도감완성으로
  한 번 데인 자리다). 딱지 없는 상태만 재면 정작 좁아지는 경우를 못 본다.
- `qa:multi` **48번 묶음**이 소스에서 못 박고, `_screens-qa` 가 [멀티게임순위] 를 실제로 눌러 본다.
  넣기 전에 왕관 경로·딱지·배율을 도로 바꿔 **검사가 실제로 실패하는 것을 먼저 확인**했다.

전량 통과: `build` · `qa:multi`(48묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` ·
`qa:bgfit` · `qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · `qa:msg` ·
**`qa:mrank`(신규)** · `qa:pwa` · `_lint-services` · `_screens-qa`(오류 0) ·
`sim:multi` + 소크 300판 · `perf:frame`(전부 60fps 예산 안) · `perf:boot`(첫 화면 **371ms**).

> 이번 회차의 교훈: **없는 칸을 요청받으면 칸을 만들거나 사실대로 말해야 한다.**
> 17~18px 은 이 저장소의 크기 사다리에 없던 값이었다. 가장 가까운 14px 로 슬쩍
> 내리면 "요청대로 했다"고 말할 수는 있지만 화면은 사용자가 상상한 것과 다르다.
> 글꼴 한 벌(797바이트)을 더 구우니 정확히 그 칸이 생겼고, 덤으로 폴백 코드가 사라졌다.

---

## 9-0-53. **10전 10승이 영원히 1위였다** — 승률왕 잠자기 규칙 · 왕관 간격 · 승패 문구 (2026-08-19 24차)

23차에 만든 것을 사용자가 써 보고 네 가지를 짚었다. 그중 하나는 **순위 시스템 자체가
죽는 구멍**이었다.

### ★ ① 승률왕이 굳어 버리는 문제 (사용자 지적)

*"10게임해서 10판 다 승리하면 100% 승률인데 그러면 그 유저는 항상 1위로 100% 상태에서
늘 고정으로 되고, 절대 안바뀌잖아"*

정확한 지적이다. 판수 하한(10게임)만 두면 **10전 10승은 더 이상 게임을 하지 않는 것이
최적 전략**이 된다 — 한 판 더 하면 승률이 내려갈 수만 있으니까. 순위표가 "잘하는 사람"이
아니라 "일찍 그만둔 사람"을 보여 주게 된다.

사용자가 준 규칙 그대로 고쳤다 — **최근 일주일 안에 한 판이라도 해야 목록에 남는다.**
제명이 아니라 **잠자기**다: 3주를 쉬었어도 한 판 하면 그 즉시 돌아온다. 그게 이 규칙의
목적이다(*"1주일 안에 다시 게임을 하도록 만들 수 있을거 같다"*).

안내 문구도 그 뜻을 그대로 말한다:
`승률왕은 멀티게임을 최소 10게임 이상 진행해야 합니다. 만약 1주일 동안 1게임도 하지 않으면, 승률왕 리스트에서 제외 됩니다.`

**이 조건은 서버 필드로 못 박을 수 없다.** 10게임 하한은 "넘기면 `winRate` 를 쓴다"로
계정 문서에 새길 수 있지만(§9-0-52), *일주일 안 함*은 **시간이 지나면 저절로 성립하지
않게 되는 조건**이다 — 잠든 사람은 앱을 안 열고, 그러면 그 문서를 아무도 안 고친다.
게다가 부등호(`where(lastMultiAt >= …)`)를 걸면 Firestore 가 **그 필드로 먼저 정렬하라**고
요구해서 "승률 순"을 못 뽑는다(§9-0-4 의 그 제약).

그래서 **승률 상위 300장을 받아 읽는 쪽에서 거르고 100명을 남긴다.** 잠든 사람이
300명 넘게 위에 쌓이지 않는 한 순위는 정확하다 — 판정 함수(`matchRules.rateEligible`)는
`firebase.js` 를 안 물게 **순수 모듈**에 뒀다. 그래야 브라우저 없이 규칙을 검사할 수 있다
(§9-0-5 에서 `periodKeys.js` 를 떼어 놓은 것과 같은 이유).

판정의 근거인 `lastMultiAt` 은 **승패를 가리지 않고** 찍는다 — 규칙이 "1게임이라도 진행"이다.

### ② 왕관과 등수 사이가 벌어져 있었다

23차에는 등수 숫자가 칸의 **오른쪽 끝**에 붙어 있었다(`flex: 1 1 auto` + 오른쪽 정렬).
줄마다 숫자의 x 를 맞추려던 것인데, 그 바람에 왕관과 `1위` 사이가 칸 폭만큼 벌어졌다.
이제 숫자를 **왕관 바로 옆**에 왼쪽 정렬로 붙이고 남는 자리는 칸 오른쪽에 남긴다 —
시작 x 는 여전히 줄마다 같다. `qa:mrank` 가 **실제 픽셀 간격**을 재서 3px 이하인지 본다.

### ③ 승률왕 줄 — 근거는 작게, 값은 크게

`총 24게임중 20승으로 83% 승률` 한 문장에서, 어느 숫자가 순위를 정하는 값인지 읽어 봐야
알 수 있었다. 사용자 지정대로 둘로 갈랐다:

| 왼쪽 (근거) | 오른쪽 (값) |
|---|---|
| `24게임중 20승` — 10px DOM 글꼴 | `승률 **83**%` — **비트맵 18px** |

숫자만 로비 통계와 같은 **비트맵 글꼴**로 찍는다. DOM 글꼴과 확연히 달라서 눈이 먼저
간다 — *"98이라는 숫자가 눈에 확 띄게"*. 좁은 폰(≤355px)에서는 **근거만** 아랫줄로
내린다. 순위를 정하는 값은 윗줄에 남는다.

### ④ 신발왕 탭 승패 칸 — `멀티게임 0승`

*"총 게임 숫자는 의미가 없다"* — 맞다. 총 판수가 궁금한 사람은 승률왕 탭으로 가면 된다.
문구가 짧아지면서 23차에 넣었던 **380px 이하 줄바꿈이 필요 없어졌다**(320px 에서만 남겼다).

### 검사 도구가 옛 코드를 보고 있었다 (또)

`qa:mrank` 가 갑자기 "왕관이 없다"로 터졌는데 **코드는 멀쩡했다.** 앞선 실행이 중간에
예외로 죽으면서 `process.kill(-vite.pid)` 가 안 돌아, **죽은 줄 알았던 dev 서버 4개가
포트 5252 를 붙들고 있었다.** 새 vite 는 다른 포트로 밀려나고 검사는 옛 서버를 봤다.

§9-0-46 에서 같은 함정을 한 번 밟고 "그룹째 죽인다"로 고쳤는데, **터졌을 때는 그 코드가
안 돌아간다**는 걸 못 봤다. 이제 `uncaughtException`·`unhandledRejection` 에서도 죽인다.
그리고 고정 시간(500ms) 대기를 **줄이 실제로 생길 때까지 기다리기**로 바꿨다 —
빈 화면을 재고 "기능이 없다"고 보고하는 실패가 제일 비싸다.

### 검사

`qa:multi` **49번 묶음** — 왕관 간격(CSS)·자격 판정 다섯 경우(10게임 미만·8일 쉼·
3주 쉬었다 복귀·기록 없음·정상)·`lastMultiAt` 이 실제로 쌓이는지·부등호 조회를 안 쓰는지.
`qa:mrank` 에 **잠든 사람이 목록에서 빠지는지**(대역의 마지막 한 명을 3주째 잠재워 뒀다)와
승률 숫자가 **비트맵 18px** 인지를 더했다. 넣기 전에 판정식과 CSS 간격을 도로 바꿔
검사가 실제로 실패하는 것을 확인했다.

전량 통과: `build` · `qa:multi`(49묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` ·
`qa:bgfit` · `qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · `qa:msg` ·
`qa:mrank` · `qa:pwa` · `_lint-services` · `_screens-qa`(오류 0) · `sim:multi` + 소크 300판 ·
`perf:frame`(전부 60fps 예산 안) · `perf:boot`(첫 화면 **373ms**).

> 이번 회차의 교훈: **순위 규칙은 "그 규칙 아래에서 최적 행동이 무엇인가"로 검산해야 한다.**
> 10게임 하한만 두면 최적 행동이 *그만두기*가 된다 — 순위표가 경쟁을 만드는 게 아니라
> 경쟁을 끝내 버린다. 사용자가 그걸 먼저 봤다.

---

## 9-0-54. 아이디 5글자 · [멀티게임순위] 를 **진한 노랑**으로 (2026-08-19 25차)

작은 요청 둘. 그런데 둘 다 **왜 그런 값이었는지**가 앞 회차에 적혀 있어서, 되돌리는
근거도 같이 적어 둔다.

### ① 아이디 다섯 글자가 잘리고 있었다

닉네임 상한은 5자다(`NICKNAME.maxLength`). 그런데 명예의 전당 신발왕 줄의 이름 칸이
**62px 고정**이었다 — 13px 한글 다섯 글자는 실측 **65px** 이라 마지막 글자가 `…` 로
잘렸다(`다섯글자…`). 23차에 "5글자 고정폭"이라고 적어 두고 **실제 폭을 안 재고** 62를
골랐던 것이다.

66px 로 올리고, 같은 종류가 더 있는지 세 화면을 전부 다시 쟀다:

| 화면 | 값 | 결과 |
|---|---|---|
| 명예의 전당 신발왕 | 62 → **66px** | 잘림 0 |
| 대기방 참가자 표 | `5.2em` → **`5.6em`** | 원래도 안 잘렸지만 `em` 이 컨테이너 글자 기준이라 67.6~72.8px 사이에서 흔들린다 — 14px 다섯 글자(70px) 아래로 내려갈 조합이 있어 못 박았다 |
| 현재접속자 | `min-width: 24px` → **66px** | 다섯 글자까지는 절대 안 줄어든다 |

**검사에 "잘림"을 넣었다.** 지금까지 `qa:hoffit` 은 이름 칸의 **폭만** 찍고 있었다 —
`다섯글자…` 로 잘려도 폭은 62px 로 멀쩡하니 통과였다. 이제 세 도구가 전부
`scrollWidth - clientWidth` 를 잰다(`qa:hoffit` · `qa:mrank` · `_fitcheck`).
넣기 전에 62px 로 되돌려 **6줄이 잘리는 것**을 먼저 확인했다.

### ② [멀티게임순위] 버튼 — 빨강은 이미 [방 입장] 의 색이었다

*"빨간색은 [방 입장] 메뉴인데 (…) 이 부분도 빨강이니깐 디자인 적으로 별로인거 같아"*

23차에 "혼자만 다른 버튼"을 만들면서 게이지 채움색(`--btn-face-on`, 붉은색)을 썼다.
그런데 **그 색은 같은 화면에서 [방 입장]이 이미 쓰고 있었다**(`primary`). 한 화면에
붉은 버튼이 둘이면 "지금 할 일"이 어느 쪽인지 흐려진다 — 강조는 **하나일 때만** 강조다.

크림색(`--btn-face` #FFDD8E)보다 한 단계 진한 **금색 #F0AE2B** 으로 바꿨다. 왕관 그림의
바탕색과 같은 값이라 버튼과 아이콘이 한 덩어리로 읽힌다. 밝은 금색 위에서는 흰 글씨가
뜨므로 글자는 어두운 갈색(#3A2408)이고, 도트 버튼의 구조(2도트 외곽선 + 위 하이라이트 +
아래 그늘)는 그대로다.

### 곁가지 — 검사 도구가 **끄는 순서를 거꾸로** 갖고 있었다

`_fitcheck.mjs` 의 마지막 두 줄이 이랬다:

```js
process.exit(ok ? 0 : 1);
await b.close(); (() => { try { process.kill(-vite.pid); } catch { vite.kill(); } })();
```

**종료가 뒷정리보다 먼저다.** 그래서 이 도구를 돌릴 때마다 dev 서버가 하나씩 남았고,
그게 포트를 붙들면 다음 실행의 vite 는 다른 포트로 밀려나 **검사가 옛 코드를 본다** —
§9-0-53 에서 정확히 그걸로 헤맸다. 순서를 바로잡고, SIGTERM 을 `npm exec` 껍데기가
삼키는 경우가 있어 **SIGKILL** 로 보낸다(어차피 그 다음 줄이 종료다).

> 곁가지의 곁가지: 남은 서버를 `pgrep -f "vite --port 5201"` 로 확인하다 **내 확인 명령
> 자체가 그 문자열을 포함해** 자기를 찾는 걸 한참 봤다. 확인 도구도 자기 자신을 세면
> 안 된다.

### 검사

`qa:multi` **50번 묶음**(이름 칸 세 값 · 버튼 색과 글자색 · 붉은 채움색을 안 쓰는지).
낡은 검사 하나를 같이 갱신했다 — 대기방 표 정의를 `5.2em` 문자열로 통째로 대조하고
있어서 이번 변경에 그대로 깨졌다.

전량 통과: `build` · `qa:multi`(50묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` ·
`qa:bgfit` · `qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · `qa:msg` ·
`qa:mrank` · `qa:pwa` · `_lint-services` · `_fitcheck` · `_screens-qa`(오류 0) ·
`sim:multi` + 소크 300판 · `perf:frame`(전부 60fps 예산 안) · `perf:boot`(첫 화면 **387ms**).

> 이번 회차의 교훈: **"5글자 고정폭"이라고 적을 때 그 5글자를 재 봐야 한다.** 23차에
> 그렇게 적어 놓고 62px 을 골랐는데, 정작 다섯 글자는 65px 이었다 — 주석이 코드보다
> 그럴듯해 보이면 아무도 다시 재지 않는다.

---

## 9-0-55. **나가기가 곧 승리 버튼이었다** — 이탈 6초 · 일시정지 1회 · 방 채팅 · 아이템 쇼핑 (2026-08-21 26차)

사용자 요청 넷. ③이 최우선이었고, 그 과정에서 **내가 낸 세 가지 안이 전부 반려**됐다 —
그 반려가 이번 회차에서 가장 중요한 대목이다.

### ★ ③ 부활-즉사-나가기 — 원가 20켤레짜리 **무패 전략**이 있었다

*"가만히 기다리다가 신발이 많이 쌓이면 죽은다음 부활 누르고 바로 죽음 다음에 나가기
누르면 승리하는건 말이 안됨"*

세 규칙이 겹쳐서 생긴 구멍이다. 하나씩 보면 전부 옳다.

| 규칙 | 어디 |
|---|---|
| 부활하면 **1위 + 20칸**에 즉시 선다 | `MULTI.reviveAhead` (§9-0-23) |
| 1:1 은 **한 명이 빠지면 그 자리에서 끝** | `matchRules.roundOver` (§9-0-28) |
| 순위 1순위는 **계단 수** | `rankPlayers` (§9-0-30, 사용자 확정) |

셋을 이으면 `부활 → (한 칸도 안 올랐는데 1등) → 나가기 → 승리`가 된다. 원가는 20켤레인데
이기면 항아리를 통째로 회수하므로 **기댓값이 항상 플러스**다. 판돈이 클수록 이득이 커져
"오래 버틴 판일수록 도망치는 게 이득"이라는, 게임을 정확히 거꾸로 만드는 전략이었다.

#### 내가 낸 세 안이 전부 반려됐다

ⓐ 확정 계단(부활로 얻은 칸은 실제로 오른 뒤에 순위에 반영) ⓑ 순위를 '오른 만큼'으로
ⓒ 자진 이탈은 판을 안 끝낸다 — ⓐ+ⓒ를 추천했다. 사용자 답:

> *"다 별로야, 게임의 묘미를 해치는 아이디어는 제출하지마. 부활을 해서 역전하고 내
> 신발을 20개를 건데, 이게 멀티게임의 꿀잼 요소야. 부활 자체는 좋은 건데, 문제는 나가기야"*

맞는 지적이었다. 셋 다 **부활의 보상을 깎아서** 문제를 없앤다 — 악용은 사라지지만
부활 전쟁도 같이 사라진다. 지금 규칙에서는 **부활 한 번이면 판이 끝나서 부활 전쟁 자체가
일어나지 않는다.** 고쳐야 하는 것은 부활이 아니라 **판을 얼리는 이탈**이었다.

#### 사용자 설계 — 6초 동안 아무것도 못 하고 지켜본다

*"나가기를 누른 순간 바로 나가지는 것이 아니라 6초후 나가집니다 라고 뜨면서 (…) 그 어떤
버튼도 못누르게 막아야해, 그냥 6초가 지나가는걸 볼 수밖에 없어. 다만 카운트다운 뒤로
화면에서 역전 당하는지도 보이면 좋겠어"*

- **패널을 안 그린다.** 문구와 큰 빨간 숫자만 얹고 뒤로 게임 화면이 그대로 보인다 —
  역전당하는 순간을 보는 것이 이 6초의 전부다.
- **입력을 통째로 막는다**(`clearInput()` + 탭 핸들러가 전부 삼킨다). 취소도 없다.
  취소가 있으면 "일단 눌러 보고 상황 보고 무르기"가 되어 6초가 공짜 정찰이 된다.
- 남은 사람의 **레이스 게이지에는 빨간 6·5·4·3·2·1** 이 뜬다. 부활 대기의 **회색** 카운트와
  색으로 갈린다 — *"나갔다는 것이 확실하게 보이게끔"*.

#### 기준 시각을 하나로 두니 모든 경로가 정확히 6초가 됐다

`exitStartedAt = deadAt || outAt` — **죽은 순간부터** 센다.

| 어떻게 나가나 | 상대가 받는 시간 |
|---|---|
| 살아서 나가기 | 누른 순간부터 6초 |
| 죽고 2초 만에 포기 | 남은 4초 |
| 죽고 부활 창을 다 흘림 | 0초 (그 6초가 이미 반격 시간이었다) |
| 부활 6회를 다 쓰고 사망 | 죽은 순간부터 6초 |

그래서 **부활 창(`reviveWindowSeconds`)과 이탈 유예(`leaveGraceSeconds`)가 같은 숫자여야
한다.** 갈리면 "죽어서 나가는 것"과 "살아서 나가는 것"이 상대에게 주는 시간이 달라진다.
사용자가 부활 창을 10 → **6초**로 줄인 것(*"너무 게임이 루즈해짐"*)과 이탈 6초가 같은 값인
것은 우연이 아니라 **같은 규칙의 두 얼굴**이다.

**1등에게만 건다**(`graceOnExit`: 단독 1등 + 반격할 사람이 남아 있을 때). *"2등이나 3등이
(…) 6초를 대기하는건 의미가 없지"* — 지고 있는 사람의 이탈은 판을 얼리는 행동이 아니라
기권이다. 붙잡을 이유가 없다.

이 한 수로 ⓐ의 실효까지 따라왔다. **부활 직후에 나가면 상대가 그 6초 안에 부활로 되받는다** —
부활의 보상은 그대로 두고 "산 자리는 지켜야 내 것"이 됐다.

### ★ 일시정지 — 없애지 않고 **모두 같이 멈춘다**

*"상대방이 신발 100개이상 먹을때까지 기다렸다가, 일시정지 풀고, 죽은 다음 부활을 누르는
악행"* — 혼자만 멈추는 일시정지는 **판돈이 불어나기를 기다리는 버튼**이었다.

그렇다고 통째로 없애면 전화 한 통에 판을 버려야 한다. 그래서 **1인 1회 · 20초 · 전원 동시
정지 · 자동 해제**(사용자 지정). 기다려서 얻는 것이 0이 되고, 급한 일에는 출구가 남는다.

**죽어 있는 사람이 하나라도 있으면 못 누른다**(`canPause`). 부활 창·이탈 카운트다운은
**서버 시각 기준이라 멈출 수가 없기 때문이다** — 아무도 죽지 않은 순간에만 열어 두면
멈춰야 할 시계가 게이지 하나로 줄어든다. 이 한 줄이 없으면 "일시정지 중에 부활 창이
지나가 버리는" 사고가 난다.

**홈 버튼이 공짜 일시정지였다** — `bindVisibility` 가 탭이 숨으면 루프를 세운다. 돌아올 때
비운 시간만큼 게이지를 소급 차감한다(진짜 일시정지 중에는 안 깎는다).

### ① 모든 멀티 방에 실시간 채팅 (비밀방 포함)

8줄 표시 · 위아래 스크롤 · **게임과 대기방을 오가도 기록 유지**(사용자 지정).

**패널을 화면당 한 번만 만든다**(`screens/multi/roomChat.js`). 대기방은 방 스냅샷마다
`nav.refresh()` 를 부르는데(§9-0-45), render 안에서 만들면 **입력 중이던 글자와 스크롤이
매번 날아간다.** 같은 노드를 다시 붙이면 입력값은 노드 속성이라 저절로 살아남고,
스크롤은 우리가 들고 있다가 되돌린다.

함정 둘:

- **처음엔 맨 아래로** 가 안 됐다. 첫 `draw()` 때 노드가 아직 화면에 안 붙어 `scrollHeight`
  가 0이다 — `#ui` 를 `MutationObserver`(subtree 없이)로 보다가 붙는 순간 되돌린다.
- 줄을 flex 로 짰더니 **긴 말이 시각 칸 위로 넘쳐 겹쳤다** → `.chat-row { display: block }`.

### ② 게임 시작 화면 우측 상단에 난이도 배지

*"말도 안하고 방장이 어려움으로 바꿔서 바로 시작 누르면 이 게임이 어려움인지 보통인지
쉬운지 알 수 없기 때문"*. `[쉬움]` 초록 · `[보통]` 노랑 · `[어려움]` 빨강.

### ④ 아이템 쇼핑 (신설) — 신발을 **쓸 데**를 만든다

*"신발이 3000~5000켤레 있는 사람들은 신발이 남아 돌아서 재미가 없으니"*. 로비 [캐릭터 변경]
바로 아래. 탭 셋 `[악세사리][날개][반려견]`, 열한 가지(1,000~10,000켤레) 전부 사용자 지정
그대로다. 한 판에 오가는 신발이 최대 121켤레이므로 **멀티를 여러 번 이겨야 하나 산다** —
그게 이 메뉴의 목적이다.

- **안 산 것도 고를 수 있다.** 값이 최대 1만 켤레인데 **사고 나서 처음 보는 일이 있으면
  안 된다** — 고르는 즉시 아래 `아이템 착용 모습`(정면·옆모습 두 컷)에 입혀 준다.
- **날개는 캐릭터보다 먼저 붙인다**(`behind`). DOM 은 나중에 붙인 것이 위에 오므로 순서가
  곧 앞뒤다(인게임 고스트를 `player.render()` 앞으로 옮긴 것과 같은 이유, §9-0-33).
- **확대는 안쪽 상자 하나가 맡는다.** 그림마다 `transform: scale(2)` 를 걸면 크기만 두 배가
  되고 **자리(`left`/`top`)는 그대로**라 전부 어긋난다 — 실제로 그렇게 짰다가 모자가
  어깨에 붙었다. 안쪽 상자를 통째로 키우면 좌표를 논리값 그대로 둘 수 있다.
- **표와 그림이 다르면 빌드가 멈춘다.** `data/items.js` 의 `w`·`h` 와 `build-items.mjs` 가
  굽는 크기가 어긋나면 그 자리에서 실패한다 — 사용자가 나중에 직접 그린 png 로 갈아 끼울
  때도 **이 표만 맞추면 된다.**

#### 도트 22장 — 실루엣 표를 세 번 버렸다

*"가급적 천천히 예쁘게 그려"* 라는 요청대로 눈으로 보고 다시 그리기를 반복했다.

| 무엇이 | 어떻게 보였나 | 어떻게 고쳤나 |
|---|---|---|
| 날개 | **쿠션** | 실루엣 표를 버리고 **부챗살 생성기**(각도·길이 배열) + 층진 가장자리 |
| 레게머리 | **갈색 단발머리** | ASCII 돔 + **절차 생성 가닥**(길이를 흩고 4줄마다 매듭, 바깥쪽에만) |
| 악마날개 | **붉은 낙서** | 구간을 4개로 줄이고 sag 8 + **맨 위 구간은 안 판다** |

`etc/` 를 안 읽으므로 Vercel 빌드에서 안전하다(§9-0-32 의 그 함정).

### 판돈은 신발 20개로 고정 (사용자 지정)

*"판돈은 신발 20개로 고정해"* — `reviveCost: 20` 그대로 둔다.

사용자가 이번 규칙에 대해 한 가지를 걱정했다: *"서로 신발 뺏기기가 싫어서 부활을 안쓰려고
할거 아니야? 그럼 실력 격차가 큰 2명의 플레이어가 역전할 기회가 사라지는 거고?"*
**정반대라고 답했다.** 죽은 뒤 부활을 안 쓰면 거의 확정 패배다(상대가 한 번만 부활해도
1위+20으로 넘어간다) — "아끼기" 균형은 애초에 존재하지 않는다. 진짜 위험은 규칙이 아니라
**판돈 크기**(최대 121켤레 손실)이고, 손잡이는 `reviveCost`·`maxRevives` 둘뿐이라
일단 20/6 그대로 두고 **실제로 돌려 본 뒤** 정하기로 했다.

### RTDB 규칙 — 새 필드 다섯 (게시 대기)

`players/$uid/outAt` · `players/$uid/pauseUsed` · `pausedBy` · `pausedAt` · `chat/$mid`.
`$other: false` 라 **규칙에 없으면 그 update 가 통째로 거부된다**(§9-0-34) — 게시 전에는
이탈 6초·일시정지·채팅이 서버에서 막힌다.

채팅 쓰기 규칙의 핵심은 **방에 있는 사람만, 새 글만, 보낸이는 나**다(덮어쓰기 없음 —
있으면 남의 말을 갈아 끼울 수 있다). `text` 60자 상한은 `multiplayer.CHAT_MAX` 와 같은 숫자다.

> ⚠ 이 회차에는 콘솔이 재인증(비밀번호)을 요구해 **게시를 못 했다.** 사용자가 크롬에
> 로그인해 준 뒤 §9-0-56 에서 `items` 까지 얹어 게시했다 — 최종 **9,221 / djb2 2824372086**
> (26차 목표였던 9,095 / 235293096 은 `items` 가 빠진 값이다).

### 아직 안 한 것 → **§9-0-56 에서 했다**

인게임에서 아이템을 입은 모습은 이 회차에는 안 그렸다(쇼핑 화면의 착용 미리보기까지였다).
`Player.render` · 고스트에 얹는 작업은 바로 다음 회차에서 끝냈다.

### 검사

`npm run qa:chat`(신규) · `npm run qa:shop`(신규) — 둘 다 import map 으로 서비스만 대역으로
갈고 **진짜 화면 코드를 그대로 마운트**한다(§9-0-50 의 그 수법). 특히 `qa:shop` 은 그림 22장이
**실제로 뜨는지**(`naturalWidth`)와 확대가 **정수배**인지를 본다.

`preview:hud` 에 `?state=exit|pause|menu` 세 상태를 넣어 눈으로 확인했다 — 실기기 두 대로만
보이는 화면이라 이게 없으면 확인할 방법이 없다.

전량 통과: `build` · `qa:multi`(51묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` ·
`qa:bgfit` · `qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · `qa:msg` ·
`qa:mrank` · **`qa:chat`** · **`qa:shop`** · `qa:pwa` · `_lint-services` · `_fitcheck` ·
`_screens-qa`(오류 0) · `sim:multi`(S1~S39) + 소크 900판 · `perf:frame`(전부 60fps 예산 안) ·
`perf:boot`(첫 화면 **401ms**).

낡은 검사 4건은 **이번에 의도한 변경**이라 같이 갱신했다(부활 창 10초 ×3, 죽음 판정에
이탈 카운트가 추가됨). `sim:multi` S22-pre 는 유예 때문에 승자가 바뀌어 정산 순서가
어긋났다 — **진 사람이 먼저 낸다**로 시나리오를 바로잡았다.

> 이번 회차의 교훈: **악용을 막는 가장 나쁜 방법은 재미의 원천을 깎는 것이다.**
> 내가 낸 세 안은 전부 부활의 보상을 건드렸고, 그러면 악용과 함께 부활 전쟁도 사라진다.
> 사용자가 짚은 자리는 부활이 아니라 **나가기** 하나였다 — 고칠 곳을 한 칸 옆으로 옮기니
> 규칙은 그대로 두고 구멍만 막혔다.

---

## 9-0-56. **계단 위에서도 아이템을 입는다** · 규칙 게시 완료 (2026-08-21 26차 후속)

*"파이어베이스 크롬에 로그인 해놨어, 콘솔 직접 들어와서 직접 진행해, 방금 만든 기능들
오류 없는지 체크해, 쇼핑 화면 착용 미리보기까지 전부 구현해"*

### ★ ① 쇼핑 화면과 **같은 표**로 계단 위에도 얹는다

`src/game/wornItems.js` 신설. 좌표를 새로 적지 않고 `data/items.js` 의 `dx`·`dy` 를
**캐릭터 기준으로 환산만** 한다:

```
relX = dx - WEAR.charX      relY = dy - WEAR.charY      (원본 도트 단위)
```

이게 이 파일의 전부이자 설계다. 여기서 좌표를 따로 적으면 두 화면이 언젠가 갈라지고,
**사용자가 나중에 직접 그린 png 로 갈아 끼울 때 한쪽만 따라온다.** 표 하나를 고치면
쇼핑 미리보기와 계단 위가 같이 움직인다.

세 가지를 조심했다.

| 무엇 | 왜 |
|---|---|
| **뒤집기는 원본 도트 단위에서** (`CHAR.w - relX - it.w`) | 화면 좌표를 뒤집으면 배율 1.5 가 곱해진 뒤라 반올림 오차가 방향마다 다르게 남는다 — 오른쪽/왼쪽에서 모자 자리가 1px 튄다 |
| **캐릭터 상자는 컷과 무관하게 35×50** | 점프 컷은 50×50 이라 좌우가 넓지만 몸통은 같은 자리다. 컷마다 상자를 바꾸면 이펙트 3프레임 동안 모자가 옆으로 튄다 |
| **사망 연출에는 안 그린다**(STARE·FALL) | 캐릭터를 1.15배로 늘리거나 낙하시키는데 아이템만 제자리에 남으면 몸에서 떨어져 나온 것처럼 보인다. 그 순간 봐야 하는 건 모자가 아니라 "죽었다"는 사실이다 |

앞뒤 순서는 캔버스 그대로다 — **날개(`behind`) → 캐릭터 → 모자·반려견.** DOM 쇼핑
화면에서 `if (item?.behind) put(...)` 로 한 것과 같은 규칙이고, 인게임 고스트를
`player.render()` 앞으로 옮긴 것(§9-0-33)과도 같은 이유다.

프로필은 **판이 시작될 때 한 번만** 읽는다(`GameScene.enter` → `this.myItems`).
렌더에서 `getProfile()` 을 부르면 `localStorage` 파싱과 신발 130칸 순회를 초당 60번
하게 된다 — §9-0-39 에서 정확히 그걸로 프레임을 잃었다.

### ② 상대 고스트에도 얹는다 — 그래서 **방에 실어 보낸다**

아이템은 1,000~10,000켤레짜리다. **남이 봐 주지 않으면 살 이유가 없다.**

`players/$uid/items` 를 한 줄 문자열로 보낸다(`"hat_crown,wing_angel,pet_dog"`).
객체로 넣으면 슬롯이 늘 때마다 규칙에 잎을 더해 **다시 게시**해야 하는데, 규칙에 없는
필드는 그 update 를 통째로 막는다(§9-0-34) — 즉 **슬롯 하나 늘릴 때마다 입장이 깨질 수
있는 구조**가 된다. 문자열 하나면 규칙이 그대로다.

포장·해제(`packItems`/`parseItems`)는 화면이 아니라 **표 옆(`data/items.js`)** 에 뒀다.
멀티 서비스와 인게임 렌더가 둘 다 쓰는데, `services/` 가 `game/` 을 물면 브라우저 전용
모듈(canvas·assets)이 딸려 와 **노드 검사(`sim:multi`)가 그 자리에서 죽는다.**
`qa:multi` 52번이 그 방향을 못 박는다.

투명도는 캐릭터와 **같은 `save` 블록 안에서** 건다 — 따로 걸면 모자만 또렷해져 몸에서
분리돼 보인다. 그림은 **상대 목록이 바뀔 때만** 확인하므로 매 프레임 비용이 0이다.

### ★ ③ RTDB 규칙 게시 — 26차 다섯 필드 + `items`

사용자가 크롬에 로그인해 둔 덕에 콘솔에서 직접 게시했다. `.CodeMirror.setValue` 로
넣고 게시한 뒤 **새로고침해 되읽어 대조**했다:

```
게시 전  8,127 / djb2 2725051820
게시 후  9,221 / djb2 2824372086   ← 로컬 파일과 완전히 같다
```

**규칙 플레이그라운드로 실제 판정까지 확인했다** — 이게 없으면 "게시했다"는 것만 알고
"통과한다"는 것은 모른다:

| 시뮬레이션 (`set /rooms/QA01`, uid=u1) | 결과 |
|---|---|
| `items` 를 포함한 방 만들기 | **허용** (게시 전이면 `$other: false` 에 걸려 거부됐다) |
| `items`·`outAt`·`pauseUsed`·`pausedBy` 를 전부 포함한 판 중 상태 | **허용** · 검증 성공 14건 |
| `items` 를 65자로 (상한 64) | **거부** — 검증이 실제로 돈다 |

> `pausedAt`·`chat/at` 은 `newData.val() == now` 라 플레이그라운드로 재현할 수 없다.
> 클라이언트가 `serverTimestamp()` 를 쓰므로 서버가 채우고, 같은 모양인
> `presence/lastActive` 가 이미 운영에서 도는 것으로 확인돼 있다(§9-0-49).

### 눈으로 확인 — `npm run preview:wear` (신규)

계단 위 자리는 **표가 맞아도 화면이 틀릴 수 있다**(배율 1.5 와 좌우 반전이 낀다).
그래서 진짜 `Player` 와 진짜 `multiGhosts` 를 불러 여섯 장을 찍는다 — 정면 · 오른쪽 ·
왼쪽 · 다른 조합 둘 · 고스트. 발끝 라인(212)과 중심선(90)을 빨간 선으로 얹어 자리를
바로 대조할 수 있게 했다.

그리고 **배포본(dist)을 실제로 띄워 한 판을 돌렸다** — 프로필에 아이템을 심고
`싱글게임` 을 눌러 계단을 올라가며 찍었다(`tools/_out/ingame_items.png`).
왕관·천사날개·강아지가 전부 제자리에 붙었고 콘솔 오류 0.

> 그 과정에서 하마터면 오판할 뻔했다. 첫 스크린샷에는 아이템이 **하나도 없었다** —
> `updateCurrent` 를 다섯 번만 돌려 아직 **인트로 팝 프레임**이었고, 거기는 캐릭터를
> 1.2배로 늘려 그리므로 설계상 아이템을 안 얹는다. 코드가 아니라 **찍은 순간**이
> 문제였다. §9-0-44 와 같은 함정이다 — 미리보기는 실제와 같은 상태에서 돌아야 한다.

### 안 한 것 (그리고 왜)

**레이스 게이지의 얼굴 아이콘(16×16)에는 아이템을 안 얹는다.** 그 그림은 머리 꼭대기에서
3도트 내려 1:1로 자른 것이라 모자를 얹을 여백이 없고, 22도트짜리 상자 안에서 이미
자리 색 테두리 6칸 + 등수 + 카운트다운이 겹쳐 있다(§9-0-28·§9-0-33 에서 겹침을 두 번
줄였다). 여기에 왕관을 하나 더 얹으면 **누가 몇 칸 남았는지가 안 읽힌다** — 이 게이지의
일은 신원과 목숨이지 치장이 아니다.

### 검사

`qa:multi` **52번 묶음**(신규) — 표를 그대로 환산하는지 · 반전이 배율 앞인지 ·
앞뒤 순서 셋(내 캐릭터·고스트·투명도 블록) · 사망 연출 제외 · 프로필을 한 번만 읽는지 ·
착용한 것만 받는지 · **서비스가 `game/` 을 안 무는지** · 규칙의 `items` 와 코드 상한이
같은 숫자인지 · 포장/해제 다섯 경우.

넣기 전에 실패를 먼저 확인했다 — `STARE·FALL 제외` 검사가 처음엔 통과했는데,
`case P_STATE.STARE:` 가 **`update()` 에도 있어서** 잘라 본 구간이 엉뚱했다.
`render()` 안의 것부터 찾도록 고치니 그제야 진짜를 봤다.

전량 통과: `build` · `qa:multi`(52묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` ·
`qa:bgfit` · `qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · `qa:msg` ·
`qa:mrank` · `qa:chat` · `qa:shop` · `qa:pwa` · `_lint-services` · `_screens-qa`(오류 0) ·
`sim:multi` · `perf:frame`(전부 60fps 예산 안 · fillRect 변동 0) · `perf:boot`(첫 화면 **424ms**) ·
**`preview:wear`(신규)** · 배포본 실제 플레이 1판.

> 이번 회차의 교훈: **좌표를 두 번 적으면 두 화면은 반드시 갈라진다.** 쇼핑 미리보기와
> 계단 위는 배율도 다르고 반전도 다르지만, 표 하나에서 환산만 하면 사용자가 나중에 직접
> 그린 png 로 갈아 끼워도 두 화면이 같이 따라온다 — 표를 진실로 두는 값이 이것이다.

---

## 9-0-57. **반려견이 주인을 밟고 오르고 있었다** · 쇼핑 개편 · 상승 번개 · 가면 둘 (2026-08-21 27차)

사용자 요청 한 묶음. 화면 개편(쇼핑) · 인게임 아트(반려견 자리·상승 번개) · 새 아이템 둘 ·
값 조정 셋이다. **하나하나 화면을 찍어 눈으로 고쳤다** — 이번 요청은 전부 "보이는 것"이라
소스만 맞추면 통과도 실패도 의미가 없다.

### ★ ① 반려견이 **주인 앞**에 서 있었다 (사용자 지적)

*"반려견은 주인 뒤에 있어야지, 주인 앞에 있으면 안됨 (…) 주인 뒤에 계단 위치를 찾에서
계단 위에 있는 것처럼 그려"*

원인이 둘 겹쳐 있었다.

| 무엇이 | 왜 앞으로 왔나 |
|---|---|
| `behind` 가 꺼져 있었다 | 캔버스는 나중에 그린 것이 위다 — 반려견을 캐릭터 **뒤에** 그리지 않으니 몸을 가렸다 |
| 좌표가 `dx: 40`(오른쪽)이었다 | 반전은 **캐릭터 상자 안에서** 뒤집으므로, 오른쪽에 두면 왼쪽을 볼 때도 **가는 쪽**으로 따라온다 — 즉 어느 방향이든 늘 앞이다 |

그래서 **자리를 컷마다 갈랐다**(`items.js` 의 `sideDx`·`sideDy`, `itemOffset()`):

```
정면(쇼핑 화면) : 옆에 나란히      dx 42, dy 49
옆모습(인게임)  : 한 계단 뒤·아래  sideDx 2, sideDy 62
```

옆모습 값은 눈대중이 아니다. 계단 간격은 가로 30 · 세로 20 **화면** 픽셀이고 캐릭터는
1.5배로 그려지므로, 원본 도트로는 **가로 20 · 세로 13**이다. 그 값을 넣으면 반려견의
발이 **바로 뒤 계단 윗면에 정확히 닿는다** — 따라 올라오는 그림이 된다.
`qa:multi` 53번이 이 환산을 그대로 계산해서 검사한다(±1도트 — 14폭과 35폭의 반쪽이
서로 홀짝이라 정확히 30은 안 나온다).

**따라다니는별만 예외**다(사용자 지정) — 등 뒤 **살짝 위**에 뜬다. 별은 걷지 않는다.

### ★ ② 계단 사이 상승 컷에 파란 번개

*"빠르게 올라가는 뒤에 파란 번개 불꽃처럼 붙어있는 느낌으로 계단과 계단 사이의
캐릭터 그림에도 그려"*

이 컷은 **딱 3프레임**이다(`ANIM.effectFrames`). 그래서 세 가지를 지켰다:

- **뒤쪽 아래로만** 뻗는다 — 올라온 자취다. 앞이나 위로 가면 속도가 아니라 잡음이 된다.
- 프레임마다 **한 도트씩 어긋난다** — 그 흔들림이 곧 속도다. 매 프레임 같으면 정지 그림이 붙은 것으로 보인다.
- **난수를 안 쓴다** — 같은 프레임이면 항상 같은 그림이어야 미리보기·검사가 성립한다.

> 처음엔 몸에서 9~20도트 뒤에서 시작했는데 **아무것도 안 보였다.** 캐릭터는 1.5배로
> 그려져 좌우로 26도트씩 차지한다 — 자취가 통째로 몸에 가려져 있었다. 22~36으로 밀어냈다.

### ★ ③ 날개는 상승 컷 그림이 **따로 있다**

*"날개는 살짝 졉혀 있고 살짝 번개 느낌이 나도록 하나씩 정성을 다해서 그려"*

활짝 편 날개를 그대로 쓰면 "떠 있다"로 읽혀서 오히려 속도가 죽는다. `makeWing(…, fold)`
가 부챗살 길이를 62%로 줄이고 **위쪽을 더 접어 뒤로 눕힌다.** 번개는 날개 끝을 따라
**각도로** 찍는다 — 난수로 뿌리면 다시 구울 때마다 그림이 달라진다.
색은 인게임 번개(`palette.spark*`)와 **같은 값**이라, 날개를 낀 사람은 번개가 한 덩어리로 이어진다.

### 상승 컷은 몸이 **1도트 왼쪽·3도트 아래**에 있다 (놓칠 뻔한 것)

가면을 얹고 상승 컷을 찍어 보니 **모자가 머리 위로 떠 있었다.** 스프라이트를 재 보니
답이 있었다 — 앞·옆 컷은 불투명 범위가 `y0` 부터인데 **점프 컷은 `y3` 부터**고,
50폭 캔버스 안에서 머리 중심도 1도트 왼쪽이다(웅크린 자세라 그렇다).

`CHAR.jumpNudgeX/Y` 로 보정하되 **몸에 붙는 것만** 옮긴다(`sideDx` 가 없는 것 = 모자·날개).
반려견은 계단을 기준으로 서 있으므로 몸을 따라가면 안 된다.

### ④ 아이템 쇼핑 전면 개편 (사용자 지정 그대로)

| 어디 | 전 | 후 |
|---|---|---|
| 맨 아래 | `[뒤로]` | **`[나가기]`** |
| 그 위 | 없음 | **`[모든 아이템 착용 해제]`** |
| 큰 버튼 | 구매하기 / 착용하기 / 벗기 | 구매하기 / **착용하기** / **착용해제** |
| 목록의 값 자리 | `보유중` / `착용중` | **`* 착용 가능한 아이템 *`** / **`* 착용한 아이템 *`** |
| 아래 두 컷 | 정면 · 옆모습 | **미리보기 · 현재 모습** (둘 다 정면) |

두 컷은 **하는 일이 다르다.** 왼쪽은 목록에서 고른 것을 **입어 본** 모습이고, 오른쪽은
실제로 **착용해 둔** 모습 — `착용하기` 를 눌러야 바뀌고 **게임에 그대로 들어가는 그림**이다.

미리보기는 고른 것을 **홀로** 보여 주지 않는다. 착용해 둔 것에서 **그 자리만** 갈아 끼운다 —
홀로 보여 주면 이미 낀 날개·반려견이 사라져 실제와 다른 그림이 된다.

두 가지를 더 손봤다:

- **뱃지를 둘로 갈랐다.** 산 것은 테두리만, 입은 것은 **채운 초록**. 같은 모양이면
  "샀다"와 "입었다"가 구별이 안 된다. 초록은 이 화면에서 여기에만 쓴다.
- **화면에 들어오면 그 자리에 착용해 둔 것부터 보여 준다.** 이 화면에 온 사람이 제일
  먼저 확인하고 싶은 건 "지금 뭘 끼고 있지"다.

`[모든 아이템 착용 해제]` 는 아무것도 안 입었으면 **왜 안 되는지 말한다**
(`착용한 아이템이 없습니다` — 사용자 문구 그대로). 조용히 아무 일도 안 하는 버튼은 고장으로 보인다.

### ⑤ 자리는 셋, 자리마다 하나 (규칙을 지키는 곳은 한 군데다)

*"악세사리를 동시에2개 착용 불가, 날개도 2개 착용불가, 반려견도 1개만 가능"*

이미 그렇게 돌고 있었고(`storageLocal.equipItem(slot, id)` — 같은 자리에 새 id 를 쓰면
덮인다), 이번에 **가면 둘도 `hat` 자리**에 넣어 그 규칙 안에 뒀다. 규칙을 화면이 아니라
저장소 한 곳이 지키는 게 중요하다 — 화면이 각자 막으면 새 화면이 생길 때마다 빠뜨린다.

`unequipAll()` 은 자리를 하나씩 지우지 않고 **빈 객체로 갈아 끼운다.** 자리가 넷이 되어도
이 함수는 안 고쳐도 된다.

### ★ ⑥ 가면 둘 — **머리 실루엣을 실측해서** 덮었다

*"각각 얼굴 전체를 가리는 아이언맨 얼굴, 베트맨 얼굴을 도트로 정성껏 그려"*

모자와 달리 가면은 **얼굴이 한 도트도 새면 안 된다.** 도면을 손으로 적으면 어느 줄
하나가 반드시 한 도트 좁아지고 거기서 살색이 비친다. 그래서 캐릭터 스프라이트의
**불투명 범위를 재서 표로 옮기고**(`HEAD_F`·`HEAD_S`) 그 실루엣을 채운다.

- **아이언맨** — 붉은 헬멧 + 가운데 금색 얼굴판 + 빛나는 눈 + 통풍구 입.
  금색 판 **둘레에 어두운 선**을 둘렀다. 없으면 붉은 헬멧과 금색이 뭉개져 "노란 달걀"로
  보인다(첫 판이 그랬다). 눈은 캐릭터의 실제 눈 높이(원본 14~15행)에 맞추고 **바깥쪽을
  한 도트 올렸다** — 수평으로 두면 순한 얼굴이 된다.
- **배트맨** — 어두운 카울 + 머리 위로 솟은 귀 + 흰 눈구멍, **입·턱은 드러난다**(가리면
  배트맨이 아니라 복면강도다). 귀는 **꼭짓점이 바깥쪽**인 삼각형이라 밖으로 눕는다 —
  좌우 대칭 삼각형으로 그렸더니 도깨비 뿔이 됐다. 옆모습의 귀는 반대로 **뒤로 눕는다**
  (앞으로 세우면 안테나로 보인다). 꼭짓점도 최소 두 도트다.

### ⑦ 값 조정 (전부 사용자 지정)

| | 전 | 후 |
|---|---|---|
| 닉네임 변경 | 200켤레 | **1,000켤레** |
| 왕관 | 4,000 | **5,000** |
| 따라다니는별 | 5,000 | **10,000** |
| 아이언맨마스크 · 배트맨마스크 | — | **각 7,000** |

닉네임 변경이 200이면 아이템이 1,000~10,000켤레짜리로 생긴 지금 **아무 때나 바꿔도 되는
값**이 된다. 개명은 순위표에서 이름이 바뀌는 일이라 아이템 한 개와 비슷한 무게여야 한다.

### 검사

`qa:multi` **53번 묶음**(신규) — 값 넷 · 자리 규칙 · 뱃지 문구와 색 규칙 · 버튼 세 얼굴 ·
`[모두 벗기]` 가 `[나가기]` **위**인지 · 두 컷의 이름과 하는 일 · **반려견의 한 계단 환산을
계산으로** · 번개가 맨 뒤인지 · 난수를 안 쓰는지 · 상승 컷 보정 · 가면 그림 존재.

`qa:shop` 에 네 묶음을 더했다 — 뱃지 두 가지(**계산된 색**으로 본다, §9-0-50) · 버튼 세 얼굴 ·
미리보기와 현재 모습이 **서로 다르게 움직이는지**(고르면 미리보기만 바뀐다) ·
모두 벗기(안 입었을 때의 문구, 입었을 때 한 번에 벗기) · `[나가기]`.

> 검사에서 두 번 헛다리를 짚었다. ⓐ `.seg` 의 카테고리 탭도 `.pbtn` 이라 "큰 버튼"을
> 고르면 `악세사리` 가 잡혔다. ⓑ 화면을 열자마자의 선택은 목록 첫 줄이라 `착용해제` 가
> 안 나왔다 — 그건 검사가 틀린 게 아니라 **화면이 그래야 했다**(착용해 둔 것부터 보여 준다).
> 검사가 화면 설계를 한 칸 밀어 준 셈이다.

눈으로 본 것: `preview:wear` 를 **아홉 장**으로 늘렸다(정면·좌우·조합 둘·고스트·**상승 컷
셋**). 그리고 **배포본으로 한 판을 돌려** 계단 위·상승 컷을 직접 찍었다
(`tools/_out/ingame_items.png` · `ingame_jump.png`) — 콘솔 오류 0.

> 여기서 하마터면 오판할 뻔했다. 첫 스크린샷에 아이템이 하나도 없어서 "안 붙었다"고
> 볼 뻔했는데, **찍은 순간이 인트로 프레임**이었다(설계상 안 얹는다). 찍기 직전에
> `player.state` 를 같이 찍고 나서야 알았다 — §9-0-56 에서 똑같이 한 번 속았다.
> **화면을 찍을 때는 그 화면이 무슨 상태인지도 같이 찍는다.**

전량 통과: `build` · `qa:multi`(53묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` ·
`qa:bgfit` · `qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · `qa:msg` ·
`qa:mrank` · `qa:chat` · `qa:shop` · `qa:pwa` · `_lint-services` · `_fitcheck` ·
`_screens-qa`(오류 0) · `sim:multi` + 소크 300판 · `perf:frame`(전부 60fps 예산 안) ·
`perf:boot`(첫 화면 **398ms**) · `preview:wear` · 배포본 실제 플레이 1판.

> 이번 회차의 교훈: **"뒤에 있어야 한다"는 그리는 순서만의 문제가 아니다.** 반려견은
> `behind` 를 켜는 것만으로는 안 됐다 — 좌표가 오른쪽에 있으면 반전이 그것을 **가는 쪽**
> 으로 데려간다. 앞뒤(z)와 자리(x)를 같이 봐야 "따라온다"가 성립한다.

---

---

## 9-0-58. **유료 아이템은 로비에서 보여야 산다** — 로비 착용 모습 · 새 아이템 여섯 (2026-08-21 28차)

사용자 요청 한 묶음. 그중 하나에 별표가 붙어 있었다 — *"가장 큰 문제는 아이템을 착용하면,
메인 로비 상단에 캐릭터 이미지가 있는 부분에 반드시 착용한 아이템이 착용된 모습으로 있어야
한다는 거야"*. 나머지는 그 한 줄을 성립시키기 위한 자리 계산과, 그 자리에 세울 새 아이템이다.

### ★ ① 로비 상단 캐릭터 = 쇼핑의 `현재 모습` **같은 부품**

`src/screens/wearFigure.js` 를 새로 뒀다. 27차에는 이 코드가 `ItemShop.js` 안에만 있었는데,
로비에도 같은 그림이 필요해지자 **복사하지 않고 떼어 냈다** — 복사하면 앞뒤 순서(`behind`)와
좌표 환산이 두 벌이 되고, 그러면 `data/items.js` 한 표로 세 화면을 묶어 둔 이유(§9-0-56)가
그 자리에서 무너진다. 사용자가 나중에 직접 그린 png 로 갈아 끼울 때 **세 화면이 함께**
따라와야 한다.

`.panel img { width: 55px }` 규칙은 **지웠다.** 이 칸에는 이제 그림이 여러 장(날개·캐릭터·
모자·반려견) 겹쳐 들어가고 장마다 크기가 다르다 — 모든 그림에 한 크기를 강요하는 규칙이
남아 있으면 통째로 깨진다. 크기와 자리는 전부 인라인 좌표가 정한다.

### ★ ② 그림틀 78×87 · 배율 1.5 — **인게임과 같은 배율**

로비 칸은 좁다. 그래서 착용 상자(60×66)를 다 보여 주지 않고 **실제로 무언가 그려지는
구간만** 잘라 낸다 — 가로 4~56(날개가 정확히 그 구간을 쓴다), 세로 4~62(위는 배트맨 귀,
아래는 발). 배율 1.5 는 **인게임 캐릭터 배율과 같은 값**이라(§3-1), 로비에서 본 그림이
계단 위에서 볼 그림과 **크기까지 같다.** 사기 전에 본 것과 사고 나서 보는 것이 다르면
안 되는 물건이라 이 값이 맞다.

| | 전 | 후 |
|---|---|---|
| 캐릭터 칸 | 73px | **78px** (+6.8%) |
| 그 안의 캐릭터 | 55×78.6 | **52.5×75** (−4.5%) |
| 보이는 것 | 캐릭터 한 장 | **날개 양쪽 + 모자 + 반려견** |

사용자가 말한 *"캐릭터의 크기를 10% 정도 줄이고"* 에 대해서는 **−4.5%** 로 멈췄다.
−10% 를 맞추려면 배율이 1.4 가 되는데, 그러면 인게임과 다른 크기가 되고 도트 격자도
1.5 보다 고르지 않다(1.5 는 1·2 가 번갈아, 1.4 는 다섯 칸 주기다). **칸을 넓히는 쪽으로
자리를 만들었으므로 겹쳐 착용한 모습은 그대로 보인다** — 목적은 충족하되 숫자는 다르다.

### ★ ③ 좁은 폰이 5px 모자랐다 — "도감 영역을 살짝 좁혀"

칸이 73 → 78 이 되자 360px 폰에서 딱지가 붙은 줄(`멀티게임 17승/50게임 [승리2위]`)이
**정확히 5px 잘렸다**(`qa:lobbyfit` 이 잡았다). 사용자 지정이 답을 이미 주고 있었다 —
*"도감 영역을 아래로 내려가지 않고 살짝 좁혀"*. 줄을 아랫줄로 내리면 패널이 통째로 길어져
로비가 밀리므로, **≤380px 에서만** 딱지 여백(3→2 · 2→1)과 패널 여백(gap 4→3 · padding 6→5)
에서 7px 을 빌려 왔다. 글자 크기는 안 건드렸다 — 딱지는 원래 8px 이고 더 줄이면 못 읽는다.

**잘림은 넘침으로 안 잡힌다**(§9-0-48). `.stats` 는 `flex: 1 1 auto` 라 자리가 모자라면
패널을 넘치는 대신 줄어들고 `ellipsis` 가 말없이 지운다 — 그래서 `qa:lobbyfit` 은 줄마다
`scrollWidth - clientWidth` 를 잰다. 그 검사에 **아이템을 전부 낀 상태**(배트맨 가면 ·
악마날개 · 무서운호랑이)를 재는 단계를 새로 넣었다. 아무것도 안 낀 상태만 재면 정작
자리가 모자라는 경우를 못 본다 — 도감완성(22차)·왕관 딱지(23차)에서 두 번 데인 자리다.

### ★ ④ **뒤 겹 안의 앞뒤가 컷마다 반대였다** (이번 회차에서 제일 늦게 드러난 것)

`behind` 는 "캐릭터보다 뒤"라는 것만 말한다. 그 안에서도 순서가 있는데, **정면과 옆모습이
서로 반대다.**

| | 무엇이 더 먼가 | 왜 |
|---|---|---|
| 정면 | **날개** | 반려견은 주인 **옆**에 선다 — 등에 붙은 날개보다 가깝다 |
| 옆모습·상승 컷 | **반려견** | 반려견은 **한 계단 뒤**에서 따라온다 — 날개보다 멀다 |

작은 강아지(14×13)는 겹치는 자리가 없어 이 차이가 안 보였다. 무서운호랑이(35×50)를
넣자마자 **두 컷에서 번갈아 가며 한쪽이 통째로 가려졌다** — 처음엔 옆모습에서 호랑이가
날개를 덮었고, 순서를 뒤집자 이번엔 정면에서 날개가 호랑이 얼굴을 덮었다.
`items.js` 의 `backFirst(cut)` 이 컷을 받아 정렬을 돌려주고, 쇼핑·로비는 `'front'`,
인게임은 그때의 컷을 넘긴다.

> **값을 만 켤레씩 주고 산 물건 둘 중 하나가 안 보이면 그건 고장이다.** 아이템을 하나씩만
> 껴 보는 검사로는 영영 안 나온다 — 이번에 미리보기를 **가장 큰 조합**으로 찍어 보고서야 알았다.

### ⑤ 반려견 정면은 **주인 옆에 딱 붙는다** (dx 42 → 38)

로비 상태창처럼 좁은 칸에서 제일 먼저 잘려 나가는 것이 이 자리다. 옆모습(인게임)은
그대로 **한 계단 뒤에서 쫓아온다** — 계단 간격(화면 가로 30·세로 20)을 배율 1.5 로 나눈
값이라 발이 바로 뒤 계단 윗면에 닿는다(§9-0-57).

### ⑥ 쇼핑 화면 세 가지

- **`[모든 아이템 착용 해제]` 를 뺐다** — *"착용 여러번 눌러서 착용 해제 하면 되기 때문이고,
  아이템이 없는 사람도 많을 것인데"*. 큰 버튼이 이미 착용/해제를 토글하므로 기능이 겹쳤고,
  처음 들어온 사람에게는 늘 흐린 채로 자리만 차지했다. 서비스 쪽 `unequipAll()` 은 남겨 뒀다.
  **검사도 "있는가"에서 "없는가"로 뒤집었다** — 지워 놓고 검사만 남기면 다음 사람이 되살린다.
- **뱃지 문구를 둘 다 일곱 글자로** — `착용 중인 아이템` / `착용 가능 아이템`.
  길이가 다르면 목록을 훑을 때 뱃지 왼쪽 끝이 줄마다 들쭉날쭉해 **다른 종류의 표시**로 보인다.
  별표를 뺀 것도 같은 이유 — 테두리가 이미 뱃지임을 말하고 있어 장식이 글자 수만 먹었다.
- **글자 한 단계 크게** (10 → **12px**). DOM 글자 사다리는 한 칸이 2px 다(§3-3).
  10px 는 값 줄(`.shop-cost`)보다 작아 "부가 설명"처럼 읽혔는데, 이 뱃지는 그 줄에서
  제일 중요한 정보다. 문구를 일곱 글자로 줄여 둔 덕에 320px 폰에서도 이름 칸을 안 밀어낸다.

### ⑦ 새 아이템 여섯 — 열세 → **열아홉** 가지

| 무엇 | 값 | 자리 |
|---|---|---|
| 파랑날개 · 노랑날개 · 초록날개 | 각 3,000 | **비둘기날개 바로 아래** |
| 귀여운사자 · 귀여운호랑이 | 각 7,000 | **고양이 다음**, 강아지와 같은 14×13 |
| 무서운호랑이 | 10,000 | 맨 끝, **캐릭터와 같은 35×50** |

- **색 날개 셋은 팔레트만 다르다.** 모양을 따로 그리지 않은 것이 설계다 — 부챗살 생성기가
  좌우 대칭과 깃털 층을 보장하므로, 색만 갈아 끼우면 여섯 벌이 **한 벌처럼 같은 품질**로
  나온다(§9-0-52 에서 왕관 세 벌을 도면 한 장으로 구운 것과 같은 이유). 손으로 세 벌을
  더 그렸으면 어느 하나는 반드시 깃털 수가 어긋났을 것이다.
- **무서운호랑이는 도면으로 돌아왔다.** 처음엔 다른 큰 그림처럼 타원·삼각형을 숫자로 두고
  구웠는데 결과가 **"뚱뚱한 주황 고양이"** 였다 — 네 발 짐승은 실루엣이 곧 종(種)이고,
  타원 몇 개를 겹치면 어느 각도에서도 덩어리가 된다. 35×50 이면 도면으로 적을 수 있는
  크기다. **앉은 자세**를 고른 이유: 네 발로 선 호랑이는 가로로 길어 35폭 안에서 머리가
  손톱만 해진다. 앉으면 머리가 크고 몸이 아래로 넓어져 세로 상자에 꼭 맞는다.
- **얼굴을 그림의 오른쪽 위(24열)에 몰아 둔 것**이 이 도면의 핵심이다. 정면 컷에서
  호랑이는 캐릭터 뒤에 절반쯤 가리는데 가려지는 것은 **왼쪽 절반**이다 — 얼굴을 한가운데
  두면 코까지 잘려 주황색 덩어리만 남는다.
- 옆모습은 **작은 반려견과 같은 규칙**(중심을 20도트 뒤, 발을 13도트 아래)이고,
  35폭이라 `sideDx = -8`. 음수여도 캔버스는 안 자른다 — 자르는 것은 쇼핑 화면의 상자뿐이다.

### 검사

- **`qa:lobbyfit`** — 320·360·390·412px 에서 ① 기본 ② 도감완성 ③ 왕관 딱지 ④ **아이템 전부
  착용**(신규) 네 상태를 잰다. 그림이 실제로 떴는지는 클래스가 아니라 **`naturalWidth`** 로
  본다(§9-0-52 왕관에서 상대 경로 404 를 그렇게 잡았다).
- **`qa:shop`** — ⑨묶음을 "모두 벗기 버튼이 **없는가**"로 뒤집고, ⑩묶음(뱃지 일곱 글자·12px)
  을 새로 넣었다. 날개 여섯·반려견 여섯의 이름과 값·**순서**도 그대로 못 박는다.
- **`qa:multi` 54번 묶음**(신규) — 로비가 쇼핑과 같은 부품을 쓰는가 · 배율이 CSS 에 두 벌로
  적혀 있지 않은가 · **뒤 겹 순서가 컷마다 반대인가** · 새 아이템 여섯의 값·크기·자리 ·
  반려견 정면이 캐릭터에 닿거나 겹치는가 · 무서운호랑이의 옆모습 환산(계단 하나 뒤·아래).
- **`preview:wear`** 를 아홉 장 → **열세 장**으로 늘렸다(색 날개 둘 · 큰 호랑이 좌우).
  그리고 **배포본으로 한 판을 돌려** 로비와 계단 위를 직접 찍었다 — 콘솔 오류 0.

전량 통과: `build` · `qa:multi`(54묶음) · `qa:rules` · `qa:rank` · `qa:wallet` · `qa:bg` ·
`qa:bgfit` · `qa:hoffit` · `qa:hofvert` · `qa:lobbyfit` · `qa:cardfocus` · `qa:msg` ·
`qa:mrank` · `qa:chat` · `qa:shop` · `qa:pwa` · `_lint-services` · `_fitcheck` ·
`_screens-qa`(오류 0) · `sim:multi` + 소크 300판 · `perf:frame`(전부 60fps 예산 안) ·
`perf:boot`(첫 화면 **390ms**) · `preview:wear` · 배포본 실제 플레이 1판.

> 이번 회차의 교훈: **아이템을 하나씩 껴 보는 검사로는 겹침 버그가 안 나온다.**
> 뒤 겹 안의 앞뒤가 컷마다 반대라는 사실은, 가장 큰 조합(가면 + 날개 + 큰 호랑이)을
> 정면과 옆모습으로 **나란히 찍어 보고서야** 드러났다. 유료 아이템은 여러 개를 같이
> 끼라고 파는 물건이므로, 검사도 같이 낀 상태를 기본으로 두어야 한다.

---

## 10. 외부 연결

| 항목 | 값 |
|------|-----|
| GitHub | `https://github.com/nivs-web/ianworld-shoes` |
| 배포 | Vercel (GitHub import 완료, main 자동 배포) |
| Firebase 프로젝트 | `find-shoes-f5c55` (RTDB 주소에서 확정) |
| Realtime DB | `https://find-shoes-f5c55-default-rtdb.asia-southeast1.firebasedatabase.app` (asia-southeast1) |
| Firebase 설정 | `.env` 에 6개 값 전부 채움 (2026-08-15). Vercel 환경변수에도 동일하게 등록 필요 |
| 도메인 | `[빈칸 — 커스텀 도메인 사용 여부]` |
